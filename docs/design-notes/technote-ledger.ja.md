# 技術ノート設計台帳

2026-09-20に、公開技術ノート（`web/content/technote.ja.md`）第9章から移した。
公開ノートには実装済みの姿と縮約した台帳だけを残し、設計判断、上流への改訂、数値登録、保留中の判断はこの文書に置く。
見出し、節番号（§9.1–§9.4、§9.3.1–§9.3.55）、HTMLアンカーはそのまま保持し、既存の参照を有効に保つ。
新しい設計知見は技術ノートではなくこの文書へ記録する。移動先に合わせて相対リンクを書き換えた以外、以下の本文は移動時点の技術ノートと同一。
英語版: `technote-ledger.md`。

---

# 9. Manifesto との対応と未解決問題

Manifesto は公約を宣言する。本章はその公約を監査する。台帳の各行は、公約と、それを果たしている機構（あるいは、まだ何も果たしていないというギャップ）を対応づけ、宣言と実装の距離をつねに見えるところに置く。*逆向きに*流れた知見——実装の結果が Manifesto の機構レベルのスケッチを書き換えた事例——は 9.3節に分けて記録する。原理は立ち続けるが、スケッチは誤りうる。

## 9.1 台帳

| Manifesto の公約 | 機構 | 節 | 状態 |
| :--- | :--- | :--- | :--- |
| 記号媒介なしの生成 | `Log2Space`+ランドスケープ。音名・音階・拍子記号はエンジンのどこにも存在しない | §2–4 | 実装済み |
| 周波数軸の地形（蝸牛/脳幹モデル） | 粗さ・調波性・協和性カーネル | §3 | 実装済み |
| 時間軸の地形（神経振動） | 創発メーター：強制リミットサイクル、ヘッブ的テンポ学習、PLV 確信度 | §4, §5.4 | metricは明示的な同期を維持。entrainedとflowは有界な音声予測と身体の参加方策を通常Voiceへ接続済み（9.3）。実機負荷と08の試聴での縮退回避は各記録の版について確認し、その後の変更への作者判定は別に扱う。全条件での関係持続は未達。小節アクセントの弱結合は任意 |
| 地形の可変性（文化・個人・未知の原理） | `roughness_k`、協和性カーネル係数、任意のhabituation erosion | §3.4–3.5, §6.3.6 | 部分——文化的音律体系は未取込 |
| 順応と期待 | Voiceごとの`AdaptationContext` + ecology/listenerの`HabituationField` + ListenerTwinの受動的な音程観測 + 音響文脈の参加記憶 | §3.5, §5 | 部分——短期の反復予測、同じ観測窓の実音で更新するエネルギー予測、経験文脈の記憶をhabitat側の参加へ接続した（9.3.32–9.3.35）。連続した関係の履歴と行動条件付きの音響仮説は研究用（9.3.8–9.3.31）。通常sampleでは音程を採点できない。認知的な保持・干渉、流れの同定、phrase/sceneの期待、行動価値学習は未解決 |
| 音響生命：知覚・代謝・自律 | Voice：異なるアーティキュレーション生命コア、正規化エネルギー、時間領域のendurance/recovery、生存可能性 | §5 | 実装済み |
| 集団：ニッチ・共生・地形変形 | クラウディング、リスポーン、閉ループ | §5 | 実装済み |
| 中央指揮者の不在 | ローカルな知覚のみ。メーターは集団自身のオンセットから創発する | §4–5, §7 | 実装済み。専用の拍打ちと時間的scaffoldは明示的な同期の実演・実験に限定（公開サンプルでは07） |
| シナリオ=マクロの演出 | ディレクターの地形操作 | §6.3.6 | 作者による構成として実装済み。長い構造を内部で知覚・記憶する役割とは区別する（9.3.55） |
| 時間構造を聴覚認知に根差させる | 本体はアーティキュレーション、グルーヴ、拍・小節、phrase、反復・変奏、section。全曲文脈は研究拡張 | §9.3.55 | 基準設計はレビュー済み。その後のM0数値・モデル境界改訂には新たな外部レビューなし。交換契約と比較手順を登録し、実装・実験は未達。短期参加と履歴は部分実装。関係の持続、境界・終止、長い文脈の生成への接続は未達 |
| DCC 第2段階：生体信号閉ループ | `[dcc]` coupling有効時、`ListenerTwin`圧力をpitch-search temperatureへ戻す | §4.1, §6.2 | 模擬loopは実装済み・デフォルト無効。実生体信号loopは未解決 |
| 実際に提示された音との認知的結合 | presentation専用分析。入力欠落時は観測を無効化し、NSGT履歴を消去。連続した1窓分の入力が揃うまでDCC圧力を停止 | §6.2 | 実装済み。実機での過負荷検証は未完了 |
| 生きた演奏としての音楽 | 楽器は音声ファイル出力を持たず、別バイナリ`conchordal-render`がオフライン検証を担う | §6 | バイナリ境界として実装済み |
| 役割の溶解・空間ランドスケープ・音色の遺伝・他領域 | 遺伝的リスポーンはアッセイとして存在 | — | 地平 |

## 9.2 実装された順応と、残る期待のギャップ

順応、期待、分節を区別して設計する。以前この節で用いた「知覚的現在は約3秒、予測窓は3–8秒、シーン窓は15–30秒」という区分は、DCCから導出した値でも、本実装で認知課題に適合した値でもない。持続時間だけで退屈、phraseの長さ、場面を変える時点を決めない。認知課題による制約、採用候補の数学モデル、数値計算の窓を分ける根拠と設計契約は9.3.1–9.3.5に記録する。Scenarioの時間構成は作者の選択として扱う。

**協和は場所ではなく食事である**、という設計上の比喩は、占有の履歴によって行動環境を変える二つの機構に対応する。各Voiceの`AdaptationContext`は基音占有場の速いtraceと遅いtraceを持ち、pitch選択へboredomとfamiliarityの補正を加える。共有環境の側では、任意の`HabituationField`が、持続するperceived-consonance activityを減価し、音が退いた後に回復する（3.5節）。habitat busとpresentation busの内容は一致するとは限らないため、ecologyと`ListenerTwin`は別々の状態を持つ。これらの更新則を実装したことだけでは、人間の退屈や注意の変化を同定したことにはならない。

残るギャップは、順応の不在ではなく**期待**である。次にどのeventが起きるはずかを表すphrase-level predictorはまだなく、scene境界を自律的に作り、評価する機構もない。現行実装では、ミクロ層（jitter、呼吸、うなり）は**body**が、メソ層（順応に駆動される移動、生と死）は**ecology**が、マクロの演出は**scenario**が受け持つ。これは現状の分業であり、長い時間構造をDCCの対象から外す設計原理ではない（9.3.55）。`ListenerTwin`のtensionとresolvabilityは、`[dcc].coupling_strength > 0`ならpitch-search temperatureを加算し、模擬feedback loopをすでに閉じられる。デフォルトは0であり、実際の聴き手の生体信号との接続は今後の課題である。既存のsample診断が使う約8秒という値も、検査上の設定であり、普遍的な退屈の閾値やシステムの不変条件ではない。

現行の`ListenerTwin`が測るのは、presentation由来の協和性地形における局所的な改善余地である。
探索中はその地形を固定し、Voiceが移動した後の地形を予測しない。帰還先のrootや直前のphraseも記憶しない。
したがって、既存の`tension_level = (1 - stability_level) * resolvability_level`は、
帰還感やphraseの終止を測る値ではない。近傍の改善余地が増えれば、stabilityが上がってもtensionは上がりうる。
この期待のギャップは未解決である。

2026-09-06の研究用比較では、単旋律の波形から音高を観測し、直前2音程を条件とする
次の音程の予測密度を、同じ履歴を順序なしで使う対照と比較した。6 seed・60条件で、
反復する輪郭には1.338〜2.275 bit/eventの平均予測利得があった。音色・音量だけの変更でも
同程度の利得を保ったが、変更した輪郭の反復も予測できるため、平均利得は変化の位置を示さない。
初期の変化候補判定は6 seed中1つでしか部分変更を検出しなかった。この時点の観測は研究スクリプト内であり、
通常のListenerTwin、生成側、終止判定へは接続していなかった。
作者は刺激を「全て表記どおりの発音にきこえる」と評価した。その後、学習済みの文脈だけで
誤差を較正する対照では、未使用の3 seedの6変更条件を1〜3音以内で検出し、
反復維持・音色変更・減音・休止の評価区間384イベントで誤検出0だった。
生成記録との照合であり、知覚されたphrase境界や終止の正解との一致ではない。
条件と限界は`docs/roadmap/phrase-expectation-evidence-2026-09-06.md`に記録した。

混合音の追加検査では、短いlagの自己相関だけで近接する二音の中間周波数を受理した。
研究用に複数周期での整合検査と周期推定を加え、近接音・非調和混合の144イベントを
音高不明として扱い、単音・倍音・弱い妨害音下の360イベントを保持した。
音声欠落を休符や再発音として数えず、欠落をまたぐ音程も作らない。修正後の66条件は
通過したが、連続して重なる声部の旋律分離は未実装である。追加の単旋律30条件では
観測を保った一方、順序変更の検出が遅れるseedもあった。旧観測器でも同じ遅延があり、
局所的な予測器の一般化の限界として残す。

この限定した観測をRustへ移し、`--report`時のListenerTwinに接続した。
render後のpresentation音声とsample indexを読み、`listener_contour`として波形周期性、
局所的な予測誤差、静穏・欠落を報告する。`time_sec`は証拠を利用できた時刻であり、
`onset_sec`から同じ音高が持続していたという意味ではない。既存のListenerStateやDCC、
生成側へは戻さない。音声窓・FFTバッファを再利用し、音程・較正の履歴は各128件に制限した。
350音の検査で構築後の観測器のRustメモリ確保は0件だった。Rust 710テスト、
Python版との90条件の照合、MOTU M2での実機12条件を通過した。
実機はreport有効・DCC 0・seed 21、各10秒の測定窓で、最大hop p99は4.938 ms、
予算10.667 ms以内、出力不足・callback errorは0だった。

一方、通常の07・08・09・12を各3 seedでrenderすると、旧版との波形は全て一致したが、
新しい観測が採点できた音程は全12条件で0件だった。音が連続して重なり、無音で区切る
単旋律の前提が成立しなかった。受動接続の動作・負荷を確認した段階であり、通常の演奏で
有用な期待モデルになったとは扱わない。連続・重畳する実音の変化の観測、終止、生成への結合は未解決である。

続いて、重なる音のlog周波数powerを正規化した分布を、音源の音高へ割り当てずに予測する
研究用比較を行った。約85.3 msの後方窓を120 ms刻みで読み、過去128遷移を上限とする。
2窓文脈を、履歴平均、1窓だけの文脈、直前の分布が続くという持続予測と比べた。
開発54条件の後、係数を変えず、新たなseedの54条件を比較した。重なる反復には順序による
追加情報があったが、音色変更に弱く、移調でも常に1窓対照を上回るわけではなかった。
同じ通常12音源では10秒以降の1643窓を採点でき、履歴平均には全条件で勝った一方、
1窓文脈と持続の対照には全条件で負けた。この候補は研究用にとどめる。周波数質量は旋律輪郭
ではなく、実行成功も採用ではない。同じ録音の再解析を独立した条件に数えない。
次は予測の時間幅を明示し、変化がいつ起きるかと、変わった場合の方向を分けて比較する。
詳細は`docs/roadmap/phrase-expectation-evidence-2026-09-06.md`に記録する。

続いて、起点の周波数分布を固定し、Hellinger距離0.25以上へ初めて離れる時刻と方向を
分ける研究用比較を行った。時刻は0.36・0.72・1.44秒の期限、方向はlog周波数重心の
0.025 octの許容幅で定め、変化なしを含む10結果の確率を出す。最後の期限まで正解を
記憶へ入れず、途中に観測不能があれば採点から除外し、ファイル末尾を終止の正解にしない。
これらは比較の仕様であり、普遍的な知覚閾値ではない。開発45条件と追加seedの45条件を
実行し、一定音・音量・glide・分布の再配置・noiseの判定対照36条件は全て通過した。
通常音源では10秒以降の1499起点を採点し、1498起点で1.44秒以内の変化が生じた。
起点間で未来の音声を共有するため、独立した変化事象の件数ではない。
直近の変化量を含む全対照より時刻予測がよい通常条件は0、方向だけでは1条件だったが、
直前1窓に対する利得は0.000137 bitsにとどまった。追加seedの反復刺激も直前1窓に負け、
音楽的な反復を捉えた尺度とは扱えない。この候補も研究用にとどめる。次は局所的な揺れと
持続する音響状態の変化を区別する表現を設計し、その前にphrase・終止へ結び付けない。

次の研究用観測器は、20枚ずつの周波数分布を隣接する二つの集合として比べる。各集合は
2.4秒分である。平方根質量のGaussian kernelから、
[Grettonら（2012）の式5](https://www.jmlr.org/papers/volume13/gretton12a/gretton12a.pdf)の
経験的なbiased MMDを計算し、√2で割る。平均スペクトルが一致しても、音が交代する場合と
同時に鳴る場合の違いを残せる。別の経路でlog周波数軸上の平均質量の輸送距離を測り、
音域の移動量を保つ。平均スペクトル間のHellinger距離は比較対象とする。
いずれも音響特徴であり、音源の音高、順序の記憶、phraseのラベルではない。

固定閾値では新しいseedで誤候補が出た。改訂版は4枚ずつの10ブロックを5対5へ分ける
126通りを比べ、その経験分位点と距離の下限からヒステリシスの閾値を定める。
相関するブロックの比較であり、iidの有意差検定や較正された誤警報確率ではない。
二つの経路のいずれかが3回連続で高ければ候補を出し、両方が6回連続で低ければ
操作上の安定状態を確認・回復する。初期状態は未確立とし、観測欠落は開いた差の区間を
打ち切って文脈を切る。初めから変化中の入力を、新たに始まった変化と呼ばない。
差が落ち着く判定は窓間の比較が低くなる意味で、以前と同じ状態への帰還や終止感を意味しない。

開発54条件の後、実装を固定した未使用3 seedの27条件が通過した。意図した持続変化12回を
1.52〜2.52秒後に検出し、対照に余分な候補はなかった。平均スペクトルだけでは交代から
同時発音への変化を見逃し、分布の集合だけでは開発・追加の各1条件で音域移動を見逃した。
同じ通常12音源の結果は限定的で、6本は両経路での安定を一度も確認できなかった。
残る6本の最後の利用可能な比較では、5本は差の区間が開いたままで、後の観測不能により
打ち切られ、1本は安定状態だった。phraseの同定や予測利得を確認した結果ではない。
観測器はofflineにとどめ、条件名を隠した試聴で、音響の変化が別の音楽的なまとまりの
開始と対応するかを作者へ渡す。合成対照の通過でこの判断や生成への結合を代用しない。

続く試聴で作者は、Aは変わらず、Bは変わる兆候があるが結局変わらず、Cは変わるが
変化先は音が鳴り続けて動きがないと報告した。Cは交代発音を固定した4音の同時発音へ
置き換える条件だった。この提示での聞こえの区別として記録し、phraseの成立や変化先の
音楽的な受入へは広げない。次の対照は同じ周波数と交代の軌跡を保ち、1音ずつの交代から
隣り合う2音の組み合わせの交代へ移る。短い逸脱も対照に置く。観測器と閾値を固定し、
状態の同一性を保ちながら、その内部で動きが続く変化先を比較する。
新たな3 seedと元の提示seedを含む計7 seedで、この持続変化と短い逸脱を区別できた。
一方、追加した検査では既存の音域移動を2条件見逃し、観測器の一般的な有用性は未確認である。
失敗はそのまま保存し、今回の試聴では観測器を採用せず、変化先の流れを判断する。

この候補へ作者は「流れは変化せず、音はかさなる」と回答した。交代の軌跡を保ち、同時に鳴る
音だけを変えた刺激であり、この提示では重なりの変化と流れの維持を聞いた所見として扱う。
新しい流れやphraseの正例にはせず、音への選好も推定しない。音の重なりだけを変える対照として
残す。次の比較候補は、重なりと平均間隔を揃え、発音間隔の並びを変えるものとする。
この操作そのものを音楽的な流れの定義にはしない。

続く研究用の比較では、音色・音長・発音数・平均間隔・間隔の集合を固定し、
「短短長長」から「短長短長」へ並びを変えた。系列全体の循環移動は並びを保つ対照とする。
既存の音響flux候補にはreleaseやエネルギーの揺れも含まれたため、研究用観測では過去20 msと
40 msの両方でエネルギーが増えた候補を採用する。元の候補は保存する。観測対象はエネルギーの
立ち上がりであり、個々の音源ではない。同じ音量のまま起こるスペクトル変化は見逃しうる。

開発24条件と実装固定後の未使用seedの24条件で、全2730発音を余分な検出なしに照合した。
最大遅延は18.7 msだった。過去2間隔から次の対数発音間隔を観測前に予測し、過去1間隔・
順序なし・直前間隔の持続と比べる。並びの変更6条件では予測誤差候補が現れ、並び維持・循環移動・
音量・音色・重なり変更の30条件では現れなかった。交互列へ移った後は、より単純な過去1間隔の
対照が勝った。同じ通常12音源では冒頭10秒以降の313間隔を採点したが、すべての対照を上回る
条件は3本に限られた。探索的な予測利得であり、phraseの正解や人間の聴取との対応は得ていない。
観測器は研究用にとどめ、エネルギーと音数を揃えた各17.819秒・49音のA/Bを作者へ提示した。
発音の並びが変わったという聞こえと、この観測を照合する。

予測器を固定したまま5種類の並びと未使用3 seedへ広げた120条件でも、全7833発音を照合できた。
並びの変更15条件のうち、誤差候補が出たのは9条件だった。同じ間隔が3回続く列からの変更3条件は、
閾値が利用できても検出しなかった。非対称の3間隔列の変更3条件では、変更直後の3秒間に較正済みの
閾値がなかった。レポートでは判定不能と、判定できたが候補が出なかった場合を分ける。
一定回数の反復だけでは、較正に必要な文脈の支持が十分とは限らない。

通常の演奏も別の3 seedで12音源を生成し、297間隔を採点した。誤差候補を判定できたのは58間隔だった。
観測前に「同じsampleの全3 seedで、16間隔以上を採点し、3対照すべてへの平均利得が正」という
次段階への条件を固定したが、該当するsampleは0だった。これは実装候補を絞る条件であり、
統計的有意性や音楽的な受入の基準ではない。この証拠では、間隔予測器を本番へ接続しない。
合成音の発音を取得できたことは、通常演奏での有用性や、目的とする変化についての作者判断を代替しない。

続いて作者は「Bのみかわった」と回答した。提示情報と音源のhashを照合し、音色・音数・エネルギーを
保った間隔順序の変更の聞き分けとして記録した。選好、phraseの同定、終止感へは広げない。
次の観測仮説は、音響変化を一本の間隔列へまとめる前に、周波数位置と順序を保つことである。
現行の粗い3帯域だけでは、その区別を十分に作れない。新しいSample 12の3音源では、
採用した発音がすべて中帯域だった。この結果は比較の動機であり、予測の失敗原因を同定したものではない。

続く研究用観測では、周波数成分の軌跡と局所的なエネルギー上昇を保つ。約85.3 msの後方Hann窓を
10 ms刻みで読み、スペクトルのピーク位置を対数周波数へ補間し、最大32本の局所的な軌跡を追う。
3回連続して成分を観測し、隣接する各3フレームの平均でエネルギーが増えた場合を上昇とする。
倍音も別成分であり、軌跡のIDは観測上の識別子にすぎない。入力の欠落で連続性を切り、
観測開始時にすでに鳴っていた成分だけから上昇を作らない。

開発対照に続く未使用seedの27条件では、全591成分の上昇を70 ms以内に照合できた。
固定周波数の対照での最大誤差は0.317 centだった。弱い音が持続音へ重なる3条件では、
全体音量による観測が25回の発音時刻中1回だけを拾うのに対し、周波数ごとの観測は25回を拾った。
連続したglideの軌跡も、余分な上昇へ分割せずに保持した。同じ通常12音源でも軌跡を得たが、
旋律を分離した結果でも、新しい予測利得を得た結果でもない。

分解能と識別の対照では、330 Hzと338 Hzの持続音から一つの軌跡と21回の上昇が出た。
交差する二音からは5個の軌跡IDと6回の上昇が出て、定常雑音でも少数の上昇が出た。
この区別を表すため、最初の`attack`という出力名を`rise`へ改め、数値が変わらないことを照合した。
エネルギーの上昇は、音源が新しく鳴るほかに、干渉や成分が分かれて見える過程でも生じる。
次の予測比較へは時刻と周波数位置を残し、同時の上昇に恣意的な音源順序を与えない。
楽器の生成処理は変更していない。

続く研究用比較では、固定した30 ms以内の上昇を一つの集合とし、各集合のパワーで正規化した
周波数分布を保つ。次の対数間隔と対数周波数の同時密度を予測し、時刻・周波数の過去2組を使う
条件を、時間のみ、直前1組、順序なし、持続の対照と比較する。入力はすべて同じ観測で、
記憶は過去128遷移までとする。現在の集合が閉じてから、次の集合が始まる前に予測を発行する。
入力欠落やEOFでは保留中の証拠を打ち切り、時間の周辺密度と、時間を条件とする周波数密度を別々に採点する。

開発18条件に続く未使用seedの18条件で、1170組をすべて照合した。音高側の順序には時間のみの
対照を上回る利得があり、間隔側の順序も失われなかった。作者が聞いた既存A/Bの再解析でも、
両方の49組を照合し、Bだけで変更後の間隔の予測誤差が増えた。これは音響と予測の検査であり、
新しい試聴回答ではない。

同じ通常12音源では、冒頭10秒以降の2207組を採点した。時間のみを上回る同時密度の利得は
0.316〜0.795 bit/groupだったが、順序なしとの差は−0.00122〜0.00216にとどまった。
各音源で条件付き分布へ置く重みの中央値は1.57×10^-8未満で、順序ありのモデルもほぼ
順序なしの分布へ退避していた。事前に定めた全3 seed・全4対照に対する利得条件を満たすsampleは0で、
本番には接続しない。

続く比較では、絶対配置／相対配置と、過去2組／3組の4条件を分けた。相対化の基準は直前の組の
8個のlog2周波数分位点の平均であり、音源の音高とは見なさない。過去の予測対象を現在の基準へ移す
変換は、順序なしを含む全学習対照にも施し、持続予測と広い事前分布は共通にした。
相対的な動きを2回残すには3組が必要なので、履歴長の効果も別に比較する。

固定後の未使用seedの33制御音源では、2145組を照合した。単音で音域が移る並びでは、相対3組の条件が
同じ相対化を施した順序なしに対して1.351〜1.415 bit/groupの利得を得た。しかし、新しい通常12音源の
2258組では、4条件のいずれも全3 seedの利得条件を満たさなかった。順序なしに対する利得は、
相対2組で−0.01171〜0.00696、相対3組で−0.000374〜0.0000457 bit/groupだった。
類似履歴の支持は依然として乏しく、相対化や混合全体の履歴延長だけでは、今回の通常演奏の限界を解消しない。
この候補も本番には接続しない。

次の設計課題は、並びの予測に先立つ音のまとまり方である。
[Elhilaliほか（2009）](https://pmc.ncbi.nlm.nih.gov/articles/PMC2673083/)は、時間平均した周波数方向の
応答が似ていても同期・非同期の成分で流れの知覚が異なることを示し、時間的な共変動によるまとまりを提案した。
[Barascudほか（2016）](https://pmc.ncbi.nlm.nih.gov/articles/PMC4747708/)は音列の規則性への感度を
逐次的な統計モデルと比較したが、任意の多声音をどうまとめるかの検証ではない。
ここからの設計上の推論は、混合全体の集合列へ反復を求める前に、成分間の時間関係と複数の局所履歴を
比較することである。これらの研究は比較の根拠であり、Conchordalの音源分離、phrase同定、
今回の予測失敗の解決を証明するものではない。

この提案の一部を確かめるため、成分間の時間関係を研究用に観測する実装を追加した。
成分のパワーを55〜7040 Hzの113個の等間隔log2周波数binへ、幅0.075 octaveの正規化した
Gaussianで投影する。振幅を時定数40 msと200 msの因果フィルタへ通し、その差を変調特徴とする。
直近2秒の共分散から、十分なパワー・占有率・変調を持つ帯域の峰同士の符号付き相関を求め、
100 msごとに報告する。観測器の状態は有界だが、Pythonの記録用配列は音源長に応じて増える。
時間的な共変動に着想を得た工学的な診断であり、引用した神経モデルの再現や実時間性能の実証ではない。

固定後の未使用3 seedの24制御音源は観測条件を満たした。同時に活動する成分の相関中央値は
0.999〜1.000、交互では−0.441〜−0.407、異なる周期では−0.008〜0.003だった。
一定の持続音はエネルギーがあっても、変調の関係は求められない。入力欠落では状態を初期化し、
完全な2秒窓を再び待つ。持続する二成分へ共通の音量変調をかけるだけでも正相関が出るため、
相関からVoiceや音源数は同定しない。glide・vibrato・うなり・雑音も曖昧さを調べる対照として残す。

配置の比較で生成した通常12音源を再利用すると、4〜10秒の720報告中718件で相関を計算できた。
別途事前に定めた比較では、冒頭・末尾を避け、特徴の重ならない完全な2秒窓を用いた。
各窓で選んだ帯域の変調系列をそれぞれ独立に循環移動する99対照を作り、値と循環自己相関を保ちつつ
帯域間の時間位置を変えた。相関0.8以上の組数が対照の95百分位を上回る窓は、利用可能143窓中28窓だった。
完全な窓は150あり、該当窓を持つ音源は9本、09の3本にはなかった。これは時間位置の対応を調べる
記述的な比較であり、窓の独立性や多重比較を補正した有意差は主張しない。
順序予測の利得や知覚上の流れも同定していない。作者が聞いた間隔順序のA/Bには単一帯域内の時間関係が
必要であり、帯域間の相関はそれを補う観測である。本番には接続せず、局所履歴の予測、phrase、
終止、生成結合は引き続き未達である。

続いて、流れの個体識別を置かずに局所履歴の振幅予測を比較した。同じ113帯域の300 ms後の振幅を予測し、
目的変数が届いた学習例だけを最大128個保持する中心化したridge回帰を、32例から使う。
帯域ごとの20観測、2観測、近傍5帯域・2観測を使って全帯域の例から係数を共有する3条件を比較した。
各条件で、履歴を値の順に並べて時間順序を捨てた同じ次元の対照、直前の観測だけの回帰、過去平均、
現在値の持続を比べる。欠落では未採点の予測と学習例を破棄する。有界な研究実装であり、
本番の実時間コストは測定していない。

同じ通常12音源で、各条件2097時点を採点した。20観測の条件は全音源で直前の観測だけの回帰に負けた。
近傍の共有回帰はその対照より誤差を0.95〜8.53%減らしたが、同じ共有を施した順序なしの対照への
削減率は−0.04〜2.06%だった。全3 seed・全対照で5%以上削減する条件を満たす候補はなかった。
これらは開発音源であり、新しい検証用の通常音源は生成していない。後に全3条件の予測を再処理し、完全一致した。

実装固定後には未使用seedの42制御音源を3条件で比較した。指定した21反復条件では、長い履歴が
全対照に対して89.10%以上の誤差削減を得た。しかし、間隔を並べ替えた音源にも順序なしに対する利得が残る。
振幅の時間的な制約は、並びを変更しても残り得る。また、一定音での約10^-11規模の誤差の比率を
有用と判定しないよう、信号エネルギーに対する変化量の条件を追加した。通常音源の不通過は変わらない。
近傍の共有回帰には、定常入力でも現在値からずれる誤差が残った。
これらの結果を音楽的な順序、phrase、終止と扱わず、本番接続は見送る。
次の設計では、振幅の予測精度と音楽的な組織を同一視せず、作者が区別した間隔順序の変更と音響的な重なりを保つ。

## 9.3 上流への改訂

*   **DCCモデルとして扱う前に、選択を認知機構まで遡って根拠づける。** DCCは音響生成を
    聴覚認知へ直接根差させる原理である。実音の入力、因果的な予測、有界な保存だけでは、
    選んだ状態や時間尺度と認知との対応は確立しない。直近の回帰・間隔記憶の比較では、
    モデル族、履歴長、閾値は工学的な選択であり、特定した認知機構からの導出を済ませていない。
    続く可変長記憶の比較で既存の15変更条件全てに候補が出ても、この導出を補う結果にはならない。
    先に認知現象と、表現・保持・干渉・更新・時間尺度に対する実験上の制約を特定し、
    数学的な近似を選ぶ。そのパラメータと計算資源の上限を分ける。DCCだけから全ての数値が
    一意に決まるわけではなく、理想観測者としての予測成績も脳の実装を同定するものではない。

*   **音響的な予測可能性だけでは、音楽的な順序は同定できない。** 間隔を並べ替えても、
    平滑化した振幅には予測できる制約が残る。同条件で順序を捨てる対照と、作者が独立に聞き分けた
    違いを保持する。相対的な利得とともに誤差の絶対的な規模を示し、定常音の微小な変動への改善を
    音楽的な単位や生成への結合の根拠にしない。

*   **共変動は関係であり、音源の識別ではない。** 異なる成分にも共通の音量変更だけで相関が出る。
    一定の持続音や入力欠落は、独立した変調と同じ証拠を与えない。利用可否を明示し、帯域内の
    順序も残す。まとまりの候補は後続の予測に役立つかで検証し、帯域数や相関からVoiceやphraseを決めない。

*   **座標への不変性と、知覚上のまとまりを区別する。** 相対周波数は移動する並びを保てても、
    混合全体が順序予測に使える文脈を持つとは限らない。一つの並びや一定数の流れを先に置かず、
    混合後にも観測できる時間関係を確かめる。共変動は検証対象であり、Voiceの識別子ではない。
    座標変更の利得は、同じ変換を施した対照と比較する。

*   **予測に使う文脈が支持された割合を示す。** 条件付きのモデルは、ほぼ順序なしの分布へ
    退避したまま、弱い対照には勝てる。退避先そのものとの比較と文脈の支持量を示してから、
    利得を順序の記憶へ帰属させる。時刻と周波数を併用する予測では、時間と条件付き周波数の
    寄与も分けて示す。

*   **成分のエネルギー上昇と、音源の発音を区別する。** 周波数を残す観測は、全体の振幅では
    見えにくい弱い活動を取り出せる。一方、干渉、分解能、軌跡の交差も観測列に作用する。
    時間的な期待を組み立てる際にも、この区別を保つ。スペクトルの軌跡を追えたことだけでは、
    Voice、旋律の流れ、聴き手の音楽的な単位を同定したとは言えない。

*   **時間的な順序とスペクトルの変化を別々に観測する。** fluxのピークは、新しいエネルギーの
    立ち上がりがなくても現れる。一方、発音間隔の分布を保ったまま、その並びは変えられる。
    各観測が残す情報と捨てる情報を明らかにしてから、予測利得を音楽的な組織へ対応付ける。
    変更後の列は、より短い記憶で予測できる場合もある。誤差候補や音声ファイルの終端だけで
    終止を確定しない。
    較正済みの閾値がない場合は判定不能として示し、変化が起きなかった証拠にしない。

*   **状態の持続は、その内部の動きを許す必要がある。** 静的な変化先は持続変化の提示を
    容易にするが、作者はそこで動きがなくなると聞いた。動きが続く二つの状態間の遷移も
    対照に置く。隣接する集合間の差が低いことは、一枚ずつの分布が不変であることを要求しない。
    変化の検出だけでは、音楽の流れの持続、終止、変化先の価値は確定しない。続く動きのある
    比較でも、作者は流れは同じで音が重なると聞いた。同時に鳴る音の組み合わせの変化と、
    時間的な組織の変化を別々の対照に置き、前者の検出を後者の検出とは報告しない。

*   **音響統計の変化は表現の候補であり、音楽的な境界ではない。** 平均すると交代発音と
    同時発音の差が消える一方、順序を持たない集合では並び方が消える。移動量を扱うなら
    周波数軸の距離を保ち、各表現が捨てる情報を明示する。変化の開始を認定する前に、
    それ以前の安定状態を観測する。合成対照の通過で、通常sampleにこの前提が欠ける問題は解消しない。

*   **ほぼ必ず起きる変化を当てても、音楽的な期待の弱い検査にしかならない。** 短い時点予測を
    長めの初通過時刻の予測へ変えると、「変化なし」の対照に勝ちやすくなる。
    変化の経験的な頻度や直近の動きに対する追加情報を求め、時刻と方向を別々に採点する。
    テクスチャの頻繁な揺れを、新しい音楽的なまとまりと同一視しない。
    採点除外と、予測起点間の証拠の重複も記録に残す。

*   **順序の記憶は、音響の持続を超える情報を加える必要がある。** 履歴の平均よりよい予測でも、
    直前の音がそのまま続くという予測に負ける場合がある。同じ未来の証拠を持続予測と
    直前状態だけの予測で採点し、順序による利得を区別する。連続音から特徴を取り出せても、
    それだけでは有用な予測対象や生成への介入の根拠にならない。

*   **単音の対照で使える観測にも、通常の音楽では成立しない前提がある。** 無音を境に一つの音を
    取り出す方法は、音が重なり続ける通常sampleで一つの音響区間しか得られなかった。
    観測件数や実行成功を、その音楽を予測できた証拠としない。既存の流れを保ったまま、
    連続した重なりの中で何の変化を捉えるかを再設計する。観測器に合わせた拍送りや強制的な
    無音の導入、生成側の個体ラベルによる代用では、この知覚の不足を埋められない。

*   **波形の周期性と、音源の音高を区別する。** 強い短いlagの一致は、近接音の干渉からも生じる。
    複数周期で整合を検査しても、欠落基音の一音と同じ波形になる複数音源を識別することはできない。
    音声だけを観測するListenerTwinへ、生成側のVoice IDでこの不足を埋めてはならない。
    観測の対象と利用できない条件を明示し、音響特徴から旋律やphraseの同定へ進む検証を別に置く。
    また、受信していない音声は沈黙の証拠ではない。欠落を検出した時点で連続した文脈を切り、
    再接続時に既に存在する音を、新たに始まった音と扱わない。

*   **未知の文脈での誤差と、学習済みの予測からの逸脱を区別する。** 初期の輪郭予測器は、
    学習開始時の大きな誤差も較正履歴へ混ぜ、学習後の変更を見逃した。十分な類似文脈を
    観測してから出した予測だけで誤差を較正すると、同じ予測密度のまま変更の検出が改善した。
    支持条件も誤差の閾値も研究上の設定であり、人間の学習回数や確信度を確定する値ではない。

*   **予測利得、変化の検出、終止の知覚を分ける。** 短い音程の順序に予測利得があっても、
    変更後の反復を学習すれば平均利得は回復する。平均値だけでは変化時点やphraseの境界は決まらない。
    また、途中の休止と、その同じ無音中でファイルを切った入力は、共通する過去から同じ休止候補を生む。
    未来の再開やファイル終端を証拠へ混ぜず、作者が感じる区切り・終止との対応を別に検査する。

現行版の試聴判定（2026-09-06）：Sample 08の縮退確認へ作者は「縮退してない」と回答した。
直前の案内はseed 21・18〜24秒で、回答自体はseed・範囲を再指定していない。
08の自然さ、09の僅かなA選好、12の流れ維持と併せ、周期・音長を分離した現行設計を採用する。
この試聴での縮退回避を、全条件での関係持続や受け渡しの成立とは同一視しない。
以下の各実験への所見は、それぞれの版と比較範囲に残す。

実装の結果は、Manifesto の原理を裏づけながら、その機構レベルのスケッチを更新してきた。

*   **時間的な振る舞いの違いを、一つの周期追従則へ還元しない。** 周期追従・音長固定へのSample 08の肯定的な所見に続き、flowを使うSample 09では作者が基準版Aを若干自然と評価した。周期追従を外した対照は09の3 seedともAを完全再現した。これは作品と方式を跨ぐ改善が未確認という結果であり、flow全般で周期追従を禁じる結論ではない。共通の音響知覚を使うことと、全Voiceのペースを反復周期へ適応させることは別の設計判断として扱う。続く12には「流れは損なわれていない」と回答した。現行のプリセットではentrainedに周期追従を残し、flowは固有周期を基準とする不規則な更新と、音響による待機・見送りを使う。

*   **共有拍への応答と、相対関係の更新を区別する。** 音長を揃えた3 Voice・3 seedの固定位置対照では、1個体の休止後に残る声の発音時刻が変わっても、拍内の固定位置は保たれた。共有拍そのものが実音に応答するため、時刻変化だけでは自律的なニッチの証拠にならない。現行の参加方策はseed 42で相対位置を更新したが、seed 1では今回測った周期内の位置は安定しなかった。これは非周期的な関係の否定ではない。同じ拍位置への集中、異なる位置の維持、周囲に応じた関係の更新を別々に検査し、音楽的な受入と分ける。この18条件は研究用rendererでmetricの保持を固有周期へ揃えた比較であり、通常のmetricの音長規則を変更したものではない。`docs/roadmap/rhythm-perception-evidence-2026-09-06.md`を参照。

*   **参加周期への適応と、音の持続への適応を分ける。** 前版も固定の固有周期と実行間隔から学ぶ参加周期を別に持っており、その区別を固有ペースの不具合とした説明は不正確だった。学習を外すだけならSample 08の3 seedともWAVは同一だった。音響周期と音長を同時に追従させた版では音が変わり、作者は変更前の流れを好んだ。その後、周期追従を保って音長を固有周期に固定した対照へ「自然になった」と回答した。現行実装はこの分離を採用する。seed・再生範囲・基準版との明示的な選好は未指定であり、音楽的な最終受入とは扱わない。音長の変更は重なり予測と後続の音響入力も変えるため、出力包絡だけの効果には分解できない。以前の短時間・132秒の反復性の測定を、この版の音楽的な価値へ読み替えない。3帯域の音事象表現は個々の音源やその関係を同定していない。時間的ニッチの目的は共通の流れの中で異なる位置を保つ関係を含み、呼びかけと応答だけに限定しない。`docs/roadmap/rhythm-perception-evidence-2026-09-06.md`を参照。

*   **発音時刻の選択だけでは、参加の仕方を選んだことにならない。** 通常Sample 08の比較に対し、作者は「違いが感じられない」と回答した。比較時の方策は身体の発音予定の前後60 ms以内を探すが、周囲に応じて今回の発音を見送ったり、持続中の音を終えたりはしなかった。試験した3 seedとも発音間隔は約0.5秒を維持し、Sample 08は約1〜1.5秒の保持の後にreleaseへ入る。その重なりの評価は候補から0・40・80 ms先だけを読み、音の持続全体にわたる結果を比較していなかった。これは行動の表現と比較の限界であり、作者が差を感じられなかった知覚上の原因を同定した結果ではない。発音記録の変化と正常なruntime動作だけでは、時間的ニッチを聞き取れることを示せない。続く実装では「今鳴る・待つ・今回は見送る」を区別し、音の保持とreleaseを含む包絡近似へ拡張した。3 Voiceの短音・長音と1個体の休止・復帰を、3 seedで固有ペースの対照と比較した。参加方策の6条件すべてで、休止後に残る2個体が発音列を変え、固有ペースの対照は変化しなかった。休止あり・なしの実音は休止前まで一致した。この範囲の音響への応答は確認できたが、作者には各個体が発音時刻を変えるだけで、受け渡しとしては聞こえなかった。現在の方策は、どの音事象を受けて応じ、その後どう続いたかを表現していない。見送りや時刻の変位を強めるだけでは、次の受入条件を満たさない。全員の沈黙や等間隔化だけを成功扱いしない。知覚・身体・行動を分ける採用済みの方向は維持し、現在の方策の音楽的な受入は未達とする。詳細は`docs/roadmap/rhythm-perception-evidence-2026-09-06.md`を参照。

2026-09-06、作者は時間知覚・身体の発音準備・発音判断を分ける方向を採用した。
その後、実音だけを使う比較で42音源を2種類の事前条件から観測した。基準の交替・欠落条件では
onsetを検出できてもbeat確信度が低く、1.4 Hz・2.6 Hzのパルスでも、無指定の推定は40秒の
試験で初期値2 Hz付近に残った。これは試験した音声経路の限界であり、Sample 08の閉ループでの
縮退原因の寄与率を測った結果ではない。

オフラインの予測器は、過去の音の出来事を遅延させた履歴から予期を作り、各hopを観測する前の
成績を適応的なイベント率モデルと比較する。反復候補を切り替える混合モデルを、有界なRust実装へ
移植し、Python版との照合と新しい24音源での検証を行った。固定窓で観測するため、音声の受け渡しの
分割幅によって証拠は変わらない。音声の欠落は無音観測へ置き換えず、履歴を失効させる。
現在は生成側のhabitat音声へ接続している。反復周期は唯一のbeat/tactusではなく、モデルの
重みも知覚上の確信度ではない。不規則な更新過程で小さな負の利得が残っており、普遍的な優越性や
音楽的な受入の証明にはしない。入力、結果、限界、残るVoiceとruntimeの作業は
`docs/roadmap/rhythm-perception-evidence-2026-09-06.md`を参照。

最初の参加方策は、自分が参加した時刻の周辺で予期していた音の特徴を記憶し、身体の準備が整う
時刻の近傍を探す。固定音源への仮想的な参加では、共有の周期を選ばずに行動が変化した一方、
間隔を並べ替えた入力でも発音候補が集中する場合があった。短い共有文脈の記憶だけでは、
時間的ニッチの成立を示せない。

続く48条件の研究実装では、短い減衰音を合成し、その実音を観測へ戻した。
個体自身が作る音の予測を差し引いてから、3帯域のエネルギーの重なりを評価する。
単独の個体では競合の予測が0となり、自身の反復を避け始めない。個体の訪問順を変えても
発音と音声は完全一致した。同じ帯域の3個体では、記憶だけの方策に対して終盤の重なりが減るか、
既に小さい状態を維持した。一方、休止・復帰の1条件では増えたため、普遍的に改善する規則とは扱わない。
この近似は心理音響上のmaskingや協和的な融合の証明ではなく、合成経路もまだ通常のVoice・
PhonationEngineとは別の研究経路だった。続く通常runtimeへの接続を以下に記す。

通常のScheduleRendererは、参加するVoice自身の合成音と、そのhabitat寄与を再利用する
バッファへ記録する。観測窓の途中で誕生しても、誕生前を既知の無音として時刻を揃える。
退出時には履歴を破棄し、残るToneの尾部を、新たに同じバッファ位置を使う個体へ帰属させない。
この記録によって混合音は変わらない。PhonationEngineでは候補選択と実行を分け、gateなどで
拒否した候補から関係を学習しない。共有位相と処理単位を変えても発音・消音時刻が一致すること、
flowの更新、200 Hzの上限、実行中の時計更新を検査した。最初の通常Sample 08のレンダリングは
全12個体が発音して完走した。その後、Sample 08の参加あり・結合0とSample 09を各3 seedで生成し、seed 21を実機で検査した。
MOTU M2への接続を確認し、生成中の出力不足とcallbackエラーは0だった。hop p99は08が1.410 ms、
09が4.350 ms。今回の個体数と短い演奏の検査であり、長時間・多数個体や音楽的な受入へ一般化しない。

旧`AttackPhaseMatch`報酬は、自律的なアーティキュレーションのattackに作用する。
Gated発音では自律attackを無効化し、発行したonsetへ通常の協和度に基づく回復を倍率1で適用する。
参加方策を成立させる新たな代謝報酬は加えていない。共有リズムを読む音高制御や振幅変調は残り、
実音の評価ではそれらの作用も確認する必要がある。

*   **自己予測は行動の結果を比較するモデルに必要である。** 共有音には個体自身の音も含まれる。予期される全エネルギーを競合とみなすと、自分の反復も不利に評価する。研究実装では共有音と自己音へ同じ時間予測を適用してその作用を分けた。他個体の内部予定を知覚上の証拠にする処理でも、共通の拍を指示する処理でもない。波形が干渉する場合、エネルギーの差し引きは近似となる。この結果は次の生成接続に対する制約であり、音楽的な受入やmasking・融合モデルの代用ではない。

*   **小節の検出には、観測に基づく位相原点が必要である。** 比率と確信度だけでは、どの拍が強拍かは定まらない。累積したオンセットphasorの角度をunwrapped beat位相から引き、観測したアクセントへ強調位置を揃える。任意の強度倍率は発音強度を直接変えるが、その後の発音時刻や生存はフィードバックを通じて変わりうる。一様なオンセットを戻す対照では架空の小節を作らない。聞こえるまとまりの判定は作者に残る。
*   **周期の共有と、発音位置の共有は別である。** 再設計前のSample 08のseed 21・42について、作者は最後に一つの拍へ縮退すると報告し、意図しない限り音楽的でないと判断した。旧Kuramotoコアには個体固有の`phase_offset`が残るが、Gated発音ではその自律attackを無効化する。実onsetを作る`CouplingClock`の目標は共通beatとscriptの`microtiming`で、このsampleでは全Voiceが0である。初期位相のランダム化だけでは個体の位相ニッチを保持できない。Coupled経路は社会的なタイミング重みも迂回し、`avoid_neighbors()`は音高に作用する。接続の欠落は確認できたが、固定offsetの再接続は対照実験の候補であり、自律的な生息域の設計を完成させるものではない。位相空間の生息域を自律選択する機構が過去に完成していた証拠でもない。監査は`docs/roadmap/phase-niche-audit-2026-09-06.md`を参照。
*   **時間の参照枠と、音を出す判断を分ける。** 再設計案では、音から得る時間的な予期、Voiceの身体的な発音準備、発音・持続・延期・休止の選択を分ける。位相は周期的な関係の内部座標として使えるが、それだけで記号的な譜面になるわけでも、全Voiceに必須の状態になるわけでもない。時間的ニッチは周囲の音との文脈に依存した関係であり、固定された拍内の席ではない。共有の分析は証拠を供給しても、全員の行動を同じ位相へ強制しない。意図したユニゾンは有効であり、一律な位相反発も一般の音楽的な目的にはしない。上記の観測と仮想行動の実験で一部を実装したが、entrainedとflowの生成経路もこの分離へ変更した。作者のマクロなScenarioを置き換えない。`docs/design-notes/rhythm-temporal-niche.md`を参照。
*   **onsetの集中と、リズムの予測可能性を分ける。** 現行のbeat確信度は、検出したonset位相の一次の円周平均を使う。位相0とπに同じ重みの証拠があれば、二つの位置が反復していても相殺する。既存のsubdivision検出は高次成分を扱うが、`CouplingClock`の結合強度はbeat確信度を読む。この相殺は統計量の性質であり、Sample 08の縮退原因を定量的に分解した結果ではない。倍テンポの解釈が成立する場合もある。今後の評価では周期候補、相対的な発音関係、実際のpresentation音声を分け、単一パルスに加えて反復する欠落や複数の発音位置を検査する。位相補正後の内部同期指標も、位相分散の最大化も、音楽的な成功の証明にはならない。この区別は[missing-pulseの実験](https://pmc.ncbi.nlm.nih.gov/articles/PMC5490067/)と整合し、[位相推定モデル](https://journals.plos.org/ploscompbiol/article?id=10.1371/journal.pcbi.1009025)は不確実性と欠落を扱う候補になる。ただし、確定した神経機構として採用するわけではない。
*   **回復と自律的な再訪には別の証拠が必要である。** 固定座標のscanにより、刺激の退出と回復は直接確認できる。平均状態が横ばいでも、特定の領域が回復した証拠にはならない。2026-09-06の27条件では、温度・混雑回避を保ったまま移動Populationの適応だけを無効化すると、habituation off・通常回復・遅い回復の全9条件で再訪候補が消えた。設定差はこのフラグだけで、初期spawnは一致し、全5 Voiceが終盤まで生存・発音した。このfixtureの再訪はVoice固有の適応に依存し、共有Habituationだけによる帰還は示していない。適応が有効なとき、通常回復では退出後の状態低下を伴う候補が多いが、総再訪件数は回復時間に対して単調ではなかった。状態の低下、盆地の順位回復、実際の帰還を区別し、自律的な閉ループの総合判定はPARTIALを維持する。持続する基準音も測定区間を生き続ける必要があり、音高のanchor指定だけでは保証されない。詳細は`docs/roadmap/stage2-evidence-2026-09-05.md`の2026-09-06追記を参照。
*   **bus分離だけでは共有変調を固定できない。** Droneの振幅も共有リズムを読むため、presentationだけへ送るDroneは厳密な固定入力対照にはならない。DCCの研究試験ではscenario IR上の基準音を一定寿命のSeqGateとし、結合強度間でpresentation WAVとlistener観測の完全一致を検査する。最大temperature bonusが既定値0.1の範囲では、今回の強度比較から解決の改善を確認できず、結合の既定値0を維持する。

*   **専用の同期補助は、音楽上の選択である。** 専用の拍打ちは、共有meterを駆動する発音の証拠を変える。habitatだけへ送っても、その影響は残る。したがって、明示的な同期が実演・実験に不可欠な場合だけ使い、sample 12はcolony自身の発音でmeterを駆動する構成へ改めた。持続音による音高のanchorは、これとは役割が異なる。同期の成立だけでは、反復する伴奏の音楽的な価値は示せない。
*   **四帯域表から、改訂可能な時間モデルへ。** Manifestoはdelta/theta/alpha/betaの固定帯域に音楽的な役割を割り当てた。現在は確信度を持つ適応的なbeat–subdivision–measure階層を実装している（第4章）。ただし、振動子、比率の候補、確信度の統計量はモデル上の選択であり、普遍的な神経機構として確定したものではない。周波数帯域や数値の位相座標それ自体が、外から押し付けた譜面になるわけではない。設計上の論点は、証拠と局所的な行動をどう結ぶかにある。[テンポに依存した神経応答は振動子モデルと誘発応答モデルの双方で再現できる](https://academic.oup.com/cercor/article/35/9/bhaf258/8263582)ため、beat周波数の活動だけから機構を決めない。時間的ニッチの再設計は、認知に根差す生成という原理を保ちながら、これらの選択を再検討する。
*   **知覚上の対称性から、生成loopの不動点要件へ。** 地形操作が生態学的に意味を持つには、知覚機構が実在し、そこへ引き寄せられたエージェントの放射スペクトルがその地形を強め返さなければならない。現在の緊張制御は地形を歪めず、ecologyが作った地形を読む。運動の緊張はpitch-search *temperature*、配置の緊張は*相対的なconsonance level*である。`docs/design-notes/tension.md`を参照。
*   **rateの考古学から、観測可能な時間契約へ。** 生のエネルギープールと毎秒rateを公開すると、エコロジーが導出できる次元まで作曲家に手計算させることになる。実装アッセイにより、独立した契約は `initial_energy` や `energy_cap` ではなく公称enduranceだと分かった。エネルギーは $[0,1]$ に正規化でき、ゼロ適合時の消費rateはenduranceと不協和形状から導出できる。一方、毎秒の連続回復とアタックごとの回復は次元が異なるため、回復時間と離散割合として分離した。同じアッセイは `Entrain`・`Seq`・`Drone` の統合も退けた。オンセット時のリセット、死亡規則、telemetry、render modulatorが観測可能に異なり、単一コア化はenumをフラグの背後へ隠すだけだからである。
*   **曖昧なオブジェクト名から、寿命を担うオントロジーへ。** `Material`、`Participant`、公開ドラフト Population は、どれも自分が表す寿命を名指さないため、説明に例外を必要とした。現在の遷移は `PopulationSpec + Placement --place()--> Population` と明示され、その生きた構成員が Voice、Landscape を共有する全 Population の集約が Community である。集団方針は PopulationSpec に、ライブ操作は Population に属し、`place()` は創始 Voice を即座にスケジュールする。`Species` は遺伝と種分化が実在する不変条件を与えるまで予約する。命名は観測可能な同一性に支えられる。Population は構成員の死とリスポーンをまたいで残るが、Voice とその世代は残らない。

*   **音源の許容域と、知覚できる範囲は別の契約である。** 初期配置は代替の抽選も含め、指定Hz範囲を守る。anchorにしたVoiceは、音源の許容域内なら解析帯域外の周波数も保持できる。Field配置に解析帯域との共通部分がなければ、指定域内の対数一様分布を使い、そこでの協和性は主張しない。その後の自由な音高探索は、利用できるLandscape上で行う。
*   **音声の欠落は、証拠の欠落である。** 失われたhopの前後をつなぐと、実際には提示されていない音を分析することになる。Listenerの回復処理はスペクトル履歴を消去し、古い観測を無効化し、連続した分析窓が揃うまで圧力を止める。同じ欠落が決定的実行で起きた場合は終了する。固定遅延で進む実行では、回復待ちのまま次の入力を生成できないためである。
*   **Listenerの集計では、intensity密度を質量へ戻す。** スペクトルfront endはLog2Spaceの各binにERB密度$d_i$を保存する。Listenerの重みは、ERB積分幅$\Delta u_i$を掛けたintensity質量$w_i=d_i\,\Delta u_i$である。stabilityとbinごとの最大到達gainは$\sum_i w_i x_i/\sum_i w_i$で平均し、無音・観測証拠の判定にも同じ総質量$\sum_i w_i$を使う。以前の密度による加重を補正し、bin幅に由来する意図しない偏りを除く。協和性の係数とtension式は変えず、記憶や期待のモデルも追加しない。実装契約は`docs/design-notes/listener-twin.md`に記録する。

### 9.3.1 時間記憶の認知的制約と観測契約

状態は設計契約であり、認知モデルの採用ではない。対象は、長さの異なる音響的な間隔の
順序を保持し、次の事象を期待する過程である。作者の間隔順序A/Bは変化の聞き分けを
示すが、phrase境界の証拠ではない。以下の一次研究は異なる課題を扱うため、その数値を
一つの普遍的な記憶長へまとめない。

| 実験上の根拠 | 制約と限界 |
|---|---|
| [Teki・Griffiths（2014）](https://doi.org/10.3389/fpsyg.2014.01329)：間隔の再現 | 1〜4間隔の負荷増加に伴い再現精度が低下した。個数による全か無かの打ち切りだけでなく、保持精度を検証する。ただし、明示的な再現課題から自動的な順序記憶や忘却時定数は同定できない。 |
| [Andreou・Griffiths・Chait（2015）](https://pmc.ncbi.nlm.nih.gov/articles/PMC4389832/)：音列の終了に対するMEG | 来なかった音への反応は時間的文脈に依存し、速い不等間隔列を課題に関連させると符号化が改善した。次の発音なしでも待機を評価する必要がある。神経応答は音楽的な終止や普遍的な注意係数を確定しない。 |
| [Harrisonほか（2020）](https://doi.org/10.1371/journal.pcbi.1008304)：PPM-Decay | 並びの長さと提示速度は別々にモデルを制約する。15項目のバッファと半減期3.5秒は、速い音高列への手動適合値である。離散記号を入力として仮定し、音響からの抽出は対象外のため、今回の間隔観測へ数値を直接移植しない。 |

**表現の選択。** 数学的な対象として、観測時刻を持つ順序付きの聴覚痕跡、検索時の
不確かさ、次に観測できる事象の時刻分布を置く。これは要件からの設計上の推論であり、
上記研究が確立した神経実装ではない。絶対的な経過時間と並びの位置を別々に参照できる
構造とする。相対間隔を保持する場合も、記憶限界の検証に必要な速度情報を捨てない。
スペクトルの上昇は音響観測であり、そのままVoice、音符、記憶項目とはならない。
刺激の「短・長」ラベル、既知の記号集合、Scenarioのイベントは入力へ渡さない。
記号列のPPMは対照に使えても、この前段の不足を埋める実装にはならない。

**待機の観測契約。** 最後の発音から次に観測できる発音までの時間を`T`とし、
利用可能な履歴`H`から発行した予測の生存確率を`S(u) = P(T > u | H)`とする。
有効な観測が連続し、時刻`u`まで発音が来ていない場合、次の区間の発音確率は

$$
P(u<T\leq u+h\mid T>u,H)=1-\frac{S(u+h)}{S(u)},\qquad S(u)>0
$$

となる。未到来が続くことで得られる証拠は`-log2(S(u+h)/S(u))`という増分であり、
次の発音まで評価を待つ必要はない。発音と未到来は整合した確率で評価し、既に採点した
待機を二重計上しない。この確率の恒等式に認知上の時定数は含まれないが、予測分布自体の
根拠づけは必要である。将来の発音がない確率をモデルが持つ場合は、それを保持する。
有限の予測範囲だけで正規化し、必ず発音すると扱わない。予測の支持不足や範囲外は
利用不能であり、終止の証拠ではない。

未到来を観測したとするには、発音を検出できる条件が必要である。観測できない事象、
音声の欠落、無音を区別する。入力欠落は最後の有効観測で予測を打ち切り、EOFは終端事象を
追加しない。打ち切り前に得た証拠は残す。採点する発行済み予測は不変とし、後続の記憶更新は
その後の予測を変える。過去の予測を事後的に書き換えない。

**記憶と計算上限。** 文脈の長さ、学習例の保持、検索精度、保存容量は別の量である。
有限バッファの仮説と精度が徐々に低下する仮説を比較し、経過時間と介在事象の影響も
切り分ける。従来の6・128・32を改名するだけでは足りない。検索則とその適合値は未確定である。
重みを共通の係数で減衰させても、正規化すると効果が消える。

$$
\frac{a(u)w_j}{\sum_k a(u)w_k}=\frac{w_j}{\sum_k w_k},\qquad a(u)>0
$$

したがって、保存した重みの減衰だけでは予測の確信が低下するとは限らない。検索ノイズ、
精度の低下、事前分布に対する支持の低下は比較すべき候補であり、三つとも自動的に追加しない。

| 量 | 必要な根拠 |
|---|---|
| 保持時間・干渉への感度 | 特定した聴覚課題への適合、不確かさ、間隔順序への転用検証。処理hopや便利な窓幅からは同定しない。 |
| 文脈の項目数・検索精度 | 何を一項目と数えるかを定め、負荷と提示速度の影響を比較する。試験用の周期長を人の記憶容量としない。 |
| 観測刻み・予測範囲 | 時間分解能、遅延、範囲外に残る確率を示す。数値上の選択であり、phraseの長さではない。 |
| 保存する痕跡の上限 | 打ち切り則を明示し、認知パラメータを固定して上限を広げた結果と比較する。資源飽和を忘却と読み替えない。 |

**現行試作の反例と実装順序。** 保存済みの可変長記憶モデルに、合成した81回の発音観測と、
その後60秒の発音なしの有効観測を10 ms刻みで渡した。待機の報告はなく、履歴、文脈、
モデル評価の記憶、発行済み予測も不変だった。時間経過と未到来を扱う機構の不足を示す検査であり、
人の忘却曲線の測定ではない。再実行用の検査とソースのハッシュは
`target/phrase-expectation/2026-09-07-dcc-temporal-contract/`に保存した。

待機の観測契約は、既存の工学的な間隔分布を試験入力として、
`scripts/evaluate_temporal_waiting.py`にオフライン実装した。発音と未到来の条件付き尤度を
報告し、欠落前の証拠を保ち、入力欠落と録音末尾で予測を打ち切る。数値的に解像できない
分布の裾は利用不能とする。確率の分割、尤度の累積、過去出力の不変性、待機、欠落、
初期無音、不正入力と分布の裾を7テストで検査し、Python全157テストが通過した。
既存の実音由来の観測列2本では、それぞれ61発音と2440の発音なしの窓を採点し、
数値的な利用不能はなかった。ソフトウェアの検査であり、認知的な較正や新しい試聴の証拠ではない。

次に、事象数と経過時間、
観測した無音と入力欠落、中断後の再出現、既存の順序変更と重なりの対照を使って検索機構を比較する。
認知パラメータは特定した課題で適合させ、通常音源の評価へ進む。課題を越えた拡張は明記し、
波形の参照は因果的に保つ。境界・終止には追加の証拠が必要であり、別の出力とする。
生成への結合は受動観測の有用性と既存の負荷検査を満たした後とし、採用済みの時間的な参加を保つ。
この契約によって第2・第3・第4段階を完了とはしない。

### 9.3.2 公開された記憶制約の再集計

[PPM-Decayの解析アーカイブv0.2.0](https://github.com/pmcharrison/pdec-analysis-2/tree/v0.2.0)から、
速い音高列の課題の応答データとモデル集計値を取得した。
`scripts/evaluate_memory_constraints.py`で、参加者の除外、試行の採用フラグ、
参加者・ブロックごとのSTEP反応時間補正、条件内の外れ値除外を再現した。
元の12,000試行から、論文の記載と同じ23人・4,439件のRAND-REG正答試行が残った。
6条件それぞれの試行数も、保存された元のPPM出力と一致した。
原課題の再解析であり、新しい間隔記憶の実験ではない。

公開コードの「参加者内中央値を参加者間で平均する」手順で、6条件の反応時間を集計した。
既に適合済みの公開モデル出力との誤差は以下のとおりである。
モデルの再実行やConchordalのパラメータ適合は行っていない。

| 公開されたモデル設定 | RMSE：最初の一巡後の追加音数 | 除外されたモデル遅延 |
|---|---:|---:|
| 元のPPM | 7.844 | 0 |
| 指数減衰 | 5.782 | 0 |
| 検索ノイズを追加 | 3.030 | 22 |
| 5項目のバッファ | 3.376 | 25 |
| 10項目のバッファ | 2.539 | 15 |
| 15項目のバッファ | 0.973 | 22 |

これらは同じ参加者データへ調整された設定であり、設定間で複数のパラメータが変わる。
記憶機構を比較する根拠にはなるが、独立検証やバッファ容量だけの因果効果ではない。
原コードは無効なモデル遅延を集計から除くため、除外数を条件付きの誤差と併記する。
全設定に共通の文脈次数4と、適合されたバッファ15項目も別の量である。
後者を「過去15事象で予測するモデル」とは扱わない。解析が指定する`ppm` v0.1.1は
現在版と分けて取得し、Gitのblobハッシュを照合した。

**測定単位もモデルの契約に含める。** 一巡が同じ0.5秒でも、20音×25 msと10音×50 msの
参加者内比較は、追加音数では+6.579音、秒数では−0.0483秒となる。
異なる応答変数であり、矛盾した測定ではない。音数による一つの差だけから、保持時間の
打ち切りを同定しない。両単位を保持し、変化検出と反応を読み出す規則を明示して、
速度と負荷の全条件を評価する。

**集計法も区別する。** 保存された適合コードは参加者内中央値を使い、論文本文は平均を記載し、
描画コードにはさらに異なる集団集計がある。中央値経路と平均経路を両方保存した。
15項目の設定はどちらでも条件付きRMSEが最小だったが、参加者単位のbootstrapによる
交互作用の区間は、どちらも論文記載の区間を厳密には再現しなかった。
試行数の一致を統計解析全体の再現と読み替えない。bootstrapは試行でなく参加者を再標本化し、
NumPyの乱数列は原解析のRと異なる。

追加した解析の4テストと、Python全161テストが通過した。取得ファイル、ハッシュ、
両集計経路と全対照の数値は`target/phrase-expectation/2026-09-07-auditory-memory-evidence/`に保存した。
参照実装との数値照合は次節に記録する。これはモデル比較の根拠であり、
15項目・3.5秒を本番の既定値とする根拠ではない。

### 9.3.3 記憶モデルの数値照合と、長さの意味

`scripts/ppm_decay_reference.py`へ、[`ppm` v0.1.1](https://github.com/pmcharrison/ppm/tree/v0.1.1)の
研究用参照実装を追加した。バッファからの退出、痕跡の減衰、検索ノイズ、文脈の混合を再現する。
入力は離散記号とその提示時刻であり、パラメータは全て明示指定する。Conchordalの既定値ではない。
痕跡の保存量は原実装と同じく無制限で、認知上の検索制約と計算機の保存容量は別である。

元のC++の数値計算を保ち、Rの入出力とシリアライザーを置き換えて比較用に実行した。
参加者1の24条件から最初の試行を選び、公開6設定で144組を比較した。境界用の合成4条件を加えた
148条件・19,808予測で、確率の最大絶対誤差は`1.55e-15`、情報量の最大絶対誤差は`3.91e-14` bitとなり、
宣言した許容値`2e-12`以内だった。C++が生成した検索ノイズをPython側へ渡している。
この条件での数値一致であり、乱数生成器、Rの全API、反応時間の読み出し、人の認知特性の独立検証ではない。

**半減期は保持時間全体ではない。** 公開された実験3のバッファ設定では、時間による退出上限
`buffer_length_time`が1000秒になっている。15項目モデルの半減期3.5秒は、痕跡がバッファから
出た後に働く。両実装の検索ノイズを除いた重みを直接照合すると、1記号を保存した後に他の記号が
来ない場合、60秒後も重みは1のままだった。先に15記号が介在すると退出時に0.6となり、
そこから3.5秒後に0.3へ減衰した。人の無音中の保持を測った結果ではなく、その設定の性質である。
原論文の説明用Figure 10は時間上限2秒を使うが、項目による退出が先に起きる連続音の課題では
この差が現れない。どちらの値も音楽の休止へ直接転用しない。

**何が来るかと、いつ来るかを分ける。** この参照実装は、指定された提示時刻における記号の
種類を予測する。到来時刻の予測や未到来の採点は実装しない。間隔への拡張では9.3.1の
発行済み予測の契約を守り、実際に到来した時刻を使って過去の予測を書き換えない。
音響からの事象抽出、連続間隔の不確かさ、無音中の保持、介在事象による干渉は、原課題からの
拡張として検証を要する。数値一致だけではphrase・終止・生成結合のモデルを選べない。

原実装の予測ベクトルと検索ノイズ、時間と項目による退出、予測と学習の厳密な境界条件の違い、
当該記号を学習する前の予測、不正入力時の状態保持、無音中の保持例を7回帰テストで検査した。
Python全168テストが通過した。入力、実行ファイルのハッシュ、全出力、比較用の入出力実装を
`target/phrase-expectation/2026-09-07-ppm-reference/`へ保存した。

### 9.3.4 連続間隔への拡張と適用限界

`scripts/evaluate_interval_retrieval.py`へ、連続間隔を扱う研究実装を追加した。
完了した間隔を対数上の長さと観測可能になった時刻で表す。「短・長」のラベル、楽譜の事象、
既知の反復周期は渡さない。過去の文脈とその後に来た間隔を一組の痕跡とし、経過時間と
介在間隔数を別々に数えて強い保持からの退出を決める。退出後は検索重みを減衰させる。
保持範囲に収まらなかった文脈は使わない。

次数`k`では、連続値の文脈の類似度と保持重みの積を`a_j`とし、次の間隔の密度を
短い文脈による密度と混合する。

$$
q_k(y)=\frac{\sum_j a_j K_{\sigma_y}(y-y_j)+\alpha q_{k-1}(y)}
{\sum_j a_j+\alpha},\qquad y=\log_2(T),\quad \alpha>0.
$$

最短の文脈の下には、明示した幅広い事前分布を置く。全痕跡の重みが小さくなると、
事前分布に対する支持も小さくなるため、正規化で減衰の効果が消えない。
連続値の類似度と事前分布との競合は新しいモデル化の仮説であり、記号版PPM-Decayの数値再現でも、
同定された神経機構でもない。間隔再現課題での負荷に伴う精度低下、初頭・新近効果は未再現である。

全パラメータを明示指定する。開発比較では、容量4/8間隔、保持上限1/4秒、半減期1/4秒、
文脈の幅0.07/0.14（log2間隔）の16設定を使った。他は次数0〜4、退出後の重み0.6、
予測先の幅0.07、事前分布の平均−1・幅2（log2秒）、事前質量1とした。
感度を調べる設定であり、人の記憶特性の推定値でも、本番の既定値でもない。
保存量は無制限のままで、モデル内の保持容量とは別である。リアルタイム実装には移植していない。

作者が聞いた49発音のA/Bそのものを、既存の音響観測器で再解析した。両方の49発音を観測し、
刺激時刻からの遅延は6.29〜16.50 msだった。刺激の正解時刻は観測器の照合にだけ使い、
予測へは渡していない。全設定で変更前の出力が一致した。変更後3秒間の待機と発音を合わせた
予測誤差は、最も長い文脈まで混合した出力で、BがAより0.006〜1.090 bit/間隔大きかった。
その後のBでは、順序なしの予測より1.017〜1.247 bit/間隔だけ誤差が小さくなった。
設定による差は大きく、この一組から記憶時間を同定したり、変化検出器を較正したり、設定を選ばない。

**観測できない陰性対照は、正しい陰性ではない。** 作者が「流れは変化せず、音はかさなる」と
聞いた持続音の比較も調べた。試聴範囲9〜22秒では、一定の対照と重なりが加わる音のどちらにも、
採点できる間隔がなかった。したがって、両種の音をまたいで作者の区別を再現したとは言えない。
持続音には、その動きを表す観測と記憶の表現が必要になる。間隔モデルのパラメータ変更では、
欠けている観測を補えない。

発行済み予測は不変とし、待機と発音を整合した確率で採点する。入力欠落では予測を打ち切り、
介在事象数が不明になる痕跡を消す。欠落前の採点は残し、EOFを終止の証拠にしない。
連続値の照合、時間と項目による保持、事前分布への退避、全時間量の比例変換、未解像の裾も含め、
追加10テストとPython全178テストが通過した。比較計画、全出力、入力の同一性、両方の観測検査を
`target/phrase-expectation/2026-09-07-interval-retrieval/`へ保存した。
認知パラメータの同定、持続音の観測、保存量を制限した実装、生成への結合は未達である。

### 9.3.5 DCCにおける知覚・記憶・行動の契約

設計の根拠を三つに分ける。聴覚表現、保持、期待には知覚上の根拠が必要になる。
個体の固有周期、音響身体、活動傾向は作者が構成する生態に属する。分析窓、保存量、
更新頻度は数値計算の選択である。脳波帯域の名前だけでは時定数の根拠にならない。
Manifestoの四帯域への音楽的役割の割当ては、改訂可能な機構のスケッチとして扱う。

| 契約 | 保持する状態と更新 | 現行実装と不足 |
|---|---|---|
| P：連続した聴覚的証拠 | 時刻を持つ周波数・振幅の軌跡を保持し、観測された一定状態、活動を検出できない状態、入力欠落を区別する。発音や関係の要約は派生表示とし、平均や共分散が捨てる順序を残す。 | Landscape、DorsalStream、研究用の成分追跡に部品がある。Log2周波数ごとの履歴は共有音場とpresentationの本番観測へ接続した（9.3.44）。作者の持続音でも成分変動を観測できるが、知覚上のまとまりや流れの同定は未達。 |
| M：関係の保持と期待 | 不確かな履歴と関係の候補を保持し、経過時間と介在情報の作用を分ける。利用可能な証拠から予測を発行し、採点する予測は不変にする。周期関係が支持された場合に位相を派生させる。 | 反復予測、実測エネルギーによる予測係数の更新、二窓の経験文脈の記憶は本番へ部分接続した。過去の順序と観測範囲を保つ連続RMS履歴も、判断時の証拠へ接続した（9.3.36）。ただし三帯域への圧縮で失う周波数配置と並びがあり、関係記憶の十分な観測とはしない（9.3.43）。履歴からのエネルギー予測と反復予測の実測比較も参加判断へ接続したが、長い予測には悪化が残る（9.3.37）。各個体の発行条件で実測との対応と未採点件数も検査した（9.3.38）。細分化した帯域の履歴・連合・credit・帯域間予測は研究用（9.3.8–9.3.14）。本番NSGT履歴からの関係予測と、文脈を保持・再利用する候補も研究用で、一様な改善は未確認（9.3.45–9.3.46）。条件付き音響候補の保持と更新も、関係そのものの記憶を同定した結果ではない（9.3.25–9.3.31）。認知的な保持時間、干渉、事象の未到来による更新、流れの同一性、境界・終止は未達。分析窓や二窓の間隔をphraseの長さとしない。 |
| A：局所行動と観測された結果 | まず発音・待機の選択について、知覚的な結果を見積もり、身体状態と生態上の傾向へ照らす。候補の選択、実行の確定、実音による結果の観測を分ける。 | TemporalParticipationは候補選択、実行確認、発音後の二窓の実測文脈を分け、記憶を次の候補比較へ戻す（9.3.35）。共有の継続予測は同じ予測先の実音で更新し、周囲の音は既知の自己波形を除いて観測する（9.3.32–9.3.34）。発音・待機の身体条件付き予測と選んだ実音による更新は研究用（9.3.21–9.3.31）。経験文脈の保持は、介入効果や行動価値の学習ではない。長い予測の信頼性、局所予測の較正、行動価値は未達。 |

行動環境は、確定した音源ラベルがなくても、観測した粗さ・調波性と、支持された時間関係を使える。
生成側が知る自分の行動意図は予測に属し、ListenerTwinの観測はpresentationの実音に根差す。
相関をVoiceの識別子や、まとまりの較正済み確率として扱わない。観測範囲の不足を確信で埋めない。
協和、同期、予測精度を一つの普遍的な報酬へ集約しない。変化しない音でも予測は完全にできる。
融合、交互の参加、独自の活動、意図したユニゾンを許す生態とし、作品全体の方向はScenarioが担う。
長い文脈、反復・変奏、帰還を知覚・記憶し、局所行動へ反映する内部表現も必要になる。
P/M/Aは全時間尺度に適用する契約であり、それだけで各尺度の認知モデルを実装したとはしない（9.3.55）。

**最初に実装した境界。** `GeneratorModel`は、最新の観測より前、または宣言した予測範囲を
超える時刻には予測を返さない。観測が一つの場合も同じである。以前は、予測範囲が減衰の指数を
制限するだけで外挿距離は伸び続け、一つの観測だけでも無期限に値を返していた。
根拠のない次ゲートの予測は利用不能となり、既存の発音経路は中立のゲインへ戻る。
新しい観測が届けば範囲内の予測を再開できる。theta/deltaから求める現行の時間範囲は
工学的な規則として明示し、この修正を認知的な記憶時間の同定とは扱わない。

修正前に失敗した2回帰テストが修正後に通過した。Rust全712テストが通過し、7件はignoreのままである。
Clippyとreleaseビルドも通過した。現行08・09・12のseed 21は、修正前後のWAVが完全一致した。
限定した回帰比較であり、新しい作者試聴や実機負荷の検証ではない。

一定の対照とmoving_textureの実音へ、既存の連続観測器も適用した。試聴範囲9〜22秒の
各130報告すべてで成分関係が利用可能となり、選択成分はそれぞれ3〜4本、4本、打ち切りはゼロだった。
この表現では発音だけを入力とする観測不能を解消できるが、作者が聞いた流れの不変を同定したとは言えない。
次の記憶契約には元の順序付き軌跡と不確かさを残し、相関を認識済みの音の流れへ読み替えない。
仕様と検査は`target/phrase-expectation/2026-09-07-dcc-state-contract/`へ保存する。

### 9.3.6 記憶の長さを選ぶ前に、観測の順序を保つ

時間モデルへの入力は、スレッドの実行順序に左右されず、観測した音の軌跡を保つ必要がある。
従来の解析workerは各音声hopを解析していたが、処理待ちの一群から最後のLandscapeだけを送信し、
受信側のListenerも最新の一枚だけを使っていた。そのため、Listenerの順応が受信のまとめ方に依存した。
再現検査では24hopから一枚しか届かず、音と後続の無音をまとめて受信すると、その音による順応が
すべて失われた。記憶時間の調整では、欠けた観測を補えない。

生成側の現在状態とListenerの観測履歴に、別の配送方針を設けた。Listener解析は有効なLandscapeを
順序どおりに送り、runtimeは受信した全観測を処理してから最新のListenerStateを通知する。
観測された無音も含め、各hopで順応を`hop / fs`だけ進める。空の受信確認や入力欠落の通知から、
聞いていない音への曝露を作らない。既存の入力64hop・出力4件のキュー容量は維持する。
一度の受信処理は開始時点で待っていた件数までとし、解析側も待機群の取得件数を開始時の
キュー長で制限する。並行した送信によって一群が際限なく伸びる経路をなくした。
解析結果の送信は受信待ちになり得るが、受信側が閉じればworkerは終了する。
実時間処理で入力が溢れた場合は、既存どおりListenerの証拠を無効化し、NSGTの再充填を待つ。

出力容量1・4・24で全24枚のスペクトルを逐次解析と照合し、送信待ちの切断終了も確認した。
受信を1枚・3枚・24枚にまとめても、順応状態は完全一致する。欠落後の再充填検査は両配送方針で通過した。
Rust全714テストが通過し、7件はignoreのままである。Clippyも通過した。
これは観測順序と更新時計の契約であり、連続した関係の記憶モデルそのものではない。
未知の入力中の保持、知覚上のまとまり、境界・終止は未達である。計画と検証記録は
`target/phrase-expectation/2026-09-07-listener-observation-order/`に保存する。

releaseビルドも通過した。比較では全てのレンダリングでreportを指定し、Listener解析を有効にした。
08・09・12のseed 21は、修正前後のWAVとListener・DCC圧力・Populationの記録が完全一致した。
順応とDCCを有効にした2対照も各版で2回ずつ生成し、同一版の反復と版間の両方で、同じ出力が一致した。
固定遅延での通常動作を保ち、意図的に処理待ちを作る回帰検査で修正を検証した。
実機の実時間性能、認知的な妥当性、音楽的な改善を示す比較ではない。

### 9.3.7 帯域内の履歴と、帯域間の関係を並行して保持する

[Solliniら（2022）](https://pmc.ncbi.nlm.nih.gov/articles/PMC9411505/)は、短い音列の先行部分で
各帯域の規則性と帯域間の共変動を操作し、その後の音の検出への影響を比較した。
この結果から本設計では、各成分の継続と、成分間の関係を並行して保持する。
音源のまとまりを先に確定し、その後だけ順序を学習する構造にはしない。
各成分が規則的でも互いに揃わない場合と、全成分が同じ不規則な動きをする場合を、
同じ「同期・予測可能性」の値へ潰してはならない。これは本システムへの設計上の推論である。

**入力表現の見直し。** 論文の背景刺激の時刻構造を再構成し、上記二つの交差対照も追加した。
0.8秒の刺激9種・3 seedについて、起動直後と2.24秒の無音を観測した後を比較した。
両観測器は同じ保存済みPCM16を読む。2.24秒は研究用の240 sampleと本番の512 sampleの
観測位置を揃える計算上の選択であり、認知的な保持時間ではない。24 kHz、未較正の音量、
標的音なしという相違があり、人の検出閾値の再現とは扱わない。

既存の細いピークを追跡する観測器は、54条件全てで成分を検出しなかった。
本番NSGTの帯域出力ではエネルギーを取得できたが、帯域ごとの窓長と遅延が異なる。
同時に鳴る規則的な音でも、無補正の帯域間相関に負値が現れた。
したがって、粗さ・調波性の評価に使うスペクトルを、そのまま時間的な共変動の証拠にはしない。
検査用のRust出力は完全なhopだけを処理し、末尾の不足分へ無音を補わない。
FFT全体の窓が埋まったという表示も、各帯域の遅延や知覚上の確信を意味しない。

研究用に、[Hohmannのgammatoneモデル](https://doi.org/10.5281/zenodo.20745033)の
4次フィルタと1 ERBの帯域幅を用いる連続包絡観測を実装した。
中心周波数を\(f_j\)、標本化周波数を\(f_s\)として、伝達関数は

\[
E_j=24.7+f_j/9.265,\quad
r_j=\exp\!\left[-\frac{2\pi E_j}{(5\pi/16)f_s}\right],\quad
a_j=r_j e^{2\pi i f_j/f_s},\qquad
H_j(z)=\frac{2(1-r_j)^4}{(1-a_jz^{-1})^4}.
\]

Log2Spaceに沿う113帯域の複素応答から、1 msごとのRMS包絡を出力する。
ピークや発音の検出を前提にせず、一定音や観測された無音も残す。入力欠落は別の記録とし、
フィルタの近似状態を初期化する。未知の入力履歴を無音とは解釈しない。
4次と帯域幅は参照モデルに由来する一方、周波数グリッドと報告間隔は数値計算の選択である。
フィルタの減衰時間は聴覚的な前処理に属し、音楽的な記憶やphraseの長さではない。
この線形近似だけで、聴神経の応答や個人差を再現したとはしない。

原著者の変更していないC++フィルタ計算に、独立した係数設定とRMS出力の検査用コードを
接続した。24/48 kHz・2グリッド・3入力の12条件、90,270値の最大絶対差は
\(5.17\times10^{-15}\)未満だった。Matlab全体や合成処理の再現ではない。
実装を固定した後の未使用3 seed・54条件でも、規則性と共変動を分ける18比較が通過した。
250–500 msの窓における、5帯域の既知の50 ms遅れに対する相関と、1 kHz対他帯域の
同時刻相関は次の範囲だった。周波数と遅れは刺激から指定した検査値であり、推定した拍ではない。

| 入力 | 帯域内の50 ms遅れ相関 | 帯域間の同時刻相関 |
|---|---:|---:|
| 全帯域が規則的で同時 | 0.998–1.000 | 0.459–0.897 |
| 各帯域は規則的、相対位置はずれる | 0.998–1.000 | −0.503–−0.114 |
| 全帯域が同じ不規則な動き | 0.170–0.619 | 0.453–0.898 |

作者が流れの不変を聞いた持続音2本でも、9–22秒の各13,001時点で連続包絡を取得した。
これは入力の可用性であり、流れの同一性や音源のまとまりの認識ではない。
通常の発音処理とDCC結合は変更していない。計画、元のPCM、原計算との比較、全帯域の
観測は`target/phrase-expectation/2026-09-07-grouping-cue-pcm/`に保存する。
帯域内の継続と帯域間の遅れを持つ関係の記憶、その不確かさ、境界・終止、
実音の結果による行動更新は引き続き課題である。

### 9.3.8 時間位置が粗くなる履歴候補と、観測範囲の区別

連続包絡を受ける履歴の候補として、[Shankar–Howard（2012）](https://sites.bu.edu/tcn/files/2015/12/ShankarHoward12-NeuralComp.pdf)
の時間表現を研究用に実装した。原モデルは、過去の入力をLaplace変換し、Postの近似逆変換で
時間位置へ戻す仮説である。時間位置の広がりが経過時間に比例し、単一の周期を必要としない。
この性質を履歴の候補として使うのであり、旋律・phrase・formを同じ機構へ還元する意味ではない。

現在を\(t\)、過去への遅れを\(d\geq0\)、各履歴セルが応答の頂点を持つ遅れを\(\tau>0\)とする。
Post次数\(k\)に対する出力核は、原式2.2と2.3から

\[
s=k/\tau,\qquad
K_{\tau,k}(d)=\frac{s^{k+1}d^k e^{-sd}}{k!},\qquad
T_j(t,\tau)=\int_0^\infty K_{\tau,k}(d)f_j(t-d)\,\mathrm{d}d
\]

となる。核の積分は1、遅れ方向の平均は\(\tau(k+1)/k\)、標準偏差は
\(\tau\sqrt{k+1}/k\)である。大きい\(k\)ほど時間位置が細かくなる。
これは入力の時刻を近似する幅であり、音長、発音周期、検索された記憶の寿命ではない。

実装では、この出力核と等価な\(k+1\)段の連続時間cascadeを使う。原論文の神経回路や
有限差分による逆変換の実装そのものではない。各観測間隔では包絡を一定と近似し、状態遷移を
解析的に積分する。同じ一定入力を細分しても結果が変わらない。小さい入力係数は正の級数で
計算し、1から近い値を引いて既知の微小な寄与を消す数値的な桁落ちを避けた。

観測範囲には、原モデルにない追加の契約を設ける。観測できた区間を\(q=1\)、欠落を\(q=0\)とし、
\(K*(qf_j)\)と\(K*q\)を別々に保持する。前者は既知の入力による寄与、後者は核のうち
観測で覆われた重みである。後者を知覚的な確信や、欠落した音についての確率とは扱わない。

| 入力の状態 | 既知の入力の寄与 | 観測で覆われた重み |
|---|---|---|
| 観測された無音 | 既存の履歴を時間に沿って更新し、新しい音の寄与は加えない | 無音を観測した区間を加える |
| 入力欠落 | 同じく既存の履歴を更新し、未知の音の寄与は作らない | 欠落区間を加えない |

欠落だけを理由に古い履歴を消去しない。両者の既知の寄与が等しくても、観測範囲は異なる。
報告を読む操作は状態を進めず、入力ファイルの終端を区切りや終止には変換しない。

今回の例では、原論文の図示にも使われる\(k=12\)と、0.125–16秒の8個の対数間隔のセルを
明示的に指定した。これらを作者や聴覚課題へ適合した値とはしない。1 msごとに全観測を更新し、
10 msごとに履歴を報告する。113帯域と観測範囲を含む状態本体は11,856スカラー、94,848 byte。
係数と作業領域は別に必要であり、実時間性能を保証する値ではない。保存する状態の大きさは
演奏時間に伴って増えない。

解析的なstep応答、独立した核の数値積分、入力区間の分割、時間の比例変換、同じ現在と
同じ活動量で順序だけが異なる入力、無音と欠落を検査した。既存音源58本・199,318観測にも
適用した。共通の末尾を持つ短音の6組では、最後の包絡が完全一致しても先行部分の違いが
履歴に残った。作者のA/BはPCMが異なり始める6.544041…秒まで履歴も一致し、最初の報告差は
6.55秒に現れた。これは因果的な更新の確認であり、変化の検出遅延やphrase認識ではない。
持続音2本への適用も、作者が聞いた流れの同一性を判定した結果ではない。

Python全201テストが通過した。Rustと標準バイナリは前節の検証時点から変更していない。
全入力、出力、明示したパラメータ、検証記録は
`target/phrase-expectation/2026-09-07-temporal-history/`に保存する。
帯域内・帯域間の連合は次節で扱う。項目間の干渉と競合を伴う検索、保持範囲と精度の同定、
予測の有用性、境界・終止、生成への結合は未達である。

### 9.3.9 時間履歴からの連合と、到来前に発行する想起

[Shankar–Howard（2012）](https://sites.bu.edu/tcn/files/2015/12/ShankarHoward12-NeuralComp.pdf)
の2.4節、式2.11–2.14の連合則を研究用に実装した。現在の帯域入力を\(f_i\)、
帯域\(j\)の時間履歴を\(T_j(t,\tau)\)とすると、原式は

\[
M_{i,\tau,j}(t)=\int_0^t f_i(u)T_j(u,\tau)\,\mathrm{d}u,
\qquad
p_i(t)=\sum_j\int M_{i,\tau,j}(t)T_j(t,\tau)g(\tau)\,\mathrm{d}\tau
\]

である。\(M\)は履歴と後続入力の共起を加算し、\(p\)は履歴から想起される活動を表す。
ここでは原論文の行動課題と同じ\(g=1\)を使う。対数間隔のセルを等しい重みで合計すると
別の測度になるため、秒単位の台形積分重みを使う。有限の時間範囲と離散化は数値近似である。

観測間で一定と近似する入力に対して、学習に使う\(\int T_j\,\mathrm{d}t\)も解析的に積分する。
区間末尾の履歴をそのまま区間全体へ掛けない。一定入力の区間を細分しても連合と観測範囲の
積算が一致する。既存の履歴の区間末尾の値は変更していない。

既知の寄与に対する追加契約は前節を引き継ぐ。入力欠落では連合を加算せず、既存の連合を
消去しない。観測された無音では音の共起を増やさないが、観測範囲を増やす。
各遅れについて\(\int q(t)(K_\tau*q)(t)\,\mathrm{d}t\)を別に保持し、現在と履歴の双方が
どれだけ観測で覆われたかを記録する。連合や想起をこの値で割る補正は行わない。

想起は、次の入力行の種類・時刻・内容を読む前に保存する。対象は次の1 msの観測区間であり、
1 msを認知的な予測時間と同定したわけではない。到来後の入力は別の結果として保存する。
欠落が判明した場合は発行済み予測を採点不能とし、EOFでは未決のまま残す。EOFを無音や終止へ
置き換えない。\(i=j\)の寄与と\(i\ne j\)の寄与を分けて報告するが、帯域間の関係は
Voice間の関係とは限らない。一つの音の複数成分も帯域間の連合を作る。

独立した時間積分、一定入力の分割、時間方向の積分格子の収束、単一の手掛かりと後続音の
学習、無音と欠落、発行後の不変性を検査した。理想的な瞬時入力対の連合を\(g=1\)で検索すると、
想起の頂点は学習遅れ\(D\)そのものではなく\(kD/(k+1)\)になる。有限幅の手掛かりを用いた
0.25・1・4秒の対照もこの位置へ近づいた。時間セルの位置と想起の時刻は同一ではない。

既存12音源・110,998観測を処理した。11,110件の事前想起のうち11,100件に観測結果があり、
10件はEOFで未決となった。無音の前置きを変えた4組で想起の既知の寄与は完全一致し、
観測範囲は異なった。作者A/Bの想起は共通PCM区間で一致し、最初の数値差は6.55秒だった。
持続音2本も共通区間の12秒まで一致した。どちらも因果的な更新の検査であり、流れの変化や
同一性を判定した結果ではない。

実音への適用では\(k=12\)、0.0125–16秒の65セル、113帯域を明示した。範囲は既存刺激の
短音と長い文脈を計算で扱うための設定で、認知的な保持時間は未同定である。連合本体は
829,985スカラー・6,639,880 byteで演奏時間によらず一定。履歴・作業領域・出力記録は別に必要となる。
Python全211テストとRust全714テスト（8件ignore）が通過した。本番のRustと標準バイナリは
変更していない。入力・全出力・数値検査は`target/phrase-expectation/2026-09-07-temporal-associations/`に保存する。

この加算則には連合の忘却、競合、反証による弱化がない。履歴の時間精度が粗くなる性質と、
学習済み連合の寿命は別に扱う必要がある。また、入力振幅を\(a\)倍すると\(M\)は\(a^2\)倍、
想起\(p\)は\(a^3\)倍になる。\(p\)は確率でも予測音量でもなく、直接DCC圧力へ渡せない。
競合の候補は次節で検査する。反証による関係の更新、到来確率の較正、認知パラメータの同定、
通常演奏での有用性、phrase・終止、局所行動と実音の結果を結ぶ学習は引き続き未達である。

### 9.3.10 手掛かりの競合と、率を到来確率へ読み替えない制約

[Goh・Ursekar・Howard（2022）](https://pmc.ncbi.nlm.nih.gov/articles/PMC8944185/)は、
過去の複数の手掛かりによる予測の二重計上を抑えるcreditの学習を提案している。
この計算を研究用に独立実装した。入力は種類を識別した離散事象の履歴と、学習済みの事象間の率である。
前節の連続包絡の振幅を、そのまま事象数や事象率と解釈して接続したわけではない。

事象\(i\)の基準率を\(\Lambda_i\)、手掛かり\(j\)のcreditを\(C_{ij}\)とすると、
原式2.2–2.3は、過去の履歴を新しい事象なしに\(\delta\)先へ移した\(\widetilde T_j\)から

\[
P_i(\delta;t)=\Lambda_i\exp\!\left[
\sum_j\int C_{ij}(\tau)\widetilde T_j(\tau;t+\delta)\,\mathrm d\tau\right]
\]

を計算する。手掛かり\(x\)が現れる直前の予測を\(P^-\)、その手掛かりから得る率を\(m\)とし、
原式2.7は\(G=e^C\)を\(G\leftarrow(1-\alpha)G+\alpha m/P^-\)で更新する。
同じ結果が既に強く予測されていれば、追加の手掛かりに与えるcreditは小さくなる。
\(C\)を対数の比へ算術的に近づける更新とは異なる。

実装はこの更新をlog領域で計算し、極小の率や大きな比も中間の指数化なしに保持する。
外から渡す投影済みの履歴、事象間のlog率、基準率、学習率、有限の時間格子を明示する。
学習率と時間範囲は認知課題へ適合した値ではない。有限範囲に入る単位事象の核の重みも返し、
端で欠ける重みを無断で正規化しない。基準率を返す中立のcredit、同時手掛かりの幾何平均、
既存予測によるcreditの減少、固定遅れの解析解、格子の収束、出力の不変性を検査した。

[原著者の公開コード](https://gitlab.com/varunu/leaky-integrator-credit-learner)の
`9d0f2122b41f227fd9be6fa7dfd3dabf1d89a829`と、同じ入力テンソルを使って計算を照合した。
単位事象の核を解析式で共有し、履歴の投影は原コードが作ったものを両方へ渡す。
432条件・64,800個の事前率と手掛かり由来率が最大相対差\(7.79\times10^{-14}\)未満で一致した。
これは計算部品の照合であり、原コードの逆変換回路、初回事象の特別処理、gradient clipping、
学習全体、論文の行動課題を再現した結果ではない。

creditの更新は79条件で原コードと一致しなかった。原コードは分母に固定値\(10^{-20}\)を加え、
\(G\leftarrow G+\alpha(r-G)\)と計算する。極小の事前率では分母補正の効果が大きく、
\(\alpha=1\)では小さな正の更新先が減算で0へ丸められる。後者は199要素で生じ、新実装には
生じなかった。最初の照合失敗と全出力を保存し、原式と原コードの数値上の処理を分けて記録した。
別の格子検査では、原コードの逆変換近似の誤差が17セルから200セルへの細分で減少した。
公開tutorialと同じ時間範囲・200セルを使う投影対照にも、解析核に対して約0.170の相対L2差があった。
これを論文全体の無効性や、全設定での誤差率へ一般化しない。既存のcascadeによる履歴は変更していない。

二つの反例を次段の制約として残す。第一に、手掛かり由来の率\(m\)に使う原式の正規化は、
固定遅れでは解析解に一致するが、一般の率を較正しない。\(k=8\)で一定の事象間率0.05を渡すと、
十分広い時間範囲でも出力は約1.7757倍になった。単純に\(m\)を正しい到来率と扱えない。
第二に、毎回1回の事象がある条件と、半分は0回・半分は近接した2回がある条件では、
平均回数は同じ1でも、1回以上起きる確率は1と0.5で異なる。後者の2回を1 ms離した対照では、
両条件の事象間の予測曲線の相対L2差は\(1.99\times10^{-6}\)未満だった。
平均率だけでは未到来をどう解釈するかを決められない。原論文5.2節も回数と共同統計の限界を述べている。

さらに、原モデルは定常統計を前提とし、事象間の関係が安定した後にcreditを学ぶ構成を推奨する。
creditの競合調整を、予想した結果の不在による関係の修正や、新しい文脈の獲得と同一視しない。
次は、連続音から何を事象として観測するかを明示し、回数・未到来・文脈を条件とする期待と、
関係の更新を検討する。phrase・終止や音楽的な価値は、この計算から自動的には定まらない。

Python全219テストとRust全714テスト（8件ignore）が通過した。本番のRust、標準バイナリ、
前節までの聴覚入力・履歴・連合の実装は変更していない。新しい試聴は行っていない。
参照版の情報、計算入力、全出力、反例、検証記録は
`target/phrase-expectation/2026-09-07-credit-assignment/`に保存する。

### 9.3.11 連続した観測と、文脈の更新を分ける

9.3.10のcreditは、種類と回数を識別済みの事象を要求する。この要求を満たすために
包絡の各フレームを一つの発音へ変換すると、観測の刻みが音数を決めてしまう。
短音の到来と持続音の変化を両方扱うには、次の区別が必要になる。

| 単位 | この設計での意味 |
|---|---|
| 連続観測 | 時刻を持つ帯域包絡とその軌跡。一定の音と音の弱まりも観測になる。フレーム数は事象数ではない。 |
| 音響事象の仮説 | 発音・消音・再発音などの候補。観測範囲と識別の不確かさが必要で、包絡の上昇だけからVoiceの発音を確定しない。一般の重なりに対する個数分布は未実装。 |
| 統計的な文脈 | 観測の続き方を説明する関係と、その関係が継続している範囲の候補。候補を新設する計算回数は、検出した出来事の数ではない。 |
| 音楽的なフレーズ・終止 | 時間関係、まとまり、期待の充足を含む別の推論対象。音響変化、予測誤差、文脈更新の一つを境界の正解としない。 |

[D-REX（Skerritt-Davis・Elhilali、2021）](https://engineering.jhu.edu/lcap/data/uploads/pdfs/jneurometh2021_skerritt.pdf)
は連続値の特徴量に対して複数の文脈を保持する枠組みを示す。同じ軌跡でも時間依存を表現するかで
文脈の解釈が変わる。先に全ての音を事象へ分割しなくても期待を更新できるが、音源分離やフレーズの成立は保証しない。

研究用に、帯域ごとに独立した文脈候補を持つBayesian autoregressionを実装した。
数理上の参照は[オンライン変化点推論](https://arxiv.org/abs/0710.3742)と
[自己回帰を含む拡張](https://proceedings.mlr.press/v80/knoblauch18a/knoblauch18a.pdf)である。
D-REXのコード移植ではない。各候補で、指定した遅れの観測から次の値を予測し、係数と分散に
normal–inverse-gamma事前分布を置く。係数・分散を積分したStudent分布を文脈の重みで混合し、
観測後にその尤度で重みと十分統計量を更新する。一帯域の変化が全帯域のリセットを強制しない一方、
帯域間のまとまりは説明しない。9.3.8–9.3.9の履歴・関係の保持と区別する。

文脈の更新率\(\lambda\)に対し、前回の学習から\(\Delta t\)後までに更新が起きる事前確率は
\(h=1-\exp(-\lambda\Delta t)\)。これはモデル内の仮定で、DCCから導かれた普遍的な値ではない。
観測後の新文脈への重みは尤度に依存する。一定の\(h\)自体を変化の検出確率としない。
入力欠落では十分統計量を保持し、次の予測で経過時間を事前確率へ反映する。
必要な直近の遅れが再び揃うまでは条件づけ用の履歴だけを補充し、予測を採点しない。
欠落をまたぐ新文脈には、確定した開始時刻に代えて、更新が起き得た区間を付す。

資源上限を超えた場合は、帯域ごとに重みの小さい候補を除き、その確率質量を記録する。
候補数の上限は記憶の寿命ではなく、古くても支持された候補は残る。各時点の小さな除去質量は、
系列全体の小さな近似誤差を保証しない。予測は次の入力を読む前に保存する。
観測した低音量やゼロは学習へ入り、欠落とEOFには未観測の結果を作らない。
出力は指定した観測量に対する尤度であり、発音の到来確率ではない。連続部分の密度は1を超え得るので、
その負の対数も負になり得る。最初のGaussian系の候補は非負の包絡にも実数全域の密度を置き、
作者の4音源では帯域・時点で平均した負領域の質量が0.304〜0.403だった。この失敗を保存した。

包絡用には、厳密なゼロへの確率質量と、正の値へのlog-Student密度を分けた。
各文脈の正値率\(\pi\)にはBeta事前分布を置く。正の観測\(y\)には
\(\pi\,t_\nu(\log y;\mu,s)/y\)、ゼロには\(1-\pi\)を使う。
正値・ゼロの回数は観測フレームの十分統計量で、音の到来回数ではない。
ゼロでは率と文脈の重みだけを学び、対数回帰へ架空の値を入れない。正値の回帰は生の対数を使い、
音量閾値やepsilonによる切り上げは追加しない。ここでのゼロはフィルタ計算上の値で、知覚的な無音を意味しない。
予測の位置パラメータは正値の対数領域を表す。log-Studentの正値平均は発散するので、
それを「期待振幅」へ指数変換して返さない。区間確率を生成の結果評価へ使えるかは別の検討を要する。

短い系列の全ての文脈分割を列挙し、各区間の回帰を一括で積分した計算とオンライン更新を照合した。
遅れなし・1遅れ・2遅れで予測密度と文脈の重みが一致した。無音と欠落、帯域の独立性、時間単位、
候補制限、予測の不変性、入力集約と末尾の打ち切りも検査した。ゼロを含む系列でも、全分割を列挙した
Beta積分と回帰の積分に一致し、ゼロの質量と正値の密度の合計が1になることを検査した。
設定は50 ms集約、遅れなし／1／4観測、更新率0.2/s、32候補、明示した回帰事前分布と
正値・ゼロそれぞれ0.5の事前観測数で固定した。いずれも認知課題への適合値ではない。
同じ作者4音源と通常12音源を再解析し、3候補で27,094時点、正値2,733,757件とゼロ327,865件の
帯域観測を採点した。候補間では最長の初期履歴を補充した後の同じ対象時点を比較する。
通常12音源では1遅れが全てで遅れなしを上回ったが、4遅れの追加は一様な改善にならなかった。
短時間の音響的な連続性による利得と、音楽的な順序の理解を区別し、本番へ接続しない。
比較と失敗の記録は`target/phrase-expectation/2026-09-07-context-expectation/`に保存する。
音響量の予測から事象の個数・未到来・音楽的な期待へ進むには、観測と出来事の対応がさらに必要になる。
予測誤差の最小化を個体の共通報酬とせず、局所行動と実音の結果を結ぶ課題を残す。

### 9.3.12 同時の共変動、過去からの予測、流れの同一性を分ける

他帯域の過去から次の観測を予測できることと、同時に変動する帯域がまとまって聞こえることは、
異なる関係である。[Elhilaliら（2009）](https://engineering.jhu.edu/lcap/data/uploads/pdfs/neuron2009_elhilali.pdf)
は、同期した音と交互の音の知覚を比較し、複数の速度でのtemporal coherenceを使うモデルを示した。
共通変調はこの種のまとまりの手掛かりにもなるため、予測の交絡を調べる際にも元の共変動を捨てない。
一方、[Skerritt-Davis・Elhilali（2021）](https://engineering.jhu.edu/lcap/data/uploads/pdfs/jneurosci2021_skerritt.pdf)
の音高・音色／位置の統計的変化の課題では、特徴間の特定の相関を持たない刺激についても、
特徴ごとの期待と後段の統合を扱う。そこでの特徴次元と、ここでの聴覚フィルタの周波数帯域は同一ではない。
両研究から本設計へ導く制約は、共変動、予測情報、知覚上のまとまりを一つの値へ統合しないことである。
これは設計上の推論であり、両論文のモデルを移植したという意味ではない。

9.3.11の研究予測器に、他の帯域の過去を条件として使う経路を追加した。
各対象帯域について、次の四候補の予測を観測前に発行し、同じ実測値で採点する。

| 候補 | 回帰へ渡す過去の観測 |
|---|---|
| own | 対象帯域 |
| partner | 対象帯域と相手帯域 |
| background | 対象帯域と周囲の包絡要約 |
| both | 対象帯域、周囲の包絡要約、相手帯域 |

周囲の要約は、組に含む二帯域それぞれの中心から±1 ERBを除いた帯域のRMS包絡から作る。
観測された共変量であり、真の共通原因を同定した値でも、独立な帯域の物理的な総パワーでもない。
9.3.11と同じゼロの質量と正値密度を使い、相手の追加による利得を

\[
\Delta_{j\to i}(t)=\log_2\frac{q_{\mathrm{partner}}(y_i(t))}{q_{\mathrm{own}}(y_i(t))},\qquad
\Delta_{j\to i\mid b}(t)=\log_2\frac{q_{\mathrm{both}}(y_i(t))}{q_{\mathrm{background}}(y_i(t))}
\]

として併記する。正の利得は、この入力・条件・モデル族での予測上の改善を表す。
因果的な影響、音源ラベル、まとまりの確率とはしない。同じ観測が複数の組に現れるため、
組ごとの利得を独立な証拠として合算しない。共変量の欠落やモデルの誤指定があれば、
条件付きの利得にも見かけの関係が残る。

正値の合成系列7種・3 seedで比較した。周囲の系列を真の共通変量として構成した対照では、
相手の追加による利得1.061–1.075 bit/対象が、条件付きでは0.0032–0.0054へ減った。
過去の相手へ直接依存する対照では1.374–1.409が残った。関係を途中で除く対照では、
除く前の評価区間で1.097–1.171、除いた後で−0.0044〜−0.0020だった。
全期間の平均が正でも、現在その関係が続いているという証拠にはならない。
同時刻の相関が0.953–0.959の不規則な共通変動には、過去の相手による利得がほぼなかった。
これは同期の知覚を否定する結果ではなく、過去からの予測だけでは同時の共変動を表せない反例である。

**参照できる遅れの範囲も証拠の一部にする。** 1観測前だけを使う候補は、2観測前への依存を見落とした。
同じ21入力について、1観測前のみと1・2観測前の二候補を固定して追加比較した。
後者ではその対照の条件付き利得が1.373–1.430となり、独立な対照は−0.0074〜−0.0051にとどまった。
追加は自分と周囲の2観測前も含むため、相手の係数だけを操作した比較ではない。
50 msという観測刻み、遅れの集合、文脈の更新率0.2/s、32候補、回帰の事前分布は数値比較用の設定である。
この追加から100 msを人間の記憶長として採用せず、利得がない結果も参照範囲を離れた関係の不存在とは解釈しない。

**観測間隔と認知的な時間範囲を混同しない。** 50 ms集約の系列は20 Hzで標本化される。
上の2009年のモデルが使う2–32 Hzの変調を、この系列ですべて表すことはできない。
実際のRMS・log1p集約へ、振幅と位相を調整した8 Hz／12 Hzの正値包絡を入力すると、
50 msごとの80観測は最大差\(2.00\times10^{-15}\)未満で一致し、どちらも8 Hzに見えた。
5 ms集約の800観測では元の8 Hzと12 Hzを区別できた。PCMや聴覚フィルタ出力を再現した刺激ではなく、
集約処理が情報を失う構成的な反例である。共変動の観測には、保存済みの細かい包絡から別経路を作り、
帯域ごとの遅延、時間分解能、窓端と欠落を検証する必要がある。変調フィルタの位相と個体の拍内位置も別の座標である。

作者4音源と通常12音源では、指定した8帯域の28組・56方向を採点した。帯域の選択は検査用であり、
推定された音源分割ではない。「Bのみかわった」と「流れは変化せず、音はかさなる」の区別を再現したとは扱わない。
初回37条件は保存時と同じNumPyで完全再現し、123,256件の尤度をSciPyの別計算と照合した。
最大対数尤度差は\(6.09\times10^{-13}\)未満。ownの506,352値は前節の帯域独立モデルと完全一致した。
共通入力区間の全候補の予測も一致した。Python 236件、Rust 714件（8件ignore）の検証が通過し、
本番Rust 101ファイルと標準バイナリは前節の検証時点から変更していない。
計画・全出力・検証・遅れの追加比較・集約の反例は
`target/phrase-expectation/2026-09-07-predictive-relations/`に保存する。
知覚上のまとまりとその継続、認知パラメータ、phrase・終止、局所行動の結果学習、本番接続は未達である。

### 9.3.13 細かい包絡の共変動と、前処理の位相応答

9.3.12の集約による情報の消失を避けるため、1 msの連続包絡を直接受ける研究用の
temporal coherence観測を追加した。[Elhilaliら（2009）](https://engineering.jhu.edu/lcap/data/uploads/pdfs/neuron2009_elhilali.pdf)
のgamma-sine核とHilbert変換の組を数理上の参照とする。原著者の実行コードや神経応答の再現ではない。

\[
g(u)=u^2 e^{-3.5u}\sin(2\pi u),\qquad
h_{r,\phi}(t)=r g(rt)\cos\phi+r\widehat g(rt)\sin\phi.
\]

速度は2・4・8・16・32 Hz、位相は\(\pi/3\)刻みの六つを使う。Hilbert変換を含む組は
未来側にも支持を持つため、有限の近似と明示的な利用可能時刻が必要になる。今回は正側4周期、
負側1周期の支持を使い、全速度を共通の0.5秒遅延へ揃えた。有限の実数核から平均を除き、
8倍以上のFFT領域でHilbert変換を計算し、切り出した複素核でもDCを除く。これらは数値近似の選択である。
全ての速度の入力は1 ms刻みのまま保持し、5 msごとに結果を報告する。
最も遅い速度には2,501観測、最も速い速度には626観測が必要になる。揃う前の速度は利用不能とし、
欠落後も速度ごとに履歴を補充する。未知の過去とEOFには無音を補わない。
`support_start_sec`は使用する最古の包絡の報告時刻であり、元のPCMの支持範囲を表す値ではない。

複素応答を\(z_{ir}\)とすると、六位相についての積の和は
\(C_{ij}^{(r)}=3\operatorname{Re}(z_{ir}\overline{z_{jr}})\)、対角は\(P_{ir}=3|z_{ir}|^2\)となる。
速度別の積とパワー、全速度の和、対角で正規化した符号付き内積を併記する。
固定の2秒窓の相関ではなく、フィルタ出力の位相方向に関する内積である。
差分入力と積分したFIR係数で計算するため、観測された一定値には数値閾値なしで厳密にゼロを返す。
パワーがゼロなら正規化は未定義であり、「関係なし」という判定にはしない。
微弱な変動でも正規化値は大きくなり得るため、必ず元のパワーを残す。
瞬時の全速度のGram行列のrankは最大10であり、113帯域の音源数をそのrankから数えない。

有限核の伝達関数を、gamma-sine核の解析的なFourier変換と比較した。
各中心速度の0.25–2倍の141点では、正周波数の複素誤差は参照の最大振幅に対して最大0.632%、
負周波数への漏れは最大0.501%だった。支持とFFT領域を広げる比較も保存した。
支持を正側8周期・負側2周期へ広げると、前者は0.199%未満へ減る一方、共通遅延は1秒になる。
この誤差と遅延の比較は、認知的な統合時間や人間の記憶長の同定ではない。
逐次更新は直接畳み込みと、六位相の明示的な積の計算にも一致した。

8秒の正値包絡11種・3 seedでは、一定値と無音、規則的な同期と交互、同時の不規則な変動、
独立な変動、二組の共変動、音量倍率、既知の遅れを比較した。同期した不規則な包絡にも内積1が得られ、
過去から予測できることを前提にしない。前節で集約後に一致した8 Hz／12 Hzの対照も区別できた。
これらは構成した包絡の検査であり、聴覚入力・音源分離・知覚上のまとまりの全体検証ではない。

**前処理の遅延は、細かい観測刻みだけでは解消しない。** 現行のgammatone前処理を含め、
110–約4,186 Hzの8搬送波を同じ位相で振幅変調する対照を作った。5速度について、
各搬送波を別々に処理した包絡と、その混合PCMからの包絡を比較した。
混合音の最低・最高帯域の平均正規化内積は、2 Hzでは0.9805、8 Hzでは0.7112、
16 Hzでは0.0813、32 Hzでは−0.8344だった。同時に変動する物理入力が、前処理後には負の内積になる。
32 Hzで測った位相等価の遅れは低帯域約14.53 ms、高帯域約1.75 msだった。
低帯域の遅れは2 Hzでは約17.50 msであり、一つの固定遅延の移動で全速度を整合できるとは限らない。
高速な照合に使ったSciPyの4段フィルタは、現行の標本ごとの更新2,000値と最大差6.25e-17未満で一致した。
この歪みを音源の独立性や、聴取者が聞いた流れの分離へ転記しない。

保存済みの作者4音源でも観測を取得したが、この前処理の問題を残したまま通常12音源へ適用しない。
共通入力区間の結果は利用可能時刻で一致した。作者A/Bの最初の差は6.545秒、持続音の対照は12.005秒だった。
フィルタの参照時刻はそれぞれ6.045秒、11.505秒となるが、変化の先読みではない。
予測や行動が使えるのは前者の利用可能時刻以降であり、参照時刻を発行時刻や出来事の境界へ読み替えない。
次は前処理の位相応答をモデル化して整合させ、中心周波数からずれた搬送波と混合音でも確かめる。
その後に予測情報との併記と、作者の流れの区別へ進む。本番の発音とDCC結合は変更していない。
Python 243件、Rust 714件（8件ignore）が通過した。計画・入力・全出力・前処理の反例は
`target/phrase-expectation/2026-09-07-temporal-coherence/`に保存する。

### 9.3.14 前処理の位相補正と、変化量だけでは表せない流れ

9.3.13で見つかった帯域別の歪みに対し、既知のgammatoneの極から固定した位相補正を導いた。
極の大きさを\(q_c\)、PCMの標本化周波数を\(f_s\)、包絡の変調周波数を\(\nu\)とすると、
中心周波数の搬送波に対する4段の基底帯域応答と補正の目標は

\[
B_c(\nu)=\left(\frac{1-q_c}{1-q_c e^{-i2\pi\nu/f_s}}\right)^4,
\qquad A_c(\nu)=\frac{\overline{B_c(\nu)}}{|B_c(\nu)|}.
\]

この位相因子をgamma-sine/Hilbert組の周波数応答へ掛け、以前と同じ支持へ切り出す。
振幅応答の逆数は使わないが、有限支持への切出しとDC除去による振幅誤差は残る。
補正は音を読む前に固定し、入力に合わせて遅れを探索したり、帯域間で信号を混ぜたりしない。
補正の有無、参照時刻、0.5秒後の利用可能時刻を保持する。
これは既知のフィルタからの工学的な導出であり、神経系がこの補正を行うという実証ではない。
[AMTのHohmann遅延処理](https://www.amtoolbox.org/amt-1.6.0/doc/modelstages/hohmann2002_delay_code.php)
は波形の再合成用であり、その遅延・位相調整の実装を包絡の認知的な整合時間として転用していない。

既存10対照と、3 seedの追加132条件を処理した。追加条件は中心から±0.5 ERBまでのずれ、
混合音、意図した6 msの時間差、独立した変動を含む。

| 対照 | 補正前 | 補正後 | 解釈の範囲 |
|---|---:|---:|---|
| 中心搬送波、32 Hz同期変調、混合音の最低・最高帯域 | −0.8344 | 0.9925 | 前処理が作った逆相関を軽減した |
| 同期変調、ずれを含む混合音 | — | 最小0.8548 | 全28組・3 seed・対象速度とずれの中での最小値。万能な補正ではない |
| 中心搬送波、32 Hz、意図した6 ms差、単独処理の最低・最高帯域 | 0.2007 | 0.3531 | 意図した差は残る。理想的な位相の内積は約0.3564 |

142入力の補正前後を独立したFFT畳み込みで照合し、複素応答10,735,200値の最大差は1.12e-18未満だった。
ずれ・時間差を含む定常正弦波の正負両周波数から包絡を別途計算し、252,000値の最大差は2.27e-13未満だった。
これらは有限近似と入力計算の検証であり、流れの識別率ではない。

続いて、保存済みの作者4音源と通常12音源へ適用した。113帯域・1 msの包絡453,109観測を保存し、
従来と同じ8帯域・28組について補正前後を比較した。帯域ごとの計算は独立しており、作者4音源の補正前応答は
以前の全帯域計算からの取り出しと完全一致した。通常音源の48 kHzと作者音源の24 kHzを区別し、再標本化は行わない。
50 msへ再集約した入力は以前の予測評価と最大差2.89e-15未満で一致した。
共変動と過去からの予測を同じ利用可能時刻で8,258件併記した。うち通常音源は6,546件である。
共変動の参照時刻と予測の発行・対象時刻は別々に残し、同じ出来事への同時の証拠とは扱わない。

**変化量の比較と、作者が答えた課題を区別する。** 元のPCMが変わる時刻は、
間隔順序のA/Bで6.5440417秒、持続音対照で12.0000417秒だった。補正前後とも共通入力区間は一致し、
最初の応答差はそれぞれ利用可能時刻6.545秒、12.005秒に出た。先読みやphrase境界の検出ではない。
比較の後区間は、最長の有限支持と最古のRMS区間を含む2.501秒が経過してからとした。
前処理のIIR履歴が消えたという意味ではない。後区間の帯域組ごとの内積をパワーで重み付けして比較すると、
間隔順序A/Bの最大絶対差は0.0343、steadyとmoving_textureの補助対照では0.5428だった。
条件付き予測利得の方向別平均の最大絶対差も、前者0.00414 bit、後者0.2378 bitだった。
この二つの要約のそれぞれを単一の変化量閾値にすると、前者を検出する閾値は後者も検出する。
ただし、間隔順序への回答を流れの同一性の判定へ読み替えることはできない。moving_textureの回答は
直前の候補に対応づけたものであり、その際の比較相手は前回Cだった。steadyとの組は後から作った補助対照である。
2026-09-08に質問文と回答記録を再確認し、この二つを共通の流れ変化ラベルとして採点した解釈を撤回した。
数値の大小関係は保たれるが、これだけでは流れの識別失敗とも成功とも判定できない（9.3.15）。

DCCの次の設計対象は、関係の候補とその順序を保持し、音量や重なりが変わっても何が継続するかを表す状態である。
共変動を高めること、予測誤差を下げること、音響変化を増やすことを、そのまま音楽的な報酬へ変換しない。
継続と変化の仮説を同じ過去の証拠から発行し、後続の実音と照合する必要がある。現行の観測器はその証拠を供給する段階であり、
流れの同定、認知パラメータ、phrase・終止、局所行動の結果学習、本番接続は未達である。
Python 247件、Rust 714件（8件ignore）が通過した。本番のRustと標準バイナリ、発音、DCC結合は直前の検証から変更していない。
計画・対照・通常音源・作者比較・図は`target/phrase-expectation/2026-09-07-peripheral-alignment/`へ保存する。

### 9.3.15 身体・励起・関係の履歴と、知覚課題の境界

**作者の回答が支持する課題を先に固定する。** 9.3.14の数値比較は再現したが、2026-09-08に
質問文と回答の出所を照合し、その解釈を修正した。間隔順序A/Bで尋ねたのは「途中で鳴り方が別の並びへ移ったか」であり、
「Bのみかわった」はその課題への回答である。音源の交代、流れの同一性、phrase、終止の判定を含まない。
moving_textureでは変化後も流れが続くかを尋ね、「流れは変化せず、音はかさなる」と回答した。
この回答自体はラベルと時刻を指定しておらず、直前の候補への回答として帰属させている。
当時提示した比較相手は前回Cのtexture_changeであり、後の数値比較で用いたsteadyではない。
したがって、二つの比較を共通の流れ変化課題の正例・負例として採点しない。未回答の属性は未知のまま残す。

[Cusimanoら（2024）のBASS](https://mcdermottlab.mit.edu/papers/Cusimano_etal_2024_listening_w_generative_models.pdf)
は、音源に共通する性質と、その音源が出す個々の励起を分け、候補を再合成して観測音を説明する参照例になる。
ただし同一音源内のeventを非重複に制限し、周波数成分ごとの異なる減衰を別音源へ分けすぎる失敗も報告する。
そのままConchordalの音響身体へ移植しない。自然音から推定した事前分布と計算用の離散化も、DCCから一意に定まる値ではない。
原論文の推論は大きな計算資源を使うため、逐次に仮説を構成するという記述を実時間動作と同一視しない。

**次に実装する状態の契約。** 以下は設計であり、観測音から推論できる状態としてはまだ実装していない。

| 状態 | 保持する内容 | 同一視しない対象 |
|---|---|---|
| 音響身体の候補 \(B_j\) | 連続した周波数、モード間の関係、成分別の減衰、音色の変化範囲 | Voice ID、音名、固定された楽器ラベル |
| 励起 \(u_j(t)\) | エネルギーの供給、再励起、継続駆動についての候補と時刻 | 発音ごとに新しい音源が生まれるという前提 |
| 鳴っている状態 \(z_j(t)\) | 励起と身体の応答から残る振動。再励起後も既存の成分を保持する | 励起終了と音の消滅、音響的な減衰と記憶の忘却 |
| 観測の帰属 | 単独・複数・継続・交代による説明の候補と不確かさ、説明できない残差 | 推定音源数と生態内の個体数、候補中の最大値と確定した知覚 |
| 関係と並び \(R\) | 帰属候補の上で保持する到来順序、間隔、遅れを持つ依存、反復と変形 | 同時の相関、共通の拍、音源の同一性、phraseの同一性 |

身体は、例えば \(z_{j,n+1}=A(B_j)z_{j,n}+G(B_j)u_{j,n}\) という状態方程式で
励起後の振動を持ち越す。具体的な基準には既存の`ResonatorBank`と`ModeParams`を使える。
これは生成側の真のパラメータをListenerへ渡すという意味ではない。Listener側では観測したpresentationから候補を作り、
候補が出す波形の和を聴覚変換した \(\Phi(\sum_j y_j)\) と観測を照合する。
非線形な包絡抽出の後で \(\sum_j\Phi(y_j)\) と足し合わせても一般には同じ観測にはならない。
音響振動の位相は状態に含められるが、拍の位相とは別であり、共有拍の座標を全個体へ強制しない。

現行ソースを固定し、スカラー版の共鳴器でこの契約の前提を検査した。
277・421・633・911 Hz、T60が0.4・1・2.5・5秒の4モードに、0秒と0.3秒で励起を与えた。
同じ身体への再励起は、二つの励起応答の和と最大差2.24e-7未満で一致した。
再励起時に身体を新規作成して古い残響を捨てる対照では、0.3秒以後の誤差エネルギーが基準の約9.88%となった。
一つの4モード身体と四つの1モード身体は、72,000標本すべてで波形が完全一致した。
この音だけから生成側の身体数を同定することはできず、知覚上の帰属には別の制約が必要になる。
状態遷移行列の固有分解による独立計算216,000値も最大差2.74e-7未満で一致した。
構成した物理的な反例であり、音源推論や作者の知覚を再現した結果ではない。

時間パラメータは由来ごとに扱う。身体のT60は音響的な応答の性質であり、現行係数では
\(r=10^{-3/(T60\,f_s)}\) に対応する。身体の候補から推定する減衰時間を、認知的な保持時間へ読み替えない。
関係の保持・干渉・検索を支配する時間や項目数は、対応する認知課題で別に制約する。
解析刻み、近似の支持、探索候補数は計算上の設定であり、候補を削った後の重みは探索範囲に条件づけられる。
未提案の説明まで含めた確率や、人間の確信としては報告しない。

実装は、まず継続する身体への再励起と別身体の追加を、同じ過去から発行した音響予測として比較する。
候補の入力は観測音だけとし、正解のモード数・励起時刻・Voice IDは検査側だけが持つ。
同じ波形を予測する説明にデータ由来の優劣を付けず、事前の選好と観測による更新を分ける。
その上で関係の並びを保持し、順序の変化を音源の交代へ直結しない。共変動は候補生成の手掛かりには使えるが、
元の音と同じデータから求めた相関・予測利得を独立の尤度として掛け合わせない。
最後に、生成側の発音・待機などの候補を同じ音響観測で評価し、選択前の予測を実音で確認する。
協和、同期、予測精度の一つだけを普遍的な報酬へ最大化しない。

この段階で完了したのは課題の帰属監査、生成側の物理的な反例、状態の設計である。
未知の混合音からの身体・励起・帰属の推論、関係の期待、認知パラメータの同定、局所行動の結果学習は未実装または未統合である。
新しい作者判定、本番の発音変更、DCC結合の変更はない。計画・原資料・反例・監査結果は
`target/phrase-expectation/2026-09-08-perceptual-state-design/`へ保存する。

### 9.3.16 観測音からの共鳴候補と、検証した予測を保つ境界

9.3.15の状態設計に対し、観測したPCMだけから共鳴の候補を作る研究実装を
`scripts/evaluate_modal_continuation.py`へ追加した。実音を減衰する振動の和として近似し、
[Hua–Sarkar（1990）のmatrix pencil法](https://intra.ece.ucr.edu/~yhua/MPM.pdf)で極を求め、
振幅と音響位相を最小二乗で推定する。正解の周波数・モード数・励起時刻・Voice IDは入力しない。
これは音響状態の候補であり、聴覚上の身体の個数や帰属を推論した結果ではない。

候補は、前回の鳴りを継続するもの、前回の極を保って状態を更新するもの、
その説明の残差へ別の成分を加えるもの、現在の証拠から極を選び直すものの四つを比較する。
状態の更新は、継続する振動への振幅・位相の増分として表せるが、励起の時刻や機構の同定ではない。
残差の成分数も新しい音源の数ではない。四候補の結果から音源の同一性や流れを自動判定しない。
比較の基準は前回の適合であり、過去の身体候補の持続・再登場・消失を追跡する推論は含まない。

計算用には40 msの適合区間、その後の既知の10 msの検証区間、次の未知の10 msの予測区間を使い、
50 msごとに予測を発行する。極の数は0・2・4・8・12・16、pencilの幅は96標本とし、
0は無音の候補である。複素共役の二つの極で一つの実正弦波を表せるが、極の数を身体数としない。
入力は24または48 kHzの元の標本率を保つ。これらの窓、候補数、相対特異値の数値閾値1e-10は
近似と計算上の選択であり、DCCから導いた記憶長や認知的な時間ではない。

**検証したモデルと、発行するモデルを同一に保つ。** 初版は過去の検証で極の数を選んだ後に
係数を再推定していた。通常音源では、検証時にはなかった増大する極が現れ、将来予測が発散した。
極と振幅を検証後に変更しないよう修正した。さらに、増大する振動を励起なしの受動的な身体として
長く延長しない。増大する推定は検証誤差とともに保存するが、受動的な候補の選択対象から除く。
持続音には、同じ角周波数で振幅一定とする別候補を明示し、観測によって比較する。
これは推定した減衰を黙ってゼロへ切り詰める処理ではない。実際の増大、持続駆動、音高変化を
説明する励起・身体モデルは別途必要であり、候補の不適合として残す。

予測は発行時に固定し、後続の音で書き換えない。入力欠落では身体の候補を消去し、
新しい連続区間から観測を再開する。無音は0の観測、欠落は未知として扱う。
末尾までに届かない予測対象は未決とし、誤差0や無音へ転記しない。

二つの標本率・三つの未使用seedで、減衰、再励起、成分追加、glide、雑音、PCM16量子化の
36条件を比較した。雑音のない減衰条件では、発行した次の10 msの波形誤差が全条件で1e-15未満だった。
縮約した固有値問題と、切り詰めた擬似逆による全行列の固有値問題との最大極差は1.81e-15未満。
現行Rust共鳴器の実出力から推定した極も、生成側の状態遷移行列の極と最大差2.05e-9未満で一致した。
一つの4モード身体と四つの1モード身体が同じPCMを作る対照では、発行した予測も一致した。
glideや未知の励起を含む条件では誤差が残る。これらは数値的・音響的な検査である。

通常12音源と作者関連4音源について、発行した波形の和を既存の聴覚フィルタへ通して比較する。
各候補、実音、無音入力の対照に同じ観測済みのフィルタ状態を渡し、直前の包絡を維持する対照も置く。
Log2空間の113帯域・1 ms包絡の二乗誤差は記述量であり、較正した知覚尤度でも流れの判定でもない。
9,058回の予測を発行し、四候補に共通する9,040窓を採点した。各音源の初回は前の身体を持たず、
末尾の2窓は未決である。通常音源の7,134窓を音源ごとに集計すると、全12本で四候補とも
包絡維持の対照より誤差が大きかった。新規適合の比は1.221–2.115倍だった。
moving_textureでは新規適合の比が0.264倍となったが、音響予測の改善であり、
9.3.15で区別した作者の知覚課題を共通の流れ変化ラベルへ戻す根拠にはしない。
実音の包絡は前段の保存値と最大差2.85e-16未満で一致した。
Python全254件とRust全714件（8件ignore）が通過した。本番のRust 101ファイルと標準バイナリは不変である。
生成側の情報を知覚へ渡さずに音響候補を作る経路までが今回の実装範囲である。
身体と励起の持続的な帰属、候補の不確かさ、関係の期待、認知パラメータ、局所行動の結果学習、
phrase・終止と本番接続は未達である。比較資料と失敗した初版は
`target/phrase-expectation/2026-09-08-modal-continuation/`へ保存する。

### 9.3.17 駆動を含む音響予測と、平均波形だけでは失われるパワー

**前段の失敗を計算上限と分ける。** 通常12音源の2・8・16・22秒に固定した48区間で、
9.3.16の行列幅と極の候補数を比較した。幅96・最大16極では、予測波形の二乗誤差は無音予測の
0.996倍だった。幅96・最大64極でも0.996倍だったが、幅384・最大64極では0.539倍となった。
適合区間の誤差も0.840倍から0.165倍へ低下した。48 kHzでは幅96は2 ms、幅384は8 msに相当する。
この診断だけで全演奏の改善や最適な幅を確定しないが、前段の失敗を持続駆動やglideの欠如だけへ帰属できない。

`scripts/evaluate_driven_acoustic_state.py`には、音響応答とイノベーションを分ける研究候補を追加した。
[Burg（1975）の第II章C、式II-64](https://sepwww.stanford.edu/data/media/public/oldreports/sep06/06_09.pdf)
に基づいて前方・後方の予測誤差から反射係数を推定し、all-pole応答をlattice形式で保持する。
高次数の多項式へ展開せず、観測された最近の標本から身体状態に相当する後方誤差状態を更新する。
係数を過去の検証後に固定することと、直近の既知の音を状態へ取り込むことは両立する。

候補内では \(x_n=-\sum_k a_kx_{n-k}+\epsilon_n\)、\(\epsilon_n\sim N(0,q)\) と仮定する。
独立したGaussianイノベーションと応答係数を条件とする分布であり、実際の駆動機構を識別したモデルではない。
Harmonicの駆動入力は決定的で、Modalは雑音駆動を許す。推定した\(q\)には、未説明の音高・音色変化や
観測精度の影響も入り得るため、生成側の`sustain_drive`や物理的な励起強度へ転記しない。

過去40 msで適合し、その後の既知の10 msの同時密度で候補を比較して、次の10 msの平均と応答を固定する。
50 msごとに新規適合、保持した係数・分散、保持した係数で分散のみ更新した候補を比較する。
係数の異なる候補を最大4個保持し、固定秒数では削除しない。保持上限による枝刈りは認知的な忘却ではない。
次数0・4・8・16・32・64、窓と保持上限は計算用の設定であり、音源数やDCC由来の記憶長ではない。
PCM16の量子幅\(\Delta=1/32768\)から\(\Delta^2/12\)を分散の数値下限に使うが、
連続Gaussian密度は量子化区間の厳密な確率ではなく、駆動と観測雑音の分離も未達である。

**波形の平均と知覚量の期待値を分ける。** 音響応答を\(h\)、聴覚フィルタを\(g_c\)、
同じ観測済みのフィルタ状態から計算した平均出力を\(\mu_c[n]\)とすると、候補内の将来パワーは

\[
E|Z_c[n]|^2=|\mu_c[n]|^2+q\sum_{j=0}^{n}|(g_c*h)[j]|^2
\]

となる。予測した平均波形だけを聴覚変換すると右辺の第二項が消える。
波形と応答を合成してから113帯域へ変換し、1 ms内の期待パワーを平均する。
その平方根はRMSの二次モーメントに基づく値であり、包絡そのものの期待値ではない。
パワー誤差と包絡誤差を別々に報告し、予測分布の改善を流れの認識へ転記しない。

反射係数の独立した二次目的関数18条件との最大差は3.67e-15未満。
独立した畳み込みによる6,780パワー値との最大差は7.81e-18未満だった。
実際に選ばれた64次の応答も、別の状態遷移行列表現と最大差4.63e-12未満で一致した。
したがって、以下の過大予測を単なるlattice計算の発散としては扱わない。

既存16音源では9,058予測・9,040共通窓を比較した。実装固定後、前段と同じ凍結renderer・scenario・
configから未使用seed 184101・184117・184139の通常12音源を生成し、7,146予測・7,134共通窓で再検証した。
凍結rendererと現在の標準バイナリはファイルとして異なるが、追加12音源を現行rendererでも再生成し、
WAV全体のSHA-256が12本すべて一致した。確認範囲はこのscenario・config・seedの組であり、
現行標準版全体や新しい推定器の本番接続の受入検査とはしない。
既存・追加の通常12本のそれぞれで、期待パワーは10本で維持対照を上回り、分散の追加は11本で平均だけより良かった。
平方根での包絡誤差は各12本すべてで維持対照より小さかったが、指標の違いを省略しない。
追加のRain・seed 184101では、期待パワーの誤差が維持対照の3.810倍となった。
7.9秒の一窓で、保持した応答へ更新した分散を入れる候補がパワーを過大予測した。
イノベーションを独立した駆動とする仮定や変化時の候補選択には不足が残る。

追加通常音源のPCMの周辺95%区間への実測包含率は94.45–95.30%だった。
一方、既存の作者関連音源では86.9–90.5%であり、一様な較正を確定できない。
係数推定の不確かさ、知覚空間の同時尤度、人間の確信は、この区間には含まれない。
保持した係数が使われた事実も、聴覚上の音源の同一性を獲得した証拠ではない。

Python全260件、Rust全714件（8件ignore）が通過した。本番のRust 101ファイルは不変である。
glideの軌跡、断続的・持続的な駆動の区別、身体への知覚的な帰属、関係の期待と認知パラメータ、
局所行動の結果学習、phrase・終止、本番接続は未達。正本の資料は
`target/phrase-expectation/2026-09-08-driven-acoustic-state/`へ保存する。

### 9.3.18 位相が連続する軌跡と、軌跡の係数・振動状態の区別

`scripts/acoustic_trajectory.py`に、観測PCMから周波数軌跡を推定する研究候補を追加した。
[Neri・Depalle・Badeau（2021）の式1](https://www.dafx.de/paper-archive/2021/proceedings/papers/DAFx20in21_paper_30.pdf)
と同じ局所的な信号族を使う。適合窓の中心を時刻原点とし、

\[
x(t)=\operatorname{Re}\sum_m b_m\exp\{d_mt+i2\pi(f_mt+\tfrac12 v_mt^2)\}
\]

と表す。\(v_m\)はHz/sの周波数変化率で、瞬時周波数は\(f_m+v_mt\)となる。
周波数を移すときも位相を積分して連続に保ち、\(b_m\)が複素振幅を表す。
\(d_m\)は局所的な振幅変化率で、正の増大を無励起の受動的な身体とは扱わない。
この信号族の採用はDCCから一意に導かれた認知モデルの同定ではない。

推定法は原論文のBayesian EMではなく、時間領域の最小二乗である。残差のFFTから候補を追加し、
Gauss–Newton方向とline searchで周波数・変化率・振幅変化率を調整し、線形の複素振幅を解き直す。
次数候補は成分0・1・2・4・8・16、適合40 ms、既知の検証10 ms、将来10 msである。
対照は最大4成分、反復は最大30回とする。これらは計算上の設定で、知覚上の音源数や記憶長ではない。
生成側の周波数、glide速度、励起の時刻、Voice ID、正解の成分数は推定器へ渡さない。

**モデル族の比較と初期値の影響を分ける。** 初版では定常候補とglide候補を別々に初期化し、
異なる局所解へ到達した。現行版は同じ成分数の定常解からもglideの推定を開始し、
二つの初期値を適合区間の誤差で比較してから、既知の検証区間で候補を選ぶ。
定常解を含む大きなモデル族の適合誤差が初期値だけで劣化しないよう検査したが、大域最適性は主張しない。
Murmuration・seed 3141593では、glide候補の将来波形誤差が無音予測比7.758から0.328へ低下した。
初版の出力と修正後の出力は別々に保存し、推定計算の失敗を信号族の限界へ転記しない。

発行する候補は、前回の軌跡の継続、同じ軌跡で複素振幅だけ更新、新しい定常候補、新しいglide候補の四つである。
過去の検証区間の波形誤差で選び、未来の観測で発行済みの予測を書き換えない。
周波数が表現可能な範囲\((0, f_s/2)\)を外れる継続は利用不能とし、無音予測へ読み替えない。
候補の選択は知覚的帰属の確率ではなく、複素振幅の更新も再励起の同定ではない。

二つの標本率と三つのseedによる42条件では、定常・glide・交差の合成音の継続を再現した。
未観測の再励起、成分追加、方向転換では予測が外れ、その後の観測で更新した。
同じPCMが続く区間では発行予測も一致し、分割入力・入力欠落・予測の不変性の検査も通過した。
これらは信号族に沿った対照であり、人間が交差する二つの流れを追跡したかという課題ではない。

通常音源は前段の既存12本と追加12本を使い、2・8・16・22秒から各10 msを評価した。
96対象窓に既知のRainの7.9秒を診断用として加えた97窓を保存した。四候補共通の通常比較は94窓であり、
2窓では保持した候補が周波数範囲を外れた。診断用の1窓は音源ごとの集計へ含めない。
候補と実音を同じ観測済みの113帯域の聴覚フィルタへ通し、波形と帯域パワーの誤差を別々に採点した。
選択後の帯域パワーは既存12本中11本、追加12本中10本で直前パワーの維持を上回った。
ただし最大誤差比はそれぞれ1.503、1.883で、改善しない音源も残る。
修正後もglide候補単独の波形誤差が定常候補より小さい音源は24本中1本だった。
この固定区間の結果を全演奏の予測成績や音楽的な改善の受入へ拡張しない。

既知のRain・7.9秒では、実音の帯域パワー配列が前段と完全一致する同じ窓で比較した。
駆動モデルのパワー誤差は維持対照の2059.59倍、今回選ばれた定常候補は0.11346倍だった。
これは前段の音源全体の3.810倍とは異なる、一窓の比較である。この反例に対する改善を
全体の解決やglideによる改善とは扱わない。今回の点予測には9.3.17の駆動分散をまだ合成していない。

保存した実音源の候補を独立した逐次振動子で再生し、最大絶対差1.10e-12未満で一致した。
Python全266テストが通過した。本番Rust 101ファイルは直前から不変で、直近のRust検査記録は714件通過・8件ignoreである。
現行版の証拠は`target/phrase-expectation/2026-09-08-acoustic-trajectory/`の`*-initialized`に保存する。

**次の状態更新の境界。** 軌跡の係数と、観測で更新する振動状態は分けなければならない。
9.3.18時点の複素振幅の更新は適合区間の末尾までで、その後の既知の検証10 msに生じた励起を取り込んでいなかった。
9.3.17で区別した「係数を固定する」と「最新の観測で状態を更新する」を、この表現でも両立させる必要がある。
軌跡に沿う継続、断続・持続駆動、観測誤差を同じ予測分布の候補として比較する接続は未達である。
持続する知覚的帰属、関係の期待、認知パラメータ、行動結果の学習、phrase・終止、本番接続も未達のままとする。

### 9.3.19 最新の実音による振幅と残差状態の条件付き更新

`scripts/acoustic_posterior.py`は、9.3.18の軌跡の周波数・変化率・振幅変化率と、
残差のAR係数・innovation分散を固定し、最新のPCMから線形振幅と残差状態を更新する研究実装である。
線形振幅の事後分布には[Rasmussen・Williams（2006）2.1節](https://gaussianprocess.org/gpml/chapters/RW2.pdf)
のGaussian線形モデルの計算を使う。事前精度をゼロとするため、観測から全ての振幅方向を識別できる条件だけを受け入れる。
これは認知的な事前分布の同定でも、正規化されたモデル事後確率の計算でもない。

**振幅と残差状態を独立に扱わない。** 軌跡の実数基底を\(B\)、線形振幅を\(\beta\)とすると、
観測した残差は\(r=x-B\beta\)であり、振幅を変えると過去の残差状態も変わる。
過去の状態から未来へ進めるARの写像を\(K\)、innovationの応答を\(L\)、
振幅の条件付き平均・共分散を\(m_\beta,P_\beta\)、innovation分散を\(q\)と書くと、

\[
M=B_{future}-K B_{past},\qquad
\mathbb E[x_{future}\mid x_{past}]=Kx_{past}+Mm_\beta,
\]
\[
\operatorname{Cov}[x_{future}\mid x_{past}]=MP_\beta M^\mathsf T+qLL^\mathsf T.
\]

振幅分散を\(B_{future}P_\beta B_{future}^\mathsf T\)として独立に足すと、過去の実音による
制約を落とす。実装はraw PCMと基底のAR状態を分けて保持し、QR更新で新しい観測を取り込む。
最初のAR次数\(p\)標本には条件を付け、未知の過去をゼロとして尤度へ入れない。
連続入力の分割は結果を数値許容差内で保ち、欠落・逆順入力は更新前に拒否する。
発行済みの予測は変更しない。予測平均と共分散は、観測済みの同じ聴覚フィルタ状態から帯域パワーへ写す。

**数値上の識別可能性。** 初版はARで白色化した基底のノルムだけで列を正規化していた。
標本率の4分の1の定常正弦波を\(x[n]+x[n-2]\)で処理すると、理論上は基底が消える。
初版は残った丸め誤差を拡大し、振幅を推定できると誤判定した。現行版は元の基底を尺度とし、
相対的な特異値と元信号に対する丸め誤差の床の両方でrankを検査する。この反例は回帰テストとして残した。
閾値は数値上の選択であり、聴覚の識別閾値ではない。

比較は、40 msの適合と既知の10 msの検証の後に、未来10 msの予測を発行する。
残差次数は0・4・8・16・32・64、軌跡の成分数は通常音源で0・1・2・4・8・16とする。
検証区間の条件付き密度で候補を選び、その区間まで状態を更新する。
振幅固定の対照も独立に候補を選ぶ対照と、更新版と同じ軌跡・AR係数を使う対照に分ける。
これらの時間窓・次数・分散の床を、DCCに由来する認知パラメータとは扱わない。

42合成条件では、既知の再励起の後の波形誤差が同一モデルの振幅固定対照より約6.5–10.4%低下した。
再励起を同定した結果ではなく、追加成分・方向転換への対応も不十分である。
未観測の再励起を加えた6組では、変化前に発行した三対照の予測ハッシュが全て一致した。
未来の励起を先取りせず、到来後の予測誤差として残す。

通常24音源の2・8・16・22秒から各10 ms、計96窓で三対照全てを比較した。
既知のRain・7.9秒は診断用の1窓として別記する。音源ごとの4窓を平均すると、更新版の帯域パワー誤差は
既存・追加の各12本中11本で直前パワー維持を上回った。同一モデルの振幅固定対照には24本全てで勝ったが、
独立に選択した振幅固定対照に勝ったのは17本だった。追加のsample 12・seed 184139では、
維持対照に対する誤差比が同一モデルの固定振幅で369.18、更新後でも2.806、独立選択の固定振幅では0.200だった。
更新だけで候補選択の失敗や過大予測を解決したとは言えない。
名目95%の周辺予測区間の包含率は音源別平均で68.3–96.1%であり、較正済みの不確かさとは扱わない。
これは短い固定窓の音響予測比較で、演奏全体の評価や音楽的な流れの認識ではない。

独立した密な共分散計算で更新前後の平均・分散・密度を照合した。実音の3診断窓については、
80桁の直接AR多項式、逐次振動子、正規方程式でも計算した。予測平均の最大絶対差は4.25e-10未満、
指定した12点の分散の相対誤差は3.79e-11未満、三つの結合log密度の差は5.77e-9未満だった。
この数値誤差では、上記の過大予測を説明できない。保存した417予測の入力・時刻・ハッシュ・誤差集計を照合した。
Python全274テストが通過。本番Rust 101ファイルは直前の検査から不変で、Rustの記録は714件通過・8件ignoreである。

今回積分した不確かさは線形振幅の条件付き分布に限られる。軌跡係数と\(q\)は点推定であり、
断続・持続励起、観測ノイズ、知覚的な帰属を推論するモデルではない。
候補のhard selectionを音源の確率へ読み替えず、実音の短期予測を認知的な期待へ直結させない。
原本は`target/phrase-expectation/2026-09-08-acoustic-posterior/`、現行比較は`controls-rank`と`audio-rank`に保存する。
初版の`controls`も保持する。関係の期待・認知パラメータ・行動結果の学習・phrase・終止・本番接続は未達である。
次は9.3.5の行動契約を具体化する。完全な音源分離を前提にせず、候補時の予測、実行の確定、
実音による結果の観測を時刻で結ぶ。観測後の誤差だけから、自分の行動が変化を起こしたと断定しない。

### 9.3.20 発音指令と、その後の実音を時刻で結ぶ

9.3.5の行動契約のうち、発音指令と観測を対応付ける経路を本番のreportへ追加した。
`ParticipationClock`が発音指令を発行した場合に、利用可能なhabitat全体の継続予測を固定し、
発音時刻以後の最初の完全な観測窓に対応付ける。候補が却下された場合は記録を発行しない。
観測窓は既存の`AcousticTemporalExpectation`の約10 msの格子を使い、新しい認知時間や報酬を導入しない。
観測済みの時刻、指令発行時刻、onsetの標本位置、対象窓の開始・終了を別々に保持する。
予測は候補計画時から保持した写しなので、指令発行時刻までの全観測を必ず取り込んだ予測とは限らない。

実際にhabitat音声を処理した後、同じ窓の三帯域エネルギーを`participation_outcome`として報告する。
予測には自己寄与を除く前のhabitat全体を使う。自己寄与を除いた意思決定用の予測と、
全体の実測エネルギーをそのまま比較しない。pending記録は個体の外側で保持し、個体が消えた後も
対象窓を待つ。入力が飛んだ場合は`input_gap`、入力末尾で未観測なら`end_of_input`とし、
観測値をnullにする。実際に観測した無音は`observed`とゼロのエネルギーで表す。

この経路はreportを付けた場合だけ予測を保存する。対象は予測を取得できたParticipationの発音であり、
全種類の発音を学習したという意味ではない。保持用のVecはdrain・retainで再利用し、
reportなしの経路には予測記録の追加割当てを入れない。聴覚モデルの入力へVoice IDを渡さず、
IDは生成側の指令を照合するキーとしてだけ記録する。

**対応付けから見つかった時刻の誤り。** 既存の`onset.time_sec`は発音の標本位置ではなく、
処理hopの先頭時刻を使っていた。対照の最初の発音は7459標本目なのに、旧reportは7168標本目相当だった。
現行版は`onset_tick`から秒へ変換し、丸めを避けた照合用に整数の`onset_frame`も出力する。
共有scaffoldの位相も実際のonset時刻で評価する。四つの比較では旧時刻が最大511標本、
48 kHzで約10.65 ms早かった。旧reportから得たIOIや位相の統計にはこのhop量子化があり、
標本精度の測定へ読み替えない。既存のWAVや作者の試聴回答を変更する修正ではない。

一つのrouting対照と、sample 08・09・12のseed 21を修正前後で生成した。四つのWAVは完全一致した。
追加の結果記録は10・202・731・166件、計1109件で、全て発音指令と対象観測窓へ対応付いた。
onset、rhythm summary、実行時間、新しい結果記録を除く既存のreport行も一致した。
対照では、最初の非ゼロPCMは7463標本目で、発音指令7459標本目の後に現れた。
reportの有無でもWAVが一致し、欠落・末尾・候補却下・予測の不変性の回帰検査も通過した。
Rust全718件、Clippy、全targetのcheck、release renderer buildが通過した。
記録は`target/phrase-expectation/2026-09-08-action-outcomes/`へ保存する。

**観測を結ぶことと、行動結果を学ぶことは異なる。** 保存した値は、発音前の継続予測と
発音後の実音であり、その行動を加えた場合の予測ではない。後者を構成するには、自分の音響身体、
実行する発音・待機、routing、既に鳴っている状態を予測へ入れる必要がある。
今回の対照の個体はpresentationだけへ送り、habitatには寄与しない。それでも別のhabitat音源の出現で
発音後の観測値は変わる。従って差をそのまま個体の寄与・成功・報酬として使うと誤る。
`observed`は窓を観測できたという状態であり、発音が聞こえた、好ましかった、因果効果が同定できたという判定ではない。
次は生成側が知る自分の行動候補を条件とする予測を作り、同じ履歴から発音・待機を分岐する対照で検査する。
その予測を実音で更新する学習、認知パラメータ、関係の期待、phrase・終止は未達のままである。

### 9.3.21 自分の音を条件とする分岐予測と、観測後の更新

行動結果の予測には、自分が既に鳴らしている状態と、これから加える発音を含める。
`ScheduleRenderer::fork_source`は、指定した一個体のToneだけを複製する研究用の経路である。
位相、共鳴器の残響、平滑化、乱数状態、予約済みの更新とtrigger、routingを保持し、
他個体のToneや自己音観測器は複製しない。次の未処理標本から、将来の自分の指令を与えて進める。
複製は元の演奏状態を進めず、待機は既に鳴っている音を消さない。割当てを伴うためRT経路には接続しない。
出力を保存する入口はオフラインの`examples/action_prediction.rs`に限定し、楽器の録音経路は追加しない。

**既知の自己状態と、周囲の知覚的推論を分ける。** 自分の過去の寄与を既知として、観測した
混合波形から波形のまま引く。残った過去の実音から9.3.19の環境予測を作る。自分の将来の波形を
その平均へ加え、条件付き共分散は保持する。自分の身体パラメータやroutingに誤差がないと置く対照であり、
未知の音源を聴覚だけで分離した結果ではない。他個体の識別子、身体状態、将来の指令は推論へ渡さない。

帯域エネルギーを足す処理にはしない。同じ周波数の成分は位相によって強め合い、打ち消し合う。
混合波形の予測を同じ観測済みの聴覚フィルタ状態へ通し、平均と共分散から期待される二乗振幅を求める。
自己音と周囲の寄与の交差項を省く対照を別に保存する。完全に逆位相の信号を加える単体検査では、
正しい予測は残差雑音だけになり、二つのエネルギーを足す対照には大きな成分が残る。

初回は3音響身体、2 routing、24/48 kHz、3 seed、周囲の追加発音の有無の72条件を比較した。
36組で過去の混合音・自己音・自己の将来分岐が完全一致し、周囲の将来だけを変えても発行予測は一致した。
自己の発音／待機の波形差と、実際に分岐した混合音の差は最大2.43e-8未満で一致した。
presentationだけへ送る個体はhabitatへの効果がゼロだった。これは既知の身体と、他個体が反応しない
対照における検査であり、社会的な応答や、学習した因果効果を検証したものではない。

**「継続」対照にもreleaseが入っていた。** 初回の`continues`は追加発音がないという意味で、
既存の周囲のToneは155 msにreleaseへ入っていた。予測発行150 msの時点で、この予定は推論に渡していない。
この条件を定常な継続と呼ばない。元の音とラベルは保存し、追加の未使用3 seedで、
170 ms以後も保持する`held`、155 msの`release`、同時に別音も加わる`release_and_onset`を分けた。
計108条件、36組の三つ組でも、150 msまでの証拠と初回予測は完全一致した。
同じ過去から、観測できない将来の指令を当てたという主張はできない。

追加比較では`sound`を実行した枝の150–160 msだけを読み、自分の実行済みの寄与を引いて環境予測を更新した。
既に発行した予測は変更せず、160–170 msを改めて予測する。取り消せない先行発音の残響を保持し、
未選択の`wait`の実音と160 ms以後の実音は更新へ渡さない。これらを改変しても初回・更新後の予測が
完全一致する回帰検査を追加した。更新は現在の音響状態の再推定であり、行動の効果や方策を学習する処理ではない。

同じ160–170 msに対する、150 ms時点の予測保持と160 msでの再予測を比較した。
下表の包含率は波形標本の周辺区間についての条件平均であり、音楽的な確信度ではない。
36条件には同じ環境を使う身体・routingの対照が含まれ、独立した36回の聴取実験としては扱わない。

| 周囲の条件 | 波形MSEが減った条件 | 帯域二乗振幅MSEが減った条件 | 名目95%区間の包含率：保持 → 更新 |
|---|---:|---:|---:|
| held | 36/36 | 36/36 | 60.4% → 80.0% |
| release | 18/36 | 19/36 | 44.0% → 88.5% |
| release_and_onset | 12/36 | 22/36 | 2.5% → 21.7% |

heldの平均波形MSEは1.20e-5から4.38e-7へ減った。しかしreleaseでは3.56e-5から6.83e-5へ、
releaseと追加発音では7.37e-3から7.62e-3へ増えた。包含率の上昇だけでは、平均予測の改善や
不確かさの較正は確定しない。既知の音を追加して再適合するだけでは、励起・releaseによる変化を十分に扱えない。
変化前の状態の継続、既存の身体の駆動・振幅の変化、新しい成分の追加を競合する仮説として保持し、
観測済みの結果で支持を更新する必要がある。単一の局所モデルを再適合し続ける仕組みを、
DCCの記憶・学習機構として採用しない。

初回と追加の180条件について、保存した証拠・予測のハッシュ、時刻、波形差と集計を照合した。
聴覚フィルタの4段再帰とは別に、閉形式のインパルス応答の畳み込みでも各初回分岐の3帯域を検査し、
二乗振幅の最大差は1.39e-17未満だった。Rust全719件通過・8件ignore、Python全278件、
Clippy、全target check、releaseのbinary・example buildが通過した。
記録は`target/phrase-expectation/2026-09-08-action-conditioned/`と、その`release-diagnostic/`に保存する。

40 msの適合、10 msの検証・再観測、20 msの初回予測、次数候補、分散の床は計算上の設定である。
入力はf32であり、継承した分散の床をPCM16観測の実際の量子化誤差とは扱わない。
認知的な保持時間、身体の知覚的な帰属、関係の期待、phrase・終止、行動結果の学習、本番接続は未達。
新しい作者試聴は追加していない。

### 9.3.22 周波数成分の候補と、振幅の変化を分けて比較する

予測仮説を一つに決める前に、同じ観測に対する複数の説明を残す。
[Pieszekら（2013）](https://journals.plos.org/plosone/article?id=10.1371/journal.pone.0053634)は、
音の出現率と視覚手掛かりに由来する予測が矛盾しても、それぞれの違反へのERP応答が生じる課題を報告した。
これは今回の音響モデル族、候補数、選択則、時間窓を導く実験ではない。複数の予測を併記する設計上の
制約と、波形を計算する近似を区別する。音響的な振幅・位相の変化の区別は
[Marchand（2012）第2節](https://dafx12.york.ac.uk/papers/dafx12_submission_35.pdf)も参照したが、
同論文の推定アルゴリズムや認知モデルを実装したという意味ではない。

研究実装`acoustic_hypotheses.py`は、既存の条件付き状態の継続、既存の周波数成分に対する複素振幅の変更、
残差から提案した成分の追加を比較する。基準時刻150 msまでの実音から候補を作り、選んだsoundの結果を
観測した後で更新する。新しい身体が生まれた、既存の身体へ再励起した、というラベルは入力しない。
同じ周波数成分を重複追加しても個別の係数を同定できない場合は、rank不足として残す。成分数を音源数にしない。

**周期性は残差モデルにも入る。** 初版は、条件付き密度で選んだモデルの明示的な正弦波成分を、
そのまま既存の周波数成分として使った。ところが、AR残差だけで周期音を予測できると、明示的な成分は
ゼロになる。この状態で振幅変更を比較したつもりでも、実際には短い残差モデルを再適合していた。
修正版では、既知の過去40 msで推定した軌跡の、後続の既知10 msに対する波形継続誤差から、
明示的な成分集合を提案する。この提案と、残差を含む予測全体の比較を分ける。
成分集合が空なら、その集合の振幅変更候補は作らない。表現の選び方が一意に同定されたという主張はしない。

**振幅の変更候補へ、別の指数変化を混入させない。** 次の版では、短い区間から提案した成分に
推定済みの指数振幅変化も残していた。一例では10 msで約5039倍・807倍になる増幅率を持ち、
既知の比較区間で選ばれた後に将来予測が崩れた。修正版の振幅変更・成分追加候補は、
推定した周波数・位相軌跡だけを保持し、複素振幅を定数または一次式として改めて推定する。
振幅の次数を\(d=0,1\)とすれば、候補は

\[
x(t)=\operatorname{Re}\sum_k\left(a_k+b_k u(t)\right)e^{i\phi_k(t)}
\]

となる。定数候補では\(b_k=0\)。\(u\)は適合区間で数値を調整する座標であり、認知的な時計ではない。
複素係数は局所的な位相補正も許す。ここでの位相は音響搬送波の位相を指し、拍内の位置とは区別する。
既存状態の継続候補は元の減衰・残差状態を保持する。
変更した候補は観測区間で推定・比較し直し、発行済み予測の後処理として振幅を切り詰めない。
上記の同一対照では、従来の再適合に対する波形MSE比が約1378万から1.063へ下がった。
極端な外挿の修正であり、その対照で従来予測を上回った結果ではない。

候補の適合区間と、候補を比較する後続の観測区間を分離する。後者を使って周波数集合を再推定しない。
複素係数と残差状態は、利用可能になった観測で条件付き更新する。各族の最良候補と比較記録を残すが、
密度の順位を知覚上の音源仮説の確率へ変換しない。族ごとの候補数は等しくなく、最大値による選択が
モデル族の事後確率を与えるわけではない。未来の実音を読む前に、予測と根拠の時刻・ハッシュを保存する。

指数変化を含む版と、位相・複素振幅を分けた版を、それぞれ未使用だった3 seed・108条件・2観測時点で比較した。
現行版のseedは184907・184913・184927。下表は各条件36件のうち、同じ将来窓の従来の再適合より
選択した候補の波形MSEが小さかった件数と、MSE比の中央値である。

| 観測済みの長さ／予測対象 | 周囲 | 改善した件数 | MSE比の中央値 | 名目95%区間の包含率 |
|---|---|---:|---:|---:|
| 10 ms／160–170 ms | held | 36/36 | 0.180 | 79.8% |
| 10 ms／160–170 ms | release | 36/36 | 0.053 | 61.9% |
| 10 ms／160–170 ms | release_and_onset | 6/36 | 1.153 | 21.2% |
| 20 ms／170–180 ms | held | 6/36 | 1.333 | 44.3% |
| 20 ms／170–180 ms | release | 36/36 | 0.006 | 88.5% |
| 20 ms／170–180 ms | release_and_onset | 24/36 | 0.956 | 27.4% |

10 ms観測は8 ms適合＋2 ms比較、20 ms観測は10 ms適合＋10 ms比較を使った。適合開始の候補は
150・152・154・156 msで、実際のrelease時刻155 msを教えていない。二行群では観測量、適合・比較区間、
予測対象時刻がともに異なるため、観測量だけの因果効果を分離した比較ではない。同じ環境を共有する
身体・routingの対照を、独立した聴取実験として数えない。

releaseでは両観測時点の全条件で誤差が減った。しかし保持条件は20 ms観測で従来の再適合に劣り、
別音を加える条件の平均MSEは両時点とも従来より大きい。20 ms観測の後者でも最悪のMSE比は3.327だった。
名目95%区間の包含率も較正できていない。短い比較区間で選ばれた仮説が長い将来を保証するわけではなく、
選択の不確かさや未表現の変化を条件付き係数分散だけで扱えない。従来の再適合を置き換える本番候補にはしない。

直接の回帰行列・共分散との照合、重複した成分集合の非同定性、未選択の結果と未観測の未来の改変に対する
予測の不変性を検査した。Rust全719件通過・8件ignore、Python全281件、Clippy・全target checkが通過した。
入力、初版の失敗、二版の固定後の比較、ソース写しと独立検査は
`target/phrase-expectation/2026-09-08-acoustic-hypotheses/`に保存する。

これらは局所的な音響近似である。PCM誤差の改善を、DCCの記憶や音楽的な関係の理解の完成条件にしない。
次は、候補が予測する知覚上の結果と関係の期待を、観測された結果に照らして扱う必要がある。
認知パラメータ、持続する知覚的帰属、phrase・終止、行動結果の学習、本番接続は未達。作者試聴は追加していない。

### 9.3.23 発音・待機の予測を、同じ分析状態から知覚量へ写す

研究用の発音・待機の予測を、生態側の実音と同じNSGT、ピーク抽出、強度平滑化、粗さ・調波性、
協和場の計算へ通す経路を追加した。`runtime::perceptual_action_assay`が本番と共通の設定構築と
`AnalysisStream`を使う。Pythonへ別のR/H計算を移植せず、testでだけ使える経路とした。
楽器への録音機能や本番の候補評価は追加していない。

**分岐時に履歴を失わない。** FFTの環状バッファ、帯域別の平滑化、ピーク強度の履歴を観測時点で複製する。
512標本のhopに満たない既知の末尾も各予測へつなぐ。完全なhopだけを処理し、末尾不足を無音で埋めない。
入力・処理済み時刻、未処理標本数、FFT容器の範囲、起動時のゼロ標本数を出力する。FFT容器の長さは
帯域別の知覚遅延や認知的な記憶時間ではない。予測は実音の観測状態を更新しない。24/48 kHzで、
全履歴と分岐後の音を最初から再生した参照と、各更新のscanが完全一致した。

従来の160–170 msの予測は、既定の512標本hopでは両標本化周波数とも新しい更新へ届かない。
170–180 msの場合も、最初の更新に入る予測側の音は16/32標本にとどまる。今回は1秒の実音を観測後、
50 msの分岐を予測した。採点する最後の更新は両方とも1.045333…秒で、起動時のゼロ標本はない。
1秒と50 msはこの検査の入力・計算範囲であり、認知的な保持時間やphrase長を定める値ではない。
研究用生成例のholdとgateを予測時点へ合わせ、準備中の意図しない消音を防いだ。従来の既定値による
252 PCMファイルは変更前後で完全一致した。

**平均波形の知覚量と、知覚量の期待値を分ける。** 相関した振幅・残差を保った32本の予測波形を、
それぞれ同じ分析状態へ通す。発音・待機には共通の環境標本を使う。正負が逆の二波形を使った対照では、
平均波形は無音でも各波形には粗さ・調波性がある。一般に`E[g(X)]`を`g(E[X])`へ置換できない。
標本化の平均・共分散は独立した条件付き状態行列と照合した。ただし、固定された音響モデルの不確かさであり、
モデル選択や未表現の変化の不確かさは含まない。

seed 184957・184961・184967、3音色、2 routing、24/48 kHz、保持・release・別音追加の108条件を実行した。
予測波形と知覚量をすべて保存してから実現分岐を分析した。36組の環境三つ組で過去と発行済み予測が一致した。
以下は、自身の音がhabitatへ入る各18条件で、予測した発音と待機の差の二乗誤差が「差はゼロ」という対照より
小さかった件数である。Log2Space binを等重みで採点し、知覚量を一つの報酬へまとめていない。

| 周囲 | 粗さのstate | 調波性のstate | 協和場のlevel |
|---|---:|---:|---:|
| 保持 | 17/18 | 17/18 | 17/18 |
| release | 17/18 | 16/18 | 17/18 |
| releaseと別音追加 | 16/18 | 15/18 | 16/18 |

presentationだけへ送る残り54条件では、habitatの予測差・実測差はすべて厳密にゼロだった。
観測busはhabitatであり、presentationを聴くListenerTwinの結果へ転記しない。他個体は反応しない制御された
分岐であり、相互作用中の因果効果を学べたとは扱わない。

粗さでは、分布を通した期待値が平均波形だけの値より各環境の平均誤差を減らした。調波性・協和場では逆だった。
周辺化の正しさは、個々の結果への誤差減少を保証しない。32標本の中央90%分位区間の被覆率は、保持で約96–98%、
releaseで約37–60%、別音追加で約33–57%。周波数binと二分岐に対する記述値であり、独立した聴取試行や較正の証明ではない。
16標本と32標本の平均の差も保存し、標本誤差を認知的な変動へ読み替えない。

Rust全721件・Python全284件、全targetのClippy・checkが通過した。通常Rust suiteのignoreは9件で、今回の
export検査は予測・実測の両段階で別途成功した。入力・分布・標本・分析結果・ソース・検査記録は
`target/phrase-expectation/2026-09-08-perceptual-consequences/`に保存する。
今回接続したのは音響予測から既存の知覚量への写像と対照評価である。次は、選んだ行動の予測と実音を同じ時刻で
保持し、環境変化と自己寄与を区別して結果を更新する。局所行動の学習、本番接続、持続する知覚的帰属、関係の期待、
認知パラメータ、phrase・終止は未達。新しい作者試聴や音楽的な受入は追加していない。

### 9.3.24 選んだ行動の実音から、残差分散も逐次更新する

前節の分布は、振幅を観測で更新しても残差の分散を固定していた。研究用の`AcousticPosterior`へ、
分散を未知量として保持・更新する選択肢を追加した。軌跡とAR応答は固定したままであり、音源の帰属や
行動価値を学習する機構ではない。分散分布の規約は[Murphy（2007）第6・10.2節](https://www.cs.ubc.ca/~murphyk/Papers/bayesGauss.pdf)
を参照し、相関した回帰状態への適用は以下の条件付き計算として別途検査した。

ARの最初の標本を条件として除き、白色化した観測数を`n`、振幅係数の次元を`k`とする。
事前分布`p(amplitude,q) ∝ 1/q`を明示し、残差平方和を`RSS`として
`q | observed ~ InverseGamma((n-k)/2, RSS/2)`を得る。これは選択済みの軌跡・AR応答を条件とした分布で、
それらの推定・選択の不確かさは含まない。正の残差平方和と有限な分散平均を要求し、条件を満たさない場合は
利用不能とする。人間の記憶や神経実装をこの事前分布から導いたとは扱わない。

新しい実音は、既知の自己寄与を波形で差し引いてから更新へ渡す。時刻が連続しない入力を拒否し、
予測を保存してから各観測を取り込む。分散を積分した同時予測はStudent分布となる。標本化では、
一つの分散標本を、相関した振幅と残差を含む軌跡全体へ共通に適用する。標本ごとの独立な分散ではない。
平均・共分散・同時密度・条件付き更新を独立した状態行列と照合し、一括更新と1 msごとの更新も一致した。

同じ108制御条件について、発音・待機をそれぞれ独立した選択として扱い、216件のepisodeを実行した。
固定分散Gaussianと学習する分散のモデルを、初期の平均・共分散が等しくなるよう構成した。
すべての初回予測を保存後、各episodeでは選んだ分岐の最初の10 msだけを観測し、残り40 msを予測した。
更新後の分布と生態側の知覚量も保存してから、残りの実音を採点した。別の行動の実音を取り込む場合は
別の状態コピーを使う。未選択の音と未観測の末尾を改変する検査でも、当該episodeの更新と予測は不変だった。

両モデルの振幅更新・平均波形は同じであり、PCMの二乗誤差も全episodeで一致する。
学習するモデルの残差分散平均は、初期値に対して下表の比率へ変わった。同じ将来音の同時密度は
全216件で固定分散より高かったが、それだけで予測を較正済みとは扱わない。
表の被覆率は、32標本の中央90%分位区間が実測を覆う割合を、周波数binとepisodeで平均した値の
R/H/C三軸の範囲である。相関したbin、routing、二つの行動を独立した聴取試行として数えない。

| 周囲（各72 episode） | 分散比の中央値 | 固定分散の被覆率 | 学習する分散の被覆率 |
|---|---:|---:|---:|
| 保持 | 1.046 | 84.6–90.6% | 95.4–97.0% |
| release | 2.047 | 17.4–49.5% | 19.6–51.2% |
| releaseと別音追加 | 2238 | 8.3–41.9% | 51.1–58.5% |

別音追加の最大分散比は約10007だった。予測区間が広がり、調波性・協和場の平均誤差は小さくなったが、
粗さでは増加した。今回の対照は分散の更新と分布族の違いを含み、両者の個別の効果を分離していない。
分散を大きくすることは、予測できなかった周波数構造や文脈を獲得することではない。
同じ軌跡・応答を無期限に維持する更新を、認知的な長期記憶として採用しない。

presentationだけへ送る54組では、二行動の更新後の予測とhabitat実測が完全一致した。
初回予測は72組の環境三つ組で一致した。habitatへ送る自己音の差引きにはfloat32の混合丸めが残り、
同じ周囲を持つ二行動から推定した外部音の最大差は1.12e-8だった。他個体の反応とは解釈しない。
観測busは引き続きhabitatであり、ListenerTwinの観測や生体信号の結果へ転記しない。

既定の固定分散経路は保持し、過去の6条件・384標本軌跡・12平均波形のハッシュが一致した。
Rust全721件・9件ignore、Python全289件、全targetのClippy・checkが通過し、native exportも両段階で成功した。
記録は`target/phrase-expectation/2026-09-08-chosen-outcome-learning/`。本番の行動選択、音、作者試聴は変更していない。
次は、予測誤差をすべて残差へ吸収せず、保持された構造の変化や別成分の追加を観測で比較する必要がある。
局所行動の価値学習、本番接続、持続する知覚的帰属、関係の期待、認知パラメータ、phrase・終止は未達である。

### 9.3.25 構造の候補を保持し、実音で予測の重みを更新する

研究用の`AcousticModelBank`を追加した。継続、複素振幅の変更、周波数成分の追加を候補として保持する。
`propose_hypotheses`で候補の構築と共通比較窓を分離し、従来の固定分散による族ごとの最良候補のAPIは保った。
各候補は振幅・残差状態・残差分散を更新するが、成分集合とAR応答は適合済みの条件として固定する。

混合とモデル内・モデル間の不確かさの計算は、[Hoetingら（1999）第1節](https://sites.stat.washington.edu/www/research/online/hoeting1999.pdf)
を参照する。ここでの候補は、既に観測した提案用データ`H`を条件とする。比較窓`B`を読む前に、
利用可能な各族へ等しい総質量を置き、族内の利用可能な候補へ等分する。その明示した`prior_j`から
`w_j ∝ prior_j * p_j(B | H)`で更新し、将来を`p(Y | H,B) = sum_j w_j * p_j(Y | H,B)`で予測する。
分散には各候補の分散と、候補の平均が混合平均から離れる量の二乗を含める。
これは条件付き予測の重みであり、成分集合・ARの適合全体を積分したモデルの証拠や、知覚上の音源の確率ではない。
族の質量、族内の候補の列挙、提案窓は実験上の選択で、DCCから導いた認知パラメータではない。
総事前質量を保存して候補を複製した場合は予測が一致するが、任意の候補追加に対する不変性は主張しない。

小さい重みも対数で保持し、指数が数値的にゼロとなった後も、後続の証拠によって支持を回復できる。
重みを理由に候補を削除せず、発行済みの予測は不変にする。標本化では、相関した軌跡全体を一つの候補から生成する。
標本時刻ごとに候補を選ぶ混合は、異なる同時分布になる。

既存108条件から、発音・待機を独立した選択として216 episodeを比較した。選んだ最初の8 msだけで候補を構築し、
適正で有限な予測と事前重みを保存してから、続く2 msで比較・更新する。残り40 msの予測と生態側の知覚量を保存した後に
実際の末尾を採点する。対照は分散学習を続ける継続モデルと、最大重みの単一候補である。
全て同じ観測済みの分析状態と未処理PCMを使い、最後の完全な分析hopを比較する。既存入力の再利用であり、
新しい未使用条件、実生体信号、独立した試聴試行ではない。

下表は混合と継続モデルの比較である。PCMの改善・同等・悪化は相対許容差1e-6で分ける。
誤差比はepisode平均の二乗誤差どうしの比で、個々の比の平均ではない。被覆率は32標本から得た中央90%区間を
周波数bin・episodeにわたり平均した値で、表示範囲はR/H/Cの差を表す。観測の相関と有限な標本数を含み、較正の証明にはならない。

| 環境（各72 episode） | PCM改善 / 同等 / 悪化 | 平均PCM誤差比 | 混合のR/H/C被覆率 |
|---|---:|---:|---:|
| 保持 | 48 / 24 / 0 | 0.182 | 83.6%–90.7% |
| 消音 | 60 / 12 / 0 | 0.294 | 36.7%–61.6% |
| 消音と別音追加 | 12 / 12 / 48 | 1.717 | 53.2%–62.1% |

別音追加での粗さ・調波性・協和場の平均二乗誤差比は、継続モデルに対して0.213 / 0.204 / 0.209となった。
一方、PCM誤差は1.717倍へ増加した。今回の比較では波形と知覚量の評価が分かれる。
知覚量の予測改善から、流れの識別、関係の期待、音楽性の改善を確定しない。
同じ未来の同時密度は、消音と別音追加の各60件で改善し、各12件は同等だった。保持では12件改善・24件同等・36件悪化した
（絶対許容差1e-7）。平均誤差の減少だけでは確率予測の較正を示さない。

最大重みの単一候補に対する混合の平均PCM誤差比は、三環境の順に0.934 / 1.003 / 0.971だった。
知覚量の平均誤差も一律には減らず、両分布のMonte Carlo標本は一致を保証しない。混合そのものの優位性は未確定である。
216件中108件では単一候補の重みが99%を超えた。利用可能な候補は各1〜69件で、52〜114件は数値・事後分布の条件を満たさない。
別音追加での単一候補の内訳は、成分追加12件、振幅変更48件、継続12件だった。族名を同定された原因と扱わない。
AR残差が周期性を担う場合もある。

予測時間別の比較では、別音追加のPCM誤差が特に20〜40 msで増えた。この環境の一次式の振幅候補の平均重みは
数値的にゼロだったため、直線的な振幅外挿を原因と特定できない。短い観測が支える成分、固定した位相・ARの動力学、
予測範囲を見直す必要がある。40 msの採点窓を、認知的な保持時間やphraseの長さとして採用しない。

54組のpresentation-only対照で、行動間の更新済み分布と生態側の実測は一致した。216件の継続対照も、前段のPCM誤差と
同時密度に数値許容差内で一致した。重みの正規化、実際のStudent候補の一括・分割更新、発行予測の不変性、
適合・比較・未観測の末尾・未選択行動の入力境界を検査した。旧APIの6条件の候補記録と予測も完全一致した。
Rust全721件・9件ignore、Python全294件、全target Clippy・checkが通過し、全episodeの予測・実測のnative exportも完了した。
記録は`target/phrase-expectation/2026-09-08-structural-predictions/`。

次は、知覚上の結果と不確かさを分けて保持しつつ、観測が支える音響の動力学と予測範囲を見直す。
局所行動の価値学習、持続する知覚的帰属、関係の期待、認知パラメータ、phrase・終止、本番接続は未達である。
本番の音、作者試聴、commitは追加していない。

### 9.3.26 周波数と、自然増幅しない成分の減衰を適合し直す

研究用に`refitted_passive`族を追加した。選んだ行動の観測PCMから、固定周波数と対数ゲインを適合し直す。
個々の成分へ`g <= 0`を課すと、`r = exp(g/fs)`は非増幅となり、減衰なしの極限も含む。
周波数・減衰と線形振幅を分けるモード表現は、[Maestreら（2017）第2節](https://dafx.de/paper-archive/2017/papers/DAFx17_paper_95.pdf)
を参照する。実装ではGauss–Newtonの各試行を制約内へ射影し、線形振幅を解き直してから誤差を比較する。
原論文の周波数領域の最適化を再現したものではなく、大域最小も主張しない。適合後に極だけを切り詰める処理とは異なる。

これは無励起の搬送成分に関する物理的な仮説である。重ね合わせた波形の振幅が単調減少するとは限らず、
残差ARの確率的駆動と他の候補も残す。対照音には立ち上がり・消音・身体の変動が含まれる。
外部音の生成パラメータは検査側の記録に限り、推論へ渡さない。ここでの位相は音響搬送波の位相で、拍や個体の参加位相を定義しない。
減衰値は観測に適合する音響パラメータである。候補数、観測窓、反復上限は数値計算の選択で、認知的な保持時間からは導いていない。

適合8 ms・共通比較2 ms・将来40 msの境界を保ち、既存108条件から216の独立した選択行動を比較した。
利用可能な族へ等しい総事前質量を置く。別の`retained`予測では旧族だけへ制限して重みを正規化し、
前段の混合を同じ発行・対象時刻で比較する。新しい適合、制約付きの動力学、候補族の追加を含む比較であり、
非増幅制約だけの効果は分離していない。既存入力の再利用で、新しい未使用入力や試聴試行ではない。

| 環境（各72 episode） | PCM改善 / 同等 / 悪化 | 平均PCM誤差比 | 平均R/H/C誤差比 |
|---|---:|---:|---:|
| 保持 | 12 / 60 / 0 | 0.9994 | 1.000 / 1.000 / 1.000 |
| 消音 | 36 / 36 / 0 | 0.6299 | 0.983 / 1.000 / 0.998 |
| 消音と別音追加 | 60 / 0 / 12 | 0.6900 | 0.019 / 0.097 / 0.051 |

比は拡張した混合と旧族の混合のepisode平均二乗誤差を比べる。PCMの分類には相対許容差1e-6を使う。
別音追加のR/H/C誤差は大きく減るが、PCM誤差は12件で増加し、最大で対照の1.603倍となった。
未来の同時密度は204件で改善し、12件は同等だった（絶対許容差1e-7）。これらの成績から音源同定、関係の期待、
音楽性、確率予測の較正は確定しない。

不確かさには不足が残る。消音でのR/H/Cの中央90%区間の被覆率は61.6 / 36.7 / 52.2%から55.2 / 26.1 / 44.5%へ低下した。
別音追加では62.1 / 53.2 / 58.7%から65.0 / 42.2 / 54.7%へ変わり、予測区間は狭くなった。
周波数・減衰・AR応答は引き続き条件付きの点推定で、その連続的な推定の不確かさを積分していない。
保持条件の32標本による知覚量の成績は完全に一致したが、混合分布全体が一致する意味ではない。
小さい重みの候補は、有限標本に現れなくても厳密な同時密度へ寄与できる。平均誤差の減少だけで不確かさの妥当性を判断しない。

新族の平均重みは三環境の順に0.000354 / 0.176891 / 0.661154で、最大重みの単一候補となった件数は0 / 12 / 48だった。
利用可能な新候補16,416件は全て対数ゲインが非正で、旧族を含む候補数は各73〜149件となった。
重みと候補数を、同定された原因や身体数として扱わない。

既知の二成分の減衰と未観測の継続を24/48 kHzで再現した。増幅入力では、制約下で振幅を解き直す適合と、
適合後の極の切り詰めを区別する検査が通過した。216件の旧族対照は前段のPCM誤差・同時密度に数値許容差内で一致し、
54組のpresentation-only対照は行動間で一致した。新機能を使わない旧族最良候補の6条件の予測は完全一致した。
適合・比較・未観測の末尾・未選択行動の入力境界も維持した。Rust全721件・9件ignore、Python全297件、全target Clippy・check通過。
432回の予測・実測native exportも成功した。記録は`target/phrase-expectation/2026-09-08-passive-dynamics/`。

次は周波数・減衰・AR推定の連続的な不確かさと、低重みの候補を含む知覚量の計算を明示的に扱う。
認知パラメータ、持続する知覚的帰属、関係の期待、phrase・終止、局所行動の価値学習、本番接続は未達である。
本番の音、作者試聴、commitは変更していない。

### 9.3.27 周波数・減衰を確定値にしない条件付き予測

`scripts/acoustic_parameters.py`に、固定した成分数とAR応答の下で周波数・減衰を積分する研究経路を追加した。
近接・重複した成分の振幅を識別できない場合にも有限な密度を持たせるため、振幅と分散の事前分布を明示する。
白色化した観測を\(z\)、実数のcos/sin基底を\(X_\eta\)、その係数を\(c\)とし、
\(c\mid q\sim N(0,q\lambda^{-1}I)\)、\(q\sim\operatorname{InvGamma}(a_0,b_0)\)とする。
振幅の基準時刻は観測ブロックの開始に固定する。数値計算用に正規化した基底の係数へ事前分布を置かない。
[Murphy（2007）](https://www.cs.ubc.ca/~murphyk/Papers/bayesGauss.pdf)の分散分布の規約を使い、
最初の\(p\)個の観測を条件として、その後のARイノベーションの尤度を積算する。

\[
V=\lambda I+X_\eta^T X_\eta,\qquad m=V^{-1}X_\eta^Tz,\qquad
a=a_0+n/2,\qquad
b=b_0+\tfrac12\left(\lVert z-X_\eta m\rVert^2+\lambda\lVert m\rVert^2\right).
\]

実装は拡大行列のQRで計算する。振幅・分散を積分した周辺尤度で、連続パラメータ\(\eta\)を比較する。
候補を保持した予測は、各候補のStudent予測の混合になる。同じパラメータ候補から一つの波形軌跡全体を生成し、
音声サンプルごとに別の候補を選ばない。成分のラベルは交換可能で、知覚上の音源名ではない。

パラメータはlog2周波数20–8000 Hzと対数ゲイン−1000–0 /sでそれぞれ一様とし、
\(\lambda=10^{-4},a_0=2,b_0=10^{-6}\)を明示した。数値上の設定と音響上の仮説であり、DCCから導出した認知定数ではない。
従来の観測済み8 ms・比較2 msから、非増幅候補の成分数・AR応答・観測開始を一つ選ぶ。
その観測区間から周波数・減衰の分布を計算し、同じ40 ms先を予測する。成分数・AR応答・区間選択の不確かさは積分していない。

対称な反射正規提案と一様提案による[Metropolis更新](https://doi.org/10.1093/biomet/57.1.97)を使う。
四つの初期状態の一つは事前分布から取り、512 sweepのwarmupだけで提案幅を調整する。
以後は提案幅を固定し、二つのsweepごとに各鎖256状態を保持する。1024状態の重複も質量として残す。
[Vehtariら（2021）](https://arxiv.org/abs/1903.08008)に沿うrank-normalized split/folded R-hatとbulk ESSを記録する。
成分を周波数で並べた量と周辺尤度を検査し、R-hat < 1.01かつbulk ESS ≥ 400を予備的な確認に使う。
この確認は尾部や予測量の収束保証ではない。基準を満たさない試行も比較から除かない。

対照には、同じ事前分布を使い、訪問した点のうち周辺尤度が最大の点を固定した予測と、元の適合点を固定した予測を置く。
最大点の大域最適性は主張しない。旧族全体の混合も別に保持し、事前分布の変更をパラメータの積分だけの効果と混同しない。
残差ARが周期音を説明する場合、明示的な周波数・減衰の分布が広く残っても予測がほとんど変わらない場合がある。
鎖間の一致、パラメータを音から識別できる程度、将来予測の精度は、それぞれ別の検査対象である。

一成分の平均・分散は独立した二次元数値積分と比較した。事前分布の周辺尤度とStudent予測は独立した密行列計算と比較し、
逐次更新の加法性、未観測の振幅方向、成分順序の不変性、未選択の実音と未観測の未来を読まない境界も検査した。
候補間でlatticeの列をまとめる計算は、個別計算と同じ演算順序を保つ。これは計算の分割単位で、認知容量ではない。

既存108条件の二つの選択行動、計216 episodeを比較した。次表の比は、パラメータを積分した予測と、
同じ事前分布で訪問した最大点を固定した予測の、episode平均二乗誤差の比である。

| 環境（各72 episode） | PCM改善 / 同等 / 悪化 | 平均PCM誤差比 | 探索の予備基準を通過 |
|---|---:|---:|---:|
| 保持 | 36 / 0 / 36 | 1.3266 | 36 / 72 |
| 消音 | 36 / 0 / 36 | 1.0290 | 48 / 72 |
| 消音と別音追加 | 0 / 0 / 72 | 1.0392 | 48 / 72 |

分類の相対許容差は1e-6。最大点より平均PCM誤差が増え、この設定で積分による改善は確認できなかった。
旧族全体の混合に対する平均PCM誤差比は234.36 / 2.679 / 0.651だったが、事前分布と成分・ARの選択も異なる比較である。
旧混合に対するR/H/Cの平均誤差比も、保持で約31424 / 25050 / 25377、消音で3255 / 295 / 543、
別音追加で108 / 18 / 39へ増加した。この新経路は旧混合の置換として採用しない。
知覚量は各32軌跡の標本平均で、低重み候補の寄与やMonte Carlo誤差を十分に評価した数値ではない。

探索の予備基準は132/216件で通過し、最大R-hatは1.1413、最小bulk ESSは20.1だった。
最大差の一条件で、事前分布を変えずwarmupを2048 sweep、保持を各鎖1024状態・4 sweep間隔へ延長すると、
最大R-hatは1.0289、最小ESSは156.0へ変わったが、基準には届かなかった。これは探索量の診断で、新しい未使用条件の精度検査ではない。
一成分の予備条件では、減衰の標準偏差が事前分布の約98%残る一方、予測差は小さかった。
固定したAR応答も周期性を説明できる構成では、明示成分の推定精度だけを検査しても身体の獲得を判定できない。

旧混合の成績と全候補の予測ハッシュは216件で完全一致し、presentation-onlyの54組も一致した。
Rust全721件・9件ignore、Python全304件、全target Clippy・check通過。入力757ファイルを以前のmanifestと照合した。
本番のRust/Cargo 104ファイルは前段から不変。今回も作者試聴、本番の音・行動選択、commitは追加していない。
実音比較の記録は`target/phrase-expectation/2026-09-08-parameter-uncertainty/`に置く。
低重み候補の知覚量への寄与、AR応答の不確かさ、観測から支えられる事前分布は引き続き課題となる。
認知的な保持時間、流れの同一性、関係の期待、phrase・終止、行動価値学習、本番への接続は未達である。

### 9.3.28 振動状態と継続励起を分けた条件付き予測

9.3.27の明示成分と残差ARは、同じ周期性を説明できる。441 Hz、対数ゲイン−17/sの
成分と同じ極を持つAR(2)を構成すると、明示成分を加えても、その振幅と残差の状態が補償する。
振幅精度を1e−8から1e4まで変えた四条件で、成分なしとの差は条件付き対数密度、予測平均、
分散ともゼロだった。振幅の不確かさを追加するだけでは、新しい身体の情報は得られない。

`scripts/acoustic_body.py`は、一つの振動状態を保持し、そこへ継続励起を加える研究モデルである。
各モードの二つの実座標について、次の線形確率モデルを使う。

\[
dx_j=(g_j I+2\pi f_j J)x_j\,dt+\sqrt{q d_j}\,dW_j,
\qquad y_n=\sum_j[1\;0]x_{j,n}+v_n,\quad v_n\sim N(0,q).
\]

ここで\(J\)は平面の回転生成子、\(g_j\leq0\)は減衰、\(d_j\geq0\)は観測分散に対する
単位時間当たりの励起強度である。二座標への独立な等方励起は音響上の仮説であり、
実際の発音機構や知覚された身体を同定したものではない。別の共鳴AR状態は加えない。
間隔\(h=1/f_s\)に対して、状態遷移は\(e^{g_j h}\)倍の回転となり、正規化した過程分散は
\(d_j\operatorname{expm1}(2g_jh)/(2g_j)\,I\)、\(g_j=0\)では\(d_jhI\)となる。
線形SDEの離散化とフィルタは[Särkkä and Solin, 2019, §6.2・§10.6](https://users.aalto.fi/~asolin/sde-book/sde-book.pdf)を参照する。

初期状態には\(x_0\mid q\sim N(0,q\lambda^{-1}I)\)、共通分散には
\(q\sim\operatorname{InvGamma}(a_0,b_0)\)を置く。条件付きKalman更新と共役分散更新により、
残る振動、将来の励起、観測誤差を含む同時Student予測を発行する。分散は軌跡全体で一度引く。
発行済みの予測は保持状態から独立した写しとし、後続観測で書き換えない。既知の自己音は波形で加え、
その和を同じnative聴覚分析状態へ通す。初期状態を繰り返し捨てたり、未来の予測で実音の分析履歴を更新したりしない。

独立した連続時間の共分散関数から密行列を作り、状態更新、周辺密度、予測平均・分散と同時密度を照合した。
励起ゼロでは、同じ事前分布の減衰成分モデルと一致する。24/48 kHzで状態遷移と励起分散の合成も一致する。
これは潜在状態の時間刻みの検査であり、異なる標本化率の聴覚・認知推論が等価だという検査ではない。
観測で生じるモード間共分散と、共通分散が生む依存は軌跡サンプルにも残る。
同じ周波数の二モードへ初期分散と励起を半分ずつ分けたモデルは、一モードと同じ予測になる。
身体数や知覚上の流れが同定可能になったわけではない。

比較器には任意の`driven_body`計画を追加した。8 msの既観測区間で得た非増幅成分の周波数・減衰と
観測開始候補を使い、残差AR次数だけが異なる重複を除く。励起強度は0・100・10,000/s、
\(\lambda=10^{-4},a_0=2,b_0=10^{-6}\)と宣言し、次の2 msで候補の重みを更新する。
適合に使った区間の条件付き密度を、構造を積分した証拠として採用しない。周波数・減衰は点推定であり、
9.3.27の連続パラメータ積分を移植したものではない。40 ms先の予測と32本の軌跡を保存してから実音で採点する。
未来・未選択行動の音を変更しても予測は不変、比較区間を変更すると更新後の予測だけが変わる検査を通した。

既存108入力×発音・待機の216 episodeを完了した。従来の構造混合に対する平均誤差比は次の通り。

| 環境 | PCM | 粗さ | 調波性 | 協和場 |
|---|---:|---:|---:|---:|
| 保持 | 0.7961 | 0.0000600 | 0.0000309 | 0.0000425 |
| 消音 | 1.8849 | 0.4143 | 0.6337 | 0.6181 |
| 消音と別音追加 | 0.8017 | 1.5418 | 0.3486 | 0.6389 |

PCM誤差の改善・悪化は保持68・4件、消音24・48件、別音追加36・36件だった。
同時予測密度は216件すべてで従来混合より低い。本番の置換には採用せず、状態を分けた研究部品として保持する。
同じ身体候補の励起ゼロ混合に対しては、PCMの平均誤差比が1.00006・0.9350・0.1605、同時密度は全件で改善した。
ただし、身体パラメータ、励起比率、観測誤差のモデルは未較正である。

固定したsine/habitat/24 kHz/seed 184957の三環境×二行動で、事前分散のscaleを100分の1・100倍にする
感度比較も行った。消音では大きいscaleでPCM誤差が約0.463倍になる一方、知覚量の誤差は約5.78–5.90倍になった。
小さいscaleはこの六条件で同時密度を改善したが、この診断的な再利用から既定値を選ばない。

保持条件の励起候補の総質量は0.000381–0.001908だった。しかし72条件×32本の計2,304軌跡は、
励起ゼロだけの混合から引いたものとすべて完全一致した。この質量を32回の抽選で一度も選ばない確率は約94.1–98.8%である。
粗さ・調波性・協和場の成績が励起ゼロ対照と同じだった結果には、この有限抽選による取り逃がしがある。
次は候補ごとの重みを保つ知覚量積分と、その数値誤差を扱う。候補を保持するだけでは知覚量評価へ反映されない。

従来混合の成績と全候補予測216件、presentation-onlyの54対、pilotの一条件の再実行は完全一致した。
Rust全721件・9件ignore、Python全311件とサイト生成を通過した。本番のRust/Cargo 104ファイルは前段と同一。
新しい作者試聴とcommitは追加していない。記録は`target/phrase-expectation/2026-09-08-driven-body-state/`。
認知的な保持時間、関係の期待、phrase・終止、局所行動の価値学習と本番接続は未達。9.3.5の契約を維持する。

### 9.3.29 候補の重みを保つ知覚量積分

9.3.28で、保持した候補が有限回の抽選で選ばれず、知覚量評価へ反映されない条件を確認した。
`scripts/perceptual_sampling.py`は候補を層として軌跡を抽出し、各候補の元の質量を保って集計する。
これは[Owen, §8.4](https://artowen.su.domains/mc/Ch-var-basic.pdf)の層別積分を、
条件付き音響候補の離散混合へ適用したものである。候補の確率や認知モデルを新しく学習する機構ではない。

候補\(k\)の重みを\(w_k\)、抽出数を\(n_k\)、その軌跡を聴覚変換した知覚量を\(f_{ki}\)とする。
残す候補の集合\(S\)に対して、\(\hat\mu_S=\sum_{k\in S}w_k\bar f_k\)を計算する。
重みを残った候補だけで再正規化しない。省いた質量を\(m\)とすれば、\(f\in[0,1]\)の期待値への
未計算の寄与は\([0,m]\)に入る。これは粗さ・調波性のstateと協和場のlevelに適用できるが、
無界のPCMやpotentialsには適用しない。

候補内の抽出誤差は、標本分散から\(\sum_k w_k^2s_k^2/n_k\)として推定する。
別に、独立した抽出と有界性から、各出力座標についてHoeffding半径
\(r=\sqrt{\tfrac12\sum_k(w_k^2/n_k)\log(2/\alpha)}\)を計算する。
期待値の数値区間は\([\hat\mu_S-r,\hat\mu_S+m+r]\)を\([0,1]\)へ制限したものになる。
これは固定した予測モデルの積分誤差を扱う区間であり、実際の未来音に対する予測区間でも、
全周波数binを同時に覆う区間でもない。浮動小数点誤差は別に残る。

各候補に最低限の軌跡を与え、さらに\(\sum_k w_k^2/n_k\leq1/N\)を満たすまで抽出数を増やす。
小さい質量への最低割当てで主要候補の抽出が不足する問題を避け、有界量の最悪分散上限を通常の\(N\)抽出以下にする。
実際の分散が常に小さくなる保証ではない。\(N\)、最低抽出数、省略質量の許容値は数値計算の設定である。
軌跡内の相関と共通分散は各モデルの抽出器が保持し、音響身体や予測モデルは変更しない。

比較器の任意の`perceptual_sampling`計画で追加できる。従来の通常抽選と予測分位点はそのまま保存し、
新しい`stratified_fields`と`integration-moments.npz`へ、重み付き平均、省略質量、数値標準誤差と区間を分けて記録する。
未知の省略寄与の中点を使った誤差は`midpoint_mse`として明示する。少数抽選の予測分位点を、数値区間で置き換えない。
重み0.001の候補を32回の通常抽選が選ばない対照でも、層別集計はその寄与を保持した。
省略質量、対数重みのunderflow、単一候補、数値標準誤差と未来入力の隔離を検査した。

既存の3身体×3環境×二行動について、24 kHz、habitat、seed 184957の18 episodeを比較した。
固定したsineの三環境には、層別抽出のseedを変えた二対照と、目標抽出数512の数値参照を各6 episode実行した。通常抽選のseedは固定した。
目標32、最低4本、省略質量1e−10を主条件とし、数値参照は512・16本・1e−12とする。
先行pilotは最低割当てだけで主要候補の抽出が不足した旧割当てを使っており、ソースと結果を別に保存した。
主条件の実抽出数は32–72本、省略質量は最大5.23e−11だった。保持条件でも励起候補から必ず軌跡を抽出した。
主条件と対照の計54予測で、従来の予測分布・通常抽選の波形・通常抽選による成績は完全一致した。

身体混合の層別集計による実際の未来音への平均誤差比は、通常抽選に対して次の通りだった。
予測モデルの変更ではなく、同じ条件付き期待値を計算する方式の比較である。

| 環境 | 粗さ | 調波性 | 協和場 |
|---|---:|---:|---:|
| 保持 | 1.2965 | 1.3257 | 1.3299 |
| 消音 | 0.9544 | 0.9001 | 0.9211 |
| 消音と別音追加 | 0.3830 | 1.2510 | 0.8372 |

512目標の数値参照は実際には512–670本を使った。その有限参照への平均偏差の二乗を、通常抽選の値で割ると、
粗さでは三つの層別seedで1.3983・1.8666・0.00618、調波性で1.3934・1.8759・0.00650、
協和場で1.3962・1.8705・0.00631となった。通常抽選は一つの固定seedであり、一般的な分散低減の実証ではない。
参照自体の推定標準誤差も、最大で粗さ5.84e−5、調波性1.97e−4、協和場6.41e−5残った。
Hoeffding半径は主条件で約0.239–0.240、参照でも約0.060と保守的であり、細かい差の収束を示すものではない。

保持候補の取り逃がしを修正し、意図して省いた質量は再正規化せず記録した。数値収束と予測較正の達成は別に残す。
Rust全721件・9件ignore、Python全315件を通過した。根拠は`target/phrase-expectation/2026-09-08-perceptual-integration/`。
知覚モデルの較正、関係の記憶、phrase・終止と、本番の行動選択は未達のままである。
本節は選んだ行動の後の10 msを観測した継続予測であり、発音前の選択価値を検査したものではない。
次は同じ発音前の証拠から発音・待機を分岐させ、周囲の予測軌跡を共有して知覚的な差を見積もる。
選択前の予測、選んだ結果による学習、未選択分岐のオフライン評価を分離する。

### 9.3.30 発音前の分岐予測と、選んだ結果による更新の限界

9.3.23の共有した周囲の予測を、9.3.25–9.3.29の候補保持と知覚量積分へ接続した。
`scripts/evaluate_prospective_actions.py`は、発音前の最後の8 msで適合し、続く2 msを共通の比較に使う。
その時点までの証拠から周囲の50 msの予測軌跡を抽出し、各軌跡に自分の発音・待機の波形をそれぞれ加える。
二つの分岐は同じ実音の分析履歴から出発し、同じ周囲の抽出を共有する。
これは共通乱数による比較である（[Owen, §8.6](https://artowen.su.domains/mc/Ch-var-basic.pdf)）。
差を直接積分し、区間を`[-1,1]`として省略質量と数値誤差を扱う。差がゼロの対照は計算上も厳密にゼロを保つ。

初期予測を確定した後、元のモデルを選択行動ごとに独立に複製し、その側の実音10 msだけで状態・重み・分散を更新する。
残りの予測を確定するまで、未選択側や選択側の後続の実音は評価に読まない。
初期・更新後とも、評価終端は発音指令から約45.33 ms後の同じ完全なnative hopとなる。
二行動は研究用に指定した独立の選択であり、モデルが行動価値から選んだ結果ではない。
8/2/10/50 msはこの比較の数値設定であり、認知的な記憶長やphraseの長さを同定していない。

既存の3身体×2 routing×24/48 kHz×3環境×3 seedの108条件を完了した。
未来だけ異なる三環境は36組で発音前の証拠が完全一致し、候補予測、抽出、native予測、知覚量の差も一致した。
native記録の条件IDだけは異なる。選択後の独立更新216件で、実音を途中から継ぎ足した分析は一括分析と完全一致した。
自己音が生態環境へ入らないpresentationの54条件では、予測と実測の行動差は全てゼロだった。
未来の改変、未選択側の改変、選んだ前半だけの改変を分けるテストでも、予測への利用境界を検査した。

「行動差はゼロ」という対照だけでは自己音を知る利点が混ざるため、主比較の後に追加の対照を設けた。
過去の実音と自分の予定波形を維持し、未来の周囲だけゼロとする。調整するパラメータはなく、三環境を対応付けた36組の代表を再利用した。
その証拠は24種類で、presentationの一部はseed間でも同一だった。
以下はhabitat各環境18条件で、行動差の平均二乗誤差をこの自己音だけの対照で割った値である。

| 環境 | 予測モデル | 粗さ | 調波性 | 協和場 |
|---|---|---:|---:|---:|
| 保持 | 構造混合 | 0.002775 | 0.001298 | 0.001559 |
| 保持 | 振動・励起の混合 | 1.619e−5 | 8.948e−7 | 2.419e−6 |
| 消音 | 構造混合 | 0.03896 | 0.04909 | 0.05226 |
| 消音 | 振動・励起の混合 | 0.04189 | 0.05592 | 0.05948 |
| 消音と別音追加 | 構造混合 | 4.332 | 0.2253 | 0.3363 |
| 消音と別音追加 | 振動・励起の混合 | 4.506 | 0.2425 | 0.3587 |

保持・消音では環境予測が有効だが、別音追加の粗さでは両モデルともこの対照より悪い。
選んだ実音による更新にも限界がある。振動・励起の混合で、保持条件の更新後の誤差は初期予測の約0.196–0.285倍だった。
一方、別音追加では待機の約19.60–22.30倍、発音の約4.02–10.93倍へ悪化した。比較は同じ将来終端で行っている。
状態・重みを更新しても、発音前に作った候補へ新しい周波数構造を追加する経路はない。
次は選んだ実音から候補を追加し、既存候補と同じ後続観測で比較する。候補不足と状態・分散更新の寄与を分ける。

この切り分けに先立ち、9.3.29の24 kHz・habitat・seed 184957の18例も再利用した。
10 msまでの実音履歴と評価終端は完全一致する。観測後に候補を作った方式の誤差を、今回の候補を保持した更新で割ると、
粗さ・調波性・協和場は保持で0.07671・0.07610・0.07685、消音で0.1922・0.2165・0.1982、
別音追加で0.1742・0.5373・0.3790だった。別音追加では個別に悪化する例も残る。
状態の事前分布を置き直す操作と抽出seedも異なるため、候補追加だけの効果とは帰属させない。

初期216予測の抽出は44–86本、省略質量は最大9.99e−11だった。周囲の共有による分散低減は一律ではない。
habitatの異なる観測18条件で、推定した行動差の数値分散を独立抽出の場合で割ると、構造混合の粗さ・調波性・協和場は
1.009・1.001・1.002、振動・励起の混合は0.890・0.924・0.909だった。一組の抽出からの推定であり、反復実験での分散ではない。
差のHoeffding半径は約0.473–0.480と保守的で、数値収束や予測較正は未達のままである。

共有処理の変更前に保存した36の身体候補バンクを再構成し、54予測と既定区間の知覚量積分810配列は完全一致した。
Rust全721件・9件ignore、Python全318件を通過。記録は`target/phrase-expectation/2026-09-08-prospective-actions/`。
周囲の音源は自分の行動へ応答しない対照であり、社会的な受け渡しの学習や作者の知覚の再現を検証したものではない。
本番の発音と行動選択は変更しない。関係の記憶、phrase・終止、認知パラメータ、局所行動の価値学習と本番接続は未達である。

### 9.3.31 選んだ実音による候補更新と、保持・初期化の切り分け

9.3.30の更新後の悪化に対し、`scripts/acoustic_revision.py`と
`scripts/evaluate_selected_revision.py`へ比較機構を実装した。
**全108条件・216の指定行動episodeを完了した。新候補の追加は有効な範囲を持つが、短い比較区間で付けた重みを本番へ採用する根拠はまだ足りない。**

発音前の推定候補を保存されたパラメータと実音履歴から再構成し、既発行の予測と完全一致することを確認する。
選んだ側の実音8 msを観測した後、以下の候補を同じ後続2 msで比較し、残る40 msを予測する。

| 比較 | 操作 |
|---|---|
| retained | 振動状態、分散の事後分布、候補の重みを保持して更新する |
| state_reset | 発音指令時点で振動状態だけを元の状態事前分布へ置き直し、同じ8 msを観測する |
| state_scale_reset | 振動状態に加え、共通分散の事前分布も置き直す |
| uniform_reset | state_scale_resetと同じ成分予測を使い、8 msの後で候補の重みだけ一様へ戻す |
| fresh | 選んだ8 msの全部または末尾から周波数・減衰・成分数の候補を作る。全候補の状態を同じ8 msで条件付け、比較前の重みを一様とする |
| revision | retainedとfreshを両方保持し、群の質量を事前指定の1:1として、同じ後続2 msで更新する |

uniform_resetとfreshは状態・分散の事前分布と状態を条件付ける区間が共通で、推定した構造と採用候補集合が異なる。
freshには共鳴成分ゼロの観測雑音候補も含める。revisionは微小な重みを対数のまま保持し、比較区間を見る前に群の質量を定める。
新候補は適合済みパラメータに条件付けた予測であり、適合区間の成績を新しい構造の事後確率とは扱わない。
この初期化は認知的な忘却モデルではなく、8/2/40 msや群の質量も認知的な保持時間から導出した値ではない。

比較用の実音は候補予測の保存後、評価対象の実音はnative予測の保存後に読む。未選択側の実音は使わない。
比較区間を変えるとその後の予測は変わるが、作成済みの候補は変わらないことをテストした。
状態と分散の初期化を分ける検査、同じ成分で重みだけを変更する検査、保持候補にない周波数の推定、
微小な対数質量と共通観測による群更新、ゼロ成分の候補も含む5テストを追加し、Python全323件を通過した。
本番のRustは変更せず、Rust全721件・9件ignoreも通過した。

216件で発音前のモデルを再現し、選んだ実音の履歴とnative評価終端は9.3.30と完全一致した。
重みだけを一様へ変える対照でも、比較対象となる成分の予測密度は全216件で一致した。
8 msと2 msを分ける更新と10 ms一括更新の差は、予測平均で最大1.71e−14、分散で6.63e−17だった。
以下はhabitatの各環境36 episodeで、freshの平均二乗誤差をretainedで割った値である。

| 環境 | PCM | 粗さ | 調波性 | 協和場 |
|---|---:|---:|---:|---:|
| 保持 | 0.3475 | 0.06119 | 0.07498 | 0.06612 |
| 消音 | 0.5369 | 0.4019 | 0.5087 | 0.5002 |
| 消音と別音追加 | 1.0838 | 0.2232 | 0.1712 | 0.2156 |

初期化だけの効果は環境によって異なった。別音追加で状態だけを置き直すと、三つの知覚量の誤差は約1.035–1.036倍、
分散も置き直すと約1.515–1.570倍へ悪化した。重みまで一様へ戻す対照では約0.738–0.799倍となった。
このため、改善を一律に状態初期化や新しい周波数だけへ帰属させない。
またfreshの未観測PCMに対する同時密度は、保持と別音追加では各36件すべてで改善したが、消音では36件すべてで悪化した。

**予測分布の追加監査。** 平均値の誤差とPCMの密度だけでは、知覚量の予測分布を評価しきれない。
主比較の後に、保存済みのnative抽出へ`perceptual_scores.py`を適用した。
[Gneiting and Raftery（2007）, 式22](https://sites.stat.washington.edu/people/raftery/Research/PDF/Gneiting2007jasa.pdf)
の符号を反転したenergy lossを、各知覚量の周波数ベクトルに対して計算する。

\[
L(P,y)=E\,d(X,y)-\tfrac12 E\,d(X,X'),
\qquad d(x,y)=\sqrt{\operatorname{mean}_{b}(x_b-y_b)^2}.
\]

同じ候補からの二つの独立な抽出には、自己対を除いた\(n(n-1)\)の分母を使う。候補の重みは再正規化しない。
省略質量を\(\epsilon\)、保持質量を\(M\)とすると、省略部分の寄与は
\([-\epsilon(2-\epsilon)/2,\epsilon]\)に入る。
一つの抽出を交換したときの変化は\((1+M)w_j/n_j\)以下なので、
[bounded-differences不等式](https://arxiv.org/pdf/1212.5796)から数値区間を別に求めた。
これは既存予測の採点であり、観測後に予測やモデルの重みを変更する操作は含まない。

以下は平均energy lossのretainedに対する比で、小さい方がよい。各セルは粗さ・調波性・協和場の順である。

| 環境 | fresh | revision |
|---|---|---|
| 保持 | 0.2745 / 0.3076 / 0.2870 | 0.7258 / 0.7982 / 0.7543 |
| 消音 | 0.6222 / 0.5705 / 0.5894 | 0.6222 / 0.5705 / 0.5894 |
| 消音と別音追加 | 0.2778 / 0.2463 / 0.2693 | 0.2778 / 0.2463 / 0.2693 |

freshは保持の各36件すべて、消音の各32件で改善した。別音追加では各30–31件で改善し、5–6件は悪化した。
revisionは保持の平均では改善したが、個別には20–21件で悪化した。
保持の2 ms比較では新候補の群が優勢となったのは6/36件であり、後の40 msではfreshの密度と知覚量の分布の採点が全件でよかった。
短い区間への適合を、そのまま長い予測の信頼度とは扱わない。比較する表現と、実際に使う予測時間に対する成績を分ける。

同じ平均・周辺分布で異なる同時分布を区別する検査、離散分布の全列挙、省略質量、微小な距離の4テストを追加した。
Python全327件を通過した。1,296予測の抽出は32–88本、省略質量は最大6.83e−11だった。
採点の保守的な数値半径は約0.472–0.480で、条件間でもseedを再利用している。平均を取ったことだけで数値収束や予測較正を認定しない。
根拠は`target/phrase-expectation/2026-09-08-selected-model-revision/`に保存する。

次の接続点も区別する。現在のTemporalParticipationは、発音時に予測していた文脈を参加の記憶へ加える。
実音の結果はreportへ対応付けるが、行動選択の更新には戻していない。
本番接続には、予測した意図、実行、観測した結果を保持し、同じ予測対象に対する成績を更新する経路が必要になる。
関係の記憶、phrase・終止、認知パラメータ、局所行動の価値学習と本番接続は未達のままである。

### 9.3.32 連続エネルギーの予測を、同じ予測先の実音で更新する

本番の`AcousticTemporalExpectation`へ、帯域エネルギーの予測成績を後続の予測へ戻す候補を接続した。
これは周囲の継続予測の更新であり、選んだ発音の価値学習ではない。発音による介入を含まない
継続予測の誤差を、その個体の成功・失敗として扱わない。TemporalParticipationの参加記憶は
引き続き発音時の予測文脈であり、行動条件付きの結果更新は未達である。

反復モデルの帯域エネルギーを\(r\)、発行時の最後の観測エネルギーが続く候補を\(p\)、
対応する将来窓の実測を\(y\)とする。各帯域と予測先について、発行時までに対象窓の観測が
完了した組だけから、次の係数を求める。

\[
A=\sum(r-p)^2,\qquad B=\sum(r-p)(y-p),\qquad
w=\operatorname{clip}(B/A,0,1),\qquad \hat y=wr+(1-w)p.
\]

これは過去の二乗誤差を最小化する凸結合である。\(A=0\)では区別する証拠がなく、従来の\(r\)を使う。
係数は認知的確信度でも音楽的報酬でもない。累積統計は忘却モデルを持たず、環境変化への追従は未解決である。
実音の観測時計で予測を発行し、問い合わせ回数やreportの有無では更新しない。
発行時の係数と二候補を保存し、実測到着後も発行済み予測を改変しない。
無音の実測も更新に使うが、入力欠落では未完了の予測と統計を破棄し、欠落中の無音を補わない。
自己寄与の除外には共有予測と同じ係数を使う。ただし帯域エネルギーの減算自体は、波形干渉を分離する厳密な音源推定ではない。

対象は、従来の表と同じ0–4秒先、20 ms間隔に始まる10 msの観測窓である。
中間時刻は補間し、範囲外の持続予測は従来どおり観測平均へ戻す。この範囲は数値上の予測表であり、
認知的な保持時間やphraseの長さではない。未完了予測は固定長の領域へ保存する。

変更前の08・09・12、seed 21のpresentation WAVを同じ観測器へ再入力した。
212,670個の係数を、発行時に利用できた実測だけから独立に再構成し、すべてf32で一致した。
以下は従来の反復予測に対する帯域エネルギーの二乗誤差比である。予測先は対象窓の開始までの時間を示す。

| Sample | 0 ms先 | 80 ms先 | 4秒先 |
|---|---:|---:|---:|
| 08 | 0.2443 | 0.6056 | 1.0914 |
| 09 | 0.3317 | 0.5552 | 1.0607 |
| 12 | 0.3917 | 0.9794 | 1.2521 |

短い予測には改善がある一方、4秒先は全3曲で悪化した。予測先を分けた更新だけで、長い予測の信頼性が確立したとは扱わない。
これは保存音の再入力であり、行動条件付きの反実仮想比較や新しい作者試聴ではない。
本番経路の再レンダリングでは08・09の発音時刻が変わり、12の発音列とWAVは完全一致した。
予測更新が行動へ届くことは確認できたが、音楽的採用は未判定である。

Rust全724件、Clippyを通過し、10件の研究検査はignoreである。
時刻・予測先・帯域を分ける検査、保存領域の周回を含む独立した最小化検査、発音検出なしの持続音による改善を追加した。
受信の分割、問い合わせ、入力欠落、自己寄与除外の既存検査も拡張した。
構築後の観測・予測経路に追加のヒープ割当はなく、未完了予測の領域は1,947,256 byteである。
検証記録は`target/phrase-expectation/2026-09-08-energy-outcome-feedback/`に保存する。
関係の記憶、認知パラメータ、phrase・終止、局所行動の価値学習は未達のままである。

4 Voice・harmonic・entrained、warmup 3秒・測定8秒の実機検査をreport有無で実行した。
両条件とも生存数4、測定窓の出力不足とcallbackエラーはゼロ、hop p99は0.461/0.497 msで10.667 msの予算以内だった。
report有効側では4 Voiceすべての反復発音を確認した。ALSA既定出力を使い、別途PipeWireの既定出力がMOTU M2であることを確認したが、
実行全体の経路追跡や機器側xrunの確認はしていない。最初のサンドボックス内の検査は音声サーバー接続が拒否され、
制限外で同じ機器検査を完了した。高密度・長時間・全音色の再検証ではない。

### 9.3.33 結果が届くまでの候補情報を、実測と分けて保持する

4秒先の悪化は曲の末尾だけでなく、中盤にも集中していた。完了済みの組だけで係数を適合すると、
長い結果待ちの間、初期の少ない結果から大きく切り替わる。そこで9.3.32の係数更新を変更した。
候補の差は発行時に既知であり、実音との対応は対象窓の終了後に初めて得られる。この二つを別に更新する。

反復予測からの変更量を使い、\(x=p-r\)、観測された残差を\(e=y-r\)とする。
発行済みの候補集合を\(I_t\)、そのうち実測が完了した集合を\(O_t\)として、各帯域・予測先で

\[
G_t=\sum_{s\in I_t}x_s^2,\qquad b_t=\sum_{s\in O_t}x_se_s,\qquad
\beta_t=\operatorname{clip}(b_t/G_t,0,1),\qquad
\hat y_t=r_t+\beta_t(p_t-r_t)
\]

を用いる。\(G_t=0\)では\(\beta_t=0\)とする。これは、完了した組の残差二乗誤差に、
未完了の候補について\((\beta x_s)^2\)を加えた目的の最小解である。
結果待ちの候補が多く、両候補の差が大きい間は、基準予測からの変更を抑える。
未観測の残差は実測として保存せず、候補を発行しても\(b_t\)は更新しない。
入力欠落による破棄と、未来の対象窓を待つ状態も区別する。

[Qiuら（2025）の遅延つき回帰](https://arxiv.org/pdf/2506.07595)も、既知の特徴量と、遅れて得る目的値を分ける。
ここではその区別を、基準予測からの変更量の凸制約へ適用した。論文の正の正則化係数や目的値範囲による
clippingと同一ではなく、そのregret保証を本実装へ移してはいない。認知的な忘却時間も導出していない。
予測表の4秒という範囲、入力の時計、自己寄与の除外、参加方策の報酬は変更していない。

開発時のseed 21では、4秒先の反復モデルに対する誤差比が08で1.0914から1.0164、
09で1.0607から1.0577、12で1.2521から1.0661へ減った。
比較前に追加seed 192001/192013を固定し、各3曲を修正前の実行ファイルで生成した。
同じ保存音へ修正後の観測器を適用し、旧係数も発行済み候補と実測から再構成した。
次は追加seedの4秒先の誤差比の範囲で、どちらも初期の反復モデルを分母とする。

| Sample | 完了組だけの適合 | 結果待ちの候補も保持 |
|---|---:|---:|
| 08 | 1.0692–1.0751 | 1.0079–1.0346 |
| 09 | 1.0696–1.1837 | 1.0016–1.0307 |
| 12 | 1.0569–1.1828 | 1.0284–1.0320 |

追加6音源すべてで旧係数より改善したが、反復モデルへの優位はまだ示していない。
中間の予測先には旧係数より悪い条件もある。全9音源・638,010個の係数を、発行時点で利用できた情報だけから
独立に再計算し、すべてf32で一致した。未観測の値を変更して係数を適合した比較ではない。

Rust全725件、Clippyが通過し、研究検査10件はignoreである。結果待ちの特徴量が実測の相関を変更しない検査、
単位の比例変更、保存領域の周回を含む目的関数の独立検査を追加・更新した。
記録は`target/phrase-expectation/2026-09-08-delayed-energy-fit/`に保存する。
継続予測の実装上の改善であり、行動条件付きの価値学習、認知的な保持時間、関係の記憶、phrase・終止は未達である。

### 9.3.34 自己音を波形で除き、周囲の観測を保つ

局所行動の結果を更新する前に、周囲の音の見積もりに残る誤りを修正した。
旧方式は混合音と自己音の帯域エネルギーを別々に予測し、前者から後者を引いていた。
帯域内の信号を自分の音 \(a\)、周囲の音 \(b\) とすると、
\(E[a+b]-E[a]=E[b]+2E[ab]\) となり、干渉項が残る。同じ波形の二音では相手を3倍、
逆位相の二音では負の値をゼロへ制限して相手を無音と見積もる。
440 Hzの二音による回帰検査で、修正前の3倍の誤りを再現した。

`OwnSoundHistory`は、実際のhabitat混合波形から、自分が生成してhabitatへ送った波形を引く。
その残りを状態付きの`DorsalStream`へ渡し、三帯域のエネルギー履歴を作る。
自分以外のVoiceの内部状態やラベルは参照しない。これは既知の自己寄与の除外であり、
聴取者が未知の音源を識別できたことを意味しない。混合・減算の浮動小数点誤差も残る。

個体の発音前から追跡し、誕生前の共有履歴、フィルタ状態、未完了の観測窓を引き継ぐ。
誕生前の音は全て周囲の音であり、ゼロで埋め直さない。後から参加方式へ切り替える個体も含め、
生存中のVoiceを追跡する。退役した個体の残響は、新しい個体の自己音へ引き継がない。
実際の波形合成とpresentationへの出力は、この観測処理から変更しない。

将来への射影には共有の反復候補と9.3.33の混合係数を使い、長い予測の範囲外には周囲の観測平均を使う。
共有係数は混合音の実測で学習しているため、個体ごとの予測較正を達成したとは扱わない。
発音文脈と周期参照、参加方策のコスト、音長は同じ規則を使う。
この修正で、実際の周囲の音を記憶・結果更新へ渡す前提が改善したが、
行動条件付きの結果学習、認知的な保持時間、関係の記憶、phrase・終止は引き続き未達である。

同位相・逆位相、観測窓の途中での誕生、presentationのみの自己音、入力分割を検査した。
Rust全726件・研究検査10件ignore、Clippy、releaseビルドが通過した。
自己音の観測を含め、構築後の観測・予測経路のヒープ割当ゼロを専用検査で確認した。

08・09・12のseed 192001/192013を再レンダリングした。08の発音数は200→200、202→205で、
両seedとも発音時刻が変わった。09・12は発音列とWAVが修正前と完全一致した。新しい作者試聴は行っていない。
16 Voice・harmonicのsustain/entrainedをreport有無で機器不要の負荷検査へかけた。
2秒のwarmup後の4秒で生存数16を維持し、worker hopのp99は1.400–1.752 ms、予算超過はゼロだった。
report付きentrainedでは全16 Voiceの発音を確認した。これは音声機器のcallbackやunderrunを検査した結果ではなく、
負荷検査本体の割当数も未計測である。検証記録は`target/phrase-expectation/2026-09-08-local-acoustic-context/`に保存する。

### 9.3.35 予測した文脈と、実際に経験した文脈を分ける

従来の参加記憶は、発音指令が確定すると、その発音前に予測したonset確率の差を保存していた。
指令の実行は、その予測が実音によって確かめられたことを意味しない。
実測された帯域エネルギーを発音確率の差へ直接代入することもできない。
そこで記憶と候補比較の表現を共に、発音に伴って経験する周囲の帯域エネルギーへ変更した。

発音時刻と、その80 ms後について、共有の観測格子上でそれぞれ最初に始まる完全な窓を選ぶ。
各窓は通常10 msで、三帯域の予測エネルギーと対象窓の始終端を候補選択時に保存する。
実際に発音した候補だけが結果待ちになる。発音を拒否された候補、意図的な見送り、
まだ結果が届いていない発音は記憶を更新しない。両窓の終了後に、9.3.34の周囲の実音履歴から
同じ窓を取得できた場合だけ、二窓×三帯域の文脈 \(c\) で記憶を更新する。

\[
\mu_{n+1}=\mu_n+0.1(c_n-\mu_n).
\]

最初の実測文脈を初期値とする。一定状態や無音も観測結果として扱うが、履歴の範囲外や格子外を
無音で補わない。更新後も候補選択時の予測値は変更しない。記録の有無は学習経路を切り替えない。
個体の終了までに揃わなかった文脈は、その個体の記憶へ取り込まない。

候補の予測エネルギーと記憶は、六成分の総エネルギーでそれぞれ正規化し、
平方Hellinger距離で形状を比較する。これは帯域と二窓へのエネルギー配分の距離であり、
知覚的な確信度ではない。両方が数値上の無音なら距離0、一方だけなら1とする。
絶対的な重なりは従来の別のコストで扱う。共通の発音位相、協和度の最大化、予測誤差の最小化を
全個体の成功条件として加えてはいない。

この実装は、選んだ発音時に経験した周囲を次の判断へ戻す。
「自分が鳴ったので周囲が変わった」という因果効果や、選ばなかった行動の結果を学んだわけではない。
自己音を含む混合音の行動条件付き予測と価値学習は引き続き別の課題である。
二窓の間隔、帯域の粗さ、0.1という更新率は工学的な設定で、認知的な保持時間を同定した値ではない。
更新率は実行済み文脈の件数に作用し、経過時間による忘却とは区別する。

結果待ちの容量は既存の最大発音率、観測の待ち時間、設定hop幅から構築時に確保する。
`participation_context`は発行時の予測、対象窓、実測、更新後の記憶を記録する。
既存の`participation_outcome`は共有の継続予測と混合音の照合として残し、両者を混同しない。
評価スクリプトにも両記録の型・窓の整合検査を追加した。

予測と異なる二種類の実音を与えると、窓の完了前には記憶が変わらず、完了後の記憶と次の候補位置が変わる。
結果の重複取り込み、欠落を無音とする更新、構築後の結果待ち領域の拡張を検査する。
Rust全729件（10件ignore）、Clippy、構築後の結果待ち・更新経路の割当ゼロ検査が通過した。
記録の有無でWAVが一致する結合検査と、既存の記録消費経路を含むPython 53件も通過した。

08・09・12のseed 192001/192013で、直前版のWAVを再現する6条件の基準と比較した。
発音数は08で200→202／205→200、09で702→702／694→694、12で602→602／624→623となり、
全条件で発音列とWAVが変わった。実測文脈2,142件を実際の発音と対応付け、観測値から
独立に再計算した記憶12,852成分はすべてf32で一致した。入力履歴の範囲外となる文脈はなく、
二窓の終了から取り込みまでの遅れは最大10 msだった。出力のfull-scale sampleは0である。
この比較は文脈の表現と更新元を共に変えた全変更の比較であり、実測feedbackだけの寄与は分離していない。

16 Voice・harmonicのentrained/flow、report有無で、2秒の予熱後に4秒の機器不要検査を行った。
生存Voice数は16、予算超過hopは0、hop p99は1.464–2.518 ms（予算10.667 ms）。
report有効条件では全16 Voiceの反復発音を確認した。これは実機のcallback・出力不足の検査ではなく、
全workerの割当を計測した結果でもない。今回の音源について新しい作者判定はない。
記録は`target/phrase-expectation/2026-09-08-observed-participation-context/`へ保存した。
関係の記憶、認知的な保持・干渉、phrase・終止、音楽的採用は未達のままである。

### 9.3.36 判断に先行した順序を、連続した履歴として保持する

二窓の経験文脈は、発音時と80 ms後に周囲がどう鳴ったかを表す。そこへ至った順序は含まれない。
低い音と高い音の順序を反転し、その後に同じ無音を置いた実音対照では、判断を0.8秒とすると、
従来の二窓のエネルギーは両方とも全成分0になった。現在の距離では区別できない。
この不足は、共変動、過去からの予測情報、流れの同一性を区別した9.3.12–9.3.14とも整合する。
共変動を一つ加えれば流れを同定できる、という設計にはしない。

9.3.8の連続時間履歴を、本番のhabitat観測と各Voiceの自己音除外後の観測へ接続した。
`AuditoryHistory`は[Shankar–Howard（2012）](https://sites.bu.edu/tcn/files/2015/12/ShankarHoward12-NeuralComp.pdf)
のPost逆変換から得るgamma核と等価な13段のcascadeを使う。三帯域のエネルギーの平方根を
各観測窓内で一定のRMSとして入力し、状態遷移を解析的に更新する。核の時間精度は遅れに比例する。
過去への応答位置0.125–16秒の8セルと次数12は、既存研究実装との比較用に明示した設定であり、
聴覚課題へ適合した保持時間でも、phraseの長さでもない。神経回路そのものを移植した実装ではない。

既知の入力の寄与と、観測が覆った核の重みを別々に保持する。無音を観測した区間は後者へ加え、
入力欠落は加えない。前向きの欠落では既存の履歴を時間に沿って進め、消去せずに残す。
未完了窓を挟む欠落では、その窓を既知のRMSで埋めない。過去へ巻き戻した入力は新しい履歴となる。
誕生前の音は共有観測器から利用可能な背景履歴として引き継ぎ、その後は既知の自己波形を除いて更新する。
これを個体が誕生前に経験した個人的な記憶とは扱わない。

`decision_external_history`は、候補選択に使える最新の観測時刻でのRMS履歴と観測範囲を保存する。
発音後の実測文脈でこの過去の記録を書き換えない。履歴への入力は実音に由来し、予定された発音を入れない。
記録の構造・時間位置・欠落・自己音除外を検査してから、先行履歴に条件づけた関係の検索と予測へ進む。
現在の発音コストと二窓の経験記憶は維持する。履歴の表現があることと、その履歴から相手への応答を選べることは別である。

同じ一定入力の分割、時間とセル位置の比例変換、独立したgamma核の数値積分、順序反転、
無音と欠落、誕生時の履歴と波形からの自己音除外を検査した。24・44.1・48 kHzの順序対照では、
二窓が同じ0でも判断前の履歴に最大0.01982–0.01984の差が残った。これはRMS表現の差であり、知覚の識別率ではない。
これら6対照と08・09・12の2seed、計12音源の18,906観測・604,992成分を既存の倍精度Python実装と照合した。
f32実装との最大絶対差は3.97×10⁻⁵未満、1e−8を床とする相対差は4.65×10⁻⁵未満だった。
Rust全732件（11件ignore）、Clippy、観測・予測・自己音除外の構築後割当ゼロ検査、Python 53件が通過した。
通常音源6条件の再生成ではWAV・発音列・既存の文脈更新が直前版と完全一致し、判断履歴2,142件を追加した。
記録の有無でWAVが一致する結合検査も通過した。16 Voice・harmonicのentrained/flow、report有無の
機器不要検査では、2秒の予熱後の4秒間に全16 Voiceを維持し、予算超過hopは0、hop p99は1.479–2.520 ms
（予算10.667 ms）だった。report有効条件では全Voiceの反復発音を確認した。実機のcallback・出力不足や
全workerの割当を測った結果ではなく、新しい音楽的な作者判定でもない。
記録は`target/phrase-expectation/2026-09-08-continuous-participation-history/`へ保存する。
認知的な保持・干渉、持続する関係の帰属、phrase・終止、行動価値の学習は未達である。

### 9.3.37 先行履歴による予測と、実測に基づく候補の比較

連続RMS履歴を、将来の周囲の帯域エネルギーの予測へ接続した。各Voiceは既知の自己波形を除いた実音から学び、判断前に発行した候補を、対応する観測窓が終了してから比較する。これは条件付きの音響予測であり、自分の行動が相手へ与えた因果効果や、関係の価値を学んだ結果ではない。

**履歴からの予測。** 入力57成分は、切片、現在の三帯域RMS、8時点×3帯域のRMS履歴、8時点の観測範囲、隣り合う時点・帯域の順序を表す21成分である。最後の成分は、古い側を \(o\)、新しい側を \(n\) として、帯域 \(a<b\) ごとの
\((o_a n_b-o_b n_a)/(\|o\|^2+\|n\|^2+10^{-12})\) とする。順序を反転すると符号が反転し、共通の強度変化だけでは生じない。床の値は数値上の設定であり、知覚的な確信ではない。これらの成分から、現在の実測エネルギーに加える補正をridge回帰する。

時点 \(i\) の特徴を \(x_i\)、実測エネルギーを \(e_i\)、予測先を \(h\) とする。特徴の行列は発行時に更新するが、応答との積は実測が届いた組だけを使う。

$$
G=0.01I+\sum_{i\in\mathrm{issued}}x_i x_i^T,\qquad
b_h=\sum_{i\in\mathrm{completed}(h)}x_i(e_{i,h}-e_i),\qquad
\widehat e_h=\max(0,e_t+x_t^T G^{-1}b_h).
$$

未完了の特徴も補正への罰則を構成する。未到着の音を0と観測した回帰ではなく、完了した組だけで行う通常のridgeとも異なる。固定値0.01と累積統計は工学的な推定器の仕様であり、認知的な保持・干渉の同定ではない。入力欠落では未完了の対応を破棄し、学習済みの統計は保つ。巻き戻しでは初期化する。

**反復予測との比較。** 履歴を加算する36成分の案では、開発用の順序対照二条件のうち一方が悪化した。57成分では両条件の誤差が減ったが、そのまま従来予測を置換すると、通常音源のsample 12の100・200 ms先で誤差が約1.6–1.9倍になった。この置換は採用しない。

現在の候補は、従来の周囲の反復予測 \(a\) と履歴予測 \(b\) を保持し、各予測先・帯域について、完了した予測と実測 \(y\) から
\(w=\operatorname{clip}_{[0,1]}\{\sum(b-a)(y-a)/\sum(b-a)^2\}\) を求め、\((1-w)a+wb\) を返す。比較実績がない場合は \(w=0\)。分母も分子も実測が届いた組だけで更新する。重みは確率や行動価値ではない。各Voiceの参加予測を要求した時点で候補を発行し、同じ観測時点への再要求は重複登録しない。報告用の共有予測の参照は、この局所候補を発行しない。比較の成績は発行した文脈に対するものであり、発行頻度に依存しない較正を主張しない。

**検証した範囲。** 14音源・95,106観測窓について、ridgeの1,008成分を独立した行列計算と比較し、最大絶対差8.63e−10未満を確認した。100 msごとに発行した混合比と混合予測の各5,734,530成分も、完了した組の独立集計と照合した。開発後に登録したseed 7301・7309、および48 kHz・振幅0.2のseed 7313では、最後の16 episodeの順序手掛かり後のエネルギー誤差比は従来比0.690・0.700・0.673だった。優勢帯域の一致数は10・8・7件／16であり、エネルギー誤差の改善を順序の知覚的識別率とは扱わない。手掛かりと応答の関係を外した対照も保存した。

保存済みの通常音源6本では、直近窓の誤差比は0.889–1.001、4秒先は1.022–1.315だった。長い予測の優位や全面的な改善は未達である。この比較は提示音の録音を単独の観測器へ通し、100 msごとに候補を発行した結果であり、演奏中の各Voiceの局所観測・発行頻度での成績とは区別する。

実音から同じ過去を学んだ後、最後の手掛かりの順序だけを反転し、判断時点を共通の無音にした。固有ペース・音長・帯域・準備時刻の81組で、混合予測を使う27組の参加時刻が変わった。そのうち26組は従来予測では同じ時刻を選んだ。結合0では全組の時刻が一致した。最初の単一身体条件では予測だけが変わり、参加時刻は変わらなかった。その結果も保存し、身体条件を跨ぐ検査を行った。これは予測から選択への接続の検査であり、受け渡しを聞き取れたという作者判定ではない。

通常sample 08・09・12の2seed、計6条件を再生成した。08の発音数は202→199、200→203となった。09は両seedでWAV一致、12は発音数602・623を保ったままWAVが変化した。実測文脈2,142件からの記憶12,852成分は独立再計算とf32で一致した。

Rust全737件（12件ignore）、Clippy、観測・予測・自己音除外経路の構築後割当ゼロ検査、Pythonの報告検査14件、報告あり／なしのWAV一致検査が通過した。16 Voice・harmonicのentrained/flow、報告有無の機器不要検査では、2秒の予熱後4秒間に全Voiceを維持し、予算超過hopは0、hop p99は1.651–2.594 msだった。予算は10.667 ms。実機のcallbackや出力不足、全workerの割当を測った結果ではない。記録は `target/phrase-expectation/2026-09-08-history-conditioned-expectation/` に保存する。認知的な保持・干渉、関係の同一性、phrase・終止、行動価値の学習、今回の音楽的採用は未達である。

### 9.3.38 個体が発行した予測と、その後に観測した周囲の実音

9.3.37の録音による比較は、提示音を一つの観測器へ通し、100 msごとに予測を発行した結果だった。本番では、各Voiceが自己波形を除いた周囲を観測し、参加判断で必要になった時点に予測を発行する。入力と発行条件が異なるため、録音での誤差比を各個体の成績へ置き換えない。

`HistoryEnergyPrediction`は、発行時の反復予測・履歴予測に加え、その時点の重みで作った混合予測も固定して保存する。後から学習した重みで過去の混合予測を作り直さない。対応する実測窓が完了したとき、三候補を同じ周囲の帯域エネルギーで採点する。診断用の予測先は0・100・200・500・1000・2000・4000 msで、実際の時刻は観測窓長の整数倍として記録する。各予測先・帯域の二乗誤差と完了件数を保持し、この診断値は行動選択や係数の更新へ戻さない。

局所予測の発行件数も数え、同じ観測時点への再要求は重複計上しない。発行直後は、まだ実測窓が完了していない場合がある。その場合も発行件数を記録するが、誤差と完了件数は0のままとする。入力欠落、個体の観測器の退場、演奏終了で結果を得られなかった予測を、無音・正解・失敗として補わない。発行件数から各予測先の完了件数を引いた差が、採点できなかった範囲となる。

`local_prediction_error`は、描画後の各Voiceの局所観測から差分を回収する。次のhopで個体の追跡が終了する前に回収し、報告の有無によって予測・行動・経験記憶を変えない。誤差を回収しない実行と、毎回回収する実行で、その後の予測が一致することも検査する。報告区間が観測窓をまたぐ場合の件数上限、観測が進まない発行のみの記録、完了件数0での誤差0を検査する。

既存の通常音源6本と順序対照2本、計37,506観測窓で、発行時の予測と後の実音から診断値を独立に再計算した。発行3,750件、予測先別の完了合計25,612件に対応する230,508成分の二乗誤差を照合した。観測値・履歴・三候補の予測値は9.3.37の保存結果と全件一致した。最初の結合検査には「4.9秒より後の採点」を要求する不適切な条件があり、最後の利用可能な対象窓がちょうど4.9秒で終了するため失敗した。固定した終端要求を除き、この試験で完了する0・200 ms先の全件と、完了しない4秒先の件数を検査するよう改めた。

**本番経路での比較。** 通常sample 6条件を再生成し、各個体が実際に発行した予測を、その後の局所観測で採点した。下表は各条件内の個体・三帯域の二乗誤差合計の比であり、個体別の比の平均ではない。三候補は同じ実現済みの音を対象とする。反復だけで演奏した場合の行動や音を比較した実験ではない。

| sample / seed | 発行 | 4秒先の完了 | 直近窓の誤差比 | 200 ms先 | 4秒先 |
|---|---:|---:|---:|---:|---:|
| 08 / 192001 | 511 | 415 | 0.915 | 1.073 | 1.052 |
| 09 / 192001 | 756 | 604 | 0.858 | 0.953 | 1.027 |
| 12 / 192001 | 192 | 116 | 0.968 | 1.084 | 0.978 |
| 08 / 192013 | 507 | 411 | 0.984 | 1.043 | 1.044 |
| 09 / 192013 | 743 | 594 | 0.935 | 0.937 | 1.036 |
| 12 / 192013 | 207 | 117 | 0.994 | 0.944 | 0.975 |

発行2,916件のうち4秒先まで完了したのは2,257件で、659件は採点していない。全発行を含む本番の4秒先の比は0.975–1.052だった。録音比較と同じく8秒以降の発行に限ると0.975–1.140となり、初期区間を含む集計では悪化が小さく見える。集計区間を揃えても入力・発行頻度・実現した音は異なり、録音比較の最大31%悪化をそのまま局所予測の成績とは扱えない。ただし集計値が個体差を隠す。sample 12・seed 192013のVoice 12では、100 ms先の23対象で約40%悪化した。同条件のVoice 14の1秒先は17対象で約29%悪化し、余分な誤差の大きい窓を追うと18.78–18.79秒の中帯域に集中していた。この窓への予測を発行する17.78秒までに、同個体の1秒先の比較で完了していたのは5件だった。少数の比較例を次の検討対象とするが、それだけを原因と確定しない。個体別・時刻別の結果も保存した。

計測の追加前後で、6条件すべてのWAV・発音列・既存の文脈および結果記録が一致した。Rust全738件（12件ignore）、Clippy、構築後割当ゼロ検査、Python 15件、報告有無のWAV一致検査が通過。16 Voice・harmonicのentrained/flow、報告有無の4条件では、2秒の予熱後4秒間に全Voiceを維持し、予算超過0、hop p99は1.673–2.673 msだった。予算は10.667 ms。実機のcallback・出力不足や、全workerの割当を測った結果ではない。

記録は `target/phrase-expectation/2026-09-08-local-forecast-outcomes/` に保存する。帯域エネルギーの二乗誤差は、関係の認識や音楽的な価値の尺度として較正されていない。認知的な保持・干渉、流れの同一性、phrase・終止、行動の因果効果・価値、音楽的採用は未達である。

### 9.3.39 結果待ちの候補情報だけでは、局所予測の混合を一様に改善できない

9.3.38で少数の比較例に悪化が集中したため、混合方法の対照を事前に登録した。新しい記憶時間を設定する比較ではない。候補案は、反復予測 \(a\) と履歴予測 \(b\) の差の二乗を発行時に加え、実測 \(y\) との積は対応する対象窓の終了後だけ加える。

$$
w_{\mathrm{pending}}=\operatorname{clip}_{[0,1]}
\frac{\sum_{i\in\mathrm{completed}}(b_i-a_i)(y_i-a_i)}
{\sum_{i\in\mathrm{issued}}(b_i-a_i)^2}.
$$

分母には今回の発行も含め、0なら重み0とする。現行の完了済みだけの分母に比べ、結果待ちの候補差が反復予測からの補正へ罰則を加える。未到着の音を無音とする観測、較正済みの不確かさ、認知的な干渉ではない。前向きの入力欠落では候補の累積統計を保って未完了の対応を破棄し、巻き戻しでは初期化する。七つの診断用予測先に、発行時の候補案を固定して保持した。行動選択へは戻さない。

既存8音源・37,506観測窓について、発行時の予測と後の実音から、追加の二乗誤差76,836成分を独立に再計算して照合した。従来の観測、履歴、予測値と記録項目も一致した。順序手掛かり後の誤差比は反復予測に対して0.691・0.673で、現行混合の0.690・0.673に近かった。再利用した対照であり、新しい検証条件や知覚的な識別率ではない。

本番経路の比較では、6音源のWAVと、実行時間を除く既存の記録が一致した。最初の照合は`hop_timing`の実測された実行・待ち時間まで一致を要求して失敗した。その記録を残し、該当する三つの実行時間項目だけを除いて再照合した。局所予測の発行2,916件、4秒先の完了2,257件も変わらない。8秒以降の発行に限った4秒先の二乗誤差について、候補案の現行混合に対する比は次の通りだった。

| sample / seed | 候補案 / 現行混合 |
|---|---:|
| 08 / 192001 | 0.9784 |
| 09 / 192001 | 0.9879 |
| 12 / 192001 | 1.0090 |
| 08 / 192013 | 0.9914 |
| 09 / 192013 | 0.9957 |
| 12 / 192013 | 1.0240 |

悪化が集中していたsample 12・seed 192013では、Voice 12の100 ms先の反復予測に対する誤差比が1.403から1.059へ、Voice 14の1秒先が1.291から1.233へ減った。一方、同じVoice 14の2・4秒先は現行混合より約18%悪化した。両方とも反復予測よりは良いが、現行混合が得ていた利点を減らす。補正を一律に弱めると、有害な補正と有用な継続予測の両方を抑える。全体平均の改善だけでは、どの文脈の補正を信頼できるかは分からない。

sample 12の両条件が、事前に決めた後半の4秒先を悪化させない条件を満たさなかったため、採用しない。一時的な本番採点とschema拡張を除き、対象の実装・検査4ファイルを変更前と完全一致する状態へ戻した。実験のソース、バイナリ、観測、局所記録、比較スクリプトと失敗した照合は`target/phrase-expectation/2026-09-08-pending-local-mixture/`へ保存する。実験時のRust全739件、復元後の738件が通過し、いずれも12件ignoreだった。新しい行動変更や作者採用はない。次は文脈ごとの予測の根拠と、一律の補正抑制を区別する必要がある。この結果から認知的な保持時間、行動価値、流れの同一性、phrase・終止の成立は導けない。

### 9.3.40 発行時の文脈、要求時刻、実際に完了した局所予測

9.3.39の一律の補正抑制では、どの文脈で補正が有用かを区別できなかった。そこで`local_prediction_match`へ、完了した局所予測の反復・履歴・混合候補、発行時の履歴重み、その時点までに完了した比較件数、既存の57入力成分を記録する経路を追加した。生の履歴のringを上書きする前に文脈を借用し、利用不能なら欠落として残す。コールバックで観測・描画から既存の記録経路へ渡し、増え続ける待ち行列や定常処理の追加割当を作らない。診断件数や記録値は推定・行動選択へ戻さず、追加の発行情報も既存の比較ring内に収める。

`forecast_observed_frame`は最後に完了した分析窓の終端、`requested_frame`はその観測状態で最初に予測を要求した音声時刻を表す。同じ状態への再要求は、最初の要求時刻と候補を保つ。対象窓は実波形の完了位置から求め、モデルのstep 0を音声frame 0と仮定しない。窓の途中の誕生、一度に複数窓を完了する入力、細かく分けた入力、ringの周回、未完了対象の破棄を検査した。名目上の0先は次の分析窓であり、要求時点でその前半が既に描画されている場合がある。要求時点から全区間が未来の波形という意味ではない。通常6条件では、要求時刻と分析済み境界の差は最大448 frame、48 kHzで9.333 msだった。観測窓は480 frameである。

既存8音源・37,506観測窓について、25,612件の対応と発行時の履歴1,459,884成分を、保存済みの観測・予測から独立に照合した。従来の値と誤差集計も一致した。本番の6条件では19,127件を記録し、二乗誤差172,143成分を再計算して数値精度内で照合した。文脈の1,090,239成分には、既存の判断履歴と対応付けた2,142件の選択文脈が含まれる。発行前の完了件数も、その要求時刻までに完了した対象だけから検査した。6本のWAVと、実行時間以外の既存記録は変わらない。

記録を読む前に、文脈の診断用比較を定めた。予測対象は`(反復予測の誤差 − 混合予測の誤差)/(両誤差の和)`とし、両方0なら0とする。行動の報酬ではない。文脈距離には、現在と履歴のRMS 27成分をまとめて正規化したもの、観測範囲を`sqrt(8)`で割ったもの、順序の対比を`sqrt(21)`で割ったものを使う。これは分析上の設定で、知覚的に較正した距離ではない。同じ個体・予測先について、要求前に結果の揃った対象だけを使い、過去の誤差差の平均、直前の誤差差、最も近い文脈の誤差差を比較する。同距離なら最後に完了した対象を選ぶ。

6条件×7予測先の42区分で、最も近い文脈を一件選ぶ方法が過去平均を上回った区分は0だった。8秒以降の要求では14,729件が比較可能、793件は完了した先行対象がなかった。最近傍が直前の結果と同じになるのは8,818件、約60%だった。二乗誤差の合計は過去平均の1.780倍、直前の結果の0.966倍で、後者を上回るのは42区分中28だった。この一件検索と距離の組では、文脈による有用な信頼性予測を確立できない。文脈記憶一般の否定でも、関係・流れの同定でもない。検索による行動変更は加えず、再利用したデータを新しい検証条件とも扱わない。

Rust全740件（12件ignore）、Clippy、Pythonの記録検査16件、報告有無のWAV一致、割当を計測した観測・対応付けコールバックの検査が通過した。16 Voice・harmonicのentrained/flow、報告有無の4条件では、2秒の予熱後の4秒間に全Voiceを維持し、予算超過0、hop p99は1.699–2.731 msだった。予算は10.667 ms。報告有効時は全Voiceから57・146回の発音を確認した。実機のcallback・出力不足や全workerの割当の合格ではない。描画時間の計測には自己音の観測と、有効な場合の対応記録も含む。

記録は`target/phrase-expectation/2026-09-08-local-context-matches/`へ保存する。次は発行時点で既知の候補差・重みと、未観測の残差を分けて扱う。近い過去の相対誤差を、そのまま文脈の信頼性へ置き換えない。認知的な保持・干渉、持続する関係、流れの同一性、行動の因果効果・価値、phrase・終止、音楽的採用は引き続き未達である。

### 9.3.41 既知の予測差と未観測の残差を分けた、局所予測の比較

発行済みの反復予測を$a$、現行混合を$m$、後から観測する同じ窓の実音エネルギーを$y$とする。補正$\delta=m-a$は発行時に既知だが、残差$r=y-a$はまだ未知である。両候補の二乗誤差の差は$\|\delta\|^2-2\delta\cdot r$となる。9.3.40で一件検索した相対誤差を直接再利用する方法に加え、今回はこの既知項と未知項を分けた研究用の比較器を作った。三帯域の残差を予測するもので、行動の因果効果や価値は予測しない。

比較するのは、過去の誤差差の平均、過去の残差ベクトルの平均、候補情報による残差回帰、候補情報と聴覚履歴による残差回帰の四つ。候補情報は反復・履歴候補のエネルギー6成分をまとめてL2正規化したものと、発行時の履歴重み3成分からなる。履歴は9.3.40と同じ正規化を使う56成分を加える。回帰の入力は9成分または65成分で、切片は別に推定し罰則を掛けない。中心化した累積統計と固定ridge罰則1.0を使う。これらは分析上の設定であり、知覚距離、認知的な保持時間、較正済みの確信度ではない。

各個体・各予測先について、要求時刻までに対象窓の完了した実測だけを学習する。全比較が同じ利用可能な文脈を使い、欠落や先行実測なしは利用不能として残す。推定器への要求に現在の対象窓の実測は渡さない。予測された誤差差が負なら現行混合、それ以外なら反復予測を選ぶ比較も行う。利用不能なら現行混合を維持する。既存方策が生成した同じ音声上で予測を選ぶだけであり、発音方策を変えた場合の音声や行動結果ではない。補正が0なら両候補の一致は既知なので、非ゼロ補正の区分を別に採点する。全7予測先、全区間と8秒以降の要求を保持し、名目上の0先の時刻境界も9.3.40から変更しない。

比較条件を先に登録し、既存6条件を開発用、未使用seed 192029/192041の08・09・12、計6条件を検証用とした。現行版と同じrenderer・設定で新しい音声を生成した。既存19,127件、新規19,420件の計38,547件について、元の記録から誤差と候補選択を独立再計算した。各条件・予測先から計84要求を選び、二回帰の計168適合を独立した一括計算と照合した。残差予測の最大絶対差は2.12e−18未満だった。未来の実測変更、完了境界、文脈欠落、エネルギーの共通倍率、既知のゼロ補正も検査した。

未使用条件の非ゼロ補正は全区間16,336件、8秒以降13,876件で、すべて先行実測がある。履歴を加えた回帰による誤差差の予測誤差は、候補情報だけの回帰に対して0.9894/0.9871倍、残差平均に対して0.9673/0.9611倍だった。しかし、誤差差を直接平均する対照に対しては1.3853/1.3334倍へ悪化した。これによる候補選択のエネルギー誤差も、現行混合に対して1.00170/1.00478倍だった。これらは非ゼロ補正区分の合計であり、認知や音楽的な良さの改善ではない。

後半の4秒先では、08・seed 192029で候補情報だけの回帰より選択誤差が約0.115%悪化した。09の両seedでは現行混合より約3.80%/2.78%悪化した。事前に定めた各条件で悪化しない要件を満たさず、参加方策へ採用しない。履歴追加の小さな集計上の改善から、一律の選択変更や認知的な文脈認識の達成を主張しない。検証結果を見た後の係数調整や予測範囲の短縮も行わない。

Pythonの評価検査338件が通過した。今回は研究用Pythonと文書のみの変更で、Rust本体は9.3.40の検証済み版を維持する。新しい実機検査や作者試聴は行っていない。記録は`target/phrase-expectation/2026-09-08-prediction-residual-reliability/`。次は、予測器全体の残差の小ささと、現在の候補差に沿った比較の根拠を区別し、選択の変更が必要な条件を扱う。認知的な保持・干渉、持続する関係、流れの同一性、行動価値、phrase・終止、音楽的採用は未達のままである。

### 9.3.42 候補比較の誤差を直接学習する対照

9.3.41の残差回帰は三帯域の残差ベクトルの二乗誤差を小さくするが、候補選択で採点するのは二候補の誤差差である。この目的の違いを切り分けるため、同じ9成分・65成分の入力、ridge罰則1.0、中心化した累積統計、罰則なしの切片を使い、出力だけを二候補の実測誤差差に変えた対照を追加した。新しい特徴、保持時間、調整用の係数探索は追加しない。候補差が一定なら、誤差差は残差の固定した線形変換と定数項になるため、両回帰の予測は一致する。これを検査で確認した。候補差が変わる場合には、同じ入力でも学習する目的が異なる。

従来の四比較はそのまま残す。新しい二比較も、要求時刻までに完了し、文脈が利用可能な同じ対象だけを学習する。候補が完全に一致する場合の誤差差は、どの実音が届いても0なので、利用可能な新しい予測ではこの既知の等値を返す。未完了の対象を学習へ加える操作はしない。主要な比較は非ゼロ補正区分とし、先行実測なしや文脈欠落は引き続き利用不能として記録する。スカラー出力を帯域残差の予測として採点することもない。

前節の12条件を開発用へ移し、未使用seed 192053/192067の08・09・12、計6条件を事前登録した検証用とした。全7予測先、全区間と8秒以降の要求を残した。従来12条件の38,547件では、既存の四比較と全既存記録値が完全に一致した。新しい19,112件を含む57,659件の誤差と選択を、元の実測から独立再計算した。18条件×7予測先の126要求では、四回帰の計504一括適合も独立に照合した。最大絶対差は帯域残差予測6.02e−18未満、誤差差予測7.28e−22未満だった。

未使用条件の非ゼロ補正は全区間16,290件、8秒以降14,001件で、すべて先行実測がある。以下の比は全区間/8秒以降の順で、1未満が比較対象より小さい誤差を表す。

| 誤差差を直接学習するモデル | 誤差差の予測誤差 / 直接の過去平均 | 誤差差の予測誤差 / 対応する残差回帰 | 選択した予測の誤差 / 現行混合 |
|---|---|---|---|
| 候補情報のみ | 1.0986 / 1.1072 | 0.8261 / 0.8995 | 0.99852 / 0.99282 |
| 候補情報と履歴 | 1.0943 / 1.1025 | 0.8306 / 0.9098 | 0.99797 / 0.99228 |

目的を直接の誤差差に合わせると、この比較では残差回帰より予測誤差を減らせた。しかし、直接の過去平均には届かない。後半4秒先のsample 12・seed 192053でも、両方の新しい選択は現行混合より約7.10%悪化した。この区分の非ゼロ補正は26件である。誤差差の予測が過去平均を上回り、かつ後半4秒先の各条件で現行より悪化しないという事前要件を満たさないため、どちらも本番へ採用しない。集計した選択誤差の改善は、未実行の発音方策の効果や、音楽的な改善ではない。

Pythonの評価検査341件と日英サイトのビルドが通過した。Rust本体とrendererは9.3.40の検証済み版を維持する。新しい実機検査、作者試聴、commitは行っていない。記録は`target/phrase-expectation/2026-09-08-direct-loss-comparison/`。本節は候補比較の学習目的を明確にしたが、認知的な関係記憶を実装したものではない。次は既存の連続観測・関係候補の研究を点検し、三帯域のエネルギー履歴が保持する情報と、持続する関係の識別に必要な情報を照合する。回帰の係数調整だけで記憶表現の不足を埋めたとしない。認知的保持・干渉、流れの同一性、行動価値、phrase・終止、音楽的採用は未達である。

### 9.3.43 時間記憶の入力が失う周波数配置と並び

候補選択の回帰を調整する前に、その記憶へ渡す観測が何を区別できるかを点検した。対象は時間参加で使う10 msごとの三帯域エネルギーであり、周波数側のLandscapeは別の観測を保持する。既存研究では、細かい周波数包絡、成分軌跡、共変動、身体候補を扱っていた（9.3.8–9.3.31）。本節ではそれらを新しい流れの認識器と呼ばず、三帯域へ圧縮する前に残る証拠との比較に使う。

**異なる波形が同じ観測になる構成。** 本番と同じ200/3000 Hzの一次フィルタから周波数応答を求めた。10 ms内で整数周期を完了する三周波数の組を六つ用意し、各組について、周期境界の二つのフィルタ状態と波形の最初・最後の標本を0にする線形制約を解いた。各基底の三帯域エネルギーと全波形パワーから作る行列の零空間を使い、異なる周波数支持を持つ二つの非負パワー混合A/Bを構成した。フィルタを窓ごとにリセットせず、どちらの周期へ切り替えても共通の状態を引き継ぐ。理想計算では、定常A、定常B、ABAB、AABBのエネルギー系列が一致する。

3構成seed・24/48 kHzで各4条件、計24音源を先に登録し、4.8秒のPCM16へ保存した。並びの一区間は0.4秒で、分析周期40個からなる検査用の構成である。フィルタの周波数応答、時間領域の再帰、零埋めしたFFT畳み込みを照合した。全理想系列の最大エネルギー差は1.74e−17未満だった。異なるスペクトル支持と、同じ総パワー・三帯域エネルギーを保つ反例であり、分析窓と切替境界を整列した構成である。任意の時刻原点や一般の音源について完全一致を主張するものではない。

PCM16量子化と本番のf32処理では微差が残る。同じ時刻の三帯域ベクトルの相対差は最大4.84e−5、約0.00484%で、八つの遅れにおける本番履歴の相対差は最大2.81e−5だった。この数値的近さを完全一致や人間の弁別不能へ読み替えない。同じPCMを既存の113帯域包絡観測器へ渡すと、1秒以降の正規化エネルギー形状の距離は、定常A/Bで0.950–0.975、ABAB/AABBで0.451–0.463だった。これらは記述距離であり、知覚の確率や認識率ではない。両観測器の報告刻みは10 msに揃えたが、フィルタ族も異なるため、差のすべてを帯域数だけの効果とはしない。

**同じ現在へ戻った後の履歴。** 上の比較後、既存対照を使う追加検査を登録した。ABABとAABBの最後の0.4秒は、復号したPCMが完全一致する。既存の連続履歴モデルを変更せず、三帯域と113帯域それぞれの包絡へ適用した。遅れの配置0.125–16秒と次数12は共通の数値設定とし、新しい認知的保持時間は導入しない。末尾の113帯域の現在値は相対差5.72e−17未満まで一致したが、履歴には0.539–0.558の相対差が残った。三帯域の現在値は一致し、履歴差は量子化などを含む1.47e−6–9.50e−6だった。過去の観測が細かい周波数位置を保つ場合には、同じ現在でも先行する並びの証拠を残せる。これは関係や流れを同定した結果ではない。

追加の履歴は、各gamma核を観測区間ごとに積分した独立の畳み込みでも照合した。履歴の最大絶対差は4.31e−16未満、観測範囲は7.22e−15未満だった。4.8秒の観測に対し、遅れ8秒・16秒のセルの観測範囲は約0.0327・0.000100にとどまる。大きな遅れのセルが存在することを、そこまでの過去を観測したことや、確かな長期記憶へ読み替えない。

既存の通常08・09・12、seed 192053も再利用し、同じPCMに両観測器を適用した。通常音源9,153窓を含む計20,673窓で、三帯域エネルギー62,019成分、履歴496,152成分、観測範囲165,384成分を独立計算と照合し、細帯域の利用可能時刻も一致した。通常三音源で同時に観測された成分は最大20/13/18本、打切りは0だった。成分の本数はVoice数や知覚上の流れの本数ではない。通常音源について作者の流れ判定を再現したとは扱わない。

Python評価検査346件、既存Rustの履歴出力アッセイ1件、日英サイトビルドが通過した。Rust本体、演奏方策、音声出力は変更していない。新しい実機検査や作者試聴も行っていない。記録と図は`target/phrase-expectation/2026-09-08-spectral-history-observability/`へ保存する。

本節からの設計上の改訂は、**三帯域エネルギーだけを、周波数間の関係を記憶する十分な観測としない**ことである。理想的に同じ入力へ写された区別は、その後の回帰や保持時間の変更では復元できない。必要なのは113という固定の帯域数ではなく、課題に必要な周波数位置・関係・順序と、観測不足を保持する契約である。次はこの証拠を既存の本番観測経路へどう渡すかを設計・検証し、発行前の関係候補と後続の実音を対応付ける。音響成分の追跡、音源への帰属、知覚上の流れ、phrase・終止、行動価値は引き続き別の未達課題として扱う。

### 9.3.44 本番の周波数観測から、配送前に履歴を保持する

9.3.43の入力不足に対し、既存のNSGT観測を使う連続履歴を本番へ接続した。各周波数のRMSを、既存の時間履歴と同じ核で独立に保持する。三成分ずつ処理するのは数値演算の再利用であり、周波数を三帯域へ集約する処理ではない。周波数座標は実行時のLog2Spaceに従い、今回の通常設定では690点になる。遅れの配置0.125–16秒と次数12は既存の数値設定を維持し、認知的な保持時間や周波数分解能の同定とは扱わない。

**配送前に更新し、利用可能時刻を残す。** 解析workerは全hopで履歴を進め、生成側へ最新の一枚だけを送る場合にも、その間の観測を履歴へ反映する。Listener側の逐次配送でも同じ履歴となる。初期のFFT充填と欠落後の再充填には観測値を作らず、欠落前の履歴を保持しながら既知の観測範囲を別に更新する。FFT容器が満たされたことは、帯域ごとの遅延や知覚的な確信度を意味しない。履歴は各hopの終端で得たNSGT推定値から更新する数値近似であり、hop内の波形を復元したものではない。

スナップショットは観測済み標本の終端、標本化率、分析窓と周波数座標を持つ。生成側のLandscapeとListenerTwinがそれぞれの共有音場・presentationの履歴を受け取り、約100 msごとの報告で生成時刻と観測時刻を分けて出力する。間引くのは報告だけで、履歴の入力ではない。欠落の通知は古い利用可能状態を無効化する。共有音場の履歴を、個体ごとの自己音を除いた周囲の証拠へ読み替えない。局所参加が使う三帯域経路と行動方策は維持する。

**実音による独立照合。** 9.3.43の24対照と通常08・09・12を再利用し、16,682個のNSGT観測窓を記録した。記録されたRMS系列にgamma核の区間積分を畳み込む独立計算と、報告した履歴8,804,400成分を照合した。最大絶対差は3.52e−7未満、観測範囲の差は2.47e−5未満だった。これは同じ観測値からの数値照合であり、NSGT自体の聴覚的妥当性を新たに示す結果ではない。

当初の共通PCM末尾0.4秒では、NSGTの現在RMSにも約0.0765–8.15%の相対差が残った。今回のFFT容器は16,384標本で、24/48 kHzでは約0.683/0.341秒に相当し、平滑化も含む。その結果を確認後、末尾の同じ周期を2.24秒延ばす追加検査を登録した。この長さは分析hopと構成周期を揃える工学的な選択である。共通PCM区間を合計2.64秒にすると、6組すべてで現在のNSGT値が一致した一方、履歴の相対差は0.0467–0.0482残った。追加5,940窓・履歴3,080,160成分も独立計算と照合した。現在の周波数状態が同じでも、先行する並びの証拠を保持できる。距離を音源・流れの認識率や音楽的価値へ読み替えない。

通常08・09・12のseed 192053は、変更前後のWAVが完全一致し、追加した履歴と実行時間の報告を除く既存74,950記録も一致した。新しい履歴はhabitatとpresentationに各848件ある。presentationの履歴を同時刻のPCM16再解析と照合した最大絶対差は8.76e−6未満だった。生のf32演奏音とPCM16の量子化差を、履歴演算の誤差とは分ける。

**処理費用と残る範囲。** 履歴更新の追加割当は0で、配送するスナップショットの配列とArcに2回の割当がある。最新配送では、中間の履歴スナップショットを確保しない。16 Voice、entrained/flow、報告有無の4条件を、同じシナリオ・設定・seedで変更前と比較した。各4秒の測定窓で全Voiceを維持し、予算超過は0。変更後のhop時間p99は2.31–3.83 msで、予算10.67 msに収まった。変更前からの増加は約0.49–0.89 msであり、報告による処理費用も増える。機器不要の検査であり、実機出力や任意の個体数を保証する結果ではない。

Rust全744テスト（12件ignore）、Clippy、履歴の割当検査、既存の実音出力アッセイによる2回の照合が通過した。記録は`target/phrase-expectation/2026-09-08-native-spectral-history/`へ保存する。次は、この周波数履歴に支えられる関係候補を保持し、発行した期待と後続の実音を対応付ける。個体への音源帰属、認知的な保持・干渉、流れの同一性、phrase・終止、行動価値、作者の採用は引き続き未達である。

### 9.3.45 周波数間の予測と、相手の現在値・履歴の切り分け

9.3.44の本番NSGT履歴を使い、ある周波数領域の先の状態を、別の領域の情報がどれだけ予測できるか比較した。研究用の`evaluate_spectral_relations.py`は、最初の約2秒の観測から最大三つの離れたエネルギーの峰と周辺領域を選び、周波数位置と振幅の基準を固定する。領域は観測の要約であり、Voiceや知覚された音源の識別子ではない。

予測する量は、基準RMSに対する領域RMSの`log1p`である。対象自身の現在値・履歴、対象と相手以外の周波数の要約、相手の現在値・履歴を組み合わせる四つの回帰を比較する。履歴入力は保持済みの各周波数RMSを領域内で二乗平均した要約であり、領域RMSを先に求めてから保持した履歴とは同一視しない。切片を罰しないridge回帰、罰則1、次数12と8個の遅れ、領域選択は数値上の選択である。累積統計には忘却・干渉・文脈変更の機構がなく、認知的な記憶の完成形として採用しない。

48 kHz・512標本hopでは、予測先を発行から48,128標本、約1.003秒後に置く。対象のFFT容器は発行以後に始まるが、NSGTの平滑化には以前の入力が残る。予測を発行時に固定し、同じ対象時刻の実測が届いた後だけ学習する。欠落は採点不能、EOFは未決として保持する。比較には同じ完了済み対象の平均と現在値の持続も含める。

**現在値だけで説明できる改善を、履歴の効果としない。** 最初に独立変動、約1.003秒遅れ、共通駆動、20秒で関係を除去、音量だけの共通変調、一定音の6種類を各3 seedで生成し、通常08・09・12のseed 192053を再利用した。関係除去には20秒での不連続な切替がある。この21条件の結果を見た後、相手の履歴ブロックを現在値の複製へ置換する二つの比較と、約2.005秒遅れの3対照を登録して追加した。入力次元と罰則を維持する比較であり、情報と正則化の効果を一意に分解した結果ではない。元の四モデルの予測と実測記録はすべて維持した。

次表は277 Hz側から733 Hz側への指定方向、対象時刻24–36秒、各3 seedの二乗誤差比の範囲である。1未満は分子のモデルが優位。「全入力」は対象自身・周囲・相手の履歴を含む。「相手なし」は対象自身と周囲、「相手現在値」は相手の履歴だけを現在値の複製へ置き換えた比較を指す。

| 対照 | 全入力／相手なし | 全入力／相手現在値 |
|---|---:|---:|
| 約1.003秒遅れ | 0.583–0.740 | 1.038–1.134 |
| 約2.005秒遅れ | 0.631–0.775 | 0.635–0.764 |
| 20秒で関係除去 | 1.072–1.180 | 0.891–0.962 |
| 共通駆動 | 0.877–0.938 | 1.030–1.049 |
| 独立変動 | 0.997–1.101 | 0.997–1.033 |
| 音量だけの共通変調 | 1.005–1.031 | 1.003–1.027 |

長い遅れでは相手の履歴が追加情報を与える。一方、短い遅れでは現在値を使う比較の方がよい。関係を除去すると、相手の情報を使い続ける累積回帰は相手なしより悪化する。共通駆動でも周囲を条件とした利得が残るが、周囲の要約は真の共通原因ではない。利得を因果的な結合や同じ流れの認識へ読み替えない。一定音の対象分散は約2.3e−11–4.2e−11と小さく、誤差比から音楽的な差を論じない。

通常音源の全方向を併記した誤差では、後半の全入力／相手なしは08が1.086、09が0.978、12が0.995だった。しかし現在値の持続に対しては、それぞれ2.325、2.749、1.464へ悪化した。08・09の末尾は28.416秒・26.816秒で、後半の対象は42件・27件、12は113件である。観測を共有する方向を独立な標本として数えない。本番方策への採用や、流れの同定を支持する結果とはしない。

24条件で発行済み特徴7,209,180成分と対象43,692成分を元のNSGT観測から独立に再計算し、最大絶対差はそれぞれ1.34e−15未満、1.78e−15未満だった。10,476件の独立した一括ridge適合は、逐次予測と最大1.24e−12未満で一致した。関係除去前の共通接頭部は各seedの169発行で一致する。Python全354テストが通過し、本番のRust/Cargo 107ファイルと両releaseバイナリは9.3.44の検証済み版と一致した。新しい本番方策、実機検証、作者試聴はない。

記録は`target/phrase-expectation/2026-09-08-native-predictive-relations/`へ保存する。次は、関係の継続・変更・再出現についての候補と、その期待を実音で更新する機構を扱う。入力履歴の時間位置、学習した関係の保持、文脈の変化を分け、単に過去の全関係を一様に弱める設定を認知的な忘却として採用しない。認知的保持・干渉、流れの同一性、phrase・終止、行動価値と作者の採用は未達である。

### 9.3.46 文脈を保持して再利用する予測と、減衰末尾の数値を区別する

関係の消失を、過去の学習を一律に消す操作へ直結させない。[Gershman・Blei・Niv（2010）](https://www.princeton.edu/~yael/Publications/GershmanEtAl2009.pdf)は条件づけと反応の復帰を潜在原因から扱う。[Skerritt-Davis・Elhilali（2021）](https://engineering.jhu.edu/lcap/data/uploads/pdfs/jneurometh2021_skerritt.pdf)は連続した聴覚特徴の期待を扱い、[Mirea・Shinら（2024）](https://nivlab.princeton.edu/wp-content/uploads/sites/938/2025/05/jocn-36-11-2442.pdf)は視覚刺激の明示的な分類課題で時間に関する事前仮定を比較した。これらは文脈を区別して保持する設計の参照であり、以下の有限状態モデルや数値を同定する根拠ではない。

研究用`relational_contexts.py`は、累積学習、新しい事前分布への更新、使っていない文脈を保持して再利用する学習を比較する。最後の候補では、文脈への割当ての仮説ごとに、三つの文脈の回帰係数・分散・ゼロの率を別々に保持する。まだ観測を割り当てていないラベルは交換可能としてまとめる。文脈は音源やVoiceの識別子ではない。

再利用する候補の遷移は、状態数\(K=3\)、状態を離れる率\(\lambda=0.2/\mathrm{s}\)の対称な連続時間Markovモデルとする。\(U\)を一様行列として、

\[
T(\Delta t)=U+e^{-\lambda K\Delta t/(K-1)}(I-U).
\]

この仮定の下で、予測先や入力欠落までの未観測の遷移を積分できる。新規更新だけの比較は\(e^{-\lambda\Delta t}\)を継続の重みとし、残りを新しい事前分布へ割り当てる。割当て経路は最大32個に制限し、除去した事後質量を記録する。状態数、遷移率、経路数は認知的な保持時間ではない。使っていない文脈の係数には時間による忘却を加えておらず、認知的な干渉も実装していない。

対象\(y\)は9.3.45と同じ非負の領域`log1p` RMSとする。厳密なゼロには確率質量を置き、正値では\(\log y\)をnormal–inverse-gamma回帰で予測する。正の領域へのStudent密度の変換には\(1/y\)を含める。正値の平均は有限ではないので、位置パラメータを指数変換して期待音量にしない。比較指標は発行済み予測の対数密度とする。事前平均0、切片精度0.01、傾き精度1、分散のshape 2・scale 0.5、正値・ゼロ各0.5の事前観測数は全候補で固定した。

発行後に別の結果が届けば、現在の学習状態は変化する。そのため、発行時に固定した予測の採点と、対象が到着した時点の最新状態への学習を分ける。対象時刻までに得た実測だけを学習し、欠落では学習せず、EOFで未決の結果を補わない。既存24条件に発行時の特徴を追加しても、元の14,853記録は維持した。

**継続・消失・再出現の比較。** 既存24条件に、60秒・3 seedの12音源を追加した。持続する正の遅延関係、20–40秒だけ独立になる関係、20–40秒だけ反転する関係、20秒で反転して戻らない関係を、モデルの結果を見る前に登録した。下表は指定した277 Hz側から733 Hz側への方向について、再利用する候補の累積学習に対する利得を、1対象当たりのbitで表す。正値が再利用する候補の優位を示す。

| 対照と対象時間 | 3 seedの利得範囲 |
|---|---:|
| 安定した関係、44–60秒 | −0.277〜−0.264 |
| 20秒で関係消失、24–36秒 | −0.254〜+0.201 |
| 独立状態から復帰、44–60秒 | −0.243〜−0.086 |
| 反転した関係から復帰、44–60秒 | −0.080〜+0.113 |
| 反転後は戻らない関係、44–60秒 | −0.049〜+0.109 |

保持と再利用の機構は動くが、実音で一様な利得を得たとは言えない。固定の切替率は、安定した関係にも未使用の文脈への重みを与える。4条件では経路数を128へ増やした。同じ方向・窓で利得の符号は維持したが、値は最大約0.053 bit変化した。1更新で除去する質量は32経路の全条件で最大20.24%、128経路の4条件でも最大17.48%となった。近似が収束したとは扱わない。

**通常音源の大きな利得は、関係の理解を意味しない。** 通常08・09・12の24秒以降では、全方向で平均した累積学習への利得が約112.3／226.7／42.5 bitとなった。結果を確認後に登録した秒ごとの診断では、この大きな差は主に減衰末尾の極小の正値へ集中した。新しい事前分布へ更新するだけの比較に対しては、それぞれ約1.066／0.969／0.118 bit悪化した。08・09の対象は42件・27件、12は36.203秒までの115件である。方向や重なる分析窓を独立な証拠として数えない。

元のPCM16をハッシュ照合して調べると、入力のFFT容器全体がゼロになった後にも、NSGTの時間平滑化による尾が残る。08・09では末尾の選択領域にf32の最小subnormal値、約1.40e−45のパワーが残っていた。現在の平滑化式はゼロ入力で\(s\leftarrow\alpha s\)となり、この領域では丸めによって減衰が停滞し得る。12の選択領域は最終的にゼロになった。極小値の密度への適応を、知覚された流れやphraseの認識へ読み替えない。診断では閾値の追加や末尾の除外を行っておらず、本番の数値処理もまだ変更していない。

36条件で13,702対象を採点し、361発行をEOF時点の未決として保持した。短系列の全文脈割当てと区間ごとの独立した回帰積分を、2状態・3状態の両方で照合した。実音では混合密度の25,432,698成分と5,844件の一括回帰を独立照合し、最大差はそれぞれ1.14e−13、2.74e−11未満だった。128経路の比較も独立照合した。音源の共通接頭部では2,592発行が一致する。Python全364テストとCIと同じ探索条件の364テストが通過し、本番Rust/Cargo107ファイルと両releaseバイナリは9.3.44を維持した。

記録は`target/phrase-expectation/2026-09-08-recurring-relational-contexts/`へ保存する。まず数値的な減衰の停滞と、観測の分解能を扱う必要がある。その上で文脈の持続・切替の仮定と推論近似を検証する。本候補は本番方策へ採用しない。認知的保持・干渉、持続する流れの同定、phrase・終止、行動価値と作者の採用は引き続き未達である。

### 9.3.47 数値的な減衰の停滞を除き、末尾の予測密度の限界を残す

9.3.46で見つかった`RtNsgtKernelLog2`の数値的な停滞を修正した。従来の帯域別平滑化の後で、
`f32`のsubnormalとなったパワーをゼロにする。境界は浮動小数点表現の最小の正のnormal値、
約\(1.17549\times10^{-38}\)であり、聴覚上の閾値、学習したパラメータ、楽句の境界ではない。
subnormal領域のパワー精度は明示的に放棄する。normalとなる値の計算式は変えず、CPU全体の
浮動小数点モード、割当、時計、分析窓も変更しない。この規約から人間の聴覚や予測モデルの
観測分解能が定まるとは扱わない。

追加した2回帰テストは修正前に失敗した。修正後はcoherent・incoherent双方で、音から無音へ
移った後のnormal領域の減衰が従来計算とビット単位で一致し、最終的にゼロへ到達する。
その後に加えた音は再び観測でき、resetでも状態が消える。subnormal値から直接始めた状態も
正の固定点として残らなくなった。

9.3.46と同じ三つのPCM16入力を本番の解析器で再解析した。8,582 hopで、先行状態に変更のない
normalパワー5,104,218成分が一致し、全入力を通してもnormalパワーの差はなかった。
変更された150,327成分はすべてsubnormalで、最大絶対差は\(1.175\times10^{-38}\)未満だった。
観測時刻、周波数座標、履歴の観測範囲と配送位置は一致する。保持したRMS履歴の最大差は
\(1.388\times10^{-17}\)であり、現在のパワーがゼロになっても履歴を消していない。
08・09・12の最後の非ゼロPCM標本は25.649／24.406／31.026秒で終わり、NSGTの全帯域は
27.264／26.048／32.608秒までにゼロへ到達した。修正前は三入力ともEOF時点で少なくとも一帯域に
最小の正のsubnormalが残った。9.3.46で12の末尾がゼロだったのは選択領域の観測であり、
今回の全周波数での比較とは対象範囲が異なる。

同じ領域選択手続きと32経路の文脈モデルで予測も再計算した。調整、末尾の除外、EOFでの
観測の補完は行っていない。領域と正規化の契約、完了772件・未決31件は維持した。
24秒以降の対象について全方向で平均した、再利用モデルの累積学習への利得は、
08／09／12で約112.3／226.7／42.5 bitから
84.4／179.1／32.3 bitへ下がったが、依然として大きい。新しい事前分布への更新だけの比較には
約1.112／1.311／0.106 bit劣る。数値的な固定点を除いても、この成績を聴覚的なまとまり、
記憶、音楽的な流れの認識へ読み替えられない。混合密度1,407,345成分と336件の一括適合を
独立照合し、最大差はそれぞれ\(1.14\times10^{-13}\)、\(9.08\times10^{-12}\)未満だった。

Rust全746テスト（12件ignore）、Clippy、releaseビルドが通過した。保存した08・09・12の
シナリオと設定、seed 192053を両release版で再生成した。3本のWAVと、周波数履歴・実行時間を
除く74,950記録は一致した。周波数履歴1,696記録のうち90記録でRMS履歴だけが変わり、
出力した数値の最大差は約\(3.001\times10^{-18}\)未満だった。発音列、Listener状態、DCC圧力、
局所予測と参加判断の記録は今回の比較で維持した。限定したオフライン回帰であり、新しい作者試聴や
実機受入ではない。楽器本体のair-gapも維持する。

記録は`target/phrase-expectation/2026-09-08-nsgt-decay-precision/`へ保存する。研究モデルを
採用する前に、観測分解能と文脈の持続・切替を扱う必要がある。認知的保持・干渉、持続する
流れの同定、phrase・終止、行動価値と作者の採用は引き続き未達である。

### 9.3.48 予測分布を採点し、数値の精度を聴覚の分解能と区別する

subnormalの固定点を除いても、聴取者が何を区別できるかは定まらない。
[Skerritt-Davis・Elhilali（2021）の2.4.3節](https://engineering.jhu.edu/lcap/data/uploads/pdfs/jneurometh2021_skerritt.pdf)では、
観測ノイズをモデルの入力と同じ次元で扱い、予測の不確かさと学習した統計量の精度の双方へ作用させる。
知覚に関するパラメータは聴取者の応答と比較する。最終的な採点尺度や浮動小数点の境界だけを変えても、
この機構を実装したことにはならない。

9.3.46の正値のlog-Student予測は、非負の領域`log1p` RMSである\(y\)の座標では有限の平均を持たず、
通常のCRPSも末尾で発散する。発行済み予測を検査するため、`evaluate_context_scores.py`は
有界な座標\(u_s(y)=y/(y+s)\)で次の損失を計算する。

\[
L_s(F,y)=\int_0^1\left[F_s(u)-\mathbf{1}\{u_s(y)\leq u\}\right]^2\,du.
\]

これは有界な座標でのCRPSであり、元の座標では\(s/(y+s)^2\)を重みとする二値Brier scoreの積分に当たる。
構成は[Gneiting・Raftery（2007）の式20・49](https://sites.stat.washington.edu/raftery/Research/PDF/Gneiting2007jasa.pdf)に従う。
正確な積分はproperな予測評価で、値は0〜1、小さいほどよい。主比較は\(s=1\)、感度比較は\(s=0.25,4\)とし、
結果を見る前に登録した。これらは評価上の選択であり、聴覚閾値や適合した認知パラメータではない。
全ての尺度と元の対数密度を残す。発行時の領域の現在値をそのまま保つ点予測も、同じ座標での絶対距離として採点する。

ゼロの確率質量と混合分布の全成分を保持する。実測が届いた時点で、発行時に保存した予測を採点し、
後の重みやEOFの架空の無音へ置き換えない。文脈モデル、特徴、領域、正規化、切替率、行動方策は調整しない。
主比較は9.3.46の構成音33条件と、9.3.47で数値修正を反映した通常音3条件を使う。
数値修正前の通常音3条件も別の比較として保存する。

当初の一様な数値積分は、狭い予測分布を取り逃し、節点を倍にしても十分ではなかった。
その結果と実装は診断として保存する。修正した積分では、各成分の16節点による分位点近似から、
混合分布の分位点に近い位置で積分区間を分ける。観測値も境界へ加え、各区間で実際のStudent混合分布の
累積確率または生存確率を積分する。区間ごとのGauss–Legendre積分8節点を、登録した6条件では16節点と比較し、
成分の位置・幅を踏まえた区間分割による適応積分とも独立照合する。区間分割や節点数は予測分布を変えず、
認知的な時間や分解能のパラメータにもならない。

同じ観測で二つのモデルを比較すると、正値の密度に含まれる\(1/y\)のJacobianは相殺する。
大きな利得はJacobianが余分に与える報酬ではなく、各モデルが\(\log y\)へ異なる尤度を与える結果である。
その尤度は文脈の重みの更新にも使われる。有界な評価損失を追加しても、学習におけるこの依存は変わらない。
観測ノイズの機構は、表示する成績だけでなく、表現する証拠とその更新を扱う必要がある。
properな予測評価も、音楽的・認知的な価値を保証するものではない。

最終の照合では、39記録の採点1,823,724成分すべてを、発行済み予測と実測へ対応付けた。
完了14,474件・EOFで未決392件のうち、主比較36条件は完了13,702件・未決361件である。
独立した適応積分は選択した478予測を対象とし、全採点の再積分ではない。
logit座標の参照積分により、先の有界座標で生じた端点付近の警告を解消した。
参照積分の報告誤差は最大\(1.17\times10^{-10}\)、切り落とした末尾の上界は
\(8.50\times10^{-18}\)で、主採点との差は最大\(7.28\times10^{-5}\)だった。
6条件で節点数を倍にした差は、個別の採点で最大\(3.63\times10^{-4}\)、
後半の窓の平均で最大\(4.08\times10^{-5}\)だった。確認した数値差の範囲であり、
すべての予測への誤差保証ではない。

主尺度では、安定した関係の構成対照で累積学習が優位だった。選択した方向の44〜60秒で、
累積学習の損失から再利用の損失を引いた利得は、3 seedで−0.00909〜−0.00846だった。
関係が戻る対照も−0.00806〜−0.00178で累積学習が優位、反転してから戻る対照は
−0.00294〜+0.00529で混在した。正の利得が再利用の優位を表す。
小さな利得には尺度によって符号が変わる条件もある。有界損失の単位はbitではなく、
この比較だけで認知的な記憶の利点を示したとはしない。

通常音源の後半について、\(s=1\)の全方向平均は次のとおりだった。

| Sample | 累積学習 | 新しい事前分布への更新 | 文脈の再利用 | 現在値の持続 |
|---|---:|---:|---:|---:|
| 08 | 0.189375 | 0.116903 | 0.135059 | 0.144183 |
| 09 | 0.265967 | 0.149178 | 0.176394 | 0.184225 |
| 12 | 0.118312 | 0.066981 | 0.074546 | 0.074750 |

24〜40秒の完了対象は順に42・27・115件で、観測の終了時刻も異なる。
各方向は同じ入力を共有し、独立した標本ではない。ここでは新しい事前分布への更新が再利用より優位だった。
12の現在値の持続との僅差は、\(s=0.25\)では逆転する。発音終了後の予測改善を、
phraseの境界や音楽的な流れの変化の認識へ読み替えない。

別に登録した学習診断では、\(y=1\)を20回観測した後、事前分布、説明変数、時刻を揃えて
ゼロまたは\(10^{-48}\)で更新した。累積学習の次の予測は、\(y=1\)での累積確率が
それぞれ0.534091と0.602428になった。ゼロでは正値の回帰を変えずにゼロの質量を更新し、
正値ではどれほど小さくても\(\log y\)で回帰を更新する。独立した5条件の一括計算でも一致した。
これは観測ノイズを持たないhurdleモデルの不連続性であり、聴覚閾値の同定やf32の不具合ではない。
観測分解能を扱う機構には、予測だけでなく尤度と統計量の更新を変える必要がある。
採点の変更や、根拠のない微小値の下限では、この契約を満たせない。

採点の7テストとPython評価全371件が通過した。Rust/Cargoと両releaseバイナリは、
検証済みの9.3.47から変更していない。新しい作者試聴や実機受入は追加していない。
記録は`target/phrase-expectation/2026-09-08-bounded-expectation-scores/`に保存する。
文脈候補は本番未採用のままとし、観測の不確かさ、認知的保持・干渉、流れの同一性、
phrase・終止、行動価値を引き続き未達として扱う。

### 9.3.49 観測の不確かさを、非負の回帰モデルの尤度と更新へ組み込む

次の研究部品では、観測の分布とBayes更新を同時に変える。
`scripts/uncertain_regression.py`の`MagnitudeRegression`は、非負の観測を

\[
y=\left|x^\top\beta+\eta+\epsilon\right|,\qquad
\eta\sim\mathcal N(0,r^2),\quad\epsilon\sim\mathcal N(0,n^2)
\]

として扱う。\(x\)は必要に応じて切片を含む説明変数、\(r\)は残差変動、
\(n\)は観測の不確かさを表す。二つの標準偏差は、この部品では固定の入力とし、
\(y\)と同じ単位を持つ。絶対値を取る規則は、非負の領域`log1p` RMSに対する
工学的な仮説であり、同定した聴覚機構ではない。ノイズは絶対値を取る前に加わる。
9.3.48で参照したD-REXの議論は予測と更新への要求の根拠とし、この尤度自体の根拠とはしない。
ゼロには連続した分布の境界密度を与え、独立した確率質量は置かない。
デジタルの量子化は別にモデル化していない。ゼロでの対数密度を、旧モデルのゼロの質量と
同じ測度の利得として直接比較してはいけない。

係数のGaussian成分を\(\beta\sim\mathcal N(m,V)\)とすると、観測密度は
平均\(x^\top m\)、分散\(x^\top Vx+r^2+n^2\)のGaussianを、
\(y\)と\(-y\)で評価した和になる。符号の候補ごとにGaussianの事後分布が得られる。
精度行列\(\Lambda=V^{-1}\)と情報ベクトル\(h=\Lambda m\)を使うと、
符号\(s\in\{-1,+1\}\)に対する更新は

\[
\Lambda'=\Lambda+\frac{xx^\top}{r^2+n^2},\qquad
h'=h+\frac{xsy}{r^2+n^2}
\]

となる。符号の確率には予測と同じ尤度を使う。混合分布を単一のGaussianへ置き換えずに保持する。
同じ観測列の符号履歴では精度行列を共有できる。同一の情報ベクトルは確率質量を足して統合する。
ゼロでは二つの同一分岐を統合するが、境界密度の係数2を維持する。
残った混合分布は宣言した経路数で制限し、除去質量を記録する。経路数は数値計算の制約であり、
忘却則ではない。発行済み予測は配列を所有し、その後の観測で書き換わらない。

各Gaussian成分内の係数の不確かさを、残差分散と観測分散から分けて報告する。
成分の位置の違いも係数の不確かさを保持するため、成分内分散を混合分布全体の分散とはしない。
\(n\)を大きくすると、観測の予測が広がると同時に、精度行列へ加える情報も小さくなる。
ただし、\(r^2\)と\(n^2\)は和としてしか尤度に現れない。残差の標準偏差0.3と観測の
標準偏差0.4を入れ替えると、尤度も学習状態も一致する。二つの項へ名前を付けても、
このデータから知覚ノイズを分離して同定したことにはならない。

8テストで、符号履歴の全列挙による一括周辺化、密度の正規化と独立積分による累積確率、
ゼロへの極限、ノイズによる学習精度の変化、単位変換、発行済み予測、分散の識別限界、
経路制限を検査した。登録した42診断条件では、\(y=1\)を20回観測した後のゼロと
\(10^{-6},10^{-12},10^{-24},10^{-48}\)による更新で、次の予測の累積確率の差は
指定した5点で最大\(1.57\times10^{-11}\)だった。観測の標準偏差は0.01・0.1・1、
残差の標準偏差は0.1、係数の事前分散は100とし、試聴結果には適合させていない。
ゼロ更新後の成分内係数分散は、順に約0.000481・0.000952・0.048072だった。
無観測では以前の状態を保ち、ゼロ観測では更新した。最小のノイズでは、ゼロと新しい水準2の
更新後の累積確率の差が検査点で最大0.3566となり、学習を止めて連続性を得たわけではない。
経路上限128と512で検査した累積確率は一致した。保持した成分は最大118で、報告した除去質量は
\(1.89\times10^{-15}\)未満だった。上限による切り捨てではなく丸めの範囲だが、
任意の入力で経路数が十分であると示す診断ではない。

Python評価全379件が通過し、分散のフィールド名を明確化した後にも対象8件が通過した。
表記の変更で42条件の診断結果は変わらない。この部品には、まだ文脈の遷移も本番への接続もない。
検査可能な観測・更新機構を実装した段階であり、関係記憶、聴覚パラメータの同定、流れの同一性、
phraseの境界、音楽的な改善は未達である。次は発行済み予測と実測の対応を保ち、文脈の持続・更新・
再利用へ接続して、通常音源と構成対照で分散の仮定と混合分布の経路数を検証する。
記録は`target/phrase-expectation/2026-09-08-uncertain-magnitude-regression/`に保存する。

### 9.3.50 観測ノイズを含む文脈推論と、事前分布の限界

`MagnitudeContexts`は9.3.49の観測・更新機構を、累積学習、新しい事前分布への更新、
有限個の文脈の再利用へ接続する。文脈ごとにGaussianの係数状態を保持し、文脈の割当ての
不確かさと、絶対値による符号の不確かさを分ける。ゼロ平均の事前分布では、係数ベクトル全体の
符号を反転しても、将来の絶対値の分布は変わらない。この対称性を使い、同一の完全な状態を
統合してから経路を制限する。符号の候補を関係の文脈として数えない。
短い符号・文脈履歴の全列挙で周辺尤度を照合し、累積学習の予測は9.3.49の符号を統合しない
回帰とも一致した。未使用ラベルの周辺化と連続時間の遷移則は9.3.46を引き継ぐ。
観測のない時間は不確かさへ作用するが、架空の無音として学習しない。

文脈数3、遷移率0.2/s、切片の事前分散100、傾きの事前分散1を使う。
残差の標準偏差は0.1とし、観測の標準偏差0.1を主条件、0.01と1を感度比較とする。
主条件の経路上限は128である。入力は保存済みの6記録で、seed 4721の持続・復帰・反転後復帰の
構成対照と、数値修正後のseed 192053の通常08・09・12を使う。3ノイズ水準の18条件に、
持続対照と12の512経路による比較を加え、計20条件とした。領域、観測間隔、対象、シナリオは
調整していない。以下の構成対照は1 seedの、以前から指定した277→733 Hz方向の結果であり、
一般的な再認識や音源識別の成績ではない。

有界な採点は9.3.48の尺度と混合分位点による区間分割を引き継ぐ。狭いGaussian混合分布の
積分を改善するため、成分の位置から8標準偏差の外側に相当する二つの境界も加えた。
末尾を含む積分範囲は維持する。再評価を含む20条件の完了7,975件・未決203件と、
採点1,004,850成分を照合した。同じ6入力と各方向を使い回しており、独立した観測数ではない。
正負のGaussian密度項180,469,362成分を再計算し、混合対数密度の差は
\(1.82\times10^{-12}\)未満だった。独立した適応積分では、尺度1の再利用予測から選んだ
228件を検査し、主採点との差は最大\(2.47\times10^{-5}\)、節点を倍にした差は
最大\(6.74\times10^{-6}\)だった。有界座標の参照積分1件で境界付近の警告が出たが、
別のlogit座標の参照積分では警告なく計算でき、元の参照との差は\(1.05\times10^{-16}\)未満だった。
選択した予測の数値検査であり、全予測への誤差保証や認知的な較正ではない。

観測の標準偏差0.1、尺度1における、構成対照44〜60秒の指定方向の損失は次のとおりだった。

| 関係 | 累積学習 | 新規への更新 | 文脈の再利用 | 現在値の持続 |
|---|---:|---:|---:|---:|
| 持続 | 0.037038 | 0.047679 | 0.043498 | 0.073497 |
| 復帰 | 0.035852 | 0.047491 | 0.046363 | 0.073497 |
| 反転後に復帰 | 0.036330 | 0.048342 | 0.042550 | 0.073497 |

残る二つのノイズ水準でも、これらの比較は累積学習が優位だった。文脈の再利用を採用する根拠にはならない。
通常音源24〜40秒の全方向平均は、主条件で次のとおりだった。

| Sample | 累積学習 | 新規への更新 | 文脈の再利用 | 現在値の持続 |
|---|---:|---:|---:|---:|
| 08 | 0.316840 | 0.270861 | 0.285121 | 0.144183 |
| 09 | 0.435577 | 0.361260 | 0.346200 | 0.184225 |
| 12 | 0.289511 | 0.234842 | 0.254367 | 0.074750 |

完了対象は順に42・27・115件である。登録したすべてのノイズ水準で、三つの文脈モデルは
これら通常音源の後半では現在値の持続より悪かった。ゼロ更新の不連続性を解消しても、
音響予測の有用性、音楽的な流れやphraseの認識が得られるわけではない。

文脈推論の数値近似も収束していない。主ノイズ水準の128経路では、相手を含む再利用モデルの
1回の更新で事後質量を最大約0.507除いた。512経路でも持続対照と12で最大約0.254・0.189が残る。
再利用モデルの全方向平均の損失は、持続対照44〜60秒で0.049755から0.052129へ、
12の24〜40秒で0.254367から0.244766へ変わった。経路数が十分であるとは言えない。
別に、実入力から発行した累積学習の48予測について精度行列を直接解いた。
条件数が最大約\(6.46\times10^6\)でも、位置の差は\(8.01\times10^{-10}\)未満、
成分内分散の差は\(3.68\times10^{-13}\)未満だった。この丸め誤差では、大きな予測損失を説明できない。

事前分布にも観測上の意味付けが必要である。通常音源の最初の予測発行時に、現在の領域観測の平均は
1.092・1.214・3.315だったが、絶対値Gaussianの事前予測平均は8.050・8.052・8.125だった。
以前の対数モデルと同じ数値の事前精度を使っても、観測座標での意味は維持されない。
この初期の差だけで後半の誤差の原因は特定できないが、引き継いだ設定を認知原理から導いた事前分布とはしない。
現行モデルは新しい文脈へ移ると、自身の継続を表す係数も含めて条件付き予測を初期化する。
次の設計では、現在の音響状態に支えられた継続と、学習した関係による補正を分け、
事前分布を実際に観測する座標で比較する。関係が新しい、または不確かであるだけで、
現在聞こえている音まで未知になる設計にはしない。適切な継続則と不確かさは引き続き比較が必要であり、
この方向を採用済みの修正とは扱わない。

Python評価388件が通過した。既定の旧モデルの経路も、保存した変更前の評価器と実入力で比較し、
1,082記録すべてが一致した。
Rust/Cargoと両releaseは9.3.47を維持し、新しい作者試聴や実機受入は追加していない。
記録は`target/phrase-expectation/2026-09-08-uncertain-relational-contexts/`に保存する。
この観測・文脈候補は本番へ採用しない。事前分布の根拠、継続予測の信頼性、推論近似を扱った上で、
認知的保持・干渉、流れの同一性、phrase・終止、状況に応じた行動価値へ進む必要がある。

### 9.3.51 現在の音を条件とする継続予測と、文脈ごとの補正

9.3.50の候補は、文脈の変更時に自身の継続を表す係数まで初期化していた。
新しい研究候補`ContinuationContexts`は、発行時に観測した対象領域の値を基準にする。

\[
y=\left|o+u^\top\beta+v^\top\delta_c+\eta+\epsilon\right|.
\]

\(o\)は発行時の領域別`log1p` RMS、\(u\)は対象領域の現在と履歴、\(v\)は周囲の特徴、
\(\delta_c\)は文脈ごとの補正である。現在の音が必ず持続するという仮定ではなく、
観測値を条件とした継続の基準を置く。入力特徴自体の観測誤差を潜在状態として推定するモデルではない。
二つの特徴群へそれぞれ切片を加え、Euclideanノルムを1にする。係数の独立なゼロ平均事前分布を
それぞれ\(\sigma_{\mathrm{correction}}^2 I/2\)とし、初期の補正全体の標準偏差を観測量の単位で指定する。
前節から基準値・特徴・事前分布を合わせて変更しており、基準値だけの効果を切り分ける比較ではない。

比較する二つの配置は、継続と関係の係数を共に文脈へ入れる`reset_all`と、継続の係数を共有する
`shared_continuation`である。後者は共有係数と各文脈の完全な同時Gaussian分布を保持する。
文脈の更新では関係側だけを独立な事前分布へ戻し、保持する変数の周辺分布を変えない。
共有係数との相関を通じ、直接観測していない文脈の係数も条件付きで更新されうる。
これは忘却の機構ではない。既知の非ゼロ基準値は前節の符号対称性を壊すため、正負の潜在符号を
保持し、完全に同じ状態だけを統合する。

2・3文脈と短い符号履歴の全列挙を、観測の共分散行列から独立に計算した周辺尤度と照合した。
補正の標準偏差0.03・0.1・0.3で、累積・更新・再利用の両配置が一致した。
共有部分の周辺分布の保持、単一文脈での配置間一致、ゼロ付近の連続性、単位変更、
予測発行後の不変性、未観測と無音の区別も検査した。既存二モデルから文脈遷移の共通処理を抽出し、
変更前との24,192項目の比較と、実入力による各491記録の比較で一致した。Python全395件が通過した。

保存済み6入力へ両配置を適用する12条件と、共有配置の持続対照・通常12を
512経路に増やす2条件、計14条件を比較した。主条件は補正・残差・観測の標準偏差を各0.1、文脈数3、
遷移率0.2/s、経路上限128とする。現在値の点予測に加え、同じ初期分散を持つ未学習の継続分布を
対照にする。これらの値は認知的な事前分布や保持時間として同定していない。

主条件の共有配置は、構成対照の指定方向で更新・再利用の損失を減らしたが、累積学習には届かなかった。
通常音源24〜40秒、尺度1の全方向平均は次のとおりである。累積学習の列は両配置で同じ値だった。

| Sample | 累積学習 | 再利用・全更新 | 再利用・継続共有 | 未学習の継続分布 | 現在値の点予測 |
|---|---:|---:|---:|---:|---:|
| 08 | 0.181365 | 0.198661 | 0.191950 | 0.156489 | 0.144183 |
| 09 | 0.214407 | 0.243626 | 0.229005 | 0.199833 | 0.184225 |
| 12 | 0.143462 | 0.126814 | 0.134074 | 0.097272 | 0.074750 |

共有による効果は一様ではなく、12では再利用の損失が増えた。新しい文脈へ更新する方式も、
両配置の通常3音源すべてで二つの継続対照を下回った。保持する状態を分ける数学的な整合だけでは、
この観測対象の予測利得や認知的な妥当性を確立できない。

通常音源の後半について、
数値上のゼロと正の観測を分けて誤差を調べた。これは比較後の診断であり、採点対象の除外や再適合ではない。
ここでゼロなのは選択した周波数領域の値であり、演奏全体の無音を意味しない。
08・09・12の方向別対象252・162・690件中、ゼロは98・70・262件だった。同じ音源・領域を再利用した数であり、
独立した聴覚観測数ではない。ゼロの区間で損失が大きく、正の区間にも予測の誤りは残る。

固定ノイズの仮定は、この誤差の一部に構造的な制約を与える。各成分の分散は残差・観測分散の和0.02以上である。
非負値の任意の評価点において、折り畳んだGaussianの生存確率は、平均0・分散0.02の成分より小さくならない。
したがって正規化された混合にも、尺度1・ゼロ観測に対する次の下限がある。

\[
L(0)\geq\int_0^\infty\frac{[2\Phi(-y/\sqrt{0.02})]^2}{(1+y)^2}\,dy
\simeq0.05972765.
\]

独立した半無限区間の積分で確認した。未学習の継続分布は分散0.03なので、基準値も実測もゼロなら損失は
約0.07164874となる。この下限は登録したノイズ仮説から生じるもので、聴覚の無音閾値ではない。
不確かさを一律に消す根拠にもならず、他の誤差を説明し尽くすものでもない。観測座標で事前分布を指定するだけでは、
観測や未来予測の変動を較正できないという制約を残す。

14条件の完了5,598件・未決142件、採点806,112成分を照合した。正負のGaussian密度項
144,895,816成分を再計算し、混合対数密度の差は\(1.37\times10^{-12}\)未満だった。
選択した160予測を独立した適応積分で確認した。主採点との差は最大\(3.43\times10^{-6}\)、
節点を倍にした差は最大\(1.73\times10^{-8}\)だった。参照積分1件の警告は別のlogit座標で検査し、
警告なく、元の参照との差\(6.94\times10^{-18}\)で一致した。独立した音源数や聴覚的な較正の成績ではない。

両配置の累積学習と未学習対照では、発行予測43,884件が完全一致し、累積学習の更新28,524件も一致した。
一方、文脈推論の近似は収束していない。128から512経路へ増やすと、共有再利用の損失は
持続対照44〜60秒の指定方向で0.067491から0.067513、通常12の後半全方向で0.134074から0.136076へ変わった。
発行済み予測の累積確率を有界座標の指定63点で比較すると、最大差は順に約0.316・0.320だった。
これは326,592点の比較であり、連続した全座標上の誤差保証ではない。
512経路でも更新時に除いた事後質量は最大約0.205・0.132であり、平均損失の差が小さいだけで
経路数が十分とは扱わない。

本候補は本番へ採用しない。次は、固定した同じ分散を全観測へ課す仮定と、実測で較正できる予測残差を区別する。
観測誤差と予測残差を根拠なく二分せず、経過時間・干渉による関係記憶の更新とも分ける必要がある。
尺度や数値設定の変更で良い成績を選んでも、認知的な保持・期待を同定したことにはならない。
認知的保持・干渉、流れの同定、phrase・終止、行動価値の学習は引き続き未達。
Rust/Cargo107ファイルと両releaseは9.3.47を維持し、新しい作者試聴や実機受入は追加していない。
記録は`target/phrase-expectation/2026-09-08-continuation-conditioned-contexts/`へ保存する。

### 9.3.52 予測残差の幅を学習し、定常な蓄積の限界を分ける

9.3.51の固定分散による下限を、観測の不確かさそのものとは扱わない。
新しい研究用の`ResidualRegression`は、入力特徴を所与とし、予測先の説明できない変動を
一つの未知分散として保持する。観測誤差と過程の誤差へ識別できないまま分割しない。

\[
y=\left|o+x^\top\beta+\sqrt{v}z\right|,\qquad
z\sim N(0,1),\quad\beta\mid v\sim N(m,vV),\quad v\sim IG(a,b).
\]

分散\(v\)を積分した予測は絶対値Student分布となる。
基礎となる正規分布と未知分散の共役計算は[Murphyの整理](https://www.cs.ubc.ca/~murphyk/Papers/bayesGauss.pdf)を参照し、
基準値と潜在符号を含む回帰への拡張は、短い全符号履歴の独立した一括計算で検査した。
各履歴の係数平均・分散のscale・確率を保持し、同一状態の統合後に経路数を制限する。
Studentのscaleの二乗、符号付き成分の分散、非負の観測分布は区別する。
発行済み予測は不変とし、ゼロ観測と入力欠落を区別する。

主条件は\(a=3\)、係数モデルの\(E[v]=0.02\)、\(V=0.25I\)とする。
前節と同じ二つの単位ノルムの特徴群により、初期の符号付き予測分散は0.03となる。
係数を持たず、\(E[v]=0.03\)から分散だけを学習する継続対照と、その未学習版を用意する。
この二つと係数モデルの初期予測分布は一致する。前節のGaussianとの分散の一致は、
事前分布や係数と残差の依存関係まで同じという意味ではない。\(a=3\)は分散自体の分散が有限となる
数学的な選択であり、認知的な標本数・保持時間としての根拠はない。

ゼロが続くと予測分散は正の固定下限なく縮み、その後の誤差で再び増えた。
一方、定常モデルは過去を蓄積し続けるため、変動幅の切替への追従は遅れる。
単位変更、逐次更新と一括計算、共分散の正定値性、遅れて届く実音・欠落・EOF、
裾の重い分布と狭い成分を含む採点を検査し、Python全403件が通過した。
既存二つの採点経路も、実入力の先頭各4完了対象・各6出力記録で変更前と完全一致した。

独立に生成した未学習のデータによる検査でも、事前分布の影響は残った。
真の標準偏差0.01の係数なし対照では、256観測後の残差分散が約0.000569、
4,096観測後も約0.000131だった。各10,000未学習標本のPITのKS距離は約0.396から0.0645へ減った。
標準偏差0.1では4,096観測後の分散約0.00996、KS距離約0.00735だった。
二係数の構成対照では、真値\([0.08,-0.05]\)に対し512観測後の重み付き平均が
約\([0.0763,-0.0492]\)となり、2,000未学習標本の90%区間被覆率は0.928だった。
これは特定の生成条件に対する診断であり、人間の聴覚的な較正ではない。
数値上のゼロだけが続く系列では、連続分布のPITはゼロに固定され、一様性の検査へ読み替えられない。

保存済み6入力への128経路の主比較と、持続対照・通常12の512経路比較、計8条件を完了した。
通常3音源の24〜40秒、尺度1、全方向の平均損失は次のとおりである。係数モデルの列は相手情報を含む。

| Sample | 前節の累積Gaussian | 今回の係数・分散学習 | 分散だけの学習 | 未学習の継続分布 | 現在値の点予測 |
|---|---:|---:|---:|---:|---:|
| 08 | 0.181365 | 0.188441 | 0.179091 | 0.154067 | 0.144183 |
| 09 | 0.214407 | 0.227478 | 0.223778 | 0.197024 | 0.184225 |
| 12 | 0.143462 | 0.182183 | 0.179684 | 0.094689 | 0.074750 |

相手情報を含まない係数モデルも、通常3音源の後半では継続対照を上回らなかった。
一方、構成した持続・再出現・逆順再出現の対照では、指定方向277→733 Hz、44〜60秒の損失が
係数・分散学習で約0.0459・0.0458・0.0458となり、分散だけの学習約0.0520、点予測約0.0735を下回った。
再出現固有の記憶利得や音源間の因果関係を示す比較ではない。

通常音源の24〜40秒では、ゼロとなる対象に対して分散だけを学習する対照が保持した残差分散の平均は、
08・09・12で約0.176・0.172・1.834だった。固定した正の下限はなくても、過去の大きな変動が
現在の予測へ残る。正の観測にも誤りがあり、この診断だけで原因を説明し尽くさない。
選択した領域のゼロは演奏全体の無音ではなく、複数方向で再利用した同じ実音は独立標本ではない。

3,221完了対象・81未決、採点289,890成分を実測と照合し、Studentの正負密度項25,509,372成分を独立に再計算した。
混合対数密度の差は最大4.26e-14だった。選択した180予測の独立積分では、
主採点との差は最大1.35e-05、32点への増加後は最大4.89e-07だった。
参照積分の警告はlogit座標で再検査し、元の参照と1e−10未満で一致した。主採点は変更していない。

128経路の主比較では、08・09で一度の更新に約0.5の事後質量を除去した。持続対照でも約0.0406を除去し、
512経路で約0.0324が残った。128と512の発行済み分布を尺度1の指定63点で比べると、
持続対照の最大累積確率差は約0.0030、通常12では約1.37e−7だった。未学習予測5,184件は完全一致した。
これは指定点の比較であり、全座標での誤差保証ではない。通常08・09の経路数感度は未検査であり、
本比較全体の近似収束とは扱わない。

本候補は本番へ採用しない。分散の学習は、環境の状態が変わる機構や、認知的な関係の保持・干渉を
導出したものではない。更新は完了した観測の数に依存し、経過時間や介在情報による記憶更新を持たない。
次は、変化する実音の予測状態と、関係を保持・再利用する記憶の状態を分け、既存の文脈候補をこの境界で点検する。
事前分布や忘却定数を採点結果へ合わせるだけでは、DCCの根拠を満たさない。
流れの同一性、phrase・終止、行動価値、作者の採用は引き続き未達。
Rust/Cargo107ファイルと両releaseは9.3.47を維持し、新しい音源・作者試聴・実機受入は追加していない。
記録は`target/phrase-expectation/2026-09-09-learned-residual-uncertainty/`へ保存する。

### 9.3.53 変化する予測のずれと、保持する係数を分ける

9.3.52の定常な蓄積に対し、研究用の`EvolvingResidualRegression`は、固定した係数と
時間変化する予測のずれを別々に保持する。発行時の基準値を\(o\)、特徴を\(x\)として、

\[
y(t)=|o+x^\top\beta+r(t)+\sqrt v\,e|,\qquad
dr=\sqrt{qv}\,dW,
\]

と置く。\(\beta\)と\(r\)の共分散、共通の未知分散\(v\)の不確かさを保持し、前節と同じ
絶対値Student分布で予測する。観測なしの時間推移は\(r\)の分散を増やすが、係数の周辺分布や
分散\(v\)の事後分布を変えない。実音が届くと両者を条件付きで更新する。初期の\(r\)は
最初の予測発行時に確定値ゼロとする。これは認知的な忘却ではなく、統計モデル上の分離である。
線形SDEの時間推移には[SärkkäとSolin（2019）§6.2](https://users.aalto.fi/~asolin/sde-book/sde-book.pdf)を参照した。
絶対値観測と未知分散を含む拡張は、不等間隔の短い全符号履歴に対する独立した一括計算で検査した。

主条件の\(q=1\,\mathrm{s}^{-1}\)、感度比較の0.1と10は、時間的共分散を定める数値上の仮説である。
保持時間や人間の変化検出率から導いていない。過程と残差の分散比を固定するので、聴覚の観測ノイズと
環境の変動を別々に同定するモデルでもない。NSGTの帯域ごとの平滑化を、FFT容器の長さから求めた
独立標本間隔で置き換えない。予測には係数・状態・両者の共分散・残差の寄与を分けて記録する。

既定経路と\(q=0\)は、新しい状態メタデータを除き、前節6入力の4,827記録、58,512予測、42,786更新と
完全一致した。発行済み予測の不変性、単位変更、入力欠落、遅延、EOFも検査し、Python全407件が通過した。
宣言した拡散過程から作った3 seedの対照では、256観測後の128逐次予測について、係数を持つ\(q=1\)の
平均損失は約0.00077〜0.00083、\(q=0\)は約0.00262〜0.00401だった。\(q=1\)の係数平均は
約0.0786〜0.0836で、生成値0.08に近かった。これは指定した生成過程の検査である。
入力特徴が一定の別対照では、係数の変化と状態の変化から同一の64観測を生成できた。変数を分けるだけでは原因を識別できない。

保存済み6入力の主比較と、持続対照・通常12の\(q=0.1,10\)比較、計10条件を128経路で完了した。
通常3音源の24〜40秒、尺度1、全方向の平均損失は次のとおりである。小さい方がよい。
係数モデルは相手情報を含み、「状態のみ」も現在値を基準とする継続予測と残差分散の学習を含む。

| Sample | 前節の定常係数モデル | 係数＋状態 | 状態のみ | 未学習の継続分布 | 現在値の点予測 |
|---|---:|---:|---:|---:|---:|
| 08 | 0.188441 | 0.235927 | 0.256376 | 0.154067 | 0.144183 |
| 09 | 0.227478 | 0.313516 | 0.321493 | 0.197024 | 0.184225 |
| 12 | 0.182183 | 0.221825 | 0.225368 | 0.094689 | 0.074750 |

相手情報を含まないモデルも、通常3音源の主評価区間で継続対照を上回らなかった。
持続・再出現・逆順再出現の指定方向277→733 Hz、44〜60秒では、係数モデルの損失は
約0.0488・0.0489・0.0486だった。未学習の継続分布約0.0576は下回ったが、前節の定常モデル約0.0459・0.0458・0.0458を上回った。
感度比較の係数モデルは、通常12で\(q=0.1,10\)の順に約0.2239・0.2283、持続対照で約0.0488・0.0556だった。
これらから認知的な時定数や採用する\(q\)を選ばない。本候補は本番へ採用しない。

4,065完了対象・101未決、採点365,850成分を実測と照合し、
Studentの正負密度項18,091,332成分を独立に再計算した。混合対数密度の差は最大4.26e-14だった。
選択した225予測の独立積分では、主採点との差は最大1.63e-05、32点への増加後は最大1.23e-09だった。
参照積分の警告13件をlogit座標で再確認した。そこで生じた極小区間の警告9件は、
区間を除いた寄与の上限が最大1.88e-13であることを確認して解消した。裾の打切り上限は8.50e-18、
元の参照との差は最大1.15e-14だった。最初の警告記録は保存し、主採点は変更していない。
これらは6入力を繰り返し用いた数値検査であり、独立した聴覚試行数ではない。

近似の収束にも限界がある。主比較の一更新で除去した事後質量は08・09で約0.5、持続対照で約0.106だった。
この3入力だけ512経路の予測・更新を追加し、10回ごとの発行と最大除去直後の発行を、尺度1の63座標で比較した。
3モデル・全方向の計121,338指定点での最大累積確率差は、08で約0.118、09で約0.00659、持続対照で約0.341だった。
未学習予測6,174件は完全一致した。512経路でも約0.5・0.5・0.0873の最大除去質量が残る。
追加3条件は予測比較のみであり、上の10条件の採点・独立密度監査には含めない。指定点の比較を全発行・全座標の収束保証としない。

**実音の状態と、予測のずれの境界。** 同じ将来の音でも、発行時の基準値が違えば\(r\)は変わる。
将来値が全て1でも、基準値が\([0.2,0.4,0.8,1]\)なら補正前のずれは\([0.8,0.6,0.2,0]\)、
基準値も全て1ならゼロになる。この変数は予測起点に条件付けられたずれであり、実音の状態や流れの同一性を表さない。
既存の`AcousticPosterior`と`DrivenBodyPosterior`は波形の状態とパラメータを分けるが、関係記憶は持たない。
`ContinuationContexts`の文脈も回帰仮説であり、認識された音の流れではない。

次の設計では、実音の観測によって更新するPの状態、個々の予測に固定した発行文脈、関係の保持・再利用を担うMを分ける。
予測を追加で要求しただけでPの観測件数やMの経験が増える経路を作らない。連続した証拠とその観測範囲を先に保ち、
状態変化による追従と、経過時間・介在情報による関係の保持を混同しない。認知的な保持・干渉、流れの同一性、
phrase・終止、行動価値、作者採用は未達のままである。Rust/Cargo107ファイルと両releaseは9.3.47を維持し、
新しい音源・作者試聴・実機受入は追加していない。記録は`target/phrase-expectation/2026-09-09-evolving-state-retained-coefficients/`へ保存する。


### 9.3.54 実音の観測と予測要求を分け、入力欠落中の状態を保つ

9.3.53の境界を既存の観測経路で点検した。本番の`SpectralHistory`と`BandEnergyHistory`は、
すでに実音の観測ごとに更新される。一方、研究用の地域間回帰は、発行した予測の完了対象で学習する。
これは予測の較正に使う標本の契約であり、その件数を連続した聴覚経験の件数にはできない。

研究用の`DrivenBodyPosterior`へ、入力が欠けた時間だけ振動状態を進める`advance_to`を追加した。
既存の線形遷移と駆動の共分散を合成し、平均と共分散を進める。実音の観測による更新は行わず、
未知分散の事後分布、対数尤度、観測済みの標本数を保つ。根拠は
[SärkkäとSolin（2019）§6.2・§10.6](https://users.aalto.fi/~asolin/sde-book/sde-book.pdf)の遷移と欠測時の更新省略である。
物理的な減衰があれば、無観測でも振動の分散が小さくなる場合がある。認知的な忘却や、新たな証拠の獲得とは扱わない。
この振動状態は波形の搬送波に関するものであり、拍の位相や音の流れの同一性ではない。

| 状態・記録 | 更新する契機 | 更新しない契機 |
|---|---|---|
| 振動状態とその時刻 | 実音による条件付け、または明示された欠落時間の推移 | 同じ時刻での予測要求 |
| 未知分散・観測済み標本数・最後の観測終端 | 新しく受け取った実音 | 入力欠落、予測の発行・採点 |
| 発行済み予測 | 発行時に固定 | 後続の状態更新、同じ対象への別の予測要求 |
| 予測対象の結果 | 対象区間の実音が全て届いた場合に採点 | 入力欠落を含む区間、EOF後の未観測部分 |

`stream_predictions`は、連続した区間を実音または欠落として受け取り、各実音標本を一度だけ状態へ反映する。
予測要求は別の時刻列で受け付ける。同じ対象に複数の予測を出しても、その音を重複して学習しない。
一つの予測は将来の波形区間全体の同時分布であるため、途中に欠落が入ればその予測を打ち切りとして記録する。
欠落中に要求された予測も発行できるが、状態時刻と最後に実音を観測した終端を別々に示す。
末尾で未完の対象には無音を補わず、未決のまま残す。予定された将来の要求も、未発行と区別する。

欠落を含む逐次更新を、実際に観測した添字だけを条件とする連続時間共分散の独立した一括計算と照合した。
モードなし、駆動なし、駆動と減衰を持つ場合を検査し、ゼロ値の観測と欠落の違い、時間推移の分割、
無効な時刻の拒否も確認した。新しい観測経路では、重なる予測、同じ要求の反復、予測要求なし、
分割幅の変更、欠落、打ち切り、未決を検査した。Python全411件が通過した。

保存済み通常08・09・12の8.0〜8.5秒を、そのままの標本化周波数と振幅で使用した。
直前40 msから受動的な0・1・2モードの候補を適合し、利用可能な最大の候補を固定した。
駆動比100、初期精度0.0001、分散の事前分布IG(2, 1e-6)は既存の数値仮説であり、この結果で選んでいない。
パラメータは予測区間より前に利用可能である。パラメータの不確かさや人間の認知的な時間尺度を同定した比較ではない。
20・100 ms先までの波形を50 msごとに要求する条件、10 msごとの要求と既存要求の反復を加える条件、
要求しない条件を比較した。入力の分割幅も512・257・24,000標本へ変えた。
同じ比較を、8.20〜8.25秒の実音だけを利用不能とする条件でも行った。

18条件で、1,008予測を完了795・入力欠落による打切り87・未決126へ照合した。
実音の更新は、予測要求の数によらず、欠落なしでは各条件24,000標本、欠落ありでは21,600標本だった。
直前40 msの初期観測1,920標本は別に数える。同じ対象に発行した396組の比較では、
予測平均の最大差は9.03e-16、予測分散は2.57e-16だった。最終状態の平均差は最大9.63e-17、
共分散差は1.61e-15、未知分散のscale差は1.41e-13、蓄積した対数尤度の差は6.04e-10だった。
区間の分割による浮動小数点の加算順序の違いを含む。保存した全区間の連続性、観測済み標本数、
発行時刻、実測対象、打切り、EOFの件数も別の読み取り経路で照合した。3入力を反復した比較であり、独立した聴覚試行数ではない。

これは連続観測と予測要求の接続を検証した結果である。推定精度や音楽的な流れが改善したという比較ではなく、
通常演奏の長時間安定性や聴覚上の較正も検証していない。標本数は条件付きの波形尤度で用いる件数であり、
認知的な記憶の強さや保持時間へ読み替えない。現行の関係記憶へこの件数を直接渡す接続は行わない。

次は、連続したPの観測とその不確かさから、時間関係の保持に何を渡すかを定める。音響状態が新しい音へ追従する機構と、
過去の関係が経過時間や介在情報によって保持・干渉される機構を分け、発行済みの予測誤差だけを入力にしない。
認知的な保持・干渉、流れの同一性、phrase・終止、行動価値、作者採用は未達である。
本変更は研究経路に限定し、Rust/Cargo107ファイルと両releaseは9.3.47を維持した。新しい音源や作者試聴は追加していない。
記録は`target/phrase-expectation/2026-09-09-continuous-acoustic-evidence/`へ保存する。


### 9.3.55 時間構造DCCの全階層と、局所生成への結合

<a id="mechanism-selection-rule"></a>

#### 機構選択規則と本体範囲の再改訂（2026-09-20採用、同日のレビューを反映）

和声側のDCCは、ラフネスと調和性という確立した心理音響モデルを持つ機構だけをランドスケープとして実装し、
慣れ（habituation）を同じ側の順応機構として置いている。そこから直接導けない秩序、たとえばVoice間のcrowdingによる
ニッチ分化や巨視的な構成は、ALifeの創発とScenarioに委ねてきた。著者はこの分担を時間構造にも同じ規則として
適用することを採用した。2026-09-13のI9除外は、この規則を一度だけ適用した判断だったと位置づける。

**選択規則。** ある時間機能をDCC側（共有の観測・期待・記憶と、それを生成へ返す経路）に置く条件は、次の三つをすべて満たすことである。

1. 対象とする聴取応答に一次研究の根拠があり、対応する認知神経機構が同定されている。
2. 採用する計算モデルが既存文献で同定済みか、閾値・尺度を明示した工学則として書ける。R/Hと同様に既存モデルの採用を優先し、新しい表現を先に作ってから人の評定で適合させる手順を取らない。
3. 完成条件に人を対象とする新規データ収集を必要としない。作者自身の試聴と可聴差の確認だけで採否を決められる。他者の試聴は任意の参考意見であり、判定に必須の収集にはしない。

三つを満たさない機能について禁止するのは、本体の生成へ接続する表現の新設と採用である。保存済みの研究実装と、
既定offの受動診断（report／UI）は例外として保持できる。生成への作用はALifeの局所判断とScenarioの構成に委ね、
ListenerTwinは受動的な観測指標に留める。

**規則の適用結果。** 各機能について三条件を別々に判定した。「未同定」は本計画の採用候補について確認した範囲での判断であり、
将来の研究を否定するものではない。

| 機能 | 判定 | 条件1（応答と機構） | 条件2（モデル） | 条件3（判定） |
|---|---|---|---|---|
| articulation: 聴いたgestureの観測（I5、共有P） | 本体 | 聴覚–運動前野の感覚運動対応 | attack／continuation／release／gapの明示尺度、未校正 | 作者試聴 |
| articulation: 動作に条件づけた自声予測（I10、Voice私有A） | 本体 | Schneider 2018、Audette 2022ほか | 有限パラメータの誤差更新。モデル・尺度・窓は実装前に登録済み | 作者試聴 |
| 拍・到来・小節（I6） | 本体 | 拍は被殻・SMA・運動前野の拍ネットワーク（Grahn & Rowe 2009）。小節・拍節解釈は想像拍節がEEG応答を変える証拠（Nozaradan 2011）に基づき、拍とは別の解釈状態として保持 | 観測accent時計上の周期・到来予測、明示閾値 | 作者試聴 |
| 短期の反復期待と慣れ（既存の反復・帯域エネルギー予測、recallの保持状態とreference_inventoryの有界因果参照） | 本体 | 記憶減衰（Harrison 2020）・暗黙記憶（Bianco 2020）の行動証拠。機構候補は一次聴覚皮質の刺激特異的順応（Ulanovsky 2003）で、対応は部分的。本体採用は既存実装の範囲に限る | 既存実装の明示則 | 作者試聴 |
| groove・参加意欲の評定head（T3。54入力・109係数の順序ロジット） | 研究拡張 | 行動証拠あり（Witek 2014ほか）。機構は部分的 | 係数は人の評定への適合が前提で、既存モデルではない | R4／R5に人の評定が必要 |
| 変形をまたぐ対応・エピソード想起（I4のmatcher・memory検索・recall graph） | 研究拡張 | 暗黙記憶の証拠あり | DTW・episode graphは本計画の新規表現 | 適合に人の評定が必要 |
| phrase・終止（T5、I7） | 研究拡張 | 境界応答の証拠あり。終止の機構は採用候補で未同定 | 82成分の適合が前提 | 人の評定が必要 |
| section・帰還（T6、I8） | 研究拡張 | event boundaryと文脈想起の証拠あり。音楽特異的でない | memory-attention graphは設計仮説。39成分の適合が前提 | 人の評定が必要 |
| 全曲文脈（T7、I9） | 研究拡張 | 2026-09-13の判断を維持 | 同 | 同 |

T3のうち、固有周期・参加周期・鳴る／待つ／見送るの参加判断は、Voiceの身体と作者の参加傾向としてALife側に既にあり、
本体ではそのまま使う。知覚的なgroove評定headは `[temporal_groove]` を明示した場合だけの既定off診断として保持する。

これに伴い、四接続のうちI12b（beat/grouping ↔ gesture）だけを本体に残す。本体でのI12bは、I6の到来期待がgesture候補の
時刻へ作用する経路と、観測したaccentが周期推定へ入る既存経路の二つに限り、除去比較もこの二経路それぞれで行う。
前者の更新則・閾値・尺度はI11の実装前登録で確定し、登録前は本体採用を保留する。登録前に有効なのは後者の既存経路だけである。
独立したgrouping推論は本体に追加しない。
I12a・I12c・I12dはI12eと同じく研究拡張へ移す。研究判定のうちR1（機関手続きを含む人の収集）、R4（適合・校正）、
R5（認知課題）は本体の完了条件から外し、研究拡張を採用する場合にだけ適用する。本体の閾値・尺度は各Iの契約検査
（O03／O09）で登録し、人の評定へ適合させない。R2・R3・A3の資源・鮮度・復旧の検査は、実際に動く処理すべてについて維持する。
本体の作者採用はA1（可聴差）・A2（作者採用）・A4（縮小した範囲の監査）で判定する。この判定は認知機構の妥当性を実証したことを
意味しない。人の収集に関わる義務O05–O08と、長期記憶容量の採用条件O16（2026-09-13に移管済み）は研究拡張にだけ適用する。
O19の旧template置換は本体ではA1／A2／A4で判定する。ここに挙げたR・A・Oは抜粋であり、義務の本文と全対応表は
`docs/roadmap/temporal-dcc/milestones.md` §1.3・§4に置く。

**維持するもの。** I7・I8とgroove評定headの既存実装・数値fixtureは削除せず、既定offの診断・report経路として保持する。
I9と同様に、技術完了の記録を認知的採用へ引き上げない。air-gap、既定off、因果的な状態更新、
Scenarioの帰還指示を聞こえた帰還の証拠にしない分離は変わらない。本改訂は文書上の範囲と判定の変更であり、
Rustと生成音の変更はない。実行上の正本は `docs/roadmap/temporal-dcc/milestones.md` §1.3に置く。

<a id="i9-scope-decision"></a>

#### 本体の認知機構と長期聴取研究の範囲（2026-09-13採用）

著者の判断により、全曲文脈I9をConchordal本体の必須工程から外し、長期の聴取応答を扱う研究拡張へ再設定した。
DCCの原理は、対応する認知神経機構の状態と動態を抽象化し、音響生成の環境へ結びつけることにある。
神経領域の機能上の対応づけは設計の手がかりだが、現行のepisode graph、DTW照合、保持・退避則、
全曲完了感headをその機構として裏づける対応は未確立である。工学的な動作や評定への適合だけでは、この隔たりは埋まらない。
DCCと両立できないと断定する変更ではなく、機構対応が未確立な全曲機能を本体へ必須化しないという採否である。

Scenarioは作者の巨視的な構成と環境条件、生成側は身体と認知環境に応じた局所判断、
ListenerTwinは実際のpresentationに対する選択した聴取応答を担う。Scenarioの帰還指示を、
Twinが聞いた帰還の証拠にしない。生成側が全曲構成を再認識することや、Twinを人間の包括的な複製にすることは前提にしない。

新たな機構を導入するときは、対象応答、認知神経機構の一次根拠、抽象化する状態・更新・相互作用、
利用先、音への具体的な作用、競合説明を区別する比較を先に定める。必要な記憶の範囲はそこから選ぶ。
長い過去の影響を残すことと、全曲の出来事を保存することを区別し、未同定の更新則を神経機構として扱わない。

終端を定めない継続運転（indefinitely running）も、有限作品と並ぶ設計対象とする。
状態更新は観測済み入力に因果的に従い、予定曲長・EOF・全曲の完了感を要求しない。
有限資源の状態で長い影響を残す方式を検討し、全履歴の保存は一般条件にしない。
全曲完了感は有限の聴取区間を選んだ研究課題で扱う。継続運転の実装・資源・実時間受入は、この方針採用では確認していない。

| 対象 | 今回採用した範囲と、残る判定 |
|---|---|
| 本体 | T1–T6、I12a–I12dの四接続、該当するI/R/A・O義務。I8までの初期診断版の技術完了は維持し、認知的採用・生成結合・作者採用は別に検証する |
| I9・I12e・T7 | 長期聴取応答の研究拡張として保留。対象機構・応答・用途を再設定してから再開する。本体の次工程やA4を止めない |
| O16 | 選んだ長期能力の容量・検索・保持の採用条件。旧30分素材と不合格は保存し、研究拡張の採用を止める。全用途への全帰還保持要件にはしない |
| O20・A4 | 本体T1–T6と四接続を監査する。旧全階層研究仕様や全曲記憶の完成を主張しない |
| I4/I8・R2/A3 | 本体が使う記憶の正しさ・損失検査と、実際に動く全処理の資源・鮮度・復旧検査を維持する。既存graphの費用や不具合を移管で免除しない |

実行上の正本は`docs/roadmap/temporal-dcc/milestones.md`、旧I9の結果と未達は
`docs/roadmap/temporal-dcc/i9-whole-context.md`へ保持する。既存Rust、数値fixture、capture、失敗結果は保存した。
今回の変更は文書上の範囲・工程・Manifestoの利用形態に限り、新たな計算機構、認知実証、継続運転能力を追加したものではない。
以下の全T行・五接続・全曲必須の記述とJSON登録は旧全階層研究の条件を保存する。
研究拡張を採用する場合だけその条件を適用し、本体の依存へ戻さない。新スコープの実験では対象・依存・負荷・fitを再登録する。

<a id="i10-sensorimotor-contract"></a>

#### 次工程の機構選定：動作に条件づけた自声予測（2026-09-13）

I9の範囲変更をcommitした後の工程として、I10の身体・帰結入力から検証できる最初の候補を選んだ。
対象は、実行する動作と身体状態から自声の特徴・生じる時刻を予測し、後に届く実音でその対応を更新する機構である。
これは開発上の候補選定であり、著者による音の採用、モデル実装、認知実証の完了を意味しない。
phrase・sectionをさらに詳しく推定する前に、局所生成が必要とする「この身体で、この動作をすると、どの音が出るか」を確かめる。

**一次根拠と抽象化の限界。** 以下は相互作用を選ぶ根拠であり、ソフトウェアの更新式を同定した研究ではない。

| 一次研究 | 確認された現象と、今回の設計への制約 |
|---|---|
| [Schneider et al. (2018)](https://doi.org/10.1038/s41586-018-0520-5) | マウスでは動作と音の対応経験に伴い、予測された周波数への聴覚応答が選択的に抑制され、運動皮質から聴覚皮質の抑制性ニューロンへの作用も変化した。動作信号・音の特徴・学習履歴を区別して扱う根拠とする。人の音楽的判断への直接の証拠ではない |
| [Audette et al. (2022)](https://doi.org/10.1016/j.cub.2022.09.064) | マウスのレバー動作では、学習した動作・音の特徴・動作内の位置に特異的な予測関連応答と逸脱応答が観測された。発音時刻だけの汎用gateと、動作―音対応の予測を比較する根拠とする |
| [SanMiguel et al. (2013)](https://pmc.ncbi.nlm.nih.gov/articles/PMC3725431/) | 人のボタン押し課題では、音のidentityを予測できる条件と、時刻だけを予測できる条件で、音の省略に対する応答が異なった。既知無音での予測違反を比較に含める。モデルの誤差scalarをERPと同一視しない |
| [Tast et al. (2025)](https://doi.org/10.1111/psyp.70094) | 人の課題で、音のidentityの予測可能性はomission N1を変えたが、通常のN1抑制を変えなかった。抑制の大きさだけを順モデルの証拠とせず、動作に伴う非特異的gateを対照に残す |

この根拠から、運動関連入力、予期される聴覚特徴、実際の聴覚入力、対応の更新という相互作用を抽象化する。
特定のニューロン集団とRustの型を一対一対応させない。旋律・phraseの認識や、全曲記憶への一般化もしない。
局所的な時間予測も候補だが、I6の到来headと回路機構の対応は別に検証する必要がある。
今回は既存の身体・自声経路へ直接つながる候補を先に置き、新たな全時間階層の完成を要求しない。

**現行コードとの違い。** `src/life/schedule_renderer.rs` はVoiceごとのbody／habitat波形を保持し、
`src/core/temporal_expectation.rs::OwnSoundHistory::process` は既知の自声PCMを混合音から正確に差し引く。
これは合成器が持つ情報を使う工学的な分離であり、学習した運動予測による聴覚応答の抑制ではない。
`src/life/temporal_participation.rs::resolve` は候補が発音列に採用された結果を受け取り、
`observe_context` はその後の外部音の2窓×3帯域エネルギーを記憶する。これだけでは実際の自声を確認した動作―音対応にならない。
`scripts/temporal_cognition_reference.py::PrivateTimingTrace` は文脈に対するonset／releaseの参加時刻記憶の数値参照である。
その保持則を、そのまま運動―聴覚の学習則として採用しない。

**状態・更新・所有者。** 数値モデルを選ぶ前に、次の意味と順序を固定する。

| 要素 | 契約 |
|---|---|
| 動作と身体 | 発行時の合法な動作、身体・recipeの状態、bus、世代、指令時刻を固定する。候補、採用済み指令、実際の発音を区別する |
| 予測状態 | 有界なパラメータ $\theta$ と未照合の観測窓を持ち、$p_\theta(y\mid a,b)$ として動作 $a$・身体 $b$ から将来窓の自声音響特徴 $y$ を予測する。発生の有無・時刻と、発生した音の特徴の条件付き予測を分ける |
| 感覚入力 | routing／gain後の私有実音を、元sample区間・全入力の支持終端・利用可能時刻つきで照合する。mix／guard前の内部測定と聴取者に届いた音の測定を混同しない |
| 学習順序 | 予測を発行して凍結し、窓の観測完了後に旧予測を採点してから状態を更新する。有限パラメータの誤差更新を第一の数値候補とするが、損失・尺度・学習率は実装前に登録し、シナプス可塑性と同一視しない |
| 未知と失敗 | 実行済み指令に対する既知無音は発生予測の誤差として扱える。取得欠落、未実行候補、観測未完了は無音ではない。未確認の発音を正のtrace creditにしない |
| 境界 | Voiceの私有状態として所有する。共有PとListenerTwinにはVoice ID・動作指令・分離自声を聴覚的な証拠として渡さない。ListenerTwinは受動聴取者であり、この運動入力の受け手ではない |
| 記憶と継続運転 | パラメータと未照合窓の上限を固定し、完了した出来事を曲全体の一覧へ蓄積しない。保持・忘却の時間尺度は課題に従って比較する。世代変更と通常の欠測を区別し、EOFなしで窓を処理する |

最初の技術単位は、固定身体・固定方策の一Voiceからonset指令と実音の帰結を通常経路へ一往復させる診断接続とする。
onsetのみの成立はI10全体の完了ではなく、release、任意身体、私有trace、候補帰結表、両busへの展開を残す。
既存の3帯域profileだけで音のidentityを識別できるとは仮定しない。I10の六記述値との対応、観測窓、特徴尺度、
発生検出と支持規則、モデルの有限容量を次の実装前に登録する。予定時刻を実音検出の答えとして使わない。

**生成への利用。** 将来のI11では、身体が許すonset-now／delayed onsetの候補について、予測した自声の持続・release tailと
既存の外部音予測から同じ時間区間の重なりを評価し、作者の参加傾向を通じて発音時刻へ返す。
同じ外部音でも、長く響く身体と短い身体では今鳴る／待つの選好が変わる、という音響差を検査する。
神経応答の抑制を出力音量の抑制に読み替えず、予測誤差の最小化を音楽全体の目的にしない。
Scenarioの構成は維持し、認知状態は局所的な選択へ作用する。

| 比較 | 区別する説明・失敗条件 |
|---|---|
| 同じ外部入力・身体・候補に異なる学習済み対応を移す | 状態の違いが予測と将来の選択へ作用するか。介入後に生成された別の入力で、介入時の因果比較を代用しない |
| 動作と音の対応を保つ／崩す、音だけの同順再生、動作非依存の音響予測 | 単なる反復適応や外部音予測との違い。identityと時刻の予測を分け、取得欠落とは別に音の省略を与える |
| 動作時の非特異的gate、固定の身体予測、学習状態を凍結した予測 | 抑制だけ、身体既知情報だけ、学習不要の説明を残す。学習が追加の予測・生成作用を支えなければ、新しい学習状態を必須化しない |
| 身体または動作―音対応の変更、未見の身体・routing | 既知の合成器から正解を引くだけの評価や、unsupported個体の除外による成功を避ける。合成器での正確な再renderは工学的参照に分ける |
| reportの有無、欠測・遅延・世代変更、候補だけを増やす対照 | 観測から学習までの因果性、未実行候補の非学習、off音声一致と有界資源を確認する |

選定と契約の記録までを今回の終了点とする。上の比較はまだ実行していない。
実装接続と数値検証はI10、認知的対応は該当R4/R5、可聴性と作者採用はA1/A2で別に判定する。
今回の文献調査は、Conchordalで学習が必要、またはこの候補が人と同じ応答をするという実証ではない。
I9の範囲変更は `6a1a8c0`、この機構選定はその後の文書作業である。Rustと生成音の変更はない。

#### I9の移管前の工学的検査記録

**I9の実装検査で得た区別（2026-09-13）：保存と検索には別の上限がある。**
30分の実音probeでは、256件bankに4,489 episodeを書き込み、4,233件が退避した。
有限履歴を大きいbankへ保持して退避をなくしても、先頭8 knotによる従来の索引は、
同じqueryの全保存・全探索参照が支持する3帰還中1帰還を取り逃がした。
最初の容量拡張では8,192件まで明示指定できるようにし、元のanchorをqueryの観測支持全体から
取る16点で順位付けする。候補境界の同点群はID順で一部だけ選ばず、群全体を未解決へ残し、
脱落数を報告する。保存済みqueryでは3帰還とも初期素材へ到達し、最終DTWは変更していない。
新索引の30分実音replayは両busで完走し、5,841 episode・退避0、同一queryの全探索参照が
支持する3帰還すべてへ到達した。これは合成音による工学的probeであり、登録長尺素材の受入、
作者採用、実時間の運用範囲の確定ではない。対応graphも元のsource/query支持と未確定phraseの
所有へ接続した。対応を2秒で捨てると長いphraseのseal前に初期の照合を失う回帰を修正し、
照合済み支持を延長せずに所有期間中保持する。16辺の枝刈りは依然発生するため、graph容量、
continuation入力、development素材の参照比較とO16全体の容量採用記録は未完了。
独立した終止headのみ・直近gap/energy・経過時間の比較controlは、支持分布の混合後に校正し、
EOF直前の因果的snapshotを保持する経路へ接続した。いずれも未校正の診断比較器である。
証拠は`docs/roadmap/temporal-dcc/i9-whole-context.md`へ集約した。以下の進行状況は移管前の記録であり、現在の再開条件は本節冒頭の採用範囲に従う。

新しいdevelopment演奏では別の上限も確認した。最初の2帰還の固定queryで、参照が支持する
初期素材59件／48件はbankに残っていたが、16候補検索の到達はともに0だった。
同一queryの容量比較では256候補／512候補で初めて到達した。これらは測定条件であり、
runtimeへの採用値ではない。実行中の全replayも8,192件bankを超えた。
別途、graphの同一episode内の変形選択を修正した。低costだが帯域端で曖昧な候補より、
有効な変形候補を優先する。回帰テストの失敗と保存済み候補で欠落を確認したが、
graph容量と後続queryによる上書きの検証は残る。

このdevelopment baselineは両busで完走し、11,888 episode・3,696退避となった。
第3帰還の固定queryでは、参照が支持する初期素材59件が全て退避していた。
現在は16,384 episode・最大1,024候補を明示設定でき、transport、受領cache、sectionのscore buffer、
graphの保存edgeまで同じ候補容量を通す。snapshotの16辺はpreviewに分け、query／ownerの辺bufferは
poolで再利用する。1,024候補の165秒replayでは最初の固定queryで参照支持59件全てに到達した。
入力とsource hashを固定し、16,384件／512候補で両busの全replayを実行中である。
このrun自身の同一query全探索・graph検査を経てからO16の容量を採用する。
現checkpointはRust 997成功、Clippy・全target check通過。I9全体の完了ではない。

続いて、音響特徴の支持とは独立した取得時計を通常observerへ接続した。無音とwarmupも取得済みとして数え、
mask退避後の古いgap prefixには上下限を出す。既存Python時計・独立sample unionと916 prefixを照合した。
これは取得証拠の供給であり、strength／interference lifecycleの接続完了ではない。
graphは後続queryの時刻だけで対応を置換せず、非曖昧・低costを優先し、同品質なら元の観測区間が長い証拠を保持する。
辺容量超過時も同じ順序を使い、未照合のsource末尾まで一致したとは扱わない。
165秒replayでは旧512候補runの1,081 episode captureとcheckpointの1,075 source recordが全て一致した。
最初の帰還から初期素材への対応を持つsourceは修正前後とも92件で、有限容量による個々の辺の入替えは残る。
固定queryの元sourceは確定endpointを持つ前にongoing所有から消えたため、そのnode欠如を辺容量損失とは分類しない。
source追跡と同一query全探索を別に記録した。現checkpointはRust 1,001成功、Clippy・全target check通過。
先行する全長runはこの変更前の版であり、全曲graph容量の採用、stage-1保持モデル、第3の全曲入力は未完了。

保持計算のRust数値準備は、既存Python参照と56回の時系列操作で照合した。上限付きstrength、競合干渉、
欠落による不確実性、generationを区別したrate履歴の失効、availabilityによる退避、固定した事前記憶の認識を含む。
まだtest専用の数値部品であり、通常のstage-1割当、bank／graph退役、developmentのrate校正には未接続。
通常経路では、確定待ちの終端を元観測時刻の順に整列してから封印するよう修正した。
30秒replayの212件の元source証拠は一致したが、34件のepisode識別番号・確定sequenceが変わった。
旧版の識別番号に基づくreplay証拠を新しい版へ流用しない。現checkpointはRust 1,006成功、Clippy・全target check通過。

今回の対象は、既存の和声・協和性ランドスケープを入力として用いる時間構造DCCである。
アーティキュレーションとグルーヴから、拍・小節、phrase、反復・変奏、section、
曲全体の経験までを対象に含める。これまでの作業は短期参加と音響履歴・予測へ偏り、
全階層の神経認知的根拠、内部表現、相互作用を結ぶ仕様には至っていなかった。
今回、`docs/design-notes/dcc-neurocognitive-hierarchy.md`を、表現・更新・接続・検証条件を
持つ設計仕様へ改訂した。音響予測の改善を、時間構造DCC全体の達成とは数えない。
実際のFable第39回レビューは改善指摘ゼロで終了した。対象メモの内容ハッシュと応答は
`docs/design-notes/dcc-hierarchy-review.md`に固定した。完成計画
`docs/roadmap/temporal-dcc-completion.md`は、監査した現行作業ツリーを出発点として、
M0–M9を通じた全時間構造と四つの判定を対象にする。レビュー通過は今回の設計作業の完了であり、
今後の楽器実装や認知的妥当性の確認を意味しない。

**統一するのは時間的な関係を保持し、生成環境へ返す原理である。** 実音の証拠から
順序、重なり、まとまり、再現、変形の候補を形成する。各候補は根拠となる観測区間、
不確かさ、時間的な範囲、持続・更新の条件、期待、他の関係との接続を保持する。
P/M/Aはその全体に適用する。異なる要約を経た同じ音を独立した証拠として重複計上しない。
関係の内部表現は許すが、ScenarioのラベルやVoice IDを知覚した構造として渡さない。

| 対象 | 提案する表現と更新 | 生成への作用と検証対象 |
|---|---|---|
| アーティキュレーション | 連続する音の軌跡と、立ち上がり・持続・release・間のまとまりの候補 | phrase文脈で持続・終了の選択が変わるか。同じonset列で音長・間の関係を変える |
| 拍・小節とグルーヴ | 不確かな反復・相対時刻・拍のまとまり。身体的な参加と、その変動の関係 | 同じ拍を参照しながら異なる位置を保てるか。同期指標と、動きたくなる感覚・選好を別に判定する |
| 反復・変奏 | 順序付きの区間関係と対応候補。保存した関係と今回の変形を分ける | 移動・速度・音色の変更や介在区間を越えた再現を、順序の異なる対照と区別する |
| phrase | 内部の動きを許す継続状態と文脈依存の境界候補。境界、終止、単なる変化を分ける | 局所的な終端音が同じでも先行文脈で期待と発音判断が変わるか。固定長・休止だけの対照と比較する |
| section・全曲文脈 | 区間の記憶と、反復・変奏・対比・未解決の関係。検索と注意の状態を持つグラフを候補とする | 同じ局所音が初出か帰還かで期待が変わり、その差が局所参加へ作用するか |

拍のまとまりとphraseは重なりを許し、複数の流れが異なる境界を持てる。
例えば音長・間の関係はphrase推定へ、phraseの期待は持続・releaseの比較へ戻る。
sectionの記憶は帰還の期待を変え、その期待は同じ音の継続や変奏の解釈へ作用する。
高次の状態が中央で音列を指揮する構造にはしない。局所的な身体と生態上の傾向が、
知覚した関係へ参加する、重なる、待つ、変える、見送るという判断を行う。
その行動選択則と代謝価値への結合は、知覚モデルとは別に比較する設計仮説である。

長い構造には音楽を用いた直接の研究がある。
[Farboodら（2015）](https://doi.org/10.3389/fnins.2015.00157)は構造尺度ごとの並べ替えと
fMRI応答の関係を、[Williamsら（2022）](https://doi.org/10.1162/jocn_a_01815)は
音楽の神経的な事象境界と聴取者の区切りの対応を調べた。
[Hołubowskaら（2026）](https://doi.org/10.1111/ejn.70481)はBach由来の刺激で、
長さが不規則なphraseも追跡される結果を報告した。
これらを長い文脈と非周期的な分節を比較する根拠とする。
全曲グラフというデータ構造、普遍的な時間窓、全音楽に共通する終止則が
神経計測から導かれたとはしない。設計メモでは各研究の観測、採る制約、適用限界を分けた。

Scenarioは作者が作品の方向を与える経路であり、内部がその流れを知覚・記憶する役割と
両立する。指示された帰還と実際に聞こえた帰還は別である。全曲文脈は現在までの経験を
表し、将来の曲長、予定された終端、既定の形式を知覚した事実として使わない。

参照実装として、単一の条件付き状態更新、変形を明示する順序付き区間の対応、
経過時間と干渉を分けた想起、持続時間を扱うphrase・sectionの遷移、独立した終止推定を選んだ。
これらは認知課題に制約された工学的仮説であり、神経機構から一意に導出したとはしない。
局所的な行動は作者が与えた傾向と、分離した文脈上の帰結から選ぶ。認知的尤度を
新しい生存報酬にはしない。メモ9–10節に適合手順、対照、資源制約、全時間構造の比較を記した。
パラメータの同定と認知的妥当性の検証は未達であり、方式の選択を妥当性の証拠と扱わない。

生成側とListener側はhabitatとpresentationを別々に処理し、Voiceは自身が実行した参加も保持する。
共有prototypeによる有界な帰結表を使いながら、個体の身体的な時刻と複数の相対位置を保つ。
未知・古い文脈から圧力を加えない。episodeの保持則は人の帰還再認、私有traceは実際の発音時刻、
grooveと参加意欲はそれぞれの聴取判断に対して同定する。一つの課題を他の確認へ転用しない。
仮説の刈込みでは列挙済みの質量をunknownとして保持し、失った同一性を報告する。
計算上限と遅延予算は、実装時に検証する工学的な制約である。
境界の課題は、同時に聞こえるいずれかの流れで最初に起きた区切りと、その種類を対象にする。
因子化した最初の事象の計算で未解決の質量を保持し、全音群の同時終了や時計のresetを要求しない。
継続・終止等の評定には、別途定義した混合音全体の集約を使う。局所的な重なりとaudibilityは
Voice自身を除いたエネルギー予測だけで評価し、共有された混合音のエネルギーで代用しない。
解析設定の変更は新epochとして認知を明示的に開始し直すが、通常の取得欠落は記憶と欠測を保持する。
これらは数値比較を伴う参照方針であり、確立した認知法則とはしない。
候補previewでは仮想特徴の支持と実際に聞こえた支持を分け、候補と身体の既定動作の差に
発行時の観測支持率を共通に使う。反実仮想のrender・再解析による移転検査を通してからordinal帰結を有効にする。
T2には省略への期待を人に尋ねる独立の課題を設けた。音が省略されても振動子が続くという出力だけでは代替しない。
研究拡張T7で有限作品の完了感を対象に選ぶ場合、回顧判断と採点headを設け、短期の終止、直近の音、経過時間だけの対照と比較する。
その研究の帰還・文脈判定と別に評価し、本体の必要条件にはしない。開いた終わりも妥当な対象であり、EOFを予測の入力にしない。
M5では完全な長尺developmentと容量の十分なオフライン参照を比較し、必要な帰還を保持できる記憶・index容量を
検証前に定める。予見できる容量不足を先に解消し、検証後に起きた失敗を容量の問題として免除しない。

公開Manifestoの日本語版は、delta／theta／alpha／betaの四帯域の実時間抽出を現行機能として記述している。
監査した`src/core/modulation.rs`ではdelta／thetaはmeter由来、alpha／betaはprecision／errorのscalarであり、
四つの神経帯域の抽出ではない。M0で公開文言を整合させ、その結果を本節へ記録する。
神経認知に基づく設計目標は保ち、現行機構の到達点を過大に記述しない。
M0は機器・通常負荷の基準も凍結する。両bus同時動作で処理枠の20%の余裕を要求し、
予算超過時はメモで定めた最適化・有界モデルの改訂・再適合の順に進める。
これらは実装時の判定条件であり、この設計の処理性能を実測した結果ではない。
音群間の受け渡しは、二groupのunionを一つのgesture候補とする有界な表現へ追加した。
音源groupやVoiceの時計は合体させない。さらに大きいunionは、検証前に測る表現容量の制約として残す。
拍のまとまりは種類と、聴取者が選んだ基準拍に対する個数を分けて採点し、
基準拍が半分／倍という違いだけでまとまりの長さまで異なると扱わない。
短期状態とphrase／sectionのunknownへの移行も分け、静けさ・取得欠落からの文脈回復を検査する。
終止・sectionの対照と適合計算の有限な枠を定め、工学的な定数を認知法則と同一視せず比較する。
phrase切替後の時計と古い未解決spanも定義した。overlapは新しいforegroundと以前の継続関係を分け、
reinterpretationは聞こえた開始時刻を継承する。切替から終止を強制しない。
人のデータ収集前にM0で最大負荷と想定負荷の数値的な計算可能性を確認する。
低費用の適合手順でも完全passageを使い、全corpusのreplayと損失変化の差の検査を通してから変更を採用する。
メモと計画は22の必須事項IDを共有し、担当と先へ進めない条件を対応させた。実験・実装成果物の作成は今後の作業である。
T1では、確実に重なるgestureがあれば、別groupが未知でも既知の重なりを保持する。
その存在確率を共同列挙と照合する。緩やかなtempo変化も比較対象とし、
新しい聴取課題の理解・利用可能な回答の基準をpilot前に登録してから実用差を決める。
楽器本体が音を保存しないair-gapと、知覚へのラベル・私有状態の漏洩防止は別の契約として保つ。
参照方針では時間modeを一演奏中固定する。受動観測は新しい時間状態を蓄積しても行動へ圧力を加えず、
局所参加はその状態を利用でき、offは新しい関係状態を作らない。新しい演奏は空のbankから始め、
assayの状態介入と実行中のmode切替を区別する。
stage-1収集前には、予定した実験規模の模擬データで時間・干渉・飽和を分離して回収できるかも検査する。
事前の許容誤差を通しても人の記憶の妥当性は確認されない。各感度比較には指標と採否・限界の規則を先に置く。
境界の回顧採点の窓末重みと、発行済み予測の重みを分け、途中の履歴喪失をhazardの事象率と別に計上する。
事象headの予測安定性も収集前に模擬検査する。O04にはtuple構築、grouping更新、個体の判断とhop CPUの余裕も含める。
初期phraseはgroup-localであり、音群をまたぐ継続をT5の専用課題と表現漏れ率で測る。
必要なhandoffを表せなければ、T5完了前にM4へ戻して改訂する。公開文言は四帯域抽出、各帯域の機能、
agentが各周期へ同調するという時間軸節の主張を文ごとに照合し、四番号条項で言語差を確認する。
再認と対応づけはreplay・接触の規則を固定し、聴取者を分ける。再認のquery自身は採点用の事前bankを補強しない。
資源改訂には個体判断・matcher・gesture view・pair-gridの縮小順と再適合を追加し、epoch・証拠・mode隔離の担当も固定した。
gesture familyの事前重みには独自の音響支持と時間窓を定め、bodyと実音groupは共通六記述値で対応づける。
grooveと参加意欲は同じ109座標を使っても、別々に適合し判断する。入力の共通化を二つの経験の同一視へ結び付けない。
sectionの対応・遷移30成分にも分類・重み・隣接・支持の式を定めた。gap handoffはrelease後の低energyへの移行を観測して扱い、
必要な長いgapの表現漏れはgesture／phraseの再設計へ戻す。T2は2–16と16超のまとまりを回答でき、周期範囲に足りる時刻尺を使う。
Manifestoの監査は時間軸節の外にも広げ、上位の二軸landscape構築という時間能力の主張も照合する。
bundle形成とhop間の持続にもmember／parentの決定則を置き、工程IDはメモと計画で双方向に対応させる。
実装上の選択を固定しても、得られるgroupingが聴取者の判断と合う証拠にはならない。
単一音列の微細な時刻関係も必須とし、他音群との対に加えて内部周期残差の独立した履歴を置く。
同じ間隔分布のswing／random jitterをT3で比較し、histogramと順序の寄与を区別する。追加状態の容量も計上する。
両busのmono規約とenergy・downmix・提示channelを固定し、解析信号と聴取刺激の対応を検査する。
pilot前の倫理審査状況、同意・謝礼と個別回答の管理はM0の収集開始条件として記録する。
音響所属の参照もmember単位の重み、残余質量、参照更新順を固定し、状態・処理量とenergy保存の検査を明記した。
ridgeのsecant、log-RMSのPearson相関、accent容量退避の順序、section検索cueの選択窓も定めた。
周期の利用先は共通のpeak・間隔選択則を使い、隣接した高いbinだけで候補を埋めない。
episode干渉は全bankの粗い比較cacheを使い、未知量と確定時の処理費用を明示する。
phrase headの入力一覧は模擬検査前に凍結し、T2の予測入力から対象区間のraw supportを完全に外す。
確定spanの終端記述は元の観測支持を基準とし、span開始で窓を切る。短い区間へ前区間の音を混ぜる方式は明示した対照として比較する。
整数groupは13を含む2–16全体へ広げ、探索上限を更新した。私有参加fitの鮮度は現在の音響対応と照合支持で測り、
Voiceが長く発音していない状態を、参照文脈が古い状態と混同しない。
到来モデルの位相は円周表現とし、sectionの重なりは解決済みgroupの割当上の共存へ限定する。
継続推定のphrase生存入力は適合時の未校正hazardを保ち、pilotの準備基準と本検証の個数回答coverage基準を区別する。
検索なし・最近だけ・経過時間だけ・順序を捨てたbagという記憶対照にも、操作、失う情報、再適合と費用を定めた。
articulation率の対照入力も固定した。新しい時間modeの出荷時の既定はoffとし、M8で名前を登録する際に既存の採用挙動を変えない。
integer境界の探索は予測時刻の両側を使い、split持続の条件をbirth支持から分けた。sectionの周期支持率には、不均等cyclic word全体の反復も含める。
episodeの観測点には10音響座標・2hop周期・欠落／部分区間の規則と、再構成誤差による圧縮・処理費用を定めた。
元時刻を使う照合でも残る密度の不一致は比較対象とする。動作7種の合法時刻と、DTWの挿入／削除費用の単位・適合・感度比較・凍結も明記した。
phrase hazardと出口分類は同じ26座標を別々に適合する。音群間位相はaccent追加前の周期で採点し、
対cacheのf32寄与とf64和を区別する。五段階headは累積ロジット比例オッズ型へ統一し、継続対照の未校正生存入力とordinal出力校正を文言でも分けた。
任意のlive bodyも自身の実際の発音から共通記述値を得る。私有解析の費用を別途計上し、新生・変異個体を実時間受入の範囲へ含める。
必須の追加対照には担当・統計量・clusterを考慮した区間・閾値・失敗時の戻り先を登録する。
現行meterと新しい候補を2／3／4へ制限した対照は別IDとし、grouping種類・個数への写像を固定する。
Manifestoの文単位監査には創発節の個体の同調を名指しし、他のagent行動の時間能力文も走査する。
いずれも設計上の必須事項であり、認知的妥当性や実時間動作の新しい実証ではない。
新しい参加traceの採用は局所参加modeの2x3記憶寄与だけを置換し、off／受動では採用済みの旧生成挙動を保つ。
出荷時の既定変更には別の作者採用と、既定mode／照合基準の改版を要する。
phrase二headの64入力上限もstage 3の係数上限と区別し、別途登録した大きいheadへ適用しない。

反復・変奏・介在する対比・帰還を含む比較から、既存の観測を使った受動モデルを全階層へ通し、
支持された関係を局所行動へ接続する。波形予測の全面的な解決を上位層の前提にしない。
参照方式が比較に失敗した場合も、対応する時間構造を完成対象から外さない。

本改訂は設計文書のみであり、新しい生成機構、音源、試聴結果を追加していない。
実装上の正しさ、認知課題との対応、意図した関係の聞き分け、作者の音楽的選好を
別々に判定する。現行の短期参加と履歴を、全階層の完成として扱わない。

#### M0の公開文言照合（2026-09-10）

基準commit `ec67b50c303d930a3d4c810333e3742f111c6ac8`と実ソースを照合し、
日英Manifestoの時間能力の記述を改訂した。`src/core/modulation.rs`の
`NeuralRhythms::from_meter_state`は、beat／subdivisionをdelta／thetaへ写し、
confidenceとその補数をalpha／betaへ格納する。四つの神経帯域を抽出する処理ではない。
`TemporalParticipation`と`ParticipationClock`は固有周期・音長・短い音響文脈による
発音・待機・見送りを実装するが、新メモの関係記憶や全曲認知を実装してはいない。

メモ3節の四番号を保ち、公開本文に採用した日本語と基準の英語を次のとおり固定する。
英語Manifestoも同じ四内容を、前後の説明につながる文章で表す。

| 番号 | 採用した日本語の全文 | 基準の英語 |
|---|---|---|
| 1 | 2026年9月時点の実装では、拍と細分拍の推定からデルタ・シータと名づけた周期信号を作り、アルファ・ベータは推定の確かさと予測誤差の指標を表す数値として扱っている。 | The current implementation derives delta/theta bands from meter estimates and represents alpha/beta as precision/error scalars. |
| 2 | 音響信号や脳信号から四つの神経帯域を抽出しているわけではない。 | It does not extract four neural bands from audio or brain signals. |
| 3 | Conchordalは、これらを支える神経認知的な時間構造とその相互作用を内部で表現することを設計目標とする。 | Representing interacting neurocognitive time structures is a design goal. |
| 4 | 以下の四帯域への機能配分は初期の案であり、再検討の対象である。 | The four-band allocation remains a revisable proposal. |

抽出の一文だけでなく、日英18組の段落を文ごとに照合した。
`docs/roadmap/temporal-dcc/manifesto-audit.json`に、旧文・新文・判定理由・行位置と
日英Manifestoおよび照合ソース15ファイルのSHA-256を記録する。
次表のIDは同記録の`O02-`に続く識別子である。

| 箇所／ID | 照合結果 |
|---|---|
| `intro`、`dcc_mapping` | 自律的な音の個体とDCCの原理を保持。生体信号の閉ループは従来から将来目標。モデルから音への既存の写像を全時間階層の完成とは解釈しない |
| `landscape` | 二軸を実時間で構築済みという文を、周波数地形・短期履歴・meterの現状と、時間方向の設計目標へ分けた |
| `temporal_mechanism`、`current_mechanism`、`allocation` | 四帯域抽出と帯域間同調によるリズム生成を実装済みとする文を改訂。現在の数値の由来と、関係の保持・期待・生成への還流という目標を明示した |
| `delta`、`theta`、`alpha`、`beta` | 大局的phrase、articulation、phrase内accent、groove／microtimingの各機能配分を、再検討可能な初期案へ改訂した |
| `long_context` | 順序記憶・想起・境界・終止を今後の表現と照合の対象として記した。全尺度を低速振動子に還元しない |
| `individual`、`population`、`emergence` | 個体の神経リズム同調を既存の局所参加判断へ置換。周波数ニッチと将来の時間的役割を分け、持続する受け渡し・phrase・全曲の創発を目標として記した |
| `scenario_scope`、`scenario_controls` | 作者による大きな時間構成を保持。既存の周期・音長・同期設定へ操作の記述を改め、知覚された長い文脈との共存は将来目標とした |
| `closing`、`name` | 自律個体・一回的な演奏という宣言と珊瑚の比喩を保持。全階層の時間認知を実装済みとする根拠にはしない |

これは公開用ソースの整合であり、サイトへのdeployや新しい認知機構の実証ではない。
この最初の照合時点の設計メモのSHA-256は
`8b88ebcc987e6a9b4a8923f68748469c544387b2ba38c1e1d47f094122d681d9`である。
同メモが指摘した旧公開文言は基準commitに対応し、本照合記録がその修正を引き受ける。
その後の数値検査に基づく仕様改訂は下段に、M0全体の進捗と未達条件は`docs/roadmap/temporal-dcc/m0.md`へ記録する。

#### M0のepisode回収検査で判明した割当不足

最初の条件付き回収設計`episode-allocation-1`を、実行前に
`docs/roadmap/temporal-dcc/episode-recovery-design-v1.json`へ登録した。
遅延、観測済みの介在素材の重み、接触回数、所属の支持、cueの質を720条件へ交差させ、
各条件4回答、計2,880回答とした。候補割当は960人に各3課題である。
人の回答や実波形はまだ取得していない。音響scoreは模擬用の工学的な固定値である。
回答欠測なし、独立な10%欠測、弱いcueに競合episodeと曖昧さに応じた欠測を加えた三条件を調べた。
この検査では、収録欠落や実際に適合した音響尺度をまだ扱っていない。

108通りのパラメータ真値それぞれについて各条件100組、計32,400組を適合した。
真値と条件を合わせた324点のうち234点が登録した個別誤差基準を満たし、90点が満たさなかった。
保持時定数2秒の81点はすべて不合格であり、残る9点は20秒の条件である。
失敗した3,272 fitは無限大の誤差として判定に残した。閾値や真値の点を除外していない。
全結果、実行したsource、seed、割当、同時誤差図は
`docs/roadmap/temporal-dcc/parameter-recovery.json`から参照できる。
この候補割当はO11を通過せず、人の収集を開始する根拠にならない。

これは人の記憶の限界を示す結果ではなく、課題設計の問題を示す。
最短の登録遅延は8秒queryの開始まで4秒であり、最初の採点時点でも強化から12秒が経過している。
この時点では主要な検索episodeの`tau=2, bias=0`と`tau=3, bias=2`の時間・bias項が一致するため、
それより遅い条件から両者を分離する必要がある。
`kappa=4, strength_max=3`の場合、全割当における回答の期待条件付き対数尤度比は、
明瞭なcueで0.310 nats、弱いcue条件で0.038 natsにとどまった。
この数値は実行後の原因診断であり、新しい合否閾値ではない。
十分な期待回答数を与えた独立検査では短い保持時間も回収できたが、
最適化の正しさだけでは現在の割当の不足を解消できない。
M0で観測・課題配置を改訂するか、採点モデルの明示的な改版を登録し、
短い保持時間の点を残したまま全回収検査をやり直す必要がある。
この最初の割当の検査時点では、受入済みメモや演奏経路は変更していない。

#### M0の補助queryによる回収と私有traceの残る定義

改訂した割当は8秒の主課題を全て残し、1／2／4秒のqueryを追加した。
短い遅延では、介在する観測区間の登録がquery開始に間に合う範囲へ量を制限した。
新しい回答を模擬生成する前に、登録した期待Fisher情報の探索で各条件4／8／16／32回答を比較した。
計画上の精度を満たさない条件点は順に6／2／0／0点であり、最小の16回答を選択した。
この計画用の基準を、変更していない回収の合否基準と置き換えていない。
2,640条件で42,240回答、候補3,840人が一人11の異なるsource familyを聞く割当になる。
音声だけで一人平均32.52分、最大44.54分を要し、説明・回答・休憩は含まない。
相応の負担を伴う設計であり、募集の承認ではない。

その後の`episode-allocation-2`は、32,400組の模擬適合で324の真値・条件点を全て通過した。
全真値、全fit誤差、割当、実行したsource、探索結果、図は`parameter-recovery.json`から参照できる。
実行は約62.14秒、模擬処理と親processは約488.64 CPU秒だった。CPU値はprocess起動費を含まない。
この結果を音声処理の実時間性能や、人の記憶の確認と扱わない。
cueは工学的な仮値であり、個人差のない模擬モデルと、収録欠落のない条件に結果が限られる。

設計メモと完成計画には、主課題と保持式を残したまま補助queryを反映した。
`recognition-instrument.json`へ長さ別の質問、replay禁止、bank copyの手順を固定し、
`control-comparisons.json`へ8秒だけで適合した対照との主課題への移転検査を登録した。
人の分割・検出力・最終的な実用差はまだ固定していない。
実際の短いqueryが音楽のまとまりとして対応づけられること、長さ別pilot、
実際の音響尺度の適合後の回収再検査も必要である。
従来のFable round 39の所見は記録された以前のメモに対応し、この数値検査後の改訂には拡張しない。
演奏経路の変更はない。

私有traceでは別に、実行済みのfractional credit、欠測・失効した参照の重み、
二つの一様な時刻分布の差の厳密なbin積分、複数の固定anchor、overflow、条件付き正規化を検査した。
全binに共通する追加の待ち時間・干渉は正規化で相殺されるため、待ち時間だけのprobeから
私有のtau／kappaを同定することはできない。
回収前に、反復の蓄積と飽和、fractionalな割当での補強と干渉の順序、
各binの観測時刻の支持をstatefulな参照として明示する必要がある。
各一回の寄与を独立な強度として保持する解釈では、一回のcreditが1以下なので1を超える飽和上限に届かず、
strength capを同定できない。残る定義は`private-trace-reference.json`へ記録した。
その後のstateful参照では、binごとの**保持された質量**に上限を置く規則を明示した。
古い質量を減衰させ、観測時刻のcreditを積分して加え、binを飽和させてから、
競合参照への割当による干渉を発音・releaseの両headへ適用する。
既知の一様な発音／anchor区間上で指数減衰の時刻モーメントを積分し、平均時刻や配送時刻では代用しない。
これは私有filterの明示的な改訂であり、episodeの強度更新式は変更しない。
独立したscalar漸化式と時刻の数値積分、headの分離、欠落支持、容量による退去、
長い待ち時間でlog質量を空のtraceと誤認しないことを検査した。

別の厳密な観測検査では、適合方法の欠陥が判明した。二つのphase binの真値を(0.8,0.2)とし、
周期の1/8だけずらした四つの1/4周期観測区間を置くと、観測区間の確率は(0.4,0.25,0.1,0.25)になる。
ところが区間内を一様としたsoft-bin配分の平均は(0.65,0.35)である。
従来のsoft-bin交差エントロピーは、無限のデータがあっても偏った分布を選ぶ。
各観測区間上でphase密度を積分した尤度なら、母集団での最適値は真値に一致する。
同じ偏りを32 bin、48 kHzで512 samplesの観測区間でも確認した。
周期は一秒で、末尾の既知の部分区間を含む。これらの診断は、32／33 binの全真値gridの回収を置き換えるものではない。
複数参照、anchorの候補、非周期のoverflow、欠測を含む一つの物理的発音の観測則を完成させてから回収する必要がある。
参照ごとに独立な発音を生成すれば、存在しない観測を増やしてしまう。
この時点では参照の32検査が通過したが、私有パラメータの回収や既存templateの置換は未達だった。
次の改訂で、この共同観測則を明示した。

各probeは発行時に、有限の物理的な機会窓、対象head、検出区間を固定する。
各参照の32／33 binを物理時刻へ写し、固定したanchor候補を保って混合する。
非周期overflowには、窓内で実際に取り得る範囲外の領域をまとめて使う。
失った参照・anchorの質量や、窓内に対応する時刻がないbinの質量は、試験用の一様な基準分布へ残す。
これに記憶や行動への作用を与えない。発行時の重みで参照の分布を混合し、一つの物理的な結果を生成する。
後から得た観測creditで予測を書き換えたり、参照ごとに独立な結果を増やしたりしない。

適合では、混合密度を既知の検出区間上で積分した尤度を使い、観測欠落も一つの結果に含める。
従来のfractionalなsoft-bin損失は、別の診断指標として残す。
anchorが曖昧な場合の区間長比を解析的に積分し、独立した適応積分と既存の単一周期参照へ照合した。
回収用に簡約したforwardと解析勾配も、両headの逐次更新へ一致した。
fractional credit、接触の欠測、複数モード、overflow、競合参照を含む検査である。

最初の私有回収割当は、三つの条件群それぞれで140種類の課題に各1,024回の独立probeを置いた。
tau／kappa／capの全36真値に各100組を生成し、108条件点のうち107点が合格した。
失敗fit 73組は無限大誤差として残した。未合格は、曖昧なanchorと欠測の条件で
tau=2秒、kappa=1、cap=3とした点であり、11 fitの失敗で90百分位が有限にならない。
全fitは保存できたが、NumPyの真偽値をJSONへ変換する不具合で最終結果と費用の保存が失敗した。
適合を再実行せずに集計を復元した。正確な最終CPU／wall時間は失われており、
最後に取得した進捗行は99.4秒を示す。この実行の費用判定を完全な合格とは扱わない。

全prefixを毎回再演すると、最初の割当は条件群ごとに約45,883時間を要するため、実データ取得には採用しない。
事前登録したFisher情報による最適化では、140種類すべてと各最低2 probeを保ち、
全真値・条件の制約を満たす割当を探した。条件群ごとに9,086 probeを選び、同じ再演時間の下限を約554時間へ減らした。
logパラメータの期待標準誤差はすべて0.2以下である。
飽和上限の非滑らかさに対処する収束補助手順も別途登録し、forward則と回収誤差の許容値は維持した。
v2の再回収は106／108点が合格した。局所的なFisher情報では、毎回の加算が上限に達し、
正規化によって上限が消える別の領域を区別できなかった。v3はその領域との識別用probeを追加し、
fitした情報行列のrankも検査した。105／108点が合格したが、蓄積が上限に届かない別の平坦な領域が露呈した。
v4は全probe数を2倍にして106／108点となり、rank欠落40組を含む43の失敗fitを残した。
失敗はすべて分母に含めている。

v5では、高い損失の収束解があると、より低い損失の非滑らかな解を精密化しないsolverの問題を修正した。
解決できない下位損失の試行やrankの欠けた最良fitは失敗とする。
割当は条件群あたり50,932 probeのまま、独立した乱数で再回収し、再び106／108点となった。
残る未合格は、曖昧・欠測条件のtau=2秒、cap=6、kappa=4／16である。
solverの修正だけでは実験の情報不足を解消できない。
登録したv6の割当探索では、既存の反復後の4課題へprobeを集中し、上限へ達しない対照のtau／kappaを適合した。
五つの候補数は、いずれも三つのstress真値すべてで事前の距離基準を満たさなかった。
割当は選ばず、新たな回収も実行していない。有限個の初期値による探索は計画用の近似であり、
大域的な最小値の保証ではない。次の改訂では情報を与える接触と観測の時間配置を見直す。
局所的なFisher情報の標準誤差だけを満たしても十分ではない。

各回収版の登録・source・全fit・図・失敗数を保存した。
実際の収録区間に揃えた時刻、身体・機会窓、波形から得た参照、採用できる再演方法は未検証である。
独立したO03の作業では、section記述子の39座標の順序、支持重み付きの関係／遷移集計、
未解決の質量、四span ring、周期提案の和集合、解決済みgroupだけの重なりを登録した。
この時点では82座標headの全入力登録とO12のmixture-first-event模擬検査も未完了だった。
記述子の登録をsectionモデルの適合完了とは扱わない。

v7は接触の時間配置を変更した。従来のA反復と独立した遅延・干渉の後、Bの発音を1／4／16回行ってから
一つの物理的なprobeを観測する。両headとその対になる実行は元のcredit・観測則に従い、
欠測したBも含めて各回の間で保持量が減衰する。B=1では元のA／遅延／干渉条件をすべて保つ。
拡張した420課題は逐次filterと勾配の検査に通過した。logパラメータの期待標準誤差0.12を基準として
事前登録した割当は条件群ごとに14,382 probeを選び、全prefix独立再演の下限は約1,034時間となった。
全10,800組を回収した結果は104／108点であり、rank欠落133組を含む152の失敗fitを残した。
曖昧な条件の短時定数・高上限の三点は依然未合格で、部分観測条件の一点も上限誤差の基準をわずかに超えた。
許容誤差は変更していない。

真値付近から始める診断では、識別できる局所解も見つかったが、保存したデータでは上限へ到達しない解の方が
損失が低くなる例がある。損失の高い解を選んで回収成功と扱うことはできない。
tau=2秒の曖昧なprefixでは、二つのmodeへ分けるcredit 0.6、五回に一回の欠測、62.5 msの間隔により、
上限を設けない場合でも一binの保持量は周期内で最大約7.8995にしか達しない。
cap=6というstress条件はこの蓄積限界に近い。次は時間配置に加え、同定に情報を与える接触支持を検討する。
Bの回数や局所Fisher情報の精度だけでは大域的な識別を証明できない。
この診断は生物学的な記憶定数の証拠や、実際の再演計画の採用を意味しない。

section headは82共変量と別の切片を登録し、hazardは83係数と明記した。
三分類のexitは、切片を含む各係数の和をゼロとする制約で166自由係数となる。
recent-onlyは41共変量と切片、elapsed-onlyは切片込み二入力である。
recent-minus-cumulativeはraw記述子から差分を作った後でdevelopmentの尺度により標準化する。
未知支持率は、候補sectionの区間内で観測できた物理支持区間の和集合から計算する。
既知の無活動は観測済みであり、区間の重複を二重に数えず、所属重みを欠測秒へ変換しない。
差分は両側の記述子が有効なときだけ有効とし、追加の学習mask座標は設けない。
予測では経過時間だけを進めて文脈を固定し、検索の支持音声が0.5秒以上古ければ検索二座標をmaskする。
四つの新しい入力組立て検査は、これらの規約、未来入力の拒否、支持区間の分割を確認した。
原音からの記述子抽出、系譜とcueの選択、尺度の適合、O12の全event-head模擬検査は残る。
改訂前後の仕様を保存し、私有traceとepisodeの節は同一と照合した。このsectionの明確化には新しい外部レビューはない。

v8は従来の420課題を残し、最初の接触の観測率とanchor支持を1とする72課題を追加した。
複数mode、周期的な欠測、弱い後続接触、曖昧なprobeは維持した。Fisher情報による局所精度は基準に達したが、
追加課題を16倍にしても上限へ到達しない別モデルとの識別基準を満たさず、v8の回収は実行していない。
v9はv8の全割当を維持し、別モデルとの距離の制約を追加しながら再演費用を最小化した。
三回のprofile検査で、条件群ごとに23,794 probe、対象四点すべてで従来の距離基準8以上を得た。
増分は追加した課題群の二課題へ集中した。この有限の多初期値探索は大域的な識別の証明ではない。
別に行ったv9の独立回収は10,800組を完走し、全108条件点が合格した。rank欠落2組と解決しない下位損失2組の
計4失敗fitを無限大誤差として残し、除外していない。全条件点・パラメータの最大中央値と90百分位の
絶対log誤差は約0.09599／0.26284で、事前の基準を満たした。実行は約534.41秒／4,198.61 CPU秒であり、
CPU値はworker起動費を含まない。全fit、source、図、条件ごとの再集計を照合して保存した。
全prefix独立再演は条件群ごとに約1,844時間を要し、
実際の取得計画としては採用していない。支持が十分な接触を追加した設計であり、弱い接触だけでの回収成功にはならない。

O12には、有限hopで最初の境界を集約する数値参照を追加した。全終端履歴の独立列挙と照合し、
文脈／pathの周辺化、出口より先に適用する全群の履歴欠落、安定IDによる同時出口の帰属、初期未知、
解決した最初の出口の保持、無条件の出口・生存・未解決の質量保存を確認した。
発行時予測と聴取後の採点には、別々の固定snapshotの種類と時刻を記録する。このinterfaceだけでは本番の保持を保証しない。
観測済みの元のhopごとに、経過時間依存hazardを二点Gauss–Legendreで積分する参照も追加した。
校正倍率は局所積分へ作用させ、後から群間集約する。事前登録した135条件で独立の適応積分とstep doublingに照合し、
絶対1e-9＋相対1e-7の許容差を満たした。九検査の追加で参照全51検査となった。
本番の履歴／registerの寿命、全生成vector、stage-2の適合／OOF／校正と各条件100組の予測安定性検査は残る。
ここで確かめたのは数値規約であり、聴覚的な分節や神経的なrateの同定ではない。

section入力には、先行段階のcacheを使う六つの参照関数と21検査を追加した。
終端の六観測記述子は元のassigned acoustic hopから計算し、epoch、終端generation、span開始と元の支持終端で切る。
対応の不確実性はpathごとに保持する。別々に周辺化した分類確率を掛けると、record間に未登録の独立性を導入してしまう。
対応・遷移30座標は未解決の隣接span、fractional support、除外したpair重みを残す。
活動九座標はadmitされた非均等word、全ての有効なoutgoing／within-group履歴、解決済みgroupだけの重なりを使う。
cue選択では実際に渡されたgeneration handleを使い、選んだ全prefixを既存queryへ返す。
公開時刻やavailabilityで古い音響対応を新しく見せない。共通六観測記述子の順序と尺度規約をO03へ登録し、
連続spanのfixtureでは39／82座標への接続を確認した。
これらは既存の音響・detector・matcher・時刻履歴cacheを入力とする全件参照であり、その上流推定器の実装ではない。
この段階では一般的な重複span／ringの統合、実際の系譜所有、640-byte record、有限fitと全条件の予測安定性は未完了だった。
部品の数値検査を、聴取されたsection、認知パラメータの同定や実時間性能の証拠へ拡張しない。

欠測区間には、最後に支持された文脈を固定し、同じ二点求積の区間数を倍増する生存積分を追加した。
端点評価も最大64評価へ含める。積分値が続けて小さくても、区間冒頭の鋭いhazardを見落とす反例がある。
そのため、端点sigmoid範囲上のBernstein係数で四階導関数を抑え、丸めの余裕を含む求積誤差上界も確認する。
絶対1e-9＋相対1e-7の許容差と失敗時の処理をfeature manifestへ固定した。
600条件の数値監査では416条件が独立の高精度適応積分に対する許容差内でpriorを返し、
184条件は保守的な上界を評価上限内で満たせず、未解決のまま残った。受理した値の最大誤差は許容差の約0.05756倍であり、
未解決を含む全条件を保存した。欠測中の退出は未観測の複数遷移を含むunknown-current-stateへ渡し、
観測した境界やinactiveの証拠を作らない。積分不能なら生存そのものを未解決とする。
解析例、校正、分割、区間冒頭の見落とし、後続の観測窓へのunknown保持など七検査を追加し、参照全58検査が通過した。
既存関数はASTで一致し、回収モデルは変更していない。184未解決条件は予測成功ではなく、
実際の適合係数による支持率、本番の系譜／epoch接続、O04の計算費用とO12全体の予測安定性は残る。
記録は`docs/roadmap/temporal-dcc/event-head-reference.json`に保存した。

続いてsection参照へ、重なるspan間で元のhopの部分区間とadmitされたaccentの帰属を保存する処理を追加した。
確定したspanのscalar membershipは分子・分母・有効時間へ一度だけ掛け、ringはその十分統計を集約する。
累積の活動統計と未観測割合は、新しいgroupの物理観測を一度だけ数える。欠測clock記録を分母から落とさず、
部分区間の計算にも観測cut以降のraw hopを借りない。追加12検査でsection参照は33検査となった。
重複spanの39／82入力、2／4／8区間の各20 prefixの全件対応参照とoccupancyの手計算、
再読、section開始、親を変えないgeneration継承を確認した。

保存recordは54個のf64累積値、六つのf64終端記述子、32 bytesの空きと128 bytesのbookkeepingを持ち、
従来の640-byte上限に収まる。累積1件とring4件で局所pathあたり3,200 bytesとなる。
二つのf32に分ける試作では90%の隣接validity判定が反転した。再現scriptを保存し、f64保存ではこの例の全件参照と一致した。
低精度化ではscalar誤差とvalidityの両方を再検査する。ただし、これはpayloadの測定と確定cache入力の検査である。
この段階では暫定帰属の生成と確定、遅延accentの配送、本番のtopology／系譜、追加状態とscratchの費用は未実装または未計測だった。
その設計メモ改訂は9.5節に限定し、以前の回収結果は変更せず、新しい外部承認へ拡張しない。
O12全体の適合、実時間性能と認知的な判定は引き続き必要であり、記録は
`docs/roadmap/temporal-dcc/section-feature-reference.json`に保存した。

続くaccent参照はgroupへのenergy保存、四つのraw hopに基づく三つのsalienceの比較と、
middle hopの不確実なevent区間・全raw支持の終端・availabilityの分離を実装した。
追加20検査はfractionalなenergy帰属、全四hopの取得と成分mask、観測sample区間の和集合、
スペクトル変化だけのaccent、plateauと厳密な閾値、未来情報の混入反例を扱う。
以前のcache入力では、event時刻が過去なら未到着の右側比較hopを使えた。sectionの三抽出経路は
全支持のcutを要求するように修正し、右側の音だけを変えても元の終端記述子・計数が変わらないと確認した。

順序付きadmissionのledgerは最大128件／32秒を保持し、時刻・ID順の追い出しと窓ごとの容量maskを維持する。
bankとは別の累積値とsectionへの配送は、eventが現在の音響deltaより前でも一度だけ加算する。
再読は加算せず、保存したsnapshotは変えない。明示的なgeneration継承では累積値を保ち、
既存のbookkeeping slot 15に置いた世代内の配送sequenceだけを初期化する。
scalarなinventory時刻はmiddle hop終端とし、時間分布の積分にはuniformなhop区間を残す。
この規約と未実行の包含時刻対照は`docs/roadmap/temporal-dcc/accent-reference.json`へ登録した。
新規20検査・section33検査・一般参照58検査の計111数値検査となった。
暫定spanの帰属と確定、任意のpacket順序回復、実波形のNSGT／group推定、実際の系譜、
O12の適合とO04の性能計測は未完了であり、この改訂に新たな外部承認や聴取の証拠はない。

次の有限入力参照では、暫定occurrenceを確定するledgerを実装した。元のoccurrence／支持ID、
終端記述子と音響payloadを保持し、jointな経路重みと元の観測支持を0.5秒の期限で一度だけ記録する。
処理が遅れた時刻は別に残す。確定writeとepisode／contextの上限なしの所属累積値は、検索・再読・
後からの解釈で書き換えない。元の確定値に対するjoint支持cellまたは未帰属支持の変化が0.25を超えると、
その元occurrenceを一度だけflagする。猶予中の欠測は不確実性として残す。遅延accentは暫定の活動統計へ
配分できるが、元の終端記述子は変えない。確定後の到着は支持損失として記録する。

追加21検査はjoint支持、欠測・未知質量、期限と閾値の感度、元の観測時刻での保持、metadataだけの改訂、
文脈の追い出し、同じcueのcoarse参照と干渉幅を扱う。100有限streamの各12 prefixについて、
episode／context支持を独立の全件和と照合した。重なるspanへの保存されたaccent帰属は、観測支持を
二重に掛けず既存の39／82入力へ接続した。隣接coverageも元の支持の和集合から計算する。
数値検査は計132件となり、`docs/roadmap/temporal-dcc/occurrence-reference.json`へ記録した。
ただしこのoracleは入力された活動payloadと経路に条件づけ、有限入力の監査履歴を全件保持する。
pending件数ガードだけでは本番の128-byte終端記録配置を満たさない。全beamの活動履歴生成、
globalなmemory clock／bankの管理、本番の資源上限、O12適合と人の判定は未完了である。
以前の回収式・結果と、外部レビューが対応する版の境界は維持した。

続くraw記述子参照は、割当済みhopからの10観測値、generation基準の1／2／4hop blockと有界圧縮を実装した。
29検査は原支持とavailabilityのcut、部分取得sampleの和集合、span支持のclip、欠測値、確定後の更新拒否と世代隔離を扱う。
十組のf64観測秒／平均／二乗誤差と時刻・由来を実際の320-byte payloadへ保存する。
128-knot bankと挿入・部分・gapのscratchは活動spanあたり41,920 bytes、両bus各1,024経路なら85,852,160 bytesとなる。
Python object、raw cache、出力、beam状態と照合／edge等の費用は別であり、O04全体のメモリ・性能合格ではない。

六つの割当済みhop入力、64／128／256 knot、1／2／4hop cadenceの全54条件で、累積誤差を未圧縮f64直接計算と照合した。
最大丸め差は8.53e-14。独立のmoment検査も100本の有限入力を扱う。既定容量・cadenceでは43.69秒の持続の復元誤差は0、
急変の積分誤差は約21.676、最大点残差は約1.307標準偏差となった。取得欠落例では欠測量とgap flagを保ったが、
一つの併合blockで正確なgap位置を失った。これは圧縮の近似損失であり、聴覚的な忘却や認知時定数の測定ではない。
数値検査は計161件。`docs/roadmap/temporal-dcc/descriptor-reference.json`へ記録し、設計メモ本文は変更していない。
実波形／group推定、元時刻に基づく照合、保持中のqueryとepisodeへの接続、実尺度fit、T4／T6とM0全体の判定は残る。

続く参照では、保持中のprefixを複製したquery、coarse anchor選択、元時刻のsubsequence照合と確定episodeを接続した。
32検査はliveなcadenceを変えないこと、末尾の取得欠落で支持音声時刻を更新しないこと、
元のoccurrence年齢と遅い配送availabilityを分けることを扱う。50小入力では全経路の独立列挙と小数点以下13桁で一致した。
欠測pairは残差支持を加えず、未対応のcue／referenceには明示した挿入／削除costを使う。引用の外側の素材は削除に数えない。
音高運動とintervalの残差は別の診断値として残し、未登録の適合costへ加えない。

上位16候補の外にも、世代を保持したcoarse costを残す。明示的なhandle対応を介して既存の確定ledgerとsection分類へ渡し、
遅い完了結果を過去の確定へ使わず、候補打切りの同点は未解決にする。15検索比較ではanchor間隔2／4が
途中引用・移調・速度変更・欠測の四例を正しく検索した。間隔8では移調した引用を検索できず、残る三例の移調推定も
-0.1875 octaveずれた。失敗を保存してこの対照を採用せず、既定間隔4の小fixtureの成功も広い素材や圧縮密度差へ拡張しない。
同じ値の並べ替えには正のcostが残ったが、人による順序の認知を示すものではない。

256 episode×128 knotの一queryは8,192 anchor、121,349 median比較、62 refinement、179,392 DP cellを処理した。
既定長の数値DP配列は最大7,824 bytes。このoffline queryの約0.276秒はworker全体の40 ms予算を超える。現Python参照をそのまま実時間へ入れる案は通らず、
本番kernelと両worker／64 Voiceの全同時負荷でO04を検証する必要がある。
queryのコピー／出力、raw推定、cadence／queue、cacheの有界配置、bank／edge／memory clockとallocatorの費用は未計測または未完了である。
数値検査は計193件。失敗を含む比較を`docs/roadmap/temporal-dcc/matcher-reference.json`へ保存し、メモ9.3節の数値規約を明確化した。
全体の適合、実音と人の判定は引き続き必要となる。

次の参照は、取得sampleによるqueryの実行間隔と、固定配置のcoarse cacheを追加した。
各groupは待機queryを一件、完了snapshotを八件保持し、各busの実行中jobは一件に限る。
取得欠落と再配送はcadenceを進めない。待機分の置換は待ち順を保ち、実行中jobを無効化しない。
世代退役とepoch変更は結果を無効化するが、完了／失敗を受け取るまでworkerの占有を保つ。
古い世代の再利用と、遅いjobが毎回捨てられて結果を出せない状態を防ぐ。

従来ledgerには、将来の全音声支持、末尾gapで更新された鮮度、異なる世代のqueryを受理する三反例があった。
反例を保存して修正し、音声支持trackerは各観測の全source終端の最大値を保持するよう変更した。
元の確定期限までにcacheへ実到着した結果だけを使い、workerの完了が早くても遅い配送を過去へ戻さない。
後のcache退避やgroup退役によって、確定済みの書込みは変わらない。

新部品22検査、occurrence追加三検査、source窓追加一検査により、数値検査は計219件となった。
100本×80回の挿入では、同点・古い終端・再読を含め、八snapshotの保持結果が全履歴の独立参照と一致した。
九つの仮想scheduleで、0.05／0.1／0.2秒のcadenceと三つの仮定した処理遅延を比較した。
既定cadenceでは一query 5 msの遅延で二秒間に152件完了し、276.46 msの遅延では六件、
完了時の音声支持年齢は最大約286 msとなった。空の検索結果を使った遅延simulationであり、性能実測や検索品質の証拠ではない。
待機slotは八件以下、cache slotは計64件以下に保たれた。

固定bufferの実配置は、待機／実行中queryとscratchに411,360 bytes、coarse recordに401,408 bytes、
共有coarse scratchに6,272 bytes、controller配列・占有印に808 bytes、合計819,848 bytes／busとなった。
scalar／objectの費用、一時的なcapture・出力、raw推定、bank／edge、memory clock、全ledgerとworker配送は別途必要となる。
記録は`docs/roadmap/temporal-dcc/query-scheduler-reference.json`に保存した。O04全体性能、全特徴の登録・適合、実波形と人の判定は未完了であり、
先の40 ms予算超過も解消していない。

記憶の参照実装は、確定occurrenceから上限付きstrength、上限を掛けないmembership、
経過時間・干渉によるavailability区間への更新を追加した。各busの取得clockは、groupをまたぐ物理的欠測を一度だけ数える。
配送が遅れても元のoccurrence時刻を保ち、反復では過去の干渉区間を消してから同じ書込みの競合分を加える。
検索と再配送は接触回数を増やさない。観測された干渉率は元時刻の(t-1,t]で数え、保存上限を超えても
実際の記憶への干渉は失わない。ただしgapの率上限は未確認となり、該当するavailabilityの下限をゼロに広げる。
上限側にも既知の干渉は残す。

最初の試作には、後のclock入力を観測してから過去時点の再認用コピーを作れる反例があった。
反例を保存し、live更新と新しいコピーの発行は、記憶と取得clockの両方の観測cutに従うよう修正した。
query前に発行した再認用コピーは、それ以前の適格episodeだけを保ち、実際のquery終端まで経過時間だけを進める。
query自身の内容は、採点する過去の記憶を強化したり干渉したりしない。

新部品27検査が通過し、数値検査は計246件となった。未圧縮のsample和集合75本、
直近一秒の全イベント和50本、独立したscalar更新50本と照合した。
接触回数・strength上限・反復支持・欠測長・率上限を組み合わせた162条件では、閉じた式との最大絶対差が2.78e-17だった。
これは数値的一致であり、記憶パラメータの適合、人の再認、O12の予測安定性の合格を意味しない。
Python全体686検査と、既存Rustの746件通過・12件ignoreも確認した。

metadata、更新scratch、gap clock、干渉率bufferの固定配置は446,720 bytes／busとなった。
うち65,536 bytesのmetadataは全bank予算に計上済みであり、二重に加算しない。
再認用コピーは追加6,176 bytesで、object、一時出力、記述子、edge、配送費用はこれらの外に残る。
72／144／288行を九つの容量負荷で比較し、overflowの明示を確認した。144行で最大負荷が成立したわけではない。
現行Pythonの記憶更新は競合支持と類似度をepisodeごとに再計算しており、O04にはcacheを使う一括更新が必要となる。
全bank／edge／handleの寿命管理、有界終端記録、実波形推定、実際の尺度適合、人の判定は未完了である。
記録は`docs/roadmap/temporal-dcc/memory-reference.json`に保存した。40 ms予算超過は未解消で、和声と本番の演奏経路は変更していない。

次のM0部品は、episodeのhandle、凍結記述子、anchorと共通上限のedge保存を管理する。
有界なID予約だけでは聞かれたepisodeにならず、取消し・退避後のhandleはslot再利用でも戻らない。
反復はstrengthとmembershipを更新するが、最初に保存した順序記述子は維持する。
contextと上流から渡された順序対応edgeは16枠を共有する。切り捨てたmembershipの分母は保ち、
代表episodeの退避では、流入するリンクを明示的な未取得にする。contextの枝分かれ・枝刈りで過去を別の枝へ付け替えず、
後の反復では新たな支持だけを加える。

実際のbank到着が3.0秒でもledgerの配送時刻0.6秒を利用可能時刻として、0.7秒期限の関係へ使える反例があった。
反例を保存し、bankの実到着とledger配送を分離した。順序対応は以前に保持された対象と元のsource／target時刻を必要とする。
18検査が通過し、独立した30本のmembership系列も全確定joint支持の和と一致した。
容量検査では全256枠を128 knot・32 anchorで埋め、代表の退避で255の流入リンクを未取得にし、
そのslotを新handleで再利用した。圧縮済みmomentとanchorの参照先は保存前後で一致した。
この構成入力の検査は、長い帰還の認識や関係グラフの推論を示すものではない。

既定bankの実配置はmetadataを含む11,862,016 bytesとなった。
edge scratch、尺度・予約配列、追加の記憶／clock状態を合わせ、既定の取得配置では12,769,616 bytes／busとなる。
object、一時出力、推論、全終端ledgerと配送は別途必要である。
数値検査は計264件、Python全体704件、Rust回帰746件通過・12件ignoreを確認した。
記録は`docs/roadmap/temporal-dcc/bank-reference.json`に保存した。関係・系譜の実際の生成、packed indexのworker接続、
容量感度、全適合、O04と人の判定は未達であり、先の40 ms予算超過も解消していない。

確定待ちendpointの参照は、実際の128-byte header、期限順heap、空きslot配列と二つのID索引を追加した。
元の確定期限と遅延配送を分け、処理中のheaderは確認応答まで固定し、slot再利用後の古いticketを拒否する。
容量超過は未確定の計算上の欠落とする。暫定改訂は元の区間・期限を動かさず、置換したactivity／joint参照を返す。
payloadの保存と元IDの生成は別の部品である。以下のbyte poolと参照管理は保存を扱い、実際の推論による生成は残る。

13検査では、同点・意図的なhash衝突を含む60本の独立した全件整列、索引の再利用、再配送、改訂、有限ledgerとの照合を行った。
容量検査では65,536 headerを保持し、追加一件を拒否した後、10,240件の六cycleと4,096件の一cycleで元の順序どおり処理した。
この検査は成功応答を明示的に与える構成条件であり、実際の推論結果を書き込んだ証拠ではない。
固定bufferはheaderに8,388,608 bytes、heapと空き配列に各262,144 bytes、索引に1,048,576 bytes、
計9,961,472 bytes／busとなった。参照先payload、処理中コピー、receipt状態と配送は別途必要となる。

headerの記録は`docs/roadmap/temporal-dcc/endpoint-reference.json`に保存した。

不変payloadのpoolは、固定320-byte block、24-byte metadata、版付きhandleと型別の参照数を持つ。
queueは四つの参照を自身で保持し、同じendpointのaliasは参照数を増やさない。改訂は新しい版の保持後に旧版を解放する。
四payloadの割当途中に失敗した場合は取得済みの参照を戻す。処理中のclaimはbyte列を保持し、
queueの退役では確定待ち・処理中の所有を未確定の計算上の欠落として解放する。
単調増加する元IDの対を発行する部品も追加したが、元spanの認識とaliasへの継承は上流推論の責務として残る。
この保存操作からbank／sectionが一度だけ更新されたとは判断しない。

初期poolの固定bufferは87,818,240 bytes／bus、endpoint queueを含む合計は97,779,712 bytes／busとなった。
他worker、入力、取り出したコピー、object／allocatorの費用は別途必要となる。
既存のactive descriptorの85,852,160 bytesは2 bus合計である。全65,536 endpointへ128 knotずつ複製すると、
記述子だけで2,684,354,560 bytes／busになる。このpoolの共有は同じ不変objectの明示的な参照保持であり、
同一byte列の自動共有や記述子内部のknotの構造共有は実装していない。

追加16検査では、独立した50本の有限byte／所有参照、失敗処理、epoch退役、実際のpacked descriptorとSectionRecordの往復を確認した。
共有条件では四object・132 blockで全65,536 headerを保持し、最後に全所有を解放できた。
個別コピー条件ではhalf／base／doubleのblock容量が992／1,985／3,971 endpointで尽きた。
有界化だけで容量は成立していない。実際の保持推論、構造共有または容量改訂によって、完全なO04検査の前に解決する。
必要な音楽的履歴を失ってよいという根拠にはしない。この監査は模擬byte列と明示的な成功応答を使い、推論されたwriteを扱っていない。

byte poolの追加時点では数値検査が計293件となり、Python全体733件、Rust回帰746件通過・12件ignoreを確認した。
新しい記録は`docs/roadmap/temporal-dcc/payload-reference.json`に保存した。payloadの意味表現、遅延receipt、
全beam生成、consumerの一度だけの適用、実際のcycle予算とO04費用の監査は残る。
全ledger、適合、人の判定は未完了であり、先の40 ms予算超過も未解消である。

続く保存処理では、不変な320-byte knotを個別に共有する。記述子の各版は順序付きu32参照列を持ち、
圧縮で位置がずれた未変更knotも共有する。ハッシュ一致後に全byteを比較し、f64統計、元時刻、coverageの入力、
世代情報をそのまま保持する。一つの版を複数の所有者が保持しても、子knotの参照は版ごとに保持する。
挿入途中の失敗は取得済み参照を戻し、最後の版の所有がなくなった時だけ、他の版に不要なknotを解放する。

二つのpoolを束ねるadapterを追加し、descriptor参照は共有保存へ、他の三payloadはbyte poolへ渡す。
整数handleが一致しても型とepochで区別する。既存queueから元のpacked記述子を取り出し、同じ所有・解放手順を使える。
全beam・遅延receiptの意味を持つpayload生成や、bank／sectionの実際の一度だけの更新は別途残る。

追加15検査では、50本の独立したbyte集合・所有参照、意図的なハッシュ衝突、索引の全tombstone化、
部分失敗、型別配送と退役を検査した。実際のmoment圧縮器に640件の割当済みraw記録を入れた80版も、
共有保存からの全byteが一致した。既存の数値結果を維持した検査であり、新たな音楽推論や音響的証拠ではない。

容量の構成条件は、1,024固定経路の各64版とした。各経路は128個の固有knotから始め、その後の各版で6個を変更する。
半分の131,072 leafは1,024版、初期262,144 leafは22,869版で尽き、524,288 leafでは65,536版・518,144個の固有knotを保持できた。
記述子の固定bufferだけで、それぞれ128,713,216／222,036,480 bytes／busとなる。他payload、queue、
active記述子、他workerと一時割当は別途必要となる。全knotが別内容の条件は、初期容量の2,048版で尽きた。
このbyte列の構成条件から、実際の経路の出生・交替・系譜、全payload、両bus同時実行の容量と性能を導かない。
それらを検査してから容量を採用する。

数値検査は計308件。Python全体748件、Rust回帰746件通過・12件ignoreを確認した。
記録は`docs/roadmap/temporal-dcc/shared-descriptor-reference.json`に保存した。

次の参照実装では、凍結済みのendpointから実際のepisode bankと指定されたsection履歴へ確定writeを渡す。
bank更新前に各受け手の入力を検査し、元ID、joint重み、元の配送時刻を固定する。bankの応答を失った場合は
完了済みwriteのdigestとsequenceを、sectionの応答を失った場合は世代・sequence・元occurrenceの時刻を照合する。
recent ringの外にある元spanもこの受領情報で復旧できる。成功という戻り値だけでは完了扱いしない。
全受け手の完了後にqueueへ応答し、payload参照を解放する。途中の合成summaryは公開しない。
bankへの作用がまだない場合だけ未確定の損失として取り消せる。次のwriteは確定sequenceの連番を保つ。

受領記録は256-byteの現在状態と1,024件の32-byte記録、計33,024 bytes／busとなる。
17検査は30本×20 writeの独立した加算照合と、bank／section／queueの応答欠落を含む。
異なる重みを持つ1,024受け手の監査では三段階の応答を失わせた。67 call、各call最大16件のsection適用で
全1,024件をそれぞれ一度だけ更新し、独立した重み付き統計との差はゼロ、queueの全参照も解放できた。
これは単一の所有者によるcallと受領の境界での復旧であり、process crashやconsumer内部の任意の部分更新は扱わない。

復号済みbatch、受け手map、検査用コピー、hash処理は別途必要となる。batchのPython表現だけで1,248,569 bytesを使う。
現在は最初のcallで全受け手を検査し、全batchを毎回hashする。最大48.490351997315884 msで、worker全体の
40 ms予算を超えた。全67 callは568.5904265847057 msだった。これは単一所有者の診断値であり、両worker同時の
preflightではない。予算16が制限するのは現時点ではsection適用数だけである。有界な意味表現と所有、検査の配分、
copy／hashの費用を解決してから本番へ接続する。実際のbeam／系譜／遅延receiptの生成、完全なrouting、
実行時の公開制御も残る。

数値検査は計325件。Python全体765件、Rust回帰746件通過・12件ignoreを確認した。
記録は`docs/roadmap/temporal-dcc/consumer-reference.json`に保存した。以前のmatcherとpayload容量の失敗、
O04全体、fit、人のgateは残る。この段階で楽器の音声や和声の実装は変更していない。

続く改訂ではconsumerが変更不能の入力packetを所有し、sectionの事前検査を分割した。
局所的な型付き符号化はf64のbit、符号付き／符号なし64-bit ID、jointのtuple key、windowのlistとmaskを保持する。
builderはsectionの各行を独立してコピーし、宣言した受け手が全部そろうまでpacketを確定しない。
fingerprintは元のclaimを含み、producerや復号結果を後から変更しても受理済みbyteは変わらない。
一callで検査または適用するsectionは最大16件。元時刻・重み・途中公開の禁止・応答欠落からの復旧を維持し、
完了または取消後に内部のbank復号と索引を解放する。この符号化だけでは実際のroutingや、queueにある
joint／系譜／遅延receiptのbyte列から意味への対応を生成したとはしない。

受領記録は33,024 bytesのまま。入力上限の初期値はbankが67,108,864 bytes、section一行が16,384 bytes、
受け手1,024件、入れ子16段とした。全モデルの保存容量として採用した値ではない。同じ重み付き監査は131 callとなり、
全1,024件の事前検査の後に各sectionを一度だけ適用した。三つの応答欠落から復旧し、重み付き統計の誤差はゼロだった。
consumerの最大callは7.179675158113241 ms、合計141.72862633131444 ms。packet作成は別に測り、
確定時の8.344630943611264 msを含め、合計34.825715236365795 msだった。
packetはbank 273,968 bytes、section計1,260,544 bytesに加え、claim・digest・object・allocator・scratchを使う。
これはbank登録1件の条件であり、call数から物理的なcycle数や音声遅延を判断しない。

最大256件・各128 knotのbank登録も検査した。全記述子のbyteと元のmembershipは一致したが、
bankのpacketだけで26,982,619 bytesを使った。符号化477.75619593448937 ms、確定1,147.6100960280746 ms、
consumerでの復号747.8895240928978 ms、適用652.5812409818172 msとなり、すべて40 ms予算を超えた。
展開済み記述子の重複保存とbankの一括処理を改める必要がある。公開と受領を一回に保ちながら処理を分割し、
producerの準備・全コピー・実際の到着遅延もO04へ計上する。

新規packet検査15件は独立したbyte列、f64のbit、改変の隔離、未完成・途中失敗と、seed固定の500記録の往復を含む。
更新したconsumer17検査と合わせ、数値検査は計340件。Python780件、Rust746件通過・12件ignoreを確認した。
記録は`docs/roadmap/temporal-dcc/consumer-packet-reference.json`に保存した。過去の失敗、全波形・推論・fitの統合、
人のgateは残る。
全producer／consumerの統合、O04、適合と人の判定は未完了である。


次の改訂で、320-byte knotを直接使う登録入力とbank準備の分割を接続した。
登録metadataを一行ずつ符号化し、全byteが等しい記述子は共有する。未使用のknot／anchor枠とedge scratchで準備し、
最後の一単位でだけ記憶を公開する。初期予算はbankが8単位、sectionが16行。一callは一段階だけを進める。
取消と後半の検査失敗では未公開分を解放する。登録のavailabilityは最後の公開cutとし、元の配送・音響時刻を別に保持する。

全256件×128 knotの同内容／全内容別の両条件で、全byteと元の1/256 membershipを確認した。
header・登録metadata・記述子の合計は186,311／10,631,111 bytes（claim・digest・object・scratch等は別）。
各条件67 call・514 bank単位となり、最大callは16.592381987720728／13.762363931164145 ms。
入力作成は別途6.20／11.25 ms。最後の公開とqueue全解放を確認した。
制御した27.0／27.65秒は実際の音声到着遅延の測定ではない。1,024受け手の監査も132 callで三応答欠落から復旧し、重み付き誤差はゼロだった。

一方、1,024 joint・4,096関係候補の最大headerは、最初のbank callが57.98464617691934 msで40 ms予算を超えた。
符号化23.47 ms・確定37.68 ms・単独復号34.79 msも一括処理のまま。membershipと共通16-edge上限、1,024件のedge欠落を照合した。
このbank単体の条件にはsection fanoutや実推論のroutingを含まない。次はheader／候補検査の分割と復号の重複解消を進める。
bank24・consumer19・packet19の検査が通り、数値検査は計352件。Python792件、Rust746件通過・12件ignoreを確認した。
三つの登録で以前の失敗を保持し、sourceと監査を`m0-bank-staging-reference-20260911`へ保存した。
O04全体、波形・推論・fitの統合、人のgateは残る。


次の改訂では関係候補を個別record化し、consumerとbankのheader復号を一回にした。
joint／候補は64行ごとに検査を区切る。初期予算8単位なら、一callの候補復号は最大512件。
全行の検査が公開の前提となる。復号済みの辞書を外から渡して所有者を置き換える経路は設けず、公開APIの復号結果は別のコピーとする。

最大header単体は43 call・最大5.531514063477516 msとなり、旧実装の記憶・knot・anchor・edge全byteと一致した。
さらに実際の有限ledger、bank、1,024 section、所有付きqueueへ時刻順の二writeを渡した。
二つ目は1,024 joint・4,096候補・1,024受け手を同時に含み、bank／section／queueの応答欠落から復旧した。
header復号は一回、候補は全件各一回。重み、二重適用の防止、所有の解放を維持した。
最大callは8.745113853365183 msだが、174 callの総処理は219.30024586617947 msを要する。
別の全256件×128 knot条件も、各callが12 ms未満でも総処理は約371〜374 msを要する。
これは制御した有限入力であり、実波形の推論、現実の到着／待ち行列の遅延、両worker同時負荷のO04を証明しない。
数値360検査、Python800件、Rust746件通過・12件ignoreを確認した。
sourceと四監査は`m0-header-staging-reference-20260911`へ保存した。以前のmatcher超過、率履歴・容量の問題、全fitと人のgateは残る。

続く監査では実時計を使い、100 ms周期・40 ms処理窓へ100 ms間隔の合成入力8件を投入した。
256 episode・1,024 joint・4,096候補・1,024 sectionの全重みと一度だけの適用を保ったが、
元期限から全受領完了まで約0.635〜5.224秒の遅延が蓄積した。処理窓を外しても約0.284〜1.747秒となった。
診断用の4-slot queueでは8件中4件を容量不足として明示的に拒否し、受理したpayloadの所有は最後にすべて解放した。
最大構成ではbank呼出しのcutから全受領完了まで約127〜201 msあり、外部公開には完了時刻を別途取得する必要がある。
別枠のprofileでは汎用codec変換とsection組立ての反復に費用が集中した。次は妥当性検査と元支持を保って総処理量を削減する。
sourceと全call/cycleのtraceは`m0-delivery-clock-audit-20260911`へ保存した。
これは100 Hzの有限な合成入力による測定であり、48 kHz実音声・device、両worker同時負荷、live evidence ageや全O04の証明ではない。
数理モデル・演奏経路・和声DCCは変更していない。M0は引き続き作業中である。

関係recordを107-byteのR1、section recordを534-byteの固定部と対応情報mapからなるS1へ変更した。
u64のID、f64の値、欠測とvalidityを分け、与えられたviewを再計算して補正しない。
最大構成の関係recordは438,272 bytes、sectionは694,272 bytesとなった。
以前の汎用codecとの照合で、section累積320 bufferとringの1,184 bufferが全byte一致した。
新規5検査でwire配置・mask・不正入力・200組の往復変換を確認し、数値365検査を含むPython805件が通過した。
同じ実時計の到着列で最大構成の測定処理量は33.2%減り、期限後遅延は約0.420〜3.234秒へ短縮した。
処理窓なしでは約0.182〜0.937秒となった。元の全重みと一度だけの適用を維持したが、待ち行列はなお増える。
全256件×128 knotの登録も総処理約372 msを要する。表現の改善であり、全O04の合格ではない。
次は途中の観測を保ちながらsection準備の重複を削減する。sourceと比較は`m0-typed-consumer-codec-20260911`へ保存した。
数理モデル・演奏経路・和声DCCは変更していない。

section配送では、確定統計の準備結果を一度だけ計算して保持する方式へ進めた。
適用時は現在の累積recordの確定統計だけを更新し、途中の音響観測・取得cut・accent受領を残す。
重みゼロの受け手には古い準備結果を適用しない。単一owner内部の形式で、一受け手1,408 bytesを追加する。
受領・準備bufferは計1,474,816 bytes／busとなる。一時copy、Python object、他のworker状態・配送は別途計上する。
旧実装との比較では、途中の観測2,304回、世代継承とaccent再配送を含め、累積1,152・ring 4,426 bufferが全byte一致した。
新規6検査を加え、数値371検査を含むPython811件が通過した。
同じ最大負荷の到着driverでは、固定codec版から測定処理量が23.4%減り、期限後遅延は約0.323〜2.324秒となった。
処理窓なしでは約0.145〜0.559秒だった。初回の22.4%減も保存した。全構成と重みを保ったが、待ち行列と処理窓超過は残る。
consumer総処理は95.230 msへ減った一方、全256件×128 knotの登録は371.016／366.019 msを要する。
sourceと根拠は`m0-section-preparation-20260911`へ保存した。
残る準備・観測／encode・bank／ledgerの費用、実音声・両worker・fit・人のgateを含む全O04とM0は未完了である。
数理モデル・演奏経路・和声DCCは変更していない。

次の実装改訂では、SectionRecordの連続したf64値を一括で読み書きする方式にした。
各座標の`old + scale * value`の計算順序を保ち、共有する分母は丸め後のbyteと符号付きゼロまで照合する。
不正値・overflowではrecord全体を変更しない。640-byte recordと1,474,816-byteの受領・準備配置は同じである。
旧scalar実装との12,000更新・96,000ブロックの読み出しが一致し、1,099条件の拒否判定も一致した。
途中観測を含む1,152 writeの比較でも累積・ringの全byteが一致した。新規3検査で数値374・全Python814検査となった。
同一最大負荷の到着driverでは処理量がさらに10.2%減り、期限後遅延は約0.312〜2.031秒、
処理窓なしでは約0.132〜0.435秒となった。consumer総処理は83.532 msへ減ったが、全256件×128 knotの登録は約371 msを要する。
最大44.858 msの処理周期と待ち行列が残り、持続的な配送性能には未達である。
sourceと根拠は`m0-section-record-blocks-20260911`へ保存した。設計メモは同じ版を維持する。
これは数値実装の等価性と有限な性能測定であり、全O04・実波形の推論・fit・人の検証を満たすものではない。

活動統計の組立てをsection観測、commit準備、occurrence payload生成で共通化した。
29個の論理値をすべて検査してから、23個のf64保存値を書き込む。累積観測には欠測した経過時間を明示的に含め、
確定spanには元の所有窓とmembershipを用いる。共有分母の丸め後byte、符号付きゼロ、不正値・overflowの検査を保つ。
隣接分類の直前統計とendingのmaskも各一回だけ読む。保存配置とモデルは同じである。
活動統計の一時表現は展開232 bytes・圧縮184 bytesとなり、Python objectとともに固定buffer以外の計上対象である。
旧field別処理との4,000条件が一致し、1,263条件では無変更で拒否した。768件のledger write、
pending／sealedの1,536 payload、2,304 section投影も一致した。以前のscalar比較と途中観測の比較も維持した。
新規3検査で数値377・全Python817検査となり、保存した五部品一式の144検査も通過した。
同じ最大構成の到着driverでは処理量4.6%減、期限後遅延約0.305〜1.919秒、処理窓なしでは約0.128〜0.377秒だった。
少数構成は処理量約0.7%増となり、全条件の速度向上を示す結果ではない。共通化だけの初回3.1%減も保存した。
consumer総処理は78.317 msへ減ったが、全登録は370.486／367.011 msを要する。
待ち行列と最大44.669 msの処理周期が残り、持続的な配送性能には未達である。
sourceと根拠は`m0-activity-blocks-20260911`へ保存した。全O04、実際の帰属・routing、実波形・fit・人の条件は残る。

続いてsectionのedge・transition・pairの31確定統計を248-byteブロックで一括処理した。
符号付きゼロや既存の不正値の検出に影響するゼロ加算も含め、全座標の計算を保つ。検査済みの元endingとmaskは再利用する。
旧処理との4,000条件で準備データと累積各2,240件、ring6,748件が全byte一致し、1,575件は無効果、185件は無変更で拒否した。
準備には337件の前sectionの先行記録を含む。全登録のprofileで記述子のscalar読出しが大きな費用と判明したため、
DescriptorKnotのsnapshotも38個のf64と二つのu64 IDを一括して読む方式へ変更した。
別の4,000条件で全floatのbit・型・mask・拒否結果が一致した。長さゼロの236拒否も維持し、不正状態を新たに受け入れない。
新規3検査で数値380・全Python820検査、保存した六部品一式の176検査が通過した。
モデル・記録配置・1,474,816-byteのconsumer固定bufferは同じであり、packed一時値とPython containerの全費用は別途計上する。
同一最大構成の処理量は直前の活動統計版から5.7%減、期限後遅延約0.302〜1.722秒、処理窓なしでは約0.127〜0.348秒となった。
少数構成は6.6%減だが、最大処理周期は44.669から46.521 msへ増えた。全256件×128 knotの登録は
370.486／367.011から262.971／266.644 msへ減り、consumer総処理は70.530 msとなった。いずれも予算超過を残す。
31統計だけを変更した初回版の最大処理量5.8%減と全登録profileも別途保存した。有限の測定であり、信頼区間や持続的な配送性能の証明ではない。
根拠は`m0-sealed-sums-20260911`。実推論・波形・fit・人・全O04の未達条件を維持する。

bank登録では、immutableなpacked記述子へのreadonly viewを保持し、従来のsource・順序・長さ・moment・gap検査後に
元の320-byte記録を未公開の空きslotへ保存する方式へ変更した。同じ不変byteから作ったviewの再構成・再展開を省く。
展開済みnative入力は従来どおり全knotを再構成し、派生viewの矛盾を検査する。
旧bankとの720取引では、実際の1／2／8／128圧縮knotを使い、240 staged packetと両順序の480混在取引を比較した。
途中・最終の1,775状態が一致し、成功296件の全bufferとreceipt、拒否424件の既存記憶・予約の保持と空き準備slotの解放が一致した。
支持不足で隠れた非有限moment、全幅ID、sourceや最終memory検査の失敗を含む。
新規3検査で数値383・全Python823検査、保存一式の179検査が通過した。
全256件×128 knotの登録は262.971／266.644から154.701／152.413 msへ短縮した。
一knotを中心に使う同一最大到着構成は処理量約0.2%増、期限後遅延約0.302〜1.729秒、最大処理周期47.354 msとなった。
処理窓なしの遅延は約0.122〜0.310秒、少数構成の処理量は2.8%減、consumer総処理は70.600 msだった。
全記述子の登録費用は改善したが、持続的な最大配送性能には未達である。モデル・固定配置は同一で、全O04と他のM0条件を維持する。
最初のoracleで圧縮後knot数を誤認した失敗と、実際の圧縮件数へ修正した記録も保存した。根拠は`m0-packed-bank-admission-20260911`。

section投影は、確定writeが所有する重複除去・整列済みpath列の二分探索へ変更した。
保存用索引は増やさず、元のmembership・cut・cached correspondenceと返却値のコピーを維持した。
0／1／2／8／64／1,024 pathの36 streamで旧全走査と6,882検索を照合し、成功5,658件のS1 packetがbyte一致した。
拒否1,224件の結果も一致し、旧新版の72保存状態は検索前後で変化しなかった。pending中の改訂、確定後の再解釈、
疎な全幅ID、correspondence欠落、外部へ返したviewの書換えを含む。数値386・全Python826・保存一式109検査が通過した。
同一最大到着条件の処理量は1,010.042から923.122 msへ8.6%減、期限後遅延は約0.232〜1.538秒、最大処理周期は43.721 msだった。
少数構成は2.9%増、容量4の診断構成と処理窓なし構成は8.2%／7.5%減だった。有限な測定であり、信頼区間や持続性能の合格ではない。
前回のfull-bank／header診断はsection投影の受け手を持たないため再測定せず、過去の証拠として維持した。
モデル・固定配置とconsumerの1,474,816 bytes／busは同一。40 ms処理窓、完全なkernel、allocator・実機負荷、
実音と他のM0条件は未達のままである。根拠は`m0-section-path-search-20260911`。

consumerのgeneric codecでは、既存schemaキー71個の符号列とliteral／scalar復号表を再利用する方式へ変更した。
wire形式、容量・深さ・型・UTF-8・mapキーの検査は維持する。旧版との9,774符号化・13,312復号を照合し、
6,724件の符号列と4,573件の復号値が一致した。3,050／8,739件の拒否結果も一致し、数値388・全Python828・保存一式84検査が通過した。
共有する符号列／UTF-8文字列はmodule全体で1,204／849 bytes。Pythonの表・文字列・object・compiled formatの費用は別途必要となる。
packetの固定配置とconsumerの1,474,816 bytes／busは同一である。
採用版の有限な最大到着測定は処理量897.324 ms、直前版から2.8%減、期限後遅延約0.227〜1.509秒だった。
最大処理周期は47.001 msへ増加し、40 ms予算は未達である。少数・容量4・処理窓なし構成は9.8%／4.2%／3.6%減、
全bank登録は152.868／152.954 msだった。これらは有限な診断であり、持続性能の合格とは扱わない。
profileでは一度復号の悪化が疑われ、復号変更を戻した別案も検査・保存した。しかし計測負荷を外した15回の順序無作為化比較では、
最大headerの符号化中央値は旧版／採用版で4.964／3.572 ms、復号は7.174／6.277 msとなり、最初の再利用案が速かった。
その検査済みsourceと配送測定を採用し、別案の全結果、交互比較、起動pathエラーも保存した。
意味payloadの由来、完全なO04、fit、人のgateは未完了である。根拠は`m0-schema-key-codec-20260911`。

DTWの元時刻band探索・残差・二行DPを、演奏経路とは独立したRust数値kernelへ移す実験を追加した。
既存Python参照と設計メモは同一である。700無作為入力・全1,024 mask等の追加9検査と、既存33検査のnative実行が通過した。
全Python837検査（31.111秒）、Rust746通過・12件ignoreを確認した。
256 episode×128 knot、cue128 knot、16候補、8,192 anchor、62変形を保った9回の交互比較では、
全queryのPython／native経由の中央値は1座標269.573／152.566 ms、10座標は426.318／189.972 msとなった。
短縮率は43.4%／55.4%だが、40 ms予算は未達である。入力検証・変換・traceback・診断を含む値であり、
prepared kernel単体の時間とは区別する。固定ctypes payloadは41,624 bytes、二行DP配列は別途2,064 bytes。
Python object・一時出力・allocator・ABI由来のstack増分・RSSと実音声の同時負荷は未計測である。
既存参照の388検査・feature版29は維持する。native実験の数値を、演奏側の変更や全M0の合格へ拡張しない。
初回測定とテスト側の全欠測経路の期待値誤りも保存した。根拠は`m0-matcher-kernel-20260911`。

続いてcoarse anchor探索を一括するRust kernelと、記述子を一括変換するadapterを追加した。
8,192 anchorの中央値・残差を評価し、詳細診断は各episodeの最良anchorで既存参照から生成する。
元時刻、mask、f64加算順、中央値の比較数、変形・同点・候補数は維持した。
16実験検査には500組の全anchorと90本の全queryを含み、既存33検査もnative経由で通過した。
全Python844検査（29.404秒）とRust746通過・12件ignoreを確認した。
9回の順序無作為化比較で、全queryのPython参照／旧DTW版／新anchor版の中央値は、
1座標270.968／147.211／74.521 ms、10座標425.345／184.874／94.553 msだった。
旧DTW版から49.4%／48.9%短縮したが、40 msは超える。54回の結果比較では型・f64 bit・経路・診断が一致した。
固定ctypes payloadは53,008 bytes、二行DP配列は2,064 bytes、anchor標本配列は128 bytesである。
Python object・変換中のlist・結果・allocator・ABI由来のstack増分・RSSは別途必要である。
初回候補は未選択anchorのRMS overflowを隠し、巨大な時刻差で参照が残すNaN診断を拒否した。
極端な算術では元参照へ戻す経路を加え、両反例を保持した。判定はf64上限と最大8標本から導く数値保護であり、認知的な閾値ではない。
初回auditのfixtureが一方の差を区別しなかった履歴も保存した。NaN診断の再現は、その入力を本番で許容する判断ではない。
既存Python参照・feature版29・設計メモ・演奏経路は同一。次は残る入力検証・変換と、実機・両worker・64 Voiceの全O04へ進む。
根拠は`m0-native-anchor-20260911`。fit、人のgateを含むM0全体は未完了である。

入力の数値検証とCPythonからの直接転送をnative実験へ追加した。通常のfloat／list／dictを
GIL保持のPyDLLで固定bufferへ転送し、特殊な型とエラー順序は既存Python経路で扱う。
数値kernelはPython非依存のまま、転送APIだけを`python-binding`で有効にする。
26実験検査は1,200件の検証比較、320件の転送byte比較、型fallback・参照解放・buffer末尾を含む。
既存33 matcher検査と全Python854検査（32.151秒）、Rust746通過／12件ignoreも確認した。
同じ256 episode×128 knot・cue128・16候補を保つ9回の四版比較で、旧anchor版→直接転送版の
全query中央値は1座標73.531→23.433 ms、10座標95.701→37.477 msだった。
72回の全出力比較で型・f64 bit・経路・診断が一致した。単体中央値が40 msを下回っても、両worker・64 Voiceのp99合格は未証明である。
固定ctypes payloadは67,344 bytes。DP配列2,064、anchor標本128、検証metadata56、転送pointer配列144 bytesは個別の配置であり、
Python object・結果・allocator・全stack・RSSはこの合計に含まれない。既存Python参照・feature版29・設計メモ・演奏経路は維持した。
根拠は`m0-native-input-validation-20260911`。consumerの処理量、実際の生成入力と同時負荷、fit、人のgateは残る。

同じ入力を用いたtail診断では、各caller 10回warmup後に200回を測定した。
1座標のp99は単独23.460 ms／2 caller 57.322 ms、10座標は単独49.625 ms／2 caller 87.469 msだった。
2 callerは独立scratchを持つPython threadで、各200組の呼出しが重なった。GILは共有する。
全1,260回で出力の型・f64 bit・経路・診断を照合した。照合とbarrier待機は計測区間の外に置いた。
この条件でも40 msを超えるため、単体中央値を根拠に実時間採用しない。
次は残る経路復元・診断計算と構築をnative側へ移し、同時呼出しを再測定する。
これは200回の合成入力診断であり、実際の両worker・64 Voice・6,000周期のO04ではない。

経路復元、座標別の逆順残差加算、隣接pairのmotion／interval診断をnative側へ移した。
`math.fsum`と最終結果構築はPython側に残し、`**2`の丸め・overflow、特殊gap型の遅延評価は元参照と照合する。
追加検査には乗算とpowが異なる固定反例と320個の広い桁、例外後の再利用、欠落gapの参照順序を含む。
最初のconfig配置ずれは修正し、五構造体のサイズをbuffer使用前に照合する。旧候補と失敗ログを保存した。
全30実験検査、既存33 matcher検査、全Python858検査（32.917秒）、Rust746通過／12件ignoreを確認した。
9回の三版比較で、入力転送版→経路復元版の全query中央値は1座標23.744→16.484 ms、
10座標38.226→21.222 msだった。54回の全出力比較で型・f64 bit・経路・診断が一致した。
単独／2 callerのp99は1座標17.303／42.039 ms、10座標32.859／50.261 ms。
各callerのwarmup10回＋200回、全1,260回の出力一致を確認したが、2 callerの40 ms予算は未達である。
固定ctypes payloadは73,152 bytesで、経路1,536・診断配列2,048・座標残差と件数120 bytesをこの内数として計上する。
Python object・結果・allocator・stack・RSSと全O04は別に残る。既存参照・feature版29・設計メモ・演奏経路は同一。
次は残るanchor診断を扱う。根拠は`m0-native-traceback-20260911`。実生成入力・consumer処理量・fit・人のgateも未達である。

最良anchorの選択・比較件数集計・残差標本をnative側へ移した。最良候補だけの標本を元順序で保持し、
powの丸めとPythonのmath.fsumを維持する。8,192 anchor・16候補・62変形と全診断は削減していない。
500組の全anchor比較を最良候補・総比較数・境界件数・RMSへ拡張し、同点・全欠測・再利用・元順序を検査した。
32実験検査、既存33 matcher検査、全Python860検査（32.544秒）、Rust746通過／12件ignoreを確認した。
最終9回の三版比較で、経路復元版→今回版の全query中央値は1座標15.650→13.588 ms、
10座標20.710→18.567 ms。54回の全出力比較で型・f64 bit・経路・診断が一致した。
単独／2 callerのp99は1座標13.594／28.807 ms、10座標30.016／44.354 ms。
各callerのwarmup10回＋200回、全1,260回の出力を照合した。1座標の2 callerは40 ms以内だが、10座標は未達である。
coarse呼出しの引数追加に合わせABI照合の入口をv2とし、旧adapter／新libraryと逆組合せの双方を呼出し前に拒否した。
固定ctypes payloadは73,296 bytesで、最良候補診断144 bytesを含む。anchor用の元標本・最良標本・sort配列は計320 bytes。
Python object・結果・allocator・全stack・RSSは別である。既存参照・feature版29・設計メモ・演奏経路は同一。
次はquery単位で転送と呼出しを束ね、所有・元cut・拒否順序とコピー費用を検査する。
根拠は`m0-native-anchor-diagnostics-20260911`。実生成入力・consumer処理量・全O04・fit・人のgateは残る。

query単位の所有bufferと三つのnative呼出しを実装した。全query中央値は直前版から
1座標13.811→11.355 ms、10座標18.482→15.380 msへ短縮した。
2 callerのp99は30.454／39.974 msだが、10座標は400回中4回が40 ms超過、最大44.548 msで、余裕は十分でない。
固定ctypes bufferは6,997,808 bytes／caller、最大queryの数値転送は5,526,528 bytes。
元ID・世代・cut・scalesと全出力を維持し、54回の全query比較、1,260回の同時呼出し比較、42回の資源監査で照合した。
38実験検査、既存33 matcher検査、全Python866検査、Rust746通過／12件ignoreを確認した。
入力転送は別の段階計測で6.286／7.371 msを占める。次は所有済みpacked表現からの転送を検討する。
根拠は`m0-native-query-batch-20260911`。登録参照・feature版29・設計メモ・演奏経路は維持し、
実生成入力・consumer処理量・全O04・fit・人のgateは未完了のまま残す。

320-byteの元記述子を単一の所有済みimmutable入力とし、辞書へ展開せずnativeで照合する経路を追加した。
全query中央値は既存の辞書一括版11.416／15.158 msから、
packed入力5.042／7.221 msへ短縮した。毎回の全コピーを含めると5.718／7.831 msだった。
コピーなし／あり・1／2 caller・1／10座標の8条件で全2,400測定が40 ms以内、全2,520出力が一致した。
2 callerのp99はコピーなし21.580／22.513 ms、コピーあり8.944／11.087 ms。順序・条件の異なる測定からコピーの高速化効果は推定しない。
固定ctypes bufferは7,001,928 bytes／caller。元payload10,526,720 bytesを保持し、コピー条件では同量を追加コピーする。
数値projectionは5,526,528 bytesを書き、借用pointerをreturn前に失効させる。原支持cut・世代・coverageの除算順序・全出力を維持した。
43実験検査、既存33 matcher検査、全Python871検査（32.693秒）、Rust746通過／12件ignoreを確認した。
72回の全query比較、84回の資源監査も全出力一致。根拠は`m0-native-packed-input-20260911`。
次はpacked exportからschedulerのdispatch・matcher・receiptまでを接続する。登録参照・feature版29・設計メモ・演奏経路は同一。
実生成入力・consumer処理量・全O04・fit・人のgateは未完了のまま残す。

#### モデルの根拠と交換境界（2026-09-11）

知覚現象、神経機構、採用した推定器、生成への有効性は別の主張として扱う。
R/Hにもモデル選択があり、[Marjiehら (2024)](https://www.nature.com/articles/s41467-024-45812-z)は
音色による協和判断の変化とモデル改訂を示した。
拍でも[Damsmaら (2025)](https://academic.oup.com/cercor/article/35/9/bhaf258/8263582)は
振動子と誘発応答の双方による神経応答の再現を示している。
これらを推定器の交換可能性へ反映し、DTW、半マルコフ状態、グラフ、評定headを神経法則として固定しない。

M0の`docs/roadmap/temporal-dcc/model-replacement.md`とメモ8.1節で、観測・支持・時刻・出力の
安定契約、モデルが所有する内部表現、交換単位を登録した。
runtimeとVoiceは10値のknot、packed配置、DTW経路、beamの内部に依存しない。
密結合する記憶・照合器・decoderは一組で交換でき、共有推論方式自体も交換対象とする。
階層間の相互作用は既定の因果的な所有・更新を保つ。出力型が同じでも以前の適合尺度を自動転用しない。

O03・O09・O12は、依存に沿った特徴の再生成・再適合・校正、開始時の空状態、版不整合の拒否、
交換後の資源再測定を引き受ける。MR1は本体組込み前の小型順序付き／順序なし照合、
MR2はM2の拍・到来比較、MR3はM1–M2最初の境界での版・状態隔離であり、すべて手順登録・実験未実施。
認知的に弱い対照でも交換境界の確認に使えるが、それを本番へ採用した証拠にはしない。
Python/Rustの数値一致は同一モデルの実装交換であり、MR1の代用にはならない。

この設計改訂は参照式・特徴配列・容量を変えず、feature版29を維持する。本体と既存和声カーネルの変更はない。
過去の検査・外部レビューは保存版にだけ対応し、進行中のpacked worker接続は未検証のまま残す。
交換実験、全M0、適合、人の判定が通過したとは扱わない。

#### 身体アフォーダンスと脳回路・認知機能の対応の採用（2026-09-12）

アーティキュレーションを、身体に根ざしたgestureの生成と、その聴覚的な知覚・予測として
扱う方針を作者が採用した。身体力学、感覚運動的な予測、音楽的文脈が局所行動を制約する。
アフォーダンスは特定の身体と音環境との関係であり、質量だけで動きやすい周期は決まらない。
[設計メモ§3.1](../design-notes/dcc-neurocognitive-hierarchy.md#31-adopted-circuit-and-embodied-affordance-interpretation-2026-09-12)に
脳回路と認知機能の対応、一次研究と限界を記録した。P/M/Aは回路を横断する機能であり、
ソフトウェアの各部を脳領野へ一対一で対応づけるものではない。

現行Manifestoの時間軸節には固有ペースと音長があるが、作者の意図する腕・脚の共振の説明は
明記されていない。この説明上の不足を台帳に残す。既存の身体設定は出発点であり、検証済みの
肢体力学モデルではない。メモ§10.2.1で身体だけ、文脈だけ、両者を交差させる介入を定め、
T1とアーティキュレーション↔フレーズ接続の検証要件として完成計画へ接続した。
参照式・適合パラメータは変更していない。今回採用した追加要件の実装・検証は未完了であり、
以前の実験や外部レビューの合格をこの追加へ転用しない。

#### Rust数値準備と小型モデル交換（2026-09-12）

M0に安全なRustのanchor／DTW・有界queryと、全記述子の順序なし対照を実装した。
同じ契約検査用consumerが二方式を受け取り、元の支持時刻を保持して、旧世代・退役・
別モデル・未来・遅延の結果を作用前に拒否する。固定Python参照と小型の全経路列挙を照合した。
同じ内容の順序反例は算法上の違いを示すが、聴取者の再認や作者の音楽的採用を示さない。
現時点の呼出し元はcrate testとオフライン資源検査であり、実音生成・runtime・適合済み再認には
未接続である。工程改訂は準備後のM1技術実装を可能にし、M0の収集開始条件は保持する。
[source・入力・出力と資源の記録](../roadmap/temporal-dcc/m0-rust-preparation.md)に限界を残す。
全O04、MR3、T4/T6の採用、M0全体の完了ではない。上記の身体要件は別の未達であり、
新しい身体法則やR/H/Cの挙動は追加・変更していない。

## 9.4 整合と拡張の順序

現在の範囲と実行順序は[2026-09-20の機構選択規則](#mechanism-selection-rule)に従い、
[2026-09-13の採用判断](#i9-scope-decision)を包含する。本体はT1・T2、T4の短期反復期待とI12b、研究拡張はT3の評定head、T4の変形対応・想起、T5–T7、I7–I9、I12a／c／d／e、O16である。以下の数値準備は各記録時点の履歴である。
本体の最初の機構候補と生成への用途は[§9.3.55の自声予測契約](#i10-sensorimotor-contract)へ具体化した。
次は固定方策の一Voiceから、指令と私有実音を区別した帰結診断を接続する。I10の実装完了ではない。

評定準備には混合後lossとbackoffの順序も追加した。temperatureは支持された周辺分布にだけ
適用し、訓練priorとunknown質量を分けて保持する。有限の統計optimizerと、素材family／参加者の
交差foldテンプレートを実装した。構成したloss・勾配・予算停止・prior隔離の検査であり、
適合済み音響モデルやO12の予測安定性を示さない。[fit準備の記録](../roadmap/temporal-dcc/m0-ordinal-fit-preparation.md)に
実際のデータ／OOF由来・全job一覧・校正とM0の未達を保持する。

M0の評定準備として、groove／参加欲求の109入力の順序、complexity・群内history除去・
networkの各対照と、未適合のproportional-odds数値部品を登録した。独立した750桁のCDF参照で、
狭いカテゴリと飽和tailを含む構成済み56条件を照合する。入力の組立てとlinkの算術の検査であり、
実音producer、fit予算、残る比較、校正、T3／M0の採用は未完である。
[評定準備の記録](../roadmap/temporal-dcc/m0-rating-preparation.md)に限界と旧入力版の扱いを残す。

M1の初期入力として、両busのNSGT処理後・結果間引き前に観測経路を接続した。
`temporal_mode("observe")`で独立した有界observerを起動し、省略時はoffを維持する。
元のsample支持、原音欠落後のwarmup、配送欠落、EOFを同じUI／reportの要約へ渡す。
off／observeとreport有無の4条件でオフラインWAVをbyte比較し、habitatの無音と
presentationの有音を区別する。音響入力と診断の経路であり、grouping・関係・適合済みモデル・
新しい生成作用はまだない。[観測実装の記録](../roadmap/temporal-dcc/m1-observation.md)に
M1／MR3と全O04／M0の残条件を保持する。数値照合は引き続きこのtapと独立したcrate testから呼ぶ。

observerへ各frameの最大7ピークと残差への配分を追加し、canonical hopのmono平均二乗
エネルギーを8個のtrajectory枠へ保存的に配分する。独立した有理数参照で106入力frameを照合する。
既知の無音、有音でスペクトル質量ゼロの状態、取得後のwarmupを区別する。
ピークbinはframe内の候補であり、持続するridgeや音源のIDではない。連続性・grouping・関係推論は残る。
[trajectory入力の記録](../roadmap/temporal-dcc/m1-trajectory-input.md)に
sourceと限定したkernel費用の測定を保存し、全O04と区別する。

続く数値準備では、有界ridge連続性の2前身リンク、secantと欠落、分裂・合流時の新identity、
観測済みの継続なし時間だけを使う退役を実装した。Decimal参照160条件と、98比較上限、
3,000 hopのlifecycle列を検査する。development尺度は呼出側が渡す設計で、実測固定はまだない。
したがってtrackerはcrate test用の部品に留め、実音接続済みのframe内tapと区別する。
[ridge準備の記録](../roadmap/temporal-dcc/m0-ridge-preparation.md)に
この境界と、残るgrouping・全負荷の条件を保存する。

音群配分の数値部品では、保存memberへの重み付き最大一致度と残差を一度だけ正規化し、
全trajectoryの配分後に参照を更新する。独立Decimal参照64配置・419有効行を照合し、
bin別配分のエネルギー保存と、unknown配分・既知の無音の区別を検査する。
896距離評価の上限とbin別エネルギーkernelは別々に測定する。development尺度の適合、
全group lifecycle、本番接続は残る。[配分の記録](../roadmap/temporal-dcc/m0-group-assignment.md)に詳細を保存する。

数値groupingには、物理的なsample支持を持つ有界Pearson窓とcomplete-link bundle形成を追加した。
欠測pairは結合を禁じるが反相関にはならず、強いA–B・B–Cから弱いA–Cを飛び越えて結合しない。
独立参照は96窓と480係数配置を検査する。fixture入力をbit表現で固定し、JSONデコード差を
相関演算の誤差と分けた。crate test用の部品であり、当時の実装範囲と残るlifecycle工程を
[相関窓の記録](../roadmap/temporal-dcc/m0-grouping-window.md)に保存する。

続くcrate test部品では、供給済みの固定したbirth／split／mergeキーに対する持続判定と競合処理を検査する。
現在の分率配分から支持量を計算し、付随するformer-parent handleはキー内に保持する。
欠測終端では持続をリセットし、成熟候補の受理ではtrajectoryと親groupの二重消費を防ぐ。
独立した有理数・辞書・集合ベースの参照と576終端で一致した。
最終bundleと前hop配分からのキー生成、必要なcross-pair全数の検査、group世代・容量の変更は未実装である。
[proposalの記録](../roadmap/temporal-dcc/m0-proposal-persistence.md)で、reducerの受理・部分的な負荷測定と
これらの残る責務を区別する。

続く数値producerで、complete-link bundleと前hop配分からのキー生成、キーごとのformer parent固定、
必要な全cross-pairの検査を持続・競合処理へ接続した。独立参照864終端と一致し、別の検査では
物理的支持を持つPearson窓からの入力も接続した。inventory上の発見として、親割当が変化する間に
複数bundleのsubsetを独立に保持すると、7親でも24個のmergeキーが生じ得る。
意図した21キー上限を守るため、9.2へ「全bundleを通じてunordered parent pairごとに一キーを保持する」
規則を補足した。有効な固定キーを優先し、新規開始時は最小cross相関が最大の候補、同点はmember handle順で選ぶ。
これは工学的な容量規則であり、実音源の回収を示さない。groupの世代・容量・退役と本番接続は
[producerの記録](../roadmap/temporal-dcc/m0-proposal-generation.md)に残る。

数値group lifecycleには配分のprepareとcommitを設け、現hopの重みを維持したまま次hop用の新世代を作る。
splitは二子の置き場を確保してから親をsupersedeし、観測退役とdormant slotの容量evictionを区別して記録する。
energy欠測で退役時間を進めず、回復時に退役したhandleを復活させない。
独立したslot／観測時計の参照768終端と一致し、別の検査では配分・物理的Pearson支持・候補生成・
group更新を接続した。本番のlow-energy尺度、local beamの初期化、episode／occurrence ledger全体の保存は
この検査範囲に含まれない。[lifecycleの記録](../roadmap/temporal-dcc/m0-group-lifecycle.md)に残る責務と
部分的な負荷測定の範囲を明示する。

連結した数値音響Frontendは、peak／rowの対応をridge、分率配分、相関、proposal、group更新まで保持する。
energy viewには更新前のgroup handleを付け、既知ゼロenergyを保持しつつゼロenergyだけのbundleを作らない。
未知のspectral shapeをgroupの既知無音へ変換しない。構成PCMの二bus検査で実NSGTを使い、
warmupとmono energy保存を確認したが、独立した音源回収・安定性assayではない。
模擬scanの異なるgridで更新頻度も異なったため、部分的な費用測定に音楽的・安定性の合格を付与しない。
本番接続、適合尺度、beam／context所有は[連結Frontendの記録](../roadmap/temporal-dcc/m0-acoustic-frontend.md)に残る。

数値Frontendに、配分済みgroup energyから登録10座標と四hop支持のaccentを抽出する処理を接続した。
event区間は中央hopを保持し、証拠のcutには全source窓と右側比較の利用可能時刻を含める。
更新前handle、superseded親のmask、単一の欠測区間によって、子groupや失われた音声へ差分を補作しない。
accentのfloor感度比較はdescriptor座標を変更しない。Python参照fixtureと構成PCMの実NSGT検査は、
この数値・因果的な範囲を支える。boundedなaccent配送、descriptor圧縮、beam／context所有、
本番接続と適合尺度は[raw featureの記録](../roadmap/temporal-dcc/m0-raw-features.md)に残る。

Rustの有界accent ledgerは、一度だけの順序付き配送と過去bankの保持を分ける。
通常expiryを先に行い、容量による欠落は密度windowをmaskする。累積creditは両方の削除後も保持する。
独立したsnapshotは後の更新で書き換えない。旧Python参照では同じIDの証拠窓を延ばすと二重creditが入り、
ID一致検査とevent／ID順序の制約で修正した。detectorと既存の特徴座標は不変である。
period pairの除去、coverage、group／context全体の所有と本番接続は
[accent ledgerの記録](../roadmap/temporal-dcc/m0-accent-ledger.md)に残る。

数値的な周期候補器は、原event終端の取得・association支持counterと32-byteのpair recordを使う。
slot再利用前に同じ保存済みf32寄与を減算し、登録cadenceでf64再構築を行い、共通のplateau規約でpeakを選ぶ。
寄与cacheが空なら加減算残差を消す。通常の有限fixtureは全bin参照と一致したが、
近接同点ではf32丸めによってP_bestが変わり、極小weightでは支持が失われた。知覚的安定性の合格へ読み替えない。
正規化二passと空cache clearを費用に含める。grouping・全負荷・本番接続の残務は
[period gridの記録](../roadmap/temporal-dcc/m0-period-grid.md)に残る。

検査内のgrouping inventoryは、この同じ周期bankを使う。整数候補は固定した予測境界の前後を探索し、
cyclic wordは連続区間に対してstep、二反復のduration、元の観測支持を個別に検査する。
stream時刻の共通cadenceで最大16件を保持し、飛ばした更新slot、元の支持時刻、容量不足を記録する。
有界な探索結果は独立全列挙fixtureと一致した。端点支持の総和では長いwordが整数候補全てを押し出す場合があり、
この順位規則の影響は下流評価に残す。beam／context所有、実音回収、全負荷の残務は
[grouping inventoryの記録](../roadmap/temporal-dcc/m0-grouping-inventory.md)に残る。

音響Frontendの数値経路は、再利用する七組の周期／grouping poolと独立したresidual ledgerを所有する。
各hopは更新前handleへcreditを付け、旧証拠frameを保存してから新世代へbindingを変える。
同一hopの容量交換でも旧countを保持し、欠測のbank失効と無音退役を分ける。
最初のinline配置で通常test stackがoverflowしたため、構築時にpoolをheap確保する配置へ変更した。
実NSGTのパルス／無音／一定振幅対照では、緩いsalience尺度は一定振幅にも目標周期を提案した。
既存testの基準尺度はこの小さな対照を区別したが、適合済みの知覚的回収とはしない。
長期occurrence／context所有、全fitと全負荷の残務は
[recurrence所有の記録](../roadmap/temporal-dcc/m0-recurrence-ownership.md)に残る。

全体のroadmapは`docs/roadmap/manifesto-alignment-and-beta.md`、時間構造DCCの
完成までの計画は`docs/roadmap/temporal-dcc-completion.md`に記録した。
9.3.55の設計レビューを終え、M0の基準保存と公開文言の照合に着手した。
T1–T7の課題・対照、特徴manifest、データ分割と判定の登録、数値preflightと模擬検査は
M0の残る作業である。先行文脈が異なる比較では局所音の一致を保持する。
続くM1で、既存の順序付き解析経路へ二領域の独立した受動認知を接続する。
以下は既存の実装・試聴の到達点であり、次の作業を短期予測へ限定するものではない。
現行版と指定seedによる比較では、拍、緊張と解放、曲全体の形を別々に判定する。
小節アクセントの生成は実装し、既定値は0とする。作者のBへの選好は、直前の案内に基づきseed 1への回答として記録した。
回答自体にseedの明示はない。この組のBは小節アクセント追加なしであり、Sample 08の現状と既定値0を維持する。
反復する強弱を聞き取れたかと、他のseedの選好は未判定である。同一領域におけるhabituationの回復は測定できたが、
自律的な閉ループはPARTIALを維持する。DCCの実用範囲の比較から、有益な非ゼロ強度は確定していない。
結果と判断境界は`docs/roadmap/stage2-evidence-2026-09-05.md`に記録する。修正後の結合式は
`tension_pressure = tension_level * coupling_strength`であり、Listenerのtensionに
含まれるresolvabilityを二度掛けない。

短期の反復・帯域エネルギーの期待と、経験文脈の記憶はhabitat側へ部分的に接続した。
presentation側の観測はListenerTwinが担う。境界・終止、持続する流れの同定、行動価値学習は未達である。
Scenarioは作者のマクロの演出を担い続ける。phraseや長編formには、
単にmeterを低速化するのではなく、それぞれの表現が必要になる。個人差、生体信号入力、
長時間の記憶には、それぞれ比較条件と受入条件を設ける。対応する機構と証拠が揃うまで、
これらの拡張を台帳上の「実装済み」へ変更しない。
