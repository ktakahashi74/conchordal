# 時間構造DCCの実装計画修正案

作成日: 2026-09-11
状態: 修正案。工程の正本へは未適用。M0は未完了、以下の実装作業は未着手。

## 1. 変更の目的と対象

今後の数値モデルはRustで実装し、そのコードを実験・性能測定・本体で共有する。
Python参照を本番相当まで高速化してからRustへ再実装する工程をやめ、
実音の観測から認知結果の配送までを早く接続する。独立検証と最終的な認知・音楽的判定は維持する。

対象は[完成計画](../temporal-dcc-completion.md)の実装言語、着手条件、作業順序と記録方法である。
[神経認知階層メモ](../../design-notes/dcc-neurocognitive-hierarchy.md)のP/M/A、T1–T7、
五つの階層間接続、全曲を含む完成範囲、[モデル交換境界](model-replacement.md)は引き継ぐ。
R/H/C、現在の参照数式・特徴・尺度・容量、既定offとair-gapを、この工程変更では変えない。
本案の採用だけで、M0・O項目・MR試験を完了へ変更しない。

2026-09-11の確認では、追加された研究コード群はPython 23ファイル・8,330行、
Pythonテスト18ファイル・7,905行、独立Rust照合器1ファイル・1,150行である。
これはコメント等を含む物理行数であり、全行の移植が必要という見積りではない。
予定した`src/temporal_cognition.rs`はまだ存在しない。
[M0台帳](m0.md)と[feasibility-preflight.json](feasibility-preflight.json)では、
全同時負荷は未検査、[feature-manifest.json](feature-manifest.json)も適合準備未完了である。

## 2. 現行工程からの変更

| 現行の進め方 | 修正案 | 品質と実装量への効果 |
|---|---|---|
| Pythonで数値・配送・所有・実時間性を作り込み、後にRustへ接続する | Rustを今後の実装正本とする。実験と本体は同じ関数・状態遷移を呼ぶ | 移植、二重保守、Python専用の高速化を減らし、本体の所有・期限の欠陥を早期に検出する |
| M0全体をM1着手の前提にする | M0の実装準備を確認してM1の技術実装を開始する。M0の収集前条件は別途満たす | 全モデルの実装準備をプロトタイプ側へ前倒しする負担を減らす |
| 参照実装の内部形式を配送経路へ持ち込む | モデル固有の表現を内部に閉じ、共通経路は意味・時刻・所有を扱う | モデル交換と不整合拒否を同じ本体経路で検査できる |
| 工程計画・設計メモ・日英technoteへ実験経過を重複転記する | 各文書の責務を分け、詳細結果は一つの実験記録へ保存する | 更新漏れと長い再読を減らし、過去の証拠と現行状態を識別しやすくする |

### Pythonと既存成果の扱い

- 検証済みのPython参照・入力・期待出力・失敗例・回収結果は、対応するsource hashとともに保存する。
  現在の作業ツリーと最後の検証済み版を分け、進行中のpacked scheduler／worker変更を昇格させない。
- Pythonには、統計・適合・回収実験・作図・結果集計と、小型の独立参照を残す。
  純粋な仮説探索はPythonでもよいが、実時間workerや本体状態の第二実装へ育てない。
- `temporal_matcher_kernel.rs`の数値算法を再利用する。CPython API、ctypesの配置検査、
  GIL管理、Python辞書の展開を本体へ移植しない。既存の橋渡しコードは比較資料として残す。
- Pythonの任意の型や例外発生順序の再現はRust本体の要件にしない。
  不正値の拒否、欠測、原時刻、世代、未知と既知ゼロの区別は意味上の契約として維持する。
- 元のf64計算・u64識別子を維持して移す。加算順序・丸め・正規化の違いを単なる言語差として流さない。
  同値性を保てない変更は理由・許容誤差を先に登録し、影響する判定をやり直す。
  f32化、候補削減、容量変更は、必要性を実測した後の別の変更とする。
- 既存テストを行単位でRustへ翻訳しない。数式、独立列挙、因果反例、配送・所有の契約を選び、
  重複するPython型／ABI検査は旧参照側へ残す。未移管の検証範囲は明示する。

## 3. M0と後続工程の依存変更

M0を完了へ呼び替えず、M0内の「実装準備」と「収集開始条件」を別欄で管理する。
M-IDは増やさない。「実装準備」は次を確認する途中の判定であり、新しい完成段階ではない。

1. O01の基準source・設定・比較入力と、検証済み／未検証変更を識別できる。
2. O02の実装と主張の対応、O03の共通契約・最初に使う数値仕様・交換境界が確認できる。
3. O09の最初の数値移植とMR1が通り、M1で検査する欠落・時刻・状態隔離のfixtureが定まっている。
4. O04・O05–O08・O11–O13の未達と担当、収集・適合・採用を止める条件が台帳に残っている。

この判定後にM1の受動経路へ接続する。Rust crate内に数値部品を置く作業はその準備として行える。
MR1の「本体組込み前」はruntimeの音声・状態経路へ接続する前を指す。
M0全体の完了は、既存の終了条件をすべて満たした後に判定する。
全座標や後続対照の登録を消さず、該当する模擬検査・収集・fitに必要なものをその開始前に固定する。

| 工程 | 採用時に変更する依存・作業順序 | 維持する終了条件 |
|---|---|---|
| M0 | Rust数値部品とMR1を実装準備として先に実施。残る登録・収集前条件はM1以降の技術実装と並行して満たす | 現行の比較・基準・収集前条件。準備成立だけでM0完了としない |
| M1 | 依存を「M0全体」から上記の実装準備へ変更。実音から順序付き観測と診断までを通す | 因果・欠落・時刻・二領域の状態隔離・report独立性。新しい生成作用はゼロ |
| M2・M3 | M1の観測契約を使い、利用可能な入力からRustで技術実装する。認知課題の採否と技術着手を分ける | T1/T2、T4/T6、必要な適合・対照・MR2。下位の未採用出力を採用済みとしない |
| M4・M5 | 必要な入出力が成立した部分から技術実装する。未接続の入力はunknownとして残す | T5–T7、長尺容量、帰還、全曲判断。全入力が揃った統合版を改めて適合・検証する |
| M6・M7 | 各機構の受動判定後に局所生成を接続し、五つの相互作用を統合する | 因果介入、可聴性、縮退回避、統合版の再適合・校正。技術実装だけで生成を有効化しない |
| M8・M9 | 計測器と不整合検査は必要な接続時に実装し、最終条件で再使用する | 実機・長時間の実時間性、全T行の四判定、作者採用、API・文書・air-gap |

O04は引き続きstage-1収集の開始条件である。最初の細い経路や照合器単体の成功では満たせない。
M1–M5の技術実装を進め、O04で要求する全処理を実装または規定の保守的な費用算入で覆った後に、
両bus・64 Voiceの想定／最大負荷を測る。未実装部分をゼロとして算入しない。
O04の数値preflightと、M8の実配送・鮮度・生成を含む最終受入は別判定のままとする。
O04が要求する帰結表・私有trace・64 Voiceの候補評価は、M6での採用を待たず、
合成係数・既存の身体からの入力で同じRust数値処理を実装して測る。
これらは負荷検査用と識別し、収集前の認知的な適合やliveの生成有効化を必要としない。
未適合を理由に全項をunknown／ゼロへ落とした負荷を使わず、現行O04の非ゼロ・最大候補条件を満たす。
人の収集を待っている間は、合成入力・既存素材・機械的検査による実装を進める。

### O01–O22の担当と停止条件の移管

IDと義務の内容を維持し、実装する工程と停止する行為を次のように分ける。
「移管」は完了の扱いを意味しない。各行の現行成果・未達はM0台帳等から引き継ぐ。

| ID | 作業を進める位置 | 依然として止める行為・判定 |
|---|---|---|
| O01 | M0の実装準備、変更時の基準保存 | 出所が不明な比較・旧成果の転用 |
| O02 | M0の実装準備、主張変更時の再監査 | 実装の裏付けがない現在形の能力主張 |
| O03 | M0で全対象と交換契約を登録し、M1–M7で使う仕様を順次具体化・固定 | 必要な座標・尺度・mask・対照等が未登録の模擬検査、収集、fit |
| O04 | M0所有のまま、M1–M5等で作る同じRust処理を使って全負荷preflight | stage-1収集。単体性能からの全体合格 |
| O05 | M0の収集準備 | 素材・分割・機関手続き・同意等が未確定の人の収集（pilotを含む） |
| O06 | M0の課題登録とM2–M5の特徴・刺激検査 | 対応する検査が不足した収集・freeze |
| O07 | M0で方法を登録し、development／pilotを経て規定時点に数値を固定 | 未登録の検証、対照・検出力が不足したcomponent freeze |
| O08 | M0で説明・foil・不明回答を登録、M7–M9で検証 | Gate 3の聴取検証 |
| O09 | M0で数値移植・MR1、M1–M2で共通境界・MR3、各モデル実装時に対応fixture | 該当する数値特徴の適合・解釈、未検査経路の採用 |
| O10 | M2–M5のモデルと感度比較 | 影響するT行のfreeze |
| O11 | M0の収集前回収、M3の実音尺度・記憶同定 | stage-1収集、episodeパラメータ採用・長期文脈の主張 |
| O12 | M0の手順・予測安定性、M2–M7の実入力・OOF・fit・校正 | 境界／type収集、未再適合モデルの採用、統合版・行動列のfreeze |
| O13 | M0の収集前回収、M3・M6の実身体・固定方策・trace適合 | stage-1収集／適合、私有trace採用・旧template置換 |
| O14 | M6・M8で両bus・反実仮想renderを検査 | ordinal生成列の有効化 |
| O15 | M6・M7の入力一致と状態介入の比較 | 因果的な関係効果の主張 |
| O16 | M5で30分replayと十分な容量の参照比較 | M5終了・M9検証着手 |
| O17 | 初期経路から計測器を使い、M8の全条件で最終測定 | 実時間採用 |
| O18 | M1で時刻を保持する経路、M8でT2実配送・消費の測定 | liveの期待・行動判定 |
| O19 | M7–M9、局所参加modeの採用判定後 | 旧templateの置換。off／受動・既定基準版の変更は認めない |
| O20 | M9の全T行・階層間接続・既存sample回帰 | 時間構造DCC全体の完了 |
| O21 | M1からmode隔離を検査し、M8–M9で作者API・通常運用・文書を完成 | 通常運用・API／文書完成・既定変更 |
| O22 | 本案の採用時に版と範囲を更新し、M9で完了監査 | 設計登録・実装・実証を混同した完了報告 |

## 4. 必要な構造変更と再利用

最初は既存crate内部の`src/temporal_cognition.rs`を入口とし、実際の責務が生じた子moduleへ分ける。
公開API・別crate・汎用trait群を先に作らない。研究用テストはcrate内部から同じ実装を呼び、
継続的に別processから使う必要が生じた時点で、オフラインの小さい入出力driverを設ける。
適合・replay用driverが状態更新を再実装することは避ける。

| 境界 | 最初に触る既存箇所 | 変更と検査の範囲 |
|---|---|---|
| 数値算法 | [Rust照合器](../../../scripts/temporal_matcher_kernel.rs) | Python非依存の処理を安全なRustの参照・slice・所有へ移す。既存の算法と小型独立参照を照合する |
| 実音の証拠 | [analysis worker](../../../src/core/analysis_worker.rs)、[AnalysisStream](../../../src/core/stream/analysis.rs) | 最新Landscapeへ集約される前に順序付き証拠を渡す。既存NSGTを共有し、support・available時刻・epoch・gapを保持する |
| 配送と状態 | [runtime](../../../src/runtime/mod.rs)の`wire_runtime`・`WorkerState`・`process_hop` | 有界入力・一所有者の更新・期限付き結果を接続。habitat／presentationの状態は別。満杯・切断・退役時も欠落を隠さず、音声callbackを待たせない |
| 消費者 | [ListenerTwin](../../../src/listener_twin/mod.rs)、後に[参加処理](../../../src/life/temporal_participation.rs) | 由来付き要約を消費し、DTW・beam・codecへ依存しない。reportの有無で認知更新を変えない |

既存の`wire_runtime`によるGUI／headless／分離rendererの共通配線と、`process_hop`の分割を使う。
ファイルが長いという理由だけの`runtime`、`Community`、`Voice`全面再構成は先行させない。
変更が必要な責務を抽出し、機械的な移動と挙動変更を判別できる差分にする。
共有所有や固定poolの選択は実際の寿命・コピー・負荷で決め、Pythonのbyte poolを無条件に複製しない。
格納形式を変更した場合は版を分け、元の意味・容量制約を保った上で物理メモリ・コピーを再測定する。

## 5. 採用後の実行単位

下表は作業の順序であり、新しいM段階ではない。各単位を実装・検証・短い結果記録まで閉じる。
W1とW2を最初の費用計測単位とし、その結果から後続の実装量と工数を見直す。

| 単位 | 成果物・主な対象 | 終了条件・次へ渡すもの |
|---|---|---|
| W0 基準と工程の確定 | 本案の正本反映、O01のsource／fixture一覧、旧参照と未検証差分の区別 | M/O対応に欠落がなく、再利用する検証済みsourceと入力が辿れる。Python worker高速化は後続作業から外す |
| W1 Rust数値部品 | `temporal_cognition`内の照合処理。現行Rust算法の再利用とPython参照の選択的照合 | 元時刻・mask・世代・f64結果・経路を検査。小型全列挙と過去の反例が通る。実際のRust呼出しと資源測定を再現できる |
| W2 MR1 | 順序付き照合と`memory_orderless`、共通controller・配送・契約検査用consumer | 同一sourceの共通経路で交換できる。順序反例、欠測、遅延、退役を通し、誤採用ゼロ。対照の認知的採用は主張しない |
| W3 M1受動経路 | 実音→既存NSGT→順序付き観測→認知状態→診断。最初の本体境界でMR3 | 二bus隔離、epoch再開、queue欠落、EOF、遅延拒否、report独立性。最初は未実装の認知出力をunknownとし、生成作用ゼロ、off基準の一致を検査する |
| W4 数値実装とM0残条件 | M2–M5の音群・到来・記憶・phrase・section、O04に必要な帰結表・trace・候補評価、全特徴・fit表、収集準備 | 各モデルの入力と独立検査を順次満たす。全負荷・回収・課題等が揃って初めて該当収集へ進む。未校正の出力は研究用と識別し、生成作用を有効化しない |
| W5 認知検証からM6–M9 | 四段階の適合・校正、受動判定、局所生成、五接続、全体受入 | 現行T1–T7×四判定と実時間・長尺・作者採用をすべて満たす |

W3の初期経路は完成した「受動mode」ではない。正本が要求するbank・帰結表・private traceの
未実装範囲を残し、それらを備える前にmode全体の完成を宣言しない。
MR3は最初の利用可能な境界で始め、fit・保存形式の実際の利用先ができるたびに対象を追加する。
ダミー係数の拒否だけで最終モデルの版隔離を合格にしない。
W4の最初は実音の観測と直結する音群・accentを優先し、W1の検索部品を実観測へ接続する。
全階層を先に作り込まず、同じ音から要約まで到達する範囲を段階的に広げる。
将来の相互作用を単純な直列モデルへ固定せず、登録した更新所有者と前回文脈のfeedbackを維持する。

## 6. 品質を落とさずに検証とトークンを減らす方法

### 残す検証と早期の停止条件

数値実装、認知課題、可聴性、作者採用は別々に判定する。Rustへの移行が直接改善するのは、
実験結果の本体への適用性と、所有・配送・実時間の検査時期である。音楽性は従来の課題で判定する。

- **独立性**: 小型全列挙・手計算・解析式と既存Python参照を使う。本体関数を呼んで生成した期待値だけにしない。
- **因果・所有**: 未来の支持、旧epoch／世代、遅延、改訂、二重受領、queue飽和、drop・終了後の所有を検査する。
  off／受動で新しい行動圧力が出た場合や、ラベル・私有状態の漏洩があれば、その経路で止める。
- **資源**: releaseで、入力生成・コピー・queue待ち・計算・消費を識別して測る。
  部品のp99を足して全体のp99とせず、O04/O17の条件で全体を実測する。容量削減で検査を通さない。
- **統計**: 変更出力に依存するOOF入力・fit・校正を再生成し、再利用する枝には根拠を記録する。
  既存の回収成功を新しい実音尺度や身体へ自動転用しない。
- **手戻りの制限**: 一つの経路で予算を超えたら、その実装をprofileし、原因と次の比較を一つ定める。
  Python側の同等経路を新設して測り直す工程へ戻らない。模式入力の成功だけで下流の採用へ進めない。

各作業では変更に対応する検査を先に行い、成果物を閉じる時点でリポジトリ必須のRust検査を行う。
`src/`変更時の全文Cargo testログ・同一shellでの終了状態記録、fmt・clippy・all-targets確認は省略しない。
通過後に追加変更や未解決の疑義がなければ、同じ全検査を理由なく繰り返さない。
Python側を変更しない作業では、関連する固定参照の比較を使い、無関係なPython全体検査を増やさない。

### 記録と再開

| 記録 | 採用後に置く内容 |
|---|---|
| 設計メモ | 認知的意味・式・不変条件・根拠と限界。性能改善の実行経過はリンクにする |
| 完成計画 | 工程、依存、終了条件、O-ID対応。過去の最適化ログを追記しない |
| M0等の段階台帳 | 現在の状態・未達・次の一単位と証拠へのリンク。結果全文を複製しない |
| 実験記録 | 固定source・入力・コマンド・出力・判定・資源測定を一か所へ保存。失敗と旧版も保持する |
| 日英technote | Manifestoとの対応、方式・実装状況・新しい第一原理上の知見。細かなtimingの履歴は参照へ移す |

既存の長い記録は削除せず保存し、採用後の更新からこの運用へ移る。
文書版と数式・特徴・適合・保存形式の版を識別する。アーキテクチャの説明変更だけで実験を再実行せず、
過去の結果を新sourceへ付け替えない。複数台帳への現行hash反映は、一つの更新処理と整合検査で扱う。
各実装単位の開始時に読むのは、短い現在の台帳、対象仕様、対象コード、対応fixtureを中心とする。
新しい依存や矛盾を発見した場合にだけ関連資料へ広げる。

削減率・残りトークン数は未計測であり、本案では数値を約束しない。
W1–W2では、追加／削除コード量、維持する参照の数、修正往復、実行時間と、取得可能な使用量を記録する。
トークンが取得できる場合は入力・キャッシュ入力・出力を区別し、取得できなければ未計測とする。
終了条件を通した単位の費用を使って残作業を再見積りし、人の回答待ちや全曲試聴時間は別枠にする。
削減が確認できない場合は、重複している実装・記録を見直す。検証基準を下げて削減率を作らない。

## 7. 採用時に正本へ反映する差分

本案は、以下を一つの工程改訂として適用できる形で提示する。適用するまで現行正本の規定を上書きしない。

1. 完成計画の§3にRust正本と再利用範囲、§4にM0準備からの着手と技術実装／採否の区別、
   §4.1に上記O対応、§6に共通実装による検査、§7にW0からの再開位置を反映する。
2. 設計メモの§8–8.1と§10.4の担当・停止条件を同期する。数値参照§9–10.3の変更は工程改訂に混ぜない。
3. M0台帳の冒頭に実装準備と収集開始条件の状態を分けて置き、再開順序をW0–W3へ変更する。
   既存の未達、未検証変更、過去の結果と版の対応は保持する。
4. モデル交換記録へRustでのMR1とruntime接続前の境界を明記し、MR2・MR3の担当を保持する。
   登録JSONは文書参照が変わる項目だけ更新し、数値結果・fit・feature版29は自動更新しない。
5. レビュー記録と日英technoteへ工程変更と実装状態を記録する。新しい外部レビューや検証済み能力とは扱わない。

本案の採用とW0の反映後、最初の実装対象はW1のRust照合部品である。
W1–W2の結果をもってM0の実装準備を再判定し、M1の受動経路へ進む。
