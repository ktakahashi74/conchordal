# M0: 比較条件と基準の固定

状態: 作業中。人のデータ収集およびM1への移行は未承認・未開始。

今回の保存範囲と検証の限界は[2026-09-11の停止地点](../checkpoint-2026-09-11.md)を参照する。

[実装計画修正案](implementation-plan-revision.md)を作成した。Rustで数値部品・MR1を先行し、
M0の実装準備と収集開始条件を分ける案である。正本へは未適用であり、M0の未達条件と以下の記録は維持する。

工程と終了条件の正本は[完成計画](../temporal-dcc-completion.md)である。
設計の正本は[神経認知階層メモ](../../design-notes/dcc-neurocognitive-hierarchy.md)。
開始時のメモのSHA256は
`8b88ebcc987e6a9b4a8923f68748469c544387b2ba38c1e1d47f094122d681d9`。
本記録は終了条件を変更しない。和声構造のDCCと現在の演奏経路には変更を加えていない。
episode回収の失敗を受け、補助queryを追加した時点のメモのSHA256は
`8eee217e943a91bee158aaef2601eb836441945925b403666ae5e71abddcc029`。
私有binの保持質量と観測尤度の問題を明示した時点のSHA256は
`df2787e8a6b2e44b2831d049796c62f6c6824a68f81e1a738d0f22096bcaeedf`。
共同観測則を明示した時点のSHA256は
`1f87855f5868037995cf7e4c40ebf248803976e5513512e524afe97e373b142f`。
82共変量と切片、支持の和集合・差分mask・検索の古さを明確化した時点のSHA256は
`e04e41581b5b27e3943d6accad15fd9e910f6f0f210fa139b9fbeb9beb6c5343`。
前版を保存し、私有traceとepisodeの節がbyte単位で同一と照合した。
重複spanの支持配分と640-byte保存の数値精度を明確化した時点のSHA256は
`e00c1c71ea2346a62180f5989233c6c79f987cd11a168f80827bf0131a5dab40`。
この変更も9.5節に限り、それ以前と9.6節以降の本文がbyte単位で同一と確認した。
四hopのaccent支持と遅延配送を明確化した時点のSHA256は
`adb1ba89ce87642018f94bdf0af2acd0900f9302dc099cfd2eacfea45c7659e0`。
改訂は9.2／9.5節に限り、episodeと私有traceの回収式・結果は変更していない。
有限入力のoccurrence確定と支持・改訂規約を明確化した時点のSHA256は
`5f60772cb8d366e5875cd354c84d547193f32a01621091116cda6725cb84ea85`。
変更は9.3／9.5節に限り、以前の回収式・結果は変更していない。
順序照合のcell規約、保持中のprefixコピーと確定episodeの時刻を明確化した時点のSHA256は
`15cca0bb510b7e53e608627e57b8977d956b95af37f09b99fc5dc36bbb1e9e56`。
この変更は9.3節に限り、その他の節と以前の回収式・結果は変更していない。
有界query schedulerとcache、元の全音声支持・世代・cache実到着時刻の確定cutを明確化した時点のSHA256は
`0d59c5aea2142c6731b25e6645abcb95f4b601ed813e6f8a88c1b46c51b6cb52`。
共通gap clock、記憶保持・干渉率と再認用コピーの因果cutを明確化した時点のSHA256は
`2e6e41da18c3dd3ba1c862f46d7a68bba430cc325ec6ecd68e1818ce46c56f14`。
固定bank／edge、非再利用handle、退避とbank実到着cutを明確化した時点のSHA256は
`35cf3cebe7206fffa7b431c6e874ac4d0ec231281cb559e256f678360352536a`。
128-byte endpoint queue、固定索引、期限・配送・確認応答を明確化した時点のSHA256は
`475714f465dbc5d3e9e4d409d877b779675688d6a81d2bcca195867d54ee54d5`。
固定byte pool・元ID発行・queueの所有／解放と個別コピーの容量不足を明確化した時点のSHA256は
`ed8772974493851532f70e204e18db7db2cd789d51d1e0fb4e573b51baab3d02`。
記述子の構造共有と型別pool配送、固定経路での条件付き容量を明確化した時点のSHA256は
`7831bd112d9deadd3675fd09596d96f413b5a852322506b91950ec0a3d3aa77c`。
確定writeの実際のbank／section配送、受領確認と最初の処理の予算超過を明確化した時点のSHA256は
`e2f06bc3d16c05d6968b54c19be010bd4e9b75896f0dbc10c261b0cbdf8cd33f`。
変更不能のconsumer入力と分割検査、最大bank登録の処理費用を明確化した時点のSHA256は
`c9fc03a6204727fbc4bae6076041ece81e79ecb974de40abbcd4f7ff41ffa0bb`。
コンパクトな登録入力とbank準備の分割、最大headerの残る超過を明確化した時点のSHA256は
`701771820b7542b2ecda2e5d456e9001c5cbf0a0384651dd7725e564b138b7fe`。
headerの共有復号と候補の分割検査、最大同時inventoryの配送を明確化した時点のSHA256は
`d1783c0f060c54dd38b03bbbba2fb50f14c84e3e8b7411020b80b6643f16fadf`。
実時計の到着・配送・公開遅延と処理量不足を明確化した時点のSHA256は
`4097a4f651dba073f4848bf25fa9a2ff90e936584f481ae68dc43ba625c7d4ec`。
R1／S1固定codecと同一到着条件での改善・未達を明確化した時点のSHA256は
`fea51b989b51ca6e25150af98d3c3e19420bdd21dbfbab949b1a918d366096e3`。
section準備の再利用・観測保持・固定容量の増分と残る遅延を明確化した時点のSHA256は
`901e19db720121ec7f27cd568a97bbf39af91d65eac01d791b39c0f877cdcc96`。
この時点の設計メモ改訂は9.3節に限る。その他の節と以前の回収結果は変更していない。
続くf64ブロック一括化は参照実装だけの変更であり、設計メモはこのSHA256のまま維持する。
モデル交換境界を8.1節とO03・O09・O12へ追加した現在のSHA256は
`afab484d8958cc595fe167f05a7d16edf79db4cbd857392ff60e8dc74ed6bd85`。
参照数式を含む9節〜10.3節は直前版と同一であり、feature版29を維持する。
過去のFableレビュー結果は開始時の版にだけ対応する。

## 完了した準備と残る条件

| ID | 現在の成果物 | 残る条件 |
|---|---|---|
| O01 | [baseline.json](baseline.json)。開始時の実作業ツリー、設定、既存の比較音・作者回答を内容ハッシュ付きで保存。1038項目をアーカイブと照合 | 現在のsourceと過去の音源は別々に識別する。過去の作者回答を新しい版・全seedへの評価に拡張しない |
| O02 | [manifesto-audit.json](manifesto-audit.json)。日英の18段落対と四番号条項を現行sourceと照合。Manifestoとtechnoteを修正し、ローカルサイトをビルド | 公開サイトへの配信は未実施。source変更時は対応する主張を再監査する |
| O03 | [feature-manifest.json](feature-manifest.json)。phrase26、section記述子39とhead82、recent-only41／elapsed-only2、共通の観測6記述子を登録。section支持の配分・有界集約・640-byte保存に加え、四hopのaccent検出・原支持cutと有限入力のoccurrence確定、10値のraw記述子・有界圧縮、保持中のquery・coarse／DTW照合と確定episodeへの接続、共通gap clock・strength／availability・再認用コピー、固定bank／edge・handle・退避と128-byte endpoint queue、不変byte poolと所有／解放、記述子の構造共有と確定writeのbank／sectionへの配送・受領確認、変更不能の入力packet、分割事前検査、コンパクトなbank入力と準備／公開の分離も参照化。[交換境界](model-replacement.md)と版対応を登録 | 他の全入力・提案・相互作用・容量と感度比較の一覧、上流の全beam活動・帰属生成、本番での有界確定・stream統合、実際のdevelopment尺度、fit版の固定 |
| O04 | [feasibility-preflight.json](feasibility-preflight.json)。[native packed入力](matcher-kernel-feasibility.md)はコピー込み全query中央値5.718／7.831 ms。8条件の2,400測定で40 ms超過なし。固定buffer7,001,928 bytes／caller、元blob10,526,720 bytesは別。既存Python参照と演奏経路は維持。generic codecのschemaキー・復号表を再利用。同一最大到着構成の処理量は直前版から2.8%減、期限後遅延約0.227〜1.509秒。少数構成は9.8%減。consumer固定bufferは1,474,816 bytes／bus。共通符号列等2,053 bytesとPython overheadは別に記録。全256件×128 knotの登録は約153／153 ms | native照合を含む実dispatch／receipt・実生成入力・全workerのp99とheadroom、待ち行列、最大処理周期47.001 msと40 ms処理窓の超過、率履歴overflow、完全なkernel・容量／copy／allocator／RSS、実出力機器・両worker・64 Voiceの同時負荷とheadroomを解決する。有限な合成入力の改善だけでは合格にしない |
| O05 | 人の収集は無効。実施機関と既存の倫理審査・手続き判断の記録をユーザーに確認中 | 素材と実波形、全分割、同意、謝礼、回答管理、機関の承認または手続き判断。回答待ちでも独立した数値準備は進める |
| O06 | Gate 3のT1–T7説明と日英文案に加え、[再認課題](recognition-instrument.json)の長さ別文言・手順・pilot基準を登録 | その他のGate 2課題・写像、提示／解析の波形対応、診断専用grouping、対応する数値検査とpilot |
| O07 | Gate 3の対照方向と共同成功条件、[主課題への移転比較](control-comparisons.json)の方法と仮の実用差を登録 | 他の全必須対照、実測developmentに基づく最終実用差・分割・検出力 |
| O08 | [relation-instrument.json](relation-instrument.json)。三説明・不明回答・分母・割当と解析方法を事前登録 | 刺激の成立、言語pilot、検出力を満たす最終募集数の凍結。文案の登録は聴取の成立を意味しない |
| O09 | f64参照は一般58検査・section43検査・accent20検査・occurrence27検査・descriptor30検査・matcher33検査・query scheduler22検査・記憶保持27検査・bank30検査・endpoint13検査・payload16検査・共有記述子15検査・consumer25検査・入力packet29検査の計388検査。重複支持・有界集約・39→82入力に加え、原支持cut、期限で一度だけの確定、metadata改訂、原時刻の維持、54条件の有界記述子圧縮、全経路列挙との照合、検索結果から確定ledgerへの接続、sample cadence・有界cache・世代／遅延結果の隔離、原時刻の記憶更新、162条件の独立式、未来clockからの過去コピー禁止、256枠の保存・退避、membershipの欠落保持とbank実到着cut、65,536 endpointの期限順処理と再利用、型別の参照保持・改訂・解放と50本の独立byte／所有照合、実圧縮器の80版と共有記述子のbyte一致・解放、実際のbank／sectionへの一度だけの適用と三段階の応答欠落からの復旧、入力packetの改変隔離と全入力検査後の配送、500記録の型・byteの往復を確認 | 小型モデル交換MR1、最初の本体境界でのMR3、ridge／group／epoch restart全体／ラベル隔離／順序照合／joint／帰結表／行動footprint等の全該当fixture。全beamの帰属・活動生成、有界な本番ledgerと現行Rustとの一致は未検査 |
| O11 | v1の不合格を保存し、[回収設計v2](episode-recovery-design-v2.json)で全324点×100組が合格。[parameter-recovery.json](parameter-recovery.json)へ全誤差・費用・図を保存。補助queryを仕様へ反映 | 42,240回答・3,840人相当の候補割当の実波形と人的実現可能性、長さ別pilot、実際の音響尺度での回収再検査、主課題への移転 |
| O12 | phrase26とsection82、sectionの二つの対照を登録。[最初の境界・有界gap積分](event-head-reference.json)、[sectionの有界集約](section-feature-reference.json)、[accentの原支持cutと配送](accent-reference.json)、[occurrence確定](occurrence-reference.json)を独立の列挙・積分・手計算・反例と照合。交換時の依存・再適合手順を登録 | 上流帰属・確定と実波形のstream接続、全生成vector・対象数・区間打切りを用いた100組／条件の予測安定性、有限fit表・予算・OOF・校正手順。gapの未解決率と本番への接続も必要。数値部品だけでは合格にならない |
| O13 | [私有traceの参照](private-trace-reference.json)へ保持質量・共同観測則を実装。[v9](private-recovery-design-v9.json)は全108条件点×100組で基準を満たした。失敗4 fitも無限大誤差で保持。v1–v5／v7の不合格、v6／v8の割当不採用も保存 | 実際の身体・波形・機会窓・収録時刻の成立、再演費用、固定方策の実行結果への適合。支持が十分な初期接触を含む条件付き回収であり、既存演奏経路との接続も別途必要 |

O10以降の後続工程の項目も、M0が引き受ける対照・感度比較の事前登録から外さない。
人の回答、認知的な妥当性、実時間性能、作者による採用はそれぞれ別の証拠を必要とする。

## モデル交換境界の登録

[モデル交換境界](model-replacement.md)をO03・O09・O12へ登録した。
観測・出力・実行・行動の意味を安定契約とし、descriptor・DTW・beam・回帰係数・保存形式を
モデル内へ閉じる。音群、拍・到来、関係記憶、phrase／section、共有推論、評定、個体の参加を
交換単位として整理し、密結合する部分は一組で交換する。

| 項目 | 状態 |
|---|---|
| エビデンスの区別、交換単位、依存方向、版・再適合範囲 | 設計と比較手順を登録 |
| MR1: 小型bankで順序付き照合／`memory_orderless`を同じcontrollerへ接続 | 未実施。M0内で本体組込み前に確認する |
| MR2: 振動子／到来モデルの比較 | 手順登録。M2で実施 |
| MR3: モデル・特徴・係数・保存形式の不整合と旧結果の拒否 | 手順登録。M1–M2の最初の本体境界で実施 |

この文書改訂はfeature版29の数式・入力配列・容量を変更しない。
過去のPython/Rust数値一致は同じモデルの実装交換に対応し、MR1の代わりにはならない。
進行中のpacked scheduler／worker接続の未検証変更は、この登録の検証済み成果に含めない。
M0全体の終了条件と、人の収集・各T行の判定は引き続き未達である。

## 保存した基準

開始時のcommitは`ec67b50c303d930a3d4c810333e3742f111c6ac8`。
commitだけでなく、未追跡の研究ファイルと選択したignored音源も保存した。
`.claude/`と`.codex`はローカルのagent設定として除外した。

初期の`target/temporal-dcc/m0-baseline-20260910/baseline.tar.gz`を変更せず、
後からrootのignored `config.toml`を追加した`baseline-with-config.tar.gz`を作成した。
設定の取得時刻と理由はmanifestに残した。この間に演奏経路の変更はない。
大きいアーカイブはGitの対象外であり、別checkoutへの引継ぎではmanifestだけでなく実体も必要になる。

既存の「縮退してない」はsample 08 seed 21の比較への回答として保存している。
回答自体にseedや時刻の再指定はない。「流れは損なわれていない」も回答にないA/Bやseedを補わない。
これらはT1–T7全体や長い時間構造の知覚の証拠ではない。

## 実行済みの検証

- 基準保存4検査、一般f64参照58検査、section33検査、accent20検査、occurrence24検査、descriptor29検査、matcher33検査、query scheduler22検査、記憶保持27検査、bank18検査、endpoint13検査、payload16検査、共有記述子15検査、episode回収・割当11検査、私有回収・割当14検査が通過。
- Pythonの既存評価検査を含む748検査が30.359秒で通過。最新ログは`target/temporal-dcc/m0-python-tests-shared-descriptor-final-20260911.log`、終了値0は同directoryの`m0-python-tests-shared-descriptor-final-status.txt`へ保存。
- 今回もRust全体を検査し、746件通過・12件ignore・失敗0。ログと終了値0は`m0-rust-tests-shared-descriptor-final-20260911.log`と`m0-rust-tests-shared-descriptor-final-status.txt`へ保存した。Rust sourceの変更や、参照との本番一致を示すものではない。
- `RUST_BACKTRACE=1 cargo test -- --nocapture`が終了値0。`test_report.txt`と`test_status.txt`へ保存。
- `/snap/zola/1447/bin/zola --root web build --output-dir /home/shafi/lwrk/conchordal/target/temporal-dcc/m0-site-20260910`が通過。
  生成HTMLの四条項と、監査対象sourceの17ハッシュ・18段落対の位置を確認した。
- 回収結果の追記後もサイトをビルドした。出力は`target/temporal-dcc/m0-site-recovery-20260910`。
  生成HTMLの結果記述と、引き続き一致する17のsourceハッシュを確認した。
- v2と私有traceの所見の追記後もビルドが通過。出力は`target/temporal-dcc/m0-site-recovery-v2-20260911`。
  日英の結果記述、監査の17 sourceハッシュ、5つの現行仕様参照、v1／v2の全成果物ハッシュを確認した。
- stateful traceと観測尤度の監査の追記後もビルドが通過。出力は`target/temporal-dcc/m0-site-trace-20260911`。
  日英HTMLの32検査と偏りの記述、17 sourceハッシュ、6つの現行仕様参照、参照sourceのハッシュを照合した。
- 私有回収v5と割当探索v6の結果追記後もサイト生成が通過。出力は`target/temporal-dcc/m0-site-private-recovery-20260911`。
  日英HTMLの106／108点、割当選択なし、39座標の記述を確認した。17 sourceハッシュ、5つの現行仕様参照、
  私有回収v1–v5とv6探索の全登録・source・結果ハッシュを照合した。`git diff --check`も通過。
- v7とsection82入力の追記後もサイト生成が通過。出力は`target/temporal-dcc/m0-site-private-v7-section-20260911`。
  日英HTMLの104／108点、166自由係数、支持区間の和集合の記述を確認。17 sourceハッシュ、5つの現行仕様参照、
  全6回収版とv6／v7探索、section仕様改訂前後のハッシュを照合した。回収図も確認した。
  26／39／82／41／2入力の長さ・順序・参照と係数数を照合し、蓄積上限を10,000回の逐次更新でも確認した。
  `git diff --check`が通過。
- `python3 scripts/capture_temporal_dcc_baseline.py verify docs/roadmap/temporal-dcc/baseline.json`で1038項目が一致。
- v8／v9と最初の境界の参照を追記後、`m0-site-private-v9-event-20260911`へサイト生成が通過。
  日英HTMLの全108条件点・4失敗fit・51参照検査と23,794 probeを確認した。全7回収版とv6–v9探索の
  成果物ハッシュ、現行参照と保存版、17の監査sourceハッシュを照合した。v9保存参照の全既存関数・classは
  現行版とASTで一致し、後から追加した観測hop積分だけが差分だった。全JSONの構文と`git diff --check`も通過。
- section入力部品の追記を確認し、`m0-site-section-inputs-20260911`へのサイト生成が通過した。
  六layoutの長さ・重複のないID、section参照のsource／仕様／保存版ハッシュと全JSON構文を照合した。
- 有界gap積分の追記後、`m0-site-gap-20260911`へのサイト生成が通過した。日英HTMLの600条件、
  416受理／184未解決、58参照検査と未完のring条件を確認した。四組の保存版、現行参照、
  仕様と17の監査sourceハッシュ、全JSON構文、六layoutを照合した。
  結果は`target/temporal-dcc/m0-gap-verification-20260911.json`に保存した。
- sectionの有界集約を追記後、`m0-site-section-ring-20260911`へのサイト生成が通過した。
  日英HTMLの33検査、640-byte記録、3,200 bytesと未完の上流条件を確認した。
  七つの現行仕様参照、17の監査sourceハッシュ、二組のsection保存版、全JSON構文と六layoutも照合した。
  検証記録は`target/temporal-dcc/m0-section-ring-verification-20260911.json`に保存した。
- accentの原支持cutと累積配送を追加後、`m0-site-accent-20260911`へのサイト生成が通過した。
  日英HTMLの新規20検査・計111数値検査、slot 15、未完のpacket順序回復を確認した。
  八つの現行仕様参照、七つの保存版参照、17の監査sourceハッシュ、35 JSONと六layoutも照合した。
  メモの変更が9.2／9.5節だけであり、episodeと私有traceを含む他の節がbyte単位で同一と確認した。
  記録は`target/temporal-dcc/m0-accent-verification-20260911.json`に保存した。
- occurrence確定参照の追記後、`m0-site-occurrence-20260911`へのサイト生成が通過した。
  日英HTMLの追加21検査・計132検査、128-byte本番配置とmemory clockが未完という境界を確認した。
  九つの現行仕様参照、八つの保存版参照、17の監査sourceハッシュ、36 JSONと六layoutも一致した。
  メモの変更が9.3／9.5節だけであり、既存の一般・section・accent参照sourceは変更していないと照合した。
  検証結果は`target/temporal-dcc/m0-occurrence-verification-20260911.json`へ保存した。

- descriptor参照の追記後、`m0-site-descriptor-final-20260911`への日英サイト生成が通過した。
  41,920 bytes、85,852,160 bytes、8.53e-14、計161検査と未完条件を両HTMLで確認した。
  十の現行仕様参照、九組の保存版、17の監査sourceハッシュ、37 JSONと七layoutを照合した。
  既存四つの数値参照sourceと設計メモは変更していない。
  記録は`target/temporal-dcc/m0-descriptor-verification-20260911.json`へ保存した。

- matcher参照の追記後、`m0-site-matcher-final-20260911`へ日英サイトを生成した。
  計193検査、179,392 DP cell、-0.1875 octaveの検索反例と40 ms予算超過を両HTMLで確認した。
  十二の現行仕様参照、十一組の保存版参照、17の監査sourceハッシュ、39 JSONと七layoutを照合した。
  メモの変更は9.3節だけ。新しい音声終端trackerを除けばdescriptorのASTは前版と同一で、既存四参照sourceも変更していない。
  記録は`target/temporal-dcc/m0-matcher-verification-20260911.json`へ保存した。

- query schedulerの固定buffer・世代・cache実到着cutを追加後、Python659検査、Rust746件通過・12件ignoreを確認した。
  source・仕様・31ファイルの最終保存版と、100本の独立cache oracle、九つの仮想scheduleは
  `target/temporal-dcc/m0-query-scheduler-reference-final-20260911/`へ保存した。
  直接のcoarse adapterにもreceived_atを必須化し、完了と配送が別時刻の場合を検査した。前版の27ファイルも変更せず保持した。
  実workerの性能や人のGateはこれらの数値検査から導かない。
  `m0-site-query-scheduler-final-20260911`へ日英サイトを生成し、219検査と819,848 bytes、未完条件を両HTMLで確認した。
  十三の現行仕様参照、保存版の全ハッシュ、17監査source、40 JSON・七layoutも照合した。
  検証結果は`target/temporal-dcc/m0-query-scheduler-verification-20260911.json`へ保存した。

- 記憶保持・共通gap clockを追加後、Python686検査、Rust746件通過・12件ignoreを確認した。
  原時刻の反復更新、取得欠測とcoarse支持の不明分、干渉率の上限超過・保存overflowを分ける。
  未来の取得clockを使った過去の再認用コピーを拒否し、query前の凍結コピーは経過時間だけを進める。
  27部品検査、162条件の閉じた式との照合、九つの率履歴容量比較と旧反例を
  `target/temporal-dcc/m0-memory-clock-reference-20260911/`の32ファイルへ保存した。
  162条件の最大絶対差は2.7755575615628914e-17。保存一式のみでも27検査が通過した。
  `m0-site-memory-clock-final-20260911`へ日英サイトを生成し、246数値検査、446,720 bytes／bus、
  6,176-byte再認用コピーと未達条件を両HTMLで確認した。固定配置のうち65,536 bytesはbankへ計上済みである。
  十四の現行仕様参照、二十組の保存版参照、17監査source、41 JSON・七layoutを照合した。
  メモ変更は9.3節だけで、既存七つの数値参照sourceは前回保存版と同一だった。
  検証結果は`target/temporal-dcc/m0-memory-clock-verification-20260911.json`に保存した。
  144行の率履歴は最大endpoint負荷を保証せず、Python更新も競合支持・expをepisodeごとに再計算する。
  これらの解決、全bank／edge、有界ledger、完全なO04・O12・人の判定を残す。M0完了やM1開始とは扱わない。

- 固定bank／edgeの追加後、Python704検査、Rust746件通過・12件ignoreを確認した。
  非再利用handle、最初の凍結記述子、共通16-edge上限、欠落分を分母へ残すcontext membershipを実装した。
  bank実到着を上流ledger配送へ遡らせる反例を保存して修正し、順序リンクは期限までに利用可能な以前の対象を要求する。
  18検査、独立した30本×20書込みのmembership照合、全256枠×128 knot・32 anchorの保存を確認した。
  容量退避で255リンクが未取得となり、slot再利用でも旧handleへ戻らない。
  `target/temporal-dcc/m0-bank-reference-20260911/`の36ファイルにsource・設計前後・反例・容量検査を保存した。
  保存一式だけでも18検査が通過し、`m0-bank-archive-tests-20260911.log`と終了値を保存した。
  bankは11,862,016 bytes、追加のedge scratch・記憶状態等を含む固定部分は既定取得配置で12,769,616 bytes／busとなる。
  容量fixtureの取得clockは100 Hz・10 sampleであり、48 kHzの実出力性能は検査していない。
  `m0-site-bank-final-20260911`へ日英サイトを生成し、264数値検査と容量・未達条件を両HTMLで確認した。
  十五の現行仕様参照、二十一組の保存版参照、17監査source、42 JSON・七layoutを照合した。
  メモ変更は9.3節のみ、既存八つの数値参照sourceは前回保存版と同一だった。
  検証結果は`target/temporal-dcc/m0-bank-verification-20260911.json`に保存した。
  このcontainerは上流の確定joint支持と対応候補を受け取る。関係・系譜・context遷移の推論、全有界ledger／worker、
  packed anchorの実行経路、容量感度、完全なO04・O12・人の判定は残る。M0完了とは扱わない。

- 128-byte endpoint queueを追加後、Python717検査、Rust746件通過・12件ignoreを確認した。
  固定heap／空きslot／二ID索引、元の期限と実配送、処理中の固定、ticketを照合する確認応答を実装した。
  13検査と60本の独立した期限順照合を通し、全65,536件を六回の10,240件と一回の4,096件に分けて処理した。
  容量超過、古いendpointの再配送、slot再利用後の古いticketを拒否し、新ticketは65,537となった。
  これは成功応答を明示的に与えたheader検査であり、推論されたwriteの適用や実時間性能の証拠ではない。
  `target/temporal-dcc/m0-endpoint-reference-20260911/`の33ファイルへsource・設計前後・容量検査を保存した。
  保存一式のみでも13検査が通過した。headerは8,388,608 bytes、索引等を含む固定bufferは9,961,472 bytes／busである。
  `m0-site-endpoint-final-20260911`へ日英サイトを生成し、277数値検査と容量・未達条件を両HTMLで確認した。
  十六の現行仕様参照、二十二組の保存版参照、17監査source、43 JSON・七layoutを照合した。
  メモ変更は9.3節のみ、既存九つの数値参照sourceは前回保存版と同一だった。
  検証結果は`target/temporal-dcc/m0-endpoint-verification-20260911.json`に保存した。
  参照先payloadの有界pool、元のoccurrence／support ID生成、参照の所有・解放、全beam活動／joint／遅延receiptと
  bank／sectionへの一度だけの適用は残る。全ledger、O04・O12・人の判定を完了扱いしない。

- 不変byte poolと型別の参照管理を追加後、Python733検査、Rust746件通過・12件ignoreを確認した。
  16部品検査では、50本×200操作の独立byte／所有参照、四payloadの途中割当失敗、改訂・alias・古いticket、
  epoch退役、実際のpacked descriptor／SectionRecordの往復を検査した。新しい元ID発行部品も非再利用を要求する。
  四payloadを共有する65,536 endpointは保持・解放できた。個別128-knotコピーでは、half／base／doubleの
  block容量が992／1,985／3,971件で尽きた。これは容量不足の証拠であり、実推論や全O04の合格ではない。
  初期poolは87,818,240 bytes／bus、queue込み97,779,712 bytes／bus。入力・コピー・allocator等は別途必要となる。
  `target/temporal-dcc/m0-payload-reference-20260911/`の45ファイルにsource・設計前後・容量検査を保存した。
  保存一式のみでも16検査が通過し、`m0-payload-archive-tests-20260911.log`と終了値0を保存した。
  `m0-site-payload-final-20260911`へ日英サイトを生成し、293数値検査と容量不足・未達条件を両HTMLで確認した。
  十七の現行仕様参照、二十三組の保存版参照、17監査source、44 JSON・七layoutを照合した。
  メモ変更は9.3節のみ、既存の11数値参照source（従来のcredit参照を含む）は前回保存版と同一だった。
  基準の1038項目と`git diff --check`も通過。検証結果は`target/temporal-dcc/m0-payload-verification-20260911.json`に保存した。
  実際のbeamによる元ID・意味表現・活動・系譜・遅延receiptの生成、構造共有、全epoch／配送と
  bank／sectionへの一度だけの適用は残る。全M0を完了扱いせず、容量課題と既存の40 ms予算超過を次へ引き継ぐ。

- 記述子の構造共有と型別pool adapterを追加後、Python748検査、Rust746件通過・12件ignoreを確認した。
  15部品検査は、50本×200操作の独立byte集合／所有参照、ハッシュ衝突、全tombstone索引、部分失敗、
  版の再利用、型別handle配送とqueue退役を扱う。実moment圧縮器の640 raw記録から得た80版も全byteが一致した。
  容量監査は1,024固定経路・各64版・更新ごとに6個の新knotという構成条件で実施した。
  half／base／doubleのleaf容量で1,024／22,869／65,536版を保持し、倍容量では518,144個の固有knotを使った。
  全保持版の全byteを照合後、全参照を解放できた。全knotが別内容の条件は初期容量の2,048版で尽きた。
  初期／倍容量の記述子固定bufferは128,713,216／222,036,480 bytes／busであり、他payload・queue等は別途必要となる。
  これは固定経路の条件付き保存結果であり、実際の出生・交替・系譜・全payloadを含む容量採用やO04合格ではない。
  `target/temporal-dcc/m0-shared-descriptor-reference-20260911/`の47ファイルにsource・設計前後・監査を保存した。
  保存一式のみでも15検査が通過し、`m0-shared-descriptor-archive-tests-20260911.log`と終了値0を保存した。
  `m0-site-shared-descriptor-final-20260911`へ日英サイトを生成し、308数値検査、容量と未達条件を両HTMLで確認した。
  十八の現行仕様参照、二十四組の保存版参照、17監査source、45 JSON・七layoutを照合した。
  メモ変更は9.3節のみ、既存12数値参照sourceは前回保存版と同一だった。基準1038項目と`git diff --check`も通過。
  検証結果は`target/temporal-dcc/m0-shared-descriptor-verification-20260911.json`に保存した。
  次は実際の元span／beam／系譜・遅延receiptのpayload生成と、確定writeのbank／sectionへの一度だけの適用を接続する。
  実推論の容量、全worker性能、fitと人の判定を残し、M0全体は未完了とする。

- 確定writeのconsumer参照を追加後、Python765検査、Rust746件通過・12件ignoreを確認した。
  17部品検査は、元ID／sequence、事前入力検査、実際のbank／sectionの受領、応答欠落、適用前の取消と次writeを扱う。
  30本×20 writeは独立したmembership／strength／section支持の総和と一致した。別の1,024受け手監査では
  異なる重みを保持し、bank／section／queueの三応答を失っても67 callで各sectionを一度だけ更新した。
  重み付き統計の誤差はゼロ、全queue参照を解放した。固定受領記録は33,024 bytes／busとなる。
  復号済みbatchは別で、そのPython表現だけで1,248,569 bytesを使った。全受け手の事前検査と毎callの全batch hashは
  最大48.490351997315884 msとなり、40 msのworker予算を超えた。全67 callは568.5904265847057 msだった。
  これは処理配分の未達としてO04へ登録した。予算16はsection適用数だけを制限し、最初の検査やhashの費用を含まない。
  `target/temporal-dcc/m0-consumer-reference-20260911/`の50ファイルにsource・設計前後・監査を保存した。
  保存一式のみでも17検査が通過し、`m0-consumer-archive-tests-20260911.log`と終了値0を保存した。
  `m0-site-consumer-final-20260911`へ日英サイトを生成し、325数値検査、容量と予算超過を両HTMLで確認した。
  十九の現行仕様参照、二十五組の保存版参照、17監査source、46 JSON・七layoutを照合した。
  メモ変更は9.3節のみ、既存13数値参照sourceと既存部品試験は前回保存版と同一だった。基準1038項目と`git diff --check`も通過。
  source、試験、登録表の照合結果は`target/temporal-dcc/m0-consumer-verification-20260911.json`に保存した。
  完全なrouting・意味を持つpayloadの生成、実workerの単一所有／公開、O04、fitと人のgateは残る。

- 変更不能のconsumer packetと分割事前検査を追加し、Python780検査、Rust746件通過・12件ignoreを確認した。
  packet15検査は独立byte列、整数／f64のbit、tuple／list、途中失敗、改変隔離と500記録の往復を扱う。
  既存consumer17検査は分割検査へ更新し、全部の入力が通るまでbank／sectionに作用しない条件を確認した。
  1,024受け手の監査は131 callで、検査と適用を各1,024件行った。各callは最大16件、三つの応答欠落から復旧し、
  重み付き統計の誤差はゼロ、queue参照と内部の復号を解放した。最大callは7.179675158113241 ms、合計141.72862633131444 ms。
  作成側は符号化4.442368168383837 ms、16行appendの最大0.3581619821488857 ms、確定8.344630943611264 ms、
  合計34.825715236365795 msだった。packetはbank 273,968 bytes、section 1,260,544 bytes、claim 174 bytes、digest 64 bytes。
  固定受領記録33,024 bytes、Python object・scratch・復号とqueue参照は別であり、全workerの合格ではない。
  最大256件・各128 knotの別監査も数値と元membershipは一致した。しかしbank入力は26,982,619 bytesとなり、
  符号化477.75619593448937 ms、確定1,147.6100960280746 ms、読み込み747.8895240928978 ms、適用652.5812409818172 msで
  すべて40 ms予算を超えた。この四段階の未達をO04へ登録し、次の処理配分の対象とした。
  `target/temporal-dcc/m0-consumer-packet-reference-20260911/`の82ファイルに変更前後のsource・設計・監査を保存した。
  保存一式のみでもpacket／consumer計32検査が通過し、`m0-consumer-packet-archive-tests-20260911.log`と終了値0を保存した。
  `m0-site-consumer-packet-final-20260911`へ日英サイトを生成し、340数値検査、入力容量と最大bankの予算超過を両HTMLで確認した。
  二十の現行仕様参照、二十七組の保存版参照、17監査source、47 JSON・七layoutを照合した。
  メモ変更は9.3節のみ。consumer以外の既存13数値参照sourceと既存部品試験は前回保存版と同一だった。
  基準1038項目と`git diff --check`も通過。最終照合結果は`target/temporal-dcc/m0-consumer-packet-verification-20260911.json`に保存した。
  意味を持つ実payload、全routing、実workerの所有・公開・epoch、全fitと人のgateは引き続き残る。

- コンパクトなbank登録と準備／公開の分離を追加後、数値352検査、Python792検査（28.950秒）、Rust746件通過・12件ignoreを確認した。
  更新したbank24・consumer19・packet19の計62検査は保存一式だけでも1.181秒で通過した。
  256件×128 knotの同内容／全内容別の入力は、それぞれ186,311／10,631,111 bytesとなった（claim・digest・object等は別）。
  各条件67 call・514準備単位で全byte、元のmembership、一回の公開とqueue解放を確認した。
  最大callは16.592381987720728／13.762363931164145 ms、入力作成は別途6.20／11.25 ms。
  1,024受け手の監査も132 call・三応答欠落・重み付き誤差ゼロで完了した。
  ただし1,024 joint・4,096関係候補のheader処理は最大57.98464617691934 msで未達となった。
  一回の呼出しと物理cycleを区別し、制御した27.0／27.65秒を実際の到着遅延の測定とは扱わない。
  sourceと三監査は`m0-bank-staging-reference-20260911/`、Python／Rustの終了値は`m0-*-tests-bank-staging-final-status.txt`、
  保存一式の終了値は`m0-bank-staging-archive-tests-status.txt`へ保存した。全M0は作業中のままとする。
  日英サイトは`m0-site-bank-staging-final-20260911/`へ生成した。現行仕様参照20件、保存版参照30組、
  47 JSON・七layout・17監査sourceを照合した。変更したbank／consumer／packet以外の数値source12件は前版と同一。
  メモ変更は9.3節だけであり、基準1038項目と`git diff --check`も通過した。
  最終照合結果は`target/temporal-dcc/m0-bank-staging-verification-20260911.json`。

- header共有復号と候補recordの分割を追加し、数値360検査・Python800検査（31.578秒）が通過した。
  bank27・consumer21・packet22の計70検査により、遅い不正入力、取消、復号回数、共有所有と一度だけの配送を確認した。
  最大header単体は43 call・最大5.531514063477516 msとなり、旧実装の四bufferと全byte一致した。
  診断用の全bank書出しは別途40.04 msを要するが、分割配送からは呼ばない。
  1,024 joint・4,096候補・1,024 sectionの同時inventoryでは、三つの応答欠落から復旧した。
  header復号1回、候補復号4,096回・各1回を確認。174 call、最大8.745113853365183 ms、総219.30024586617947 ms。
  全256件×128 knotの同内容／全内容別登録も67 call・518単位で一致し、最大約11.72／11.61 msとなった。
  総処理・現実の到着遅延・両worker負荷は別の終了条件であり、処理分割だけではO04合格にしない。
  四監査は`m0-header-staging-reference-20260911/`、検査の終了値は`m0-*-tests-header-staging-final-status.txt`へ保存した。
  保存一式だけでも70検査が1.175秒で通過し、`m0-header-staging-archive-tests-status.txt`へ終了値0を保存した。
  Rustは746件通過・12件ignore・失敗0。日英サイトは`m0-site-header-staging-final-20260911/`へ生成した。
  現行仕様参照20件、保存版参照33組、47 JSON・七layout・17監査sourceを照合した。
  変更した三部品以外の数値source12件は前版と同一。メモ変更は9.3節だけであり、基準1038項目も一致した。
  最終照合結果は`target/temporal-dcc/m0-header-staging-verification-20260911.json`へ保存した。全M0は作業中。

- 実時計の配送監査を追加した。16件の小構成は期限後4.216〜4.553 msで8件が届いたが、最大構成は約0.635〜5.224秒の遅延となった。
  最大cycle実処理46.566 ms、処理窓の最大超過6.875 ms。処理窓を外した対照でも待ち行列が増えた。
  4-slot診断では4件受理・4件を容量損失として拒否し、受理分の全重み・一度だけの適用・所有解放を確認した。
  個々のcallの実時計区間とcycleへの割当を保存した。最終公開時刻をbank内部cutで代用していない。
  数値source・既存検査は前版と同一。今回の変更は配送監査と設計・登録・公開台帳であり、全M0は作業中。
  保存一式だけでbank／consumer／packetの70検査が2.142秒で通過した。Rust746件通過・12件ignoreも再確認した。
  日英サイトは`m0-site-delivery-clock-20260911/`へ生成し、両HTMLの結果記述を確認した。
  実時計traceの因果順序・cycle割当、現行仕様参照20件・47 JSON・保存版33組、既存360数値検査のsource一致を独立照合した。
  基準1038項目も一致。`m0-delivery-clock-audit-20260911/verification.json`と各終了値を保存した。
  feature登録は`m0-registration-21`、数値アルゴリズムの変更はない。

- R1（107 bytes）／S1（固定534 bytes＋対応map）を実装した。旧形式fallbackは追加せず、全候補・受け手を保持した。
  新規5検査によりpacket27・数値365検査となり、全Python805件が30.842秒で通過した。
  保存一式だけでもbank／consumer／packetの75検査が1.815秒で通過。Rust746件通過・12件ignoreを確認した。
  旧generic codecとの16流れ×20回の比較で、累積320・ring1,184 bufferが全byte一致した。
  最大同時構成のconsumer総処理は約219.300 msから143.571 msへ減り、三つの応答欠落からも復旧した。
  同一実時計driverの最大構成では測定処理量33.2%減、期限後遅延は約0.420〜3.234秒となったが、queueはなお増える。
  4-slot診断は4件受理・4件容量損失のまま。元の重み・効果回数・所有解放を維持した。
  全256件×128 knot登録の総処理約372 ms、matcher・全O04・fit・人のgateは残る。
  feature登録は`m0-registration-22`。数理モデル・Rust演奏経路・和声DCCの変更はない。
  日英サイトを`m0-site-typed-codec-20260911/`へ生成し、改善量・遅延・805検査の記述を両HTMLで確認した。
  source・検査・監査143ファイルを固定し、現行仕様20件・47 JSON・保存版36組と基準1038項目を照合した。
  変更した二つ以外の数値source13件は前版と同一。bankの全class／関数ASTも同一で、座標名の正本をcodecへ移しただけである。
  実時計traceの因果順序・同一driver／投入条件・処理量差も独立照合した。終了値0と
  `m0-typed-consumer-codec-20260911/verification.json`へ結果を保存した。全M0は作業中。

- section準備の再利用と、途中の観測・accent受領・世代・元ID・ring・既適用状態の保持を実装した。
  旧直接commitとの48流れ×24回で累積1,152・ring4,426 bufferが全byte一致。2,304回の途中観測、
  284無効果・15前sectionの先行記録・96世代継承を含む。六つの回帰検査も追加した。
  数値371検査、全Python811件（28.491秒）、保存一式の114部品検査が通過。Rust746件通過・12件ignore。
  同一driver最終測定は処理量23.4%減。初回22.4%減も保持し、遅延と40 ms処理窓の超過をO04へ残した。
  固定準備bufferの増分1,441,792 bytes／busも登録した。feature版は`m0-registration-23`。
  日英ローカルサイトを`m0-site-section-preparation-20260911/`へ生成。M0全体は作業中。

- SectionRecordの連続f64値を一括で読み書きする方式へ変更した。加算順序、共有分母の丸め後byteと符号付きゼロ、
  不正値・overflow時に変更しない条件を保つ。旧scalar実装との12,000更新・96,000読み出しが一致し、
  1,099条件の拒否判定も一致した。途中の観測を含む1,152 writeの比較も全byte一致した。
  新規3検査で数値374・全Python814検査（31.118秒）、保存一式の117検査が通過。Rust746件通過・12件ignore。
  同一最大構成では処理量10.2%減、期限後遅延約0.312〜2.031秒。待ち行列と40 ms処理窓の未達を保持した。
  固定配置と設計メモは同一。feature版は`m0-registration-24`。日英サイトは`m0-site-section-record-blocks-20260911/`。

- 活動統計の組立てをobserve・commit準備・occurrence stageで共通化した。29論理値を23個のf64へ保存し、
  全検査後に184 bytesを一括更新する。232-byteの展開済みpacked一時値やPython objectの費用は固定bufferと別に計上する。
  累積観測のphysical gap、元の加算順序、丸め後の共有分母と符号付きゼロ、sealed支持への一度だけのmembership乗算を保った。
  前区間のedge統計とending maskも同一owner呼出し内で一度だけ読む。
  旧field別組立てとの4,000条件の結果が一致し、1,263拒否条件で全recordを維持した。768 write・1,536 payload・2,304投影も一致した。
  新規3検査で数値377・全Python817検査（30.484秒）、保存一式の144部品検査が通過。Rust746件通過・12件ignore。
  同一最大構成の測定処理量は4.6%減、少数構成は0.7%増。共通組立てだけの初回版は3.1%減であり、sourceと結果を別途保存した。
  設計メモと固定容量は同一。feature版は`m0-registration-25`。日英サイトは`m0-site-activity-blocks-20260911/`。
  実装、三つの旧処理照合、四有限監査と実時計traceの根拠は`target/temporal-dcc/m0-activity-blocks-20260911/`。

- 31確定統計を一括処理し、検査済みendingの再利用も実装した。4,000条件で準備・累積各2,240件とring6,748件が全byte一致。
  1,575件の無効果、185件の無変更拒否、337件の前sectionの先行記録を含む。
  全登録のprofileを受け、DescriptorKnotの38f64・二u64も一括読出しへ変更した。別の4,000条件で全bit・型・maskと236件の長さゼロ拒否が一致。
  新規3検査で数値380・全Python820検査（28.348秒）、保存一式の176検査が通過した。Rust746件通過・12件ignore。
  最終最大構成の処理量は直前版比5.7%減、全128-knot登録は262.971／266.644 ms。40 ms予算と待ち行列は未達を維持した。
  31統計だけを変更した初回版（5.8%減）と全登録profileはsealed-only/へ保存した。最初のdriver起動引数エラーも保持した。
  設計メモ・固定配置は同一。feature版は`m0-registration-26`。根拠は`target/temporal-dcc/m0-sealed-sums-20260911/`。

- bank登録では変更不能のpacked記述子を検査後に直接保存し、同じbytesの再構成・再展開を省いた。
  moment・gap・source世代・availability等の既存検査は維持し、展開済みnative入力は従来どおり再構成して全viewの整合を検査する。
  旧版との720取引・1,775途中状態が一致した。成功296件は全状態が一致し、拒否424件は既存記憶を維持して準備slotを解放した。
  1／2／8／128個の実圧縮knot、240件の段階配送、480件のpacked／native混在取引を両順序で含む。
  初回oracleの圧縮後knot数の取り違えによるfixture構築エラーも保存し、アルゴリズムの反例とは区別した。
  新規3検査で数値383・全Python823検査（30.926秒）、保存一式179検査が通過。Rust746件通過・12件ignore。
  全登録は154.701／152.413 msとなり、直前版から約41〜43%減。一方、最大配送構成の処理量は0.2%増で改善は未確認。
  設計メモと固定配置は同一。feature版は`m0-registration-27`。根拠は`target/temporal-dcc/m0-packed-bank-admission-20260911/`。

- section投影は確定writeが所有する整列済みpath列を二分探索する方式へ変更した。追加の保存用索引はなく、
  pending改訂・確定後の再解釈・外部viewの書換えでも元のmembership、支持cutとコピー境界を維持した。
  0／1／2／8／64／1,024 pathの36 streamで旧全走査との6,882検索が一致した。成功5,658件のS1 packetがbyte一致し、
  拒否1,224件も同じ結果だった。72保存状態の検索前後の不変、既存768 write・2,304投影の照合も確認した。
  新規3検査で数値386・全Python826検査（30.052秒）・保存一式109検査が通過。Rust746件通過・12件ignore。
  同一最大到着条件は処理量8.6%減、期限後遅延約0.232〜1.538秒。最大処理周期43.721 msは依然40 msを超える。
  少数構成は2.9%増、容量4診断は8.2%減、処理窓なしは7.5%減だった。全構成での改善とは扱わない。
  モデル・固定配置は維持し、feature版は`m0-registration-28`。根拠は`target/temporal-dcc/m0-section-path-search-20260911/`。

- generic codecのschemaキー71個とliteral／scalar復号表を再利用した。9,774符号化・13,312復号を旧版と照合し、
  成功6,724／4,573件のbytes・型、拒否3,050／8,739件の結果が一致した。wire形式と容量・深さ・UTF-8等の検査は維持する。
  数値388・全Python828検査（30.797秒）・保存一式84検査が通過。Rust746件通過・12件ignore。
  最大構成の処理量は2.8%減、期限後遅延約0.227〜1.509秒。最大周期47.001 msは予算を超え、前回43.721 msから増えた。
  profileを受けて復号変更を戻した別案も試したが、計測負荷なしの15回の順序無作為化比較では初回案が速かった。
  最大header中央値は旧版／採用版で符号化4.964／3.572 ms、復号7.174／6.277 ms。検査済み初回sourceと配送測定を採用し、
  別案・交互比較・起動pathエラーも保存した。局所比較と全配送、profileの計測負荷を区別する。
  共通の符号列／UTF-8文字列はmodule全体で1,204／849 bytes。Pythonの表・文字列・object・compiled formatは別途必要である。
  固定配置と設計メモは同一。feature版は`m0-registration-29`。根拠は`target/temporal-dcc/m0-schema-key-codec-20260911/`。

## 再開順序

次は、packed exportをschedulerのdispatch・native照合・receiptまで接続し、全経路の費用と所有を測る。
320-byteの元記述子を単一の所有済みimmutable入力とし、辞書へ展開せずnativeで照合する経路を追加した。
全query中央値は既存の辞書一括版11.416／15.158 msから、
packed入力5.042／7.221 msへ短縮した。毎回の全コピーを含めると5.718／7.831 msだった。
コピーなし／あり・1／2 caller・1／10座標の8条件で全2,400測定が40 ms以内、全2,520出力が一致した。
2 callerのp99はコピーなし21.580／22.513 ms、コピーあり8.944／11.087 ms。順序・条件の異なる測定からコピーの高速化効果は推定しない。
固定ctypes bufferは7,001,928 bytes／caller。元payload10,526,720 bytesを保持し、コピー条件では同量を追加コピーする。
数値projectionは5,526,528 bytesを書き、借用pointerをreturn前に失効させる。原支持cut・世代・coverageの除算順序・全出力を維持した。
43実験検査、既存33 matcher検査、全Python871検査（32.693秒）、Rust746通過／12件ignoreを確認した。
72回の全query比較、84回の資源監査も全出力一致。根拠は`m0-native-packed-input-20260911`。
次はpacked exportからschedulerのdispatch・matcher・receiptまでを接続する。登録参照・feature版29・設計メモ・演奏経路は同一。
実生成入力・consumer処理量・全O04・fit・人のgateは未完了のまま残す。

query slotと実際のepisode bankから元metadata・blobを切り離す実験exportを追加した。
既存QueryScheduler.takeはまだ辞書exportを行うため、dispatchからの全費用を今回のquery値で代用しない。
新たなPackedKnotsは元byte列を単一の情報源とし、Python参照には必要なときだけviewを返す。
従来の10座標fixtureはvalueだけを変更しmomentを更新していなかった。新素材は全matcher観測値・時刻・maskを同一に保ち、対応するmomentから生成した。
80無作為query、400組のprojectionでcoverage閾値・f64・u64を照合し、後のゼロ長recordが前の例外を上書きしないことも検査した。
ABI v4で旧新の不一致二組を入口で拒否した。独立段階計測でprojectionは約0.43〜0.52 ms、GIL保持pointer借用は約0.003〜0.005 msだった。
Python queryのtracemalloc peak差はコピーなし1,545,536／1,558,928 bytes、コピーあり12,198,265／12,211,657 bytes。
これはallocator・全stack・RSSの上限ではない。元bank・全queue・producer・consumer・実機を含むO04を完了扱いしない。

consumer経路も引き続き未達である。採用版の最大8到着は処理量897.324 ms、期限後遅延約0.227〜1.509秒、最大周期47.001 msだった。
少数・容量4・処理窓なし構成の処理量は9.8%／4.2%／3.6%減、全bank登録は152.868／152.954 msだった。
固定buffer1,474,816 bytes／busと全256 episode／1,024 joint／4,096候補／1,024 sectionを維持した。
module共通の符号列等2,053 bytesとPython overheadは別に計上する。容量や入力検査を減らして予算を通さない。
source、84部品検査、fanout／full-bank／header／combined監査、実時計trace、旧codecとの照合は
`target/temporal-dcc/m0-schema-key-codec-20260911/`へ保存。`with-decoder-reuse/`は採用した初回版、
`encoder-only/`は復号変更を戻した別案、`codec-timing.json`は固定した三sourceの15回交互比較である。
profileは計測負荷・素材生成・監査用snapshotを含むため、その累積時間だけで採否を決めず実時計の同条件比較と照合する。
matcherの同時負荷p99、率履歴144行のoverflow、全容量・copy・allocator・RSS、実音声・両worker・64 Voice、
live evidence age、完全な意味payload・routing、全波形・fitと人のgateを含むM0全体の終了条件は変更しない。

1. 10値のraw記述子と320-byte knotの有界圧縮を追加し、29検査と54条件の未圧縮参照照合を通した。
   続いて保持中のquery prefix、coarse anchor／元時刻DTW、確定episode生成、既存ledger／sectionへの接続を32検査で通した。
   [query scheduler参照](query-scheduler-reference.json)は一group一最新query・八snapshot・一bus一実行jobを実装した。
   取得sampleのcadence、待機分の置換と実行中jobの区別、世代退役・epoch変更・古いticketを22検査で扱う。
   三つの旧ledger反例を保存して修正した。query世代・元の全音声支持・cache実到着が確定cutを満たす必要がある。
   100本×80挿入のcacheは独立した全履歴oracleと一致し、九つの仮想遅延条件でも固定容量を保った。
   固定bufferは819,848 bytes／busだが、Python object／一時出力／allocator・実thread・bank／全ledgerの費用は別。
   共通memory clock・強度更新と固定bank／edgeは有限入力で接続した。
   未圧縮／圧縮密度差の検索を広く比較し、実際の推論・有界配送へ接続する。
   間隔8のanchor対照では途中引用が検索できず、移調量もずれた。小fixtureの間隔4の成功を全条件へ拡張しない。
   入力は割当済み音響hopであり、実波形のNSGT／group推定は未実装。
   sectionの有界集約、accentの原支持cut・累積配送、有限入力のoccurrence確定ledgerも検査済み。
   元ID、期限、joint支持、確定後のmetadata改訂、暫定spanへの遅延accent帰属と元の隣接coverageを扱える。
   [記憶保持参照](memory-reference.json)で共通gap clock、strength／availability、干渉率と再認用コピーを27検査・162条件で通した。
   [bank参照](bank-reference.json)は非再利用handle、共通16-edge上限、context membershipと退避を18検査で扱う。
   全256枠の128 knot保存と255の未取得リンクも検査した。次は関係／系譜／context遷移の実際の生成へ接続する。
   [endpoint参照](endpoint-reference.json)は128-byte header、固定heap／ID索引、期限順処理と確認応答を13検査で扱う。
   [payload参照](payload-reference.json)は不変byte pool、元IDの対の発行と型別の所有・解放を16検査で扱う。
   次は実際のbeamで元IDを生成・継承し、descriptor／activity／joint／lineageの意味表現と遅延receiptを接続する。
   共有fixtureは65,536 endpointを保持したが、個別128-knotコピーではhalf／base／double容量が992／1,985／3,971件で尽きた。
   [共有記述子参照](shared-descriptor-reference.json)は正確な320-byte knot共有、固定参照列と型別poolのqueue接続を15検査で扱う。
   実圧縮器の80版も全byteが一致した。固定1024経路・各64版・各更新6 knotの条件では、half／base／doubleが
   1,024／22,869／65,536版を保持し、倍容量のdistinct knotは518,144だった。全knotが別内容なら初期容量は2,048版で尽きる。
   共有記述子の固定bufferは初期128,713,216、倍容量222,036,480 bytes／busであり、auxiliary pool・queue・他workerは別途必要となる。
   実際の保持beamの交替・出生・系譜と全payloadを作り、容量・コピー・欠落を対象モデルを縮小せず検査する。
   既存の85,852,160-byte active記述子は2 bus合計。全体の容量採用やO04の実時間合格はまだない。
   [consumer参照](consumer-reference.json)はcallerが渡す確定writeをbank／sectionへ一度だけ適用し、
   実際の状態から受領を確認する。17検査で無効入力、応答欠落、世代・元ID、abort後の次writeと1,024受け手を扱った。
   上流の全beam活動／帰属・遅延receiptから意味を持つ確定writeを作る経路、完全なroutingと実workerへの接続は残る。
   144行の率履歴overflowと、episodeごとの競合支持・exp再計算も残る。cache一括更新と最大負荷の容量・費用を解決する。
   oracleの全件監査履歴を本番の保存上限とみなさず、保持中・未確定spanとscratchの配置も計上する。
   上流の実波形・NSGT／group推定・広い検索成立・不確実な時刻履歴と任意のpacket順序回復も必要。phrase／sectionの全生成vector・
   対象数・区間打切り・有限fit／OOFを登録して、各条件100組の予測安定性を検査する。
   最初の境界の集約、観測hop積分、未知gapの64評価以内の積分とfallbackは部品として検査済み。
   gap監査600条件中184条件は未解決のままであり、本番のpath／epochの更新への接続と、
   実際の適合係数による未解決率・計算費用の検査も残る。
2. 私有traceのv9条件付き回収は全108点が合格した。実際の身体・波形・機会窓・取得時刻、
   支持が十分な初期接触を含む割当の実現性と再演費用を検査する。現行の約1,844時間／条件群は未採用。
   真値・弱い課題・誤差基準を保ち、実際の音響尺度を用いる場合は必要な回収を再検査する。
3. 残る座標と必須対照を登録し、完全な数値処理による資源preflightを行う。
4. 人の収集条件が整った後に、素材の成立・課題理解・分割・検出力を固定する。

実施機関からの判断、未取得の聴取回答、未実装kernelの性能を仮定してM0を完了扱いしない。

## Episode回収v1の結果と再生成

三つの条件でそれぞれ108真値×100組を適合した。欠測なしは80／108点、10%の独立欠測も
80／108点、弱いcue・競合episode・曖昧さに応じた欠測の条件は74／108点が合格した。
保持時定数2秒は全81点が不合格であり、その他の不合格9点は20秒である。
3,272の失敗fitを無限大誤差として残した。個別の真値・条件で判定し、全体平均のよさで置き換えていない。

実行は約70.33秒、workerの模擬処理と親processを合わせて約557.84 CPU秒だった。
CPU測定はprocess起動費を含まない。8 worker、各BLAS 1 threadを用いた。
これはPythonの回収検査の費用であり、実時間の音声処理やO04の性能合格を示さない。
候補となる人の割当は960人に各3課題、計2,880回答であり、募集の決定や承認ではない。

最初の実行ではPythonのforkserverがsandbox内のUnix socketを作れず、模擬処理の開始前に失敗した。
`target/temporal-dcc/m0-episode-recovery-v1/`に失敗記録を保存した。
同じ登録・seed・数理・solverのまま、process起動をspawnへ変えて実行した。
成功した実行の正確なsourceは`m0-episode-recovery-v1-spawn/runner.py`に保存した。

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  /tmp/conchordal-context-scores-venv/bin/python \
  target/temporal-dcc/m0-episode-recovery-v1-spawn/runner.py run \
  --design docs/roadmap/temporal-dcc/episode-recovery-design-v1.json \
  --output target/temporal-dcc/m0-episode-recovery-v1-replay --workers 8

MPLCONFIGDIR=/tmp/conchordal-matplotlib \
  /tmp/conchordal-context-scores-venv/bin/python \
  scripts/evaluate_temporal_memory_recovery.py report \
  --run target/temporal-dcc/m0-episode-recovery-v1-spawn \
  --output-ledger docs/roadmap/temporal-dcc/parameter-recovery.json
```

回収はNumPyとSciPy、図はmatplotlibを用いる。版は結果manifestと環境に記録した。
runnerは出力先の再利用を拒み、登録・source・全fitのハッシュを固定する。
報告時に32,400の識別子の重複・欠落、全真値、全条件点の再集計を照合した。
追加の8検査では勾配、scalar参照、割当、十分な期待回答数での回収、観測ゼロ、
失敗fitの分母保持、予算切れを確認した。

弱いcueで`tau=2, kappa=4, strength_max=3, bias=0`とした場合、全2,880割当の期待yesは約3.29件だった。
`tau=3, bias=2`へ変えた対照との回答の期待条件付き対数尤度比は約0.038 natsである。
両者は最短採点の12秒では同じ時間・bias項を持ち、長い遅延からはほとんどyesを得られない。
この説明用診断を新しい閾値にせず、割当の不採用理由としてtechnoteの最終章にも記録した。

## Episode回収v2

[探索の事前登録](episode-recovery-planning-v2.json)では、8秒の全720条件を保ち、
1／2／4秒のqueryと、観測区間の登録が間に合う短い遅延を追加した。
1.25秒遅延で介在支持を1以下、2.25秒で4以下に制限し、4秒以上では0／1／4／16の交差を保つ。
構築上の上限であり、実際の音響対応がこの支持量を与える証拠はまだない。

真値から計算した期待Fisher情報で、各条件4／8／16／32回答を比較した。
不適格な条件点は順に6／2／0／0であり、事前の規則により最小の16回答を選んだ。
その後の32,400組の模擬回収は、変更していない全真値・誤差基準で324点すべて合格した。
結果は`target/temporal-dcc/m0-episode-recovery-v2/`、探索は`target/temporal-dcc/m0-episode-allocation-v2/`。
全fitの識別子と真値、ハッシュ、全点の再集計を報告時に照合した。

候補となる再認development fitの割当は3,840人に各11課題、計42,240回答である。
音声だけで平均1,950.93秒（32.52分）、最大2,672.5秒（44.54分）。
説明・回答・休憩、対応づけcohort、校正・最終検証cohortは含まない。
人数は予測上の候補であり、資金や実施機関の承認を示さない。
実行は約62.14秒、模擬処理と親processのCPU時間は約488.64秒であり、process起動費を含まない。

8秒を主課題として残す補助queryの仕様を、設計メモ・完成計画・再認課題へ反映した。
実際の短い音からの対応づけ、言語別・長さ別の課題理解、実際の音響尺度での回収、
独立した8秒課題への移転とその人数・実用差は未検証である。O11全体やM0の完了を意味しない。

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  /tmp/conchordal-context-scores-venv/bin/python \
  target/temporal-dcc/m0-episode-recovery-v2/runner.py run \
  --design docs/roadmap/temporal-dcc/episode-recovery-design-v2.json \
  --output target/temporal-dcc/m0-episode-recovery-v2-replay --workers 8
```

episode v2時点の参照コードは20の数値検査と11の回収・割当検査を持つ。
私有traceのパラメータをepisodeの回収結果で代用せず、次の工程として独立に扱う。

## 私有traceの保持質量と観測監査

2026-09-11に、`PrivateTimingTrace`と`integrate_retained_timing_bins`をf64参照へ追加した。
各binでは古い保持量を減衰させ、元の観測時刻にわたるcreditの積分を加え、上限を適用した後に
競合参照の干渉を掛ける。発音／releaseの加算は別々で、干渉は両headへ一度だけ作用する。
小さな追加creditで過去全体を新しくせず、正規化前のlog質量と非周期overflowを残す。
episodeの強度更新と私有binの保持質量は異なる機構であり、共通の認知的同定を主張しない。

数値参照では、既知の観測区間の終端・head・IDの順で一つの発音の全参照を更新する。
既存の順序付き配送・遅延・ID重複排除・発行時予測の保持との本番接続は未実装である。
単体の更新直前snapshotを、途中で更新があった発行時snapshotの代わりに採点してはいけない。
周期提案の許容範囲は0.125–4秒と確認した。回収の反復頻度や身体の可行性もこれに合わせて登録する必要がある。

観測監査では二binの解析例に加え、32 bin・一秒周期・48 kHz／512 samplesの実際の観測幅を使った。
後者では、真のbin分布とsoft-label採点の母集団最適分布の最大差は約0.006365となった。
正しい観測尤度で評価すると、soft-label最適分布は真値より約0.009429 nats／観測悪い。
これはモンテカルロ誤差ではなく、既知の生成分布に対する厳密な期待値の比較である。
真値・観測区間・観測確率・二種類の損失を`private-trace-observation-audit.json`へ保存した。

私有traceのstateful参照と新たな観測検査を含む32検査が通過した。
この時点ではO13の全パラメータ回収は未実行だった。以下の共同観測則と回収検査は、その後の改訂である。


## 私有traceの共同観測則と回収v1–v5、割当探索v6

発行時に固定した有限の機会窓内で、参照・anchor・周期の代替候補を一つの物理的発音の密度へ混合する。
各binと窓の交わりの上で正規化し、非周期overflowも保持する。交わりのないbin、失われたanchor、
未割当の重みは試験用の一様基準分布へ残す。参照ごとに独立な発音を生成しない。
観測区間と既知の検出率へ積分し、取得欠落も一つの結果として数える。
適合はこの一つの物理的観測の尤度で行い、未来のcreditで再重み付けしない。
従来のsoft-bin損失は診断用に残す。38の参照検査が、独立積分や既知の単一周期例と一致した。
回収forwardの勾配と省略計算は、二headの実際の逐次更新にも照合した。

| 版 | probe数／条件群 | 合格条件点 | 失敗fit | 全prefix独立再演時間／条件群 |
|---|---:|---:|---:|---:|
| v1 | 143,360 | 107／108 | 73 | 約45,883時間 |
| v2 | 9,086 | 106／108 | 10 | 約554時間 |
| v3 | 25,466 | 105／108 | 58 | 約647時間 |
| v4 | 50,932 | 106／108 | 43 | 約1,295時間 |
| v5 | 50,932 | 106／108 | 36 | 約1,295時間 |

各版は三つの条件群それぞれに全36真値と100組を含む。失敗fitを無限大誤差として数え、
正値パラメータの中央値ln(1.25)／90 percentile ln(2)という閾値を維持した。
v1の全fitは保存されたが、NumPy boolのJSON化で最終集計が失敗した。保存した推定値から集計を復元し、
fitを再生成していない。最終の正確なwall／CPUカウンタは失われ、計算費用の合格は主張しない。
v2は約134.14秒／1,045.89 CPU秒、v3は約133.63秒／1,049.30 CPU秒、
v4は約127.55秒／998.32 CPU秒、v5は約131.21秒／1,027.23 CPU秒。CPU値はworker起動費を含まない。

v2の期待Fisher情報による割当は局所的な精度を確保したが、上限が一回の加算量より低い領域では
正規化により上限が消え、別の解へ取り違えた。v3はこの領域全体との距離を測る校正probeを追加した。
同時にfitした情報行列のrankを検査し、上限が観測に影響しない解を成功と数える問題を除いた。
これにより、曖昧・欠測条件の短い時定数と大きな上限で、蓄積が上限へ達しない別の平坦な領域が露呈した。
v4は全課題数を2倍にしたが、40のrank欠落fitを含む43の失敗を残した。

v4の診断では、三つの初期値から同じ平坦な解へ収束する例に加え、より低い損失の非滑らかな解を
精密化せず、高い損失の収束解を採用するsolverの問題が見つかった。
[fit改訂v5](private-recovery-fit-v5.json)は低い損失の試行も精密化する。
収束できない試行の方が良いままなら失敗とし、rankの検査も維持する。
[回収設計v5](private-recovery-design-v5.json)はモデル・割当・真値・閾値を変えず、独立したseedで再検査した。
106／108点が合格し、rank欠落34組を含む36の失敗を残した。未合格は曖昧・欠測条件の
tau=2秒、cap=6、kappa=4／16である。
以前の失敗を新しいsolverで合格へ書き換えない。

全成果物は`target/temporal-dcc/m0-private-recovery-v1/`から版ごとのdirectoryへ保存した。
報告時に全10,800 fitの識別子・真値・誤差と108集計点、登録・source・結果のハッシュを照合する。
これらの再演時間は候補割当の下限であり、実データの収集計画として採用していない。
実波形からの参照重み、身体が実行できる機会窓、共通の取得時計に沿った観測時刻、再演方法の検証が残る。

```bash
OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  /tmp/conchordal-context-scores-venv/bin/python \
  target/temporal-dcc/m0-private-recovery-v5/runner.py run \
  --design docs/roadmap/temporal-dcc/private-recovery-design-v5.json \
  --output target/temporal-dcc/m0-private-recovery-v5-replay --workers 8
```


[割当探索v6](private-recovery-planning-v6.json)では、N=128、遅延4秒、干渉0／1／4／16の
既存4課題へ各256／1,024／4,096／16,384／65,536 probeを置く候補を事前登録した。
元の割当は減らさず、上限へ到達しないモデルのtau／kappaを元の範囲で適合し、
三つの短時定数・高上限の真値とのBhattacharyya距離を全課題で合計した。
最大候補でも最小の探索距離は約4.137／6.432／8.810となり、全点で8以上という基準に届かなかった。
`m0-private-allocation-v6/separation.json`は選択なしを記録し、v6の回収は開始していない。
profile探索は約6.46秒／6.45 CPU秒。観測kernelの準備と成果物保存費用を含まない。
各10初期値からの有限探索は大域最小値の証明ではない。
全候補・全初期値・勾配・sourceハッシュを保存した。局所Fisher情報に加え、別のパラメータ領域との
区別を調べる必要があるという所見を、technoteの最終台帳にも記録した。

## Section入力の登録

section記述子は、関係5質量、順序付き遷移25質量、articulation占有4値、参加統計5値の計39座標を
仕様の順序で登録した。未解決の関係は第五の質量として残し、遷移は25セル全体で正規化する。
観測秒と所属の重み、90%の支持条件、section開始／四span ringを跨がないpair、
元の支持区間から保存する終端記述子を明示した。非均等wordを含む周期提案の和集合と、
解決済みgroupだけを数える重なりも仕様へ照合した。
この時点では82座標headの全入力、尺度の適合、全対照の統計量・採否、実際の数値抽出fixtureが未完了だった。
この登録はO03全体やsectionのO12合格を意味しない。


## 私有回収v7とsection headの明確化

[接触設計v7](private-recovery-exposure-v7.json)は、最初のA反復と独立した遅延／干渉の後、
Bを1／4／16回繰り返してから一つの物理probeを観測する。B間も元の保持則で時間が経過し、
五回に一回の欠測counterは各prefixで始め直す。対になるonset／releaseも別の実行として含む。
元の140課題はB=1で全て残り、計420課題となった。勾配と正規化、省略計算と実際の両head逐次更新、
再演時間を同じevent時計へ照合した。

[事前の割当探索](private-recovery-planning-v7.json)は、全36真値×3条件で期待log標準誤差0.12以下を要求し、
各課題最低2 probeとhead間の均等割当を保った。615回の最適化で条件群ごとに14,382 probeを選び、
最大期待標準誤差は約0.11881／0.11988／0.11977となった。探索は約17.77秒。
全prefixを独立再演する場合の下限は条件群ごとに約1,034.44時間であり、実データの取得計画には採用していない。

[回収設計v7](private-recovery-design-v7.json)の独立10,800組は、104／108点が合格した。
152 fitが失敗し、内訳はrank欠落133、解決していない下位損失15、solver失敗4。
曖昧・欠測条件のtau=2秒／cap=6における三つのkappaの点と、部分観測条件のtau=2秒／kappa=1／cap=6が未合格。
後者のcapの90百分位は約0.69933で、ln(2)の基準を超えた。基準は変更していない。
実行は約439.31秒／3,454.69 CPU秒。CPU値はworker起動費を含まず、O04の実時間性能ではない。
元の数値sourceと登録は実行開始時に保存した。実行中のsection追加はその後の別の参照版である。

保存した失敗例では、真値に近い初期値から識別できる局所解も得たが、上限へ届かない解の方が
尤度の高い場合がある。高い損失の局所解へ置き換えて合格と扱ってはいけない。
曖昧条件の非周期Aでは、tau=2秒、credit 0.6を二modeへ配分、62.5 ms間隔、五回に一回欠測という
規約から、一binの飽和なし周期上限は約7.89948となる。cap=6はこの上限の約76%に位置する。
`branch-start-diagnostic.json`と`exposure-ceiling-diagnostic.json`はこの診断を保存し、再回収と区別する。
次の設計では接触支持も検討するが、真値、弱い接触の課題、欠測と許容誤差を勝手に削らない。

sectionの82共変量と切片を分離し、hazard83係数、和ゼロ制約を課す三分類exit166自由係数と明記した。
recent-onlyは41共変量と切片、elapsed-onlyは切片込み二入力。原記述子の差分を作ってから各入力を
凍結したdevelopment尺度で標準化する。未知支持率は候補section内の有効な物理支持の和集合から計算し、
重複区間や所属の重みを未知秒へ変換しない。差分は両側が有効なときだけ使う。
予測では経過時間だけを進めて文脈を固定し、検索支持が0.5秒以上古ければ検索二入力をmaskする。
四つの数値組立て検査が通過した。原音からの39記述子抽出、系譜／同一cue選択、尺度fit、
有限fitとO12全体の模擬検査は未完了である。
仕様の前後と参照は`target/temporal-dcc/m0-section-feature-clarification-20260911/`へ保存した。
私有traceとepisodeの節はbyte単位で同一であり、以前の回収の登録や結果ハッシュは変更しない。

## 接触支持v8と識別に基づく割当v9

[接触支持v8](private-recovery-exposure-v8.json)は従来の420課題を残し、最初のA接触の観測率と
anchor支持を1にした72課題を追加した。二mode、五回に一回の欠測、B接触、probeの曖昧さは維持した。
局所Fisher情報に基づく割当は15,350 probe／条件群で最大log標準誤差約0.11719／0.11953／0.11860を満たした。
全prefix独立再演の下限は約1,082.02時間／条件群。探索は約27.35秒である。

事前に登録した識別検査は、v7で失敗した四条件点に対し、上限へ到達しない別モデルのtau／kappaを
十初期値から適合した。追加72課題だけを1／2／4／8／16倍にする候補はすべて不合格だった。
16倍でも最小探索距離は約21.133／1.591／2.268／2.666となり、四点すべてで8以上という基準に届かない。
約18.24秒／18.21 CPU秒のprofile探索と全試行を保存し、v8の回収を開始していない。
この費用は観測kernel準備・成果物保存を含まない。数値参照は支持追加後の逐次更新と勾配にも一致した。

[割当v9](private-recovery-planning-v9.json)は同じ492課題と全最低割当を維持し、識別に必要な増分を選ぶ。
別モデルとの一観測あたりのBhattacharyya距離を制約へ加え、全prefix再演時間をLPで最小化する。
増分を認めるのは追加した72課題だけで、整数化はhead間の均衡を保つ偶数への切上げとする。
各反復で別モデルを再適合し、それまでの制約をすべて維持する。LP側の目標は余裕を含む8.1、
最終判定は以前と同じ8である。16反復、600秒／600 CPU秒、独立再演10,000時間／条件群を上限とし、
解決しないprofile・予算超過・不合格では回収を開始しない規則を、実行前に登録した。

三回のprofile検査で基準を満たした。増えたのは二課題の8,444 probeで、計23,794 probe／条件群。
最終の最小探索距離は約126.242／8.079／8.075／8.088、局所log標準誤差の最大も約0.11742以下となった。
探索は約11.18秒／11.26 CPU秒で、kernel準備・profile・LPを含み、成果物保存を含まない。
全prefix独立再演は約1,843.71時間／条件群であり、実データの取得計画としては採用していない。
有限の多初期値探索は大域的な識別の証明ではなく、全真値・全条件での独立回収が次の条件となる。

[回収設計v9](private-recovery-design-v9.json)で独立した10,800組を生成・適合し、全108条件点が合格した。
失敗fitは完全観測条件の4組で、rank欠落2組、解決しない下位損失2組。すべて無限大誤差として残した。
部分観測と曖昧・欠測条件の失敗fitは0。全条件点・パラメータでの最大中央値は約0.09599、
最大90百分位は約0.26284で、事前のln(1.25)／ln(2)基準を満たした。
実行は約534.41秒／4,198.61 CPU秒で、CPU値はworker起動費を含まない。
全fitの識別子・真値・誤差と108集計点、登録とsourceのハッシュをreportで照合し、回収図も確認した。
実体は`target/temporal-dcc/m0-private-recovery-v9/`、台帳は`parameter-recovery.json`。
v8は引き続き回収なし、過去の失敗をv9の結果で置き換えていない。
これは追加の十分な接触支持を含む模擬条件での回収合格である。弱い接触だけからの回収や、
実データ収集の成立、M0全体の完了を意味しない。

## 最初の境界の集約と観測hopの積分

[event-head-reference.json](event-head-reference.json)は、phrase／sectionの別々のhazardを受け取る
有限hopの集約参照を固定した。単一の時点で固定した共有文脈と局所pathの重みを用い、まず音群内で
pathを周辺化し、その後で音群を組み合わせる。各hopでは全群の履歴欠落を出口判定より先に扱い、
同時出口は最小の安定group handleへ割り当てる。既知の無活動は生存確率1、初期未知と履歴喪失は
未解決の質量として残す。解決済みの最初の出口を、その後の通常の観測欠落で消さない。
R、無条件CDFとtype別質量、生存、途中の未解決、全未解決の保存を、共有文脈・複数pathを持つ
三群の全終端履歴の列挙と照合した。単一／二／四／八群の解析解、同時刻の順序、初期未知、
発行時予測と聴取後採点の重みの違いも確認した。

観測hopのhazardは二点Gauss–Legendreで積分し、文脈を固定したまま経過秒を進める。
1秒の基準単位を維持し、sectionの標準化済み経過時間係数は同値な切片・raw log経過時間係数へ変換する。
累積hazardの校正倍率は局所積分へ作用させ、その後で群間集約する。開始時刻が未知なら年齢0を補わない。
事前に登録した135の係数・開始時刻・hop幅の組を独立の適応積分とstep doublingに照合した。
許容差は絶対1e-9＋相対1e-7。定数hazard、校正倍率、section標準化、未知開始を含む三検査も通過した。

九つの追加検査を含め参照全51検査が通過した。O12の全生成vector、区間打切りと対象数、
有限適合／OOF／校正、各条件100組の予測安定性は未実行である。この時点で未実装だった未知gapの積分は後掲の参照へ追加した。
本番の系譜・保存register・overlap後継clock・epoch境界も別途必要となる。
数値集約の検査を、実音の境界知覚や本番の配送・snapshot保持の検証へ拡張しない。
この集約を追加した時点では仕様メモのhashを変えなかった。v9回収は観測hop積分を追加する前の参照sourceを保存して実行した。

## Sectionの観測・対応・cue入力参照

[section-feature-reference.json](section-feature-reference.json)は、先行段階のcacheを入力とする
六つの数値関数と21検査を固定した。sourceは`scripts/temporal_section_reference.py`、
保存版は`target/temporal-dcc/m0-section-input-reference-20260911/`にある。
観測hopのNSGT解析、grouping／accent検出、DTWや時刻履歴を新たに実装したものではない。

終端の六記述子は、元のassigned energy／spectrum、既存の正のrise／flux、固有IDのaccentから計算する。
スペクトルは物理支持でpoolし、既に割り当て済みのenergyへ所属確率を二度掛けない。
窓はepoch、終端generation、span開始、元の最終支持時刻からの二秒で切る。
後のcommit時刻や別generationの音を借りず、短いspan、空の窓、既知の無音と未知、
座標ごとの支持不足、retrospectiveなaccent bankの容量欠落を区別した。
この共通六座標の順序と尺度規約をO03の`observable_6`へ登録した。実データの尺度fitは未実施である。

対応は各局所pathの一つの割当をexact／transformed／contrast／unmatched／unresolvedへ分類する。
空・未完・曖昧な検索をnonrecurrenceにせず、同定できない変形があっても別の既知のexactness条件が
失敗すればtransformedとなる。departureは両端で有効な六記述子の共通座標上のRMSを使う。
不確実な別pathは別々の統計として保ち、周辺化した対応確率同士を掛けて未登録の独立な遷移を作らない。
物理的なassignment秒へspan membershipを一度掛け、alias・retrieval・重複commitは加算しない。
未解決spanを飛ばさず、隣接の最小重みで25遷移全体を正規化する。支持が足りないpairの重みを残し、
有効pair重みが90%未満なら25座標をmaskする。section開始前の記述子は直前spanとのdeparture判定に
使えるが、その古いrecordや跨ぐpairを新しいsectionの統計へ加えない。

活動九座標は既存articulationと音響支持、実際にadmitされた整数／word提案、全ての有効なoutgoing／
within-group履歴cacheを使う。非均等wordを周期提案へ含め、P_bestだけでは周期性の入力にしない。
持続offsetはmodeからゼロ基準への距離、residualは先行段階が保存した不確実なrecordからmodeへの
正規化済み平均距離を使う。後者を点時刻で近似する処理は追加していない。重なりは解決済みの他groupに限り、
残余energyだけでは成立しない。式のD_gで割る密度／周期／重なりと、有効重みで割るoccupancy／時刻平均を
区別し、それぞれの物理的な支持率を返す。

cueは既存queryの観測時点で選び、実際に渡されたgeneration handleの支持を0.5秒窓で積分する。
同点は最終支持時刻、開始時刻、安定occurrence IDで解き、ongoingがなければ最新の支持済みcommitを使う。
選んだ全prefix記述子をそのまま再利用する。同じcue・観測時点・元の支持終端を持つqueryから、
各episodeの最良の音響変換scoreを取り、availabilityやsection年齢を加えない。
元の支持が0.5秒以上古ければ、公開時刻が新しくても検索二座標をmaskする。

連続した六spanでは、cumulative／recentの39記述子から既存の82入力組立てへ接続した。
これは一般的な重複span、fractional membershipの時間配置、once-onlyなaccentと四span ringの
統合証明ではない。実際の系譜所有・配送、640-byte record、上流の音響抽出と不確実な履歴の構築も残る。
参照は全入力を読むoffline oracleであり、本番のメモリ容量や実時間性能の合格を主張しない。
全生成vector、有限fit／OOF／校正と各条件100組のO12予測安定性は引き続き未実行である。

## 欠測区間の有界生存積分

`gap_survival`を既存f64参照へ追加し、最後に支持された文脈を固定したpriorとして、
欠測中の経過時間だけを進める。観測hopと同じ二点Gauss–Legendre則を使い、区間数を倍増する。
両端のsigmoid評価2回と、1／2／4／8／16区間の求積2＋4＋8＋16＋32回で最大64評価となる。
絶対1e-9＋相対1e-7の許容差とfallbackをfeature manifestのregistration 5へ固定した。

逐次の積分値の差だけでは収束を判定しない。例えば`a=20,b=-40,d=0,gap=1200 s`では、
区間冒頭の鋭いhazardを求積点が捉えず、小さい積分値が続く。この参照は四階導関数の多項式を
端点sigmoid範囲上のBernstein係数で抑え、丸めの余裕を含む誤差上界も確認する。
上界を満たさない場合、近似値は診断だけに残し、支持された生存確率へ昇格させない。

600条件を独立の高精度適応積分と照合し、全条件と全出力を
`target/temporal-dcc/m0-first-event-reference-v3-gap-20260911/gap-audit.json`へ保存した。
416条件が許容差内で生存を返し、184条件は予算内で収束を確定できずunknownを返した。
受理した値の最大誤差は許容差の約0.05756倍だった。未解決184条件を予測成功に含めない。
保守的な上界による未解決率、実際の適合係数での支持率、全負荷での計算費用はO12／O04に残る。

積分できた場合も、欠測中の退出確率は未観測の複数遷移を含むunknown-current-stateへ渡す。
観測した境界、型付き退出、inactiveの証拠は追加しない。積分不能・開始未知・数値異常では
生存そのものを未解決とする。後続の観測窓へ、このunknownを捨てずに渡す小さい集約例も検査した。
定数hazard、解析的な積分例、局所校正、分割、評価上限と反例の七検査が通過した。
保存済みv2の全既存関数・classは現行版とASTで一致する。新しい関数だけが差分であり、
episode／私有traceの回収モデルや和声・演奏経路は変更していない。
この部品は本番の系譜・再観測・epoch更新や、O12全体の適合／予測安定性を完了させない。

## 重複支持と有界section履歴

`partition_span_activity`は、上流で固定されたspan帰属を使う全件参照である。元のhopをspanの端点で切り、
同じ物理区間への帰属重みの和が1を超えないことを確認する。同じaccent IDへの重みも保存し、重複した
コピーは加算しない。元のraw hop全体が観測cut以前に揃っている必要があり、先の音を借りて部分区間を
作らない。欠測時間のrecordを省いた入力は拒否し、欠測を分母から消さない。

各spanの分子・重み付き分母・有効な物理時間・所有する時間窓を保存する。`SectionHistory`は、
確定recordのscalar membershipをこれらへ一度だけ掛けてringへ入れ、値の平均ではなく十分統計を集約する。
ringから落ちたrecordをまたぐ遷移も除く。累積の活動統計は新しいcanonical group hopを一度ずつ消費し、
spanのmembershipを掛けない。したがって、fractionalな帰属だけではsectionの観測時間は欠落しない。
重なる二spanと共有accentの手計算例から39／82座標への接続を確認した。

保存する`SectionRecord`は54個のf64累積値、六つのf64終端記述子、32 bytesの空き、
16個のu64 bookkeepingからなり、1件640 bytesである。標準の累積1件＋ring4件は3,200 bytes、
標準の1,024局所pathでは3,276,800 bytes／busとなる。この値はrecord payloadの測定であり、
Python object、全件参照の入力、暫定span、graph、scratchや実workerの実測費用を含まない。

初期の二つのf32へ分ける保存では、有効pair重量0.45／全pair重量0.5の境界例が
0.4499999999999999／0.5000000000000002となり、90%の判定を誤ってmaskした。
f64保存はこの例の全件参照と一致する。反例を再現するscriptと結果を
`target/temporal-dcc/m0-section-ring-reference-20260911/precision-counterexample.py`および同名JSONへ保存した。
この変更はbyte上限を拡大しない。将来の低精度化ではscalar誤差だけでなくvalidityも比較する。

追加12検査では2／4／8区間の各20 prefixを全件対応参照と照合し、保持区間のoccupancyも手計算した。
元の親を変更しない明示的なgeneration継承、新しいslot／epochとの分離、重複・alias・順序違反、
新sectionの直前記述子、10,000回の小さい加算、保存round tripも検査した。
旧参照の五関数はASTで同一であり、活動集約の旧出力項目も40条件でbit単位に一致した。
現在の33 section検査は、帰属重みやcommit列そのものを音から生成した証拠ではない。
この段階では古いcommitの再解釈、遅延accentの配送、上流の確定ledgerと暫定状態の保存が残っていた。
O12全体のfit／予測安定性、実時間性能、人の認知的な判定、M0の完了へ拡張しない。

## 四hopのaccentと元の支持を守る配送

[accent-reference.json](accent-reference.json)へenergy保存、四raw hopの局所最大検出、
原支持cut、順序付きadmissionと有界bankを実装・登録した。middle hopのevent区間、全raw支持の終端、
観測時刻上のavailability、受信時刻を分ける。scalar inventory時刻にはmiddle hop終端を用い、
時間分布の積分には元のuniform区間を保持する。包含時刻の開始点／中点対照は登録したが未実行である。

保存した反例ではevent区間が0.2–0.3秒、必要なraw支持が0.4秒まであり、元の記述子cutは0.3秒だった。
以前の抽出器では未来の比較hopから得たaccentを加えるとlog密度が0から0.980829253へ変化した。
修正後はaccentの全支持をcutで検査し、元の六記述子の全出力がbit単位で不変となる。
反例と前版は`target/temporal-dcc/m0-accent-reference-20260911/`に保存した。
これはcache入力の因果性の証拠であり、実波形によるT2の発行予測全体の検査はまだ必要である。

bankは最大128件／32秒とし、先に時間で失効させ、満杯なら最古の時刻・stable ID順で追い出す。
重みで追い出さず、容量で失った最も新しい時刻を保持して各窓の密度をmaskする。
累積admissionはbankの追い出し・失効とは独立に保持する。部分取得はsample支持の和集合で報告するが、
四hop全部と両成分が計算できない比較からfractionalなaccentを作らない。

sectionは音響deltaをaccent抜きで集約し、その受信区間の新しい配送を一度だけ消費する。
event区間がdeltaより前でも、元のeventがsectionに属せば現在の累積値へ加える。
再読で加算せず、保存した過去snapshotを更新しない。slot 15を世代内の配送sequenceに用いるので
640-byteのrecord上限は変わらない。明示的なgeneration継承では累積値を保ち、そのsequenceだけを初期化する。

新規20検査は手計算、欠測・成分mask、plateau、spectral-only flux、閾値／floor／容量の感度、
64／128／256件bankの全件参照、時刻tie、全八窓の密度、未来の右側支持、遅延配送・世代継承を扱う。
新規20・section33・一般58の計111数値検査が通過した。これは検出器と配送の数値部品であり、
実波形のNSGT／ridge／group推定、実際のdevelopment尺度、任意のpacket再順序化、
終了済み暫定spanへの帰属と0.5秒の確定ledger、全O12適合とO04の同時負荷は未完了である。
過去の回収結果・人の聴取・M0全体の完了をこの部品検査で代用しない。

## 有限入力のoccurrence確定ledger

[occurrence-reference.json](occurrence-reference.json)へ、元のoccurrence／支持IDを保持した暫定帰属・
期限での確定・metadata改訂を実装した。入力済みの活動payloadに条件づけ、音響所属秒／元span長を
観測支持とし、jointなepisode／context経路重みへ一度だけ掛ける。独立したmarginalの積で補わず、
未知・未保持の重みは既知のepisodeへ再分配しない。上流の全beam活動履歴そのものを作る処理は未実装である。

0.5秒の期限まで帰属を更新でき、その時点で一度だけ記録する。処理が後になっても原時刻と確定期限を残し、
期限後の情報を暫定支持へ混ぜない。再解釈は元の確定writeを変更せず、joint支持cellまたは未帰属支持の
変化の最大値が厳密に0.25を超えた元occurrenceを一度だけflagする。分母は支持された全確定occurrenceである。
文脈のA／U累積はstrengthの上限とは別に保持し、追い出したcontextの重みを他へ配らない。

遅延accentは保存した所有割合に従って暫定活動統計へ一度だけ配分する。先に届いたreceiptを後からstageする
ownerが使う場合も原支持を検査する。終端記述子は独立して固定し、確定後のaccentは支持損失として記録する。
元の支持終端と同一cueのcoarse snapshotを用い、既知の競合・未知のepisode／参照による干渉幅を区別した。
欠測のgap不確実性を重なるoccurrenceごとに加算する処理は行わず、共通memory clockへの接続を残す。

21検査はalias、未聴取・epoch・容量損失、期限／閾値の半分・倍、欠測、原時刻の保持、再解釈、
context追い出し、参照cut、遅延accentと原支持の隣接coverageを含む。100有限streamの各12 prefixで
A／Uを独立の全件和と照合し、重複spanを39／82座標へ接続した。これは支持の代数検査であり、
O12の適合済み予測安定性100組／条件の検査ではない。一般58・section33・accent20・今回21で計132検査となった。

再生成可能な例を`target/temporal-dcc/m0-occurrence-reference-20260911/commitment-audit.py`へ保存した。
原終端0.3秒、確定0.8秒、支持0.5の例で、猶予中に変わった帰属を0.375／0.125として一度だけ記録した。
再解釈と確定後のaccentを加えても確定writeは変わらず、late support lossを別に保持する。

このoracleは有限入力のwrite・receipt・改訂を全件保持する。65,536件のpending数ガードは
128-byte本番終端recordの配置や全メモリ上限を満たす証拠ではない。全beamの活動・帰属生成、
boundedな本番管理、strength／gap／bankの全ライフサイクル、O04同時負荷、全O12適合、
実波形と人の収集条件は未完了であり、M0全体の完了扱いにはしない。


## Raw記述子と有界圧縮の数値参照

[descriptor-reference.json](descriptor-reference.json)と
`scripts/temporal_descriptor_reference.py`を追加した。設計メモの本文・SHA256は今回変更していない。
feature manifestは`m0-registration-9`へ進み、既存六layoutにepisode rawの10座標を追加した。
入力はcanonical hopの割当済みgroup energy／spectrumであり、group推定やVoice identityは生成しない。
二つのbin走査で重心・幅・三つのmass比と包絡／fluxを求める。全raw支持とavailabilityをcutで検査し、
不足した先行hopから差分を作らない。取得sampleの和集合が部分的なら音響値をmaskし、clipした実観測秒と欠測秒を分ける。

各knotは実際の320-byte payloadに十組のf64 W／平均／二乗誤差、代表時刻と観測秒、
元の支持・availability・欠測秒・epoch／generationを収める。bankは128件の固定bytearrayとし、
挿入scratch1件、部分blockとgap各1件も固定領域に持つ。既定の活動spanのpayloadは41,920 bytes、
両busで各1,024 pathを全copy保持した場合は85,852,160 bytesとなる。Pythonのobject、raw cache、
出力snapshot、全beam、matcher、edge等は別途必要であり、O04全体のメモリ／性能合格ではない。

原hopの組はgeneration最初のsampleから1／2／4hop単位に揃える。部分span端とgapで閉じ、
連続したgapは一件へまとめる。取得再開時の最大三挿入、重複不加算、新generationへの非継承、確定後の更新拒否を検査した。
上限超過では最初と最新のknotを保ち、全体尺度で標準化した誤差増分が最小の隣接組を併合する。
欠測は有効観測量へ加えず、90%未満のcoordinateは照合値をmaskする。

29検査に加え、短い持続、43.69秒の持続、急変、取得欠落、半速、移調の六入力を、
64／128／256 knotと1／2／4hopの全54条件で比較した。100本の40-hop有限入力も元の各値から直接再構成した。
54条件の累積誤差統計と未圧縮f64直接計算の最大差は8.53e-14だった。既定128 knot／2hopでは
長い持続の復元誤差は0、急変の積分誤差は約21.676、最大点残差は約1.307標準偏差となった。
取得欠落の例ではgap flagと欠測量を保ったが、併合で正確なgap位置を失ったblockが一件あった。
これは圧縮の近似損失であり、聴覚的な忘却や認知時定数の測定ではない。

入力とsource、仕様、manifest前後、検査と再生成scriptを
`target/temporal-dcc/m0-descriptor-reference-20260911/`へ保存した。
この検査の速度変更は割当済み観測列の時間変更であり、実波形の聴取証拠ではない。
採用尺度、ordered matcherとの等価性、T4／T6の保持、全O12 fitと両worker／64 VoiceのO04は残る。


## 保持中のquery・coarse／DTW照合と確定episode

[matcher-reference.json](matcher-reference.json)と`scripts/temporal_matcher_reference.py`を追加した。
32検査では、部分blockを含むqueryがliveなcadence／圧縮を変えないこと、gapが原音の支持時刻を更新しないこと、
既存の確定writeから元時刻を保つepisodeを作り、実際の配送前には検索候補に入れないことを確認した。
SpanDescriptorの変更は最後の支持音声終端を保持する二箇所だけであり、既存の圧縮計算は変更していない。

coarse処理は最初の最大8 cue knotと各anchorの元位置を保って比較し、移調とtempo比をそれぞれ中央値で推定する。
不明な変形は0を適用してmaskし、推定／丸めが範囲外または境界に達すれば未知とする。
上位16 episodeを各最大4変形で細かく照合する。上位から外れた候補にもcoarseなcostと類似度を残す。
欠測pairは残差も支持も増やさず、挿入／削除とは分ける。共通して有効な10値の平均二乗残差を対角のcostとし、
内部の挿入／削除に明示した費用を加える。引用の前後にあるreference素材は削除に数えない。
元時刻のaffine変換で探索帯を置き、同距離なら早いreference時刻を選ぶ。
相対音高運動・interval変形の残差も別に記録し、未登録のcost係数へ追加しない。

50個の小さなランダム入力では、全経路を独立に列挙したcostと小数点以下13桁で一致した。
元ID／世代に対応する明示的なhandleだけをcoarse cacheから既存の確定ledgerへ渡し、
遅いsnapshotは過去の確定に使えず、候補打切り同点はsectionの未解決分類になると確認した。

五fixtureとanchor間隔2／4／8の15比較では、間隔2／4が元の引用・移調・半長時間・欠測の四例を
正しい変形・cost 0で検索した。同じ値の並べ替えには正のcostが残った。
間隔8では移調した引用を検索できず、元の引用・速度変更・欠測でも移調推定が-0.1875 octaveずれた。
これは明示的な検索失敗であり、新規素材や認知的な忘却へ読み替えない。間隔8の対照は採用しない。
既定間隔4の広い素材での採用判断、圧縮密度差、検索率、実尺度とT4／T6の判定は残る。

256 episode×128 knot、128 cue knotの単一queryでは8,192 anchor、121,349 median比較、
62 refinement／照合、179,392 DP cellを処理した。数値配列のDP作業領域は既定長で最大7,824 bytes。
この一queryの数値処理は約0.276秒で、worker全体の40 ms予算を超えた。現Python参照をそのまま実時間へ入れる案は通らない。
本番kernelと両worker／64 Voiceの全同時負荷でO04を検証する必要がある。
raw抽出、queryコピー／decode、queue／24-byte cache、bank、objectとallocatorを含む全配置・同時負荷は未計測である。

source、仕様前後、登録前後、検査と失敗を含む比較結果は
`target/temporal-dcc/m0-matcher-reference-20260911/`へ保存した。
有限入力の接続はできたが、query cadence／supersession、八snapshot保持、bank／edge／memory clock、
全beam／実波形、全fitと人のgateは未完了であり、M0全体を完了とはしない。
